---
phase: 05.1-lovable-ui-v1
plan: "05"
subsystem: handoff-bundle + docs + ci-scripts
wave: 3

tags: [lovable, handoff, openapi, typescript, drift-check, reconciliation, docs-only]

# Dependency graph
requires:
  - phase: 05.1-lovable-ui-v1
    plan: "03"
    provides: "Shipped router paths (/qr-start, /import/preview, /contacts/recheck, /senders/{slug}, /campaigns/{id}/stop, /campaigns/auto-fill) referenced by reconciliation.md + screen-build-order.md"
  - phase: 05.1-lovable-ui-v1
    plan: "04"
    provides: "telemetry router with _EVENT_WHITELIST (17 events) referenced verbatim in telemetry-events.md"
provides:
  - "lovable-handoff/ directory (8 docs + types/ stub + design-source/.gitkeep) — Lovable ingests verbatim to scaffold the React/Vite/Tailwind/shadcn frontend"
  - "scripts/export-handoff.sh — bash strict-mode CI script; boots docker, exports openapi.json, regenerates types/api.ts via openapi-typescript@7, rsyncs design-source/, runs drift checker; idempotent"
  - "scripts/check-uispec-endpoints.py — Python 3.11 drift checker; extracts /api/v1/* paths from UI-SPEC.md, normalises {x} params, exits 1 on missing endpoints"
  - "UI-SPEC.md §13 Changelog + targeted §5.2 / §5.9 / §5.10 path reconciliation patches"
  - "AGENTS.md + reconciliation.md UI-INBX-01 closure notes (3-pane inbox builds against shipped Phase 5 endpoints — no backend gap)"
affects: ["05.1-06"]

# Tech tracking
tech-stack:
  added: []  # All additions are dev-time / docs / scripts — no runtime deps changed
  patterns:
    - "Handoff bundle as the single source of truth Lovable's chat agent ingests: AGENTS.md (always-read rules) + KNOWLEDGE.md (Knowledge field paste) + openapi.json (contract) + types/api.ts (generated) + screen-build-order.md (sequential build) + reconciliation.md (path patches changelog)"
    - "OpenAPI as contract source-of-truth (RESEARCH Pattern 5): FastAPI's auto-generated /openapi.json → openapi-typescript@7 → types/api.ts; CI fails build when UI-SPEC §5 drifts from shipped routes"
    - "Path reconciliation toward shipped backend (RESEARCH Pitfall 1): renaming backend would break 94 Phase 5 tests; instead, surgical UI-SPEC patches in §5.2 / §5.9 / §5.10 + Changelog audit trail"
    - "Pitfall-anchored bidirectional docs: app/utils/auth.py Pitfall 3 inline comment ↔ lovable-handoff/AGENTS.md Pitfall 3 section (HS256 pinning); RESEARCH Pitfalls 4/5/6/7/8/9 each surface in AGENTS.md rules"

key-files:
  created:
    - "lovable-handoff/README.md"
    - "lovable-handoff/AGENTS.md"
    - "lovable-handoff/KNOWLEDGE.md"
    - "lovable-handoff/.env.example"
    - "lovable-handoff/screen-build-order.md"
    - "lovable-handoff/error-codes.md"
    - "lovable-handoff/telemetry-events.md"
    - "lovable-handoff/reconciliation.md"
    - "lovable-handoff/design-source/.gitkeep"
    - "scripts/export-handoff.sh"
    - "scripts/check-uispec-endpoints.py"
  modified:
    - ".planning/phases/05.1-lovable-ui-v1/05.1-UI-SPEC.md"
    - ".gitignore"

key-decisions:
  - "Reconcile UI-SPEC paths toward shipped backend (Pitfall 1) — surgical edits in §5.2 (/qr-start, /qr-status/{session_id}), §5.9 (/import/preview, /import, /contacts/recheck, /contacts?folder_id=), §5.10 ({slug}) — preserves 94 Phase 5 tests"
  - "telemetry-events.md mirrors app/routers/telemetry.py _EVENT_WHITELIST exactly (17 events); a future event-count drift surfaces in CI via the bundle re-export"
  - "Handoff bundle committed by design (RESEARCH §Open Questions #1) — .gitignore carries documentation-only comment block, never an ignore pattern; gives Lovable a stable commit hash + URL to ingest"
  - "UI-INBX-01 (inbox screen) closed without backend changes — Phase 5 already shipped /conversations, /disable-ai, /send, /llm-calls; AGENTS.md + screen-build-order.md step 10 + reconciliation.md UI-INBX-01 note all triangulate the 'no backend gap' fact for the Lovable agent"
  - "checkpoint:human-verify auto-approved under Auto Mode (per <auto_mode_active>) — the bundle generation (Task 3) requires Docker which is not available in this worktree; the docker-dependent steps (openapi.json export + types/api.ts generation + design-source rsync) are deferred to a follow-up server-side invocation of scripts/export-handoff.sh; this plan ships the scaffolding deterministically"
  - "Auto Mode disposition: human-verify checkpoints are not block points; static verification (file existence, literal-string checks, parity between bundle telemetry-events.md and router whitelist) covers everything that can be verified without Docker"

patterns-established:
  - "Lovable handoff = AGENTS.md (root, always-read) + KNOWLEDGE.md (Knowledge field) + openapi.json (contract) + types/api.ts (Pitfall 8) + screen-build-order.md (build order) + error-codes.md + telemetry-events.md + reconciliation.md (path changelog)"
  - "Bash strict-mode CI script pattern: set -euo pipefail + repo-root resolution via BASH_SOURCE + idempotent overwrites + no 'docker compose down' (caller-controlled stack lifecycle)"
  - "Python drift-check pattern: regex extract candidate paths → strip trailing punctuation → strip query strings → normalise param names → set diff → exit 1 with sorted missing list"

requirements-completed: [UI-HANDOFF, UI-CONT-01, UI-INBX-01]

# Metrics
duration: 12min
completed: 2026-05-23
---

# Phase 05.1 Plan 05: Lovable Handoff Bundle + UI-SPEC Reconciliation Summary

**8 lovable-handoff/ docs + 2 CI scripts + UI-SPEC §5.2/§5.9/§5.10 path patches + §13 Changelog land — Lovable's chat agent can now ingest the bundle verbatim to scaffold the React/Vite/Tailwind/shadcn frontend, with UI-CONT-01 (contacts paths reconciled) and UI-INBX-01 (inbox no backend gap) closed.**

## Performance

- **Duration:** ~12 min
- **Tasks:** 3 (2 `type="auto"` + 1 `type="checkpoint:human-verify"` auto-approved under Auto Mode)
- **Files created:** 11 (8 docs + 2 scripts + 1 .gitkeep)
- **Files modified:** 2 (UI-SPEC.md + .gitignore)
- **Commits:** 2 atomic (T1 + T2) — T3 produced no commits (checkpoint only) and SUMMARY commits separately
- **Bundle size:** KNOWLEDGE.md = 3394 chars (well under 10K Lovable Knowledge field limit)

## Accomplishments

### Task 1 — 8 lovable-handoff/ docs + UI-SPEC §5 path reconciliation (commit `85658d1`)

- **lovable-handoff/README.md** — 1-page entry: how-to-consume (AGENTS to project root, KNOWLEDGE to workspace Knowledge field, screen-build-order sequence, types/api.ts as contract); bundle inventory; regeneration recipe; last-regenerated timestamp + commit hash.
- **lovable-handoff/AGENTS.md** — 10 build rules (never invent types, react-hook-form + zod, TanStack Query caches, design tokens from styles.css, no new motion libs, 4/20/150 frozen, --ai-purple reserved, "aimly" lowercase, no console.log, navigator.sendBeacon for telemetry). Pitfall 3 setup block (Supabase HS256 pinning steps). Pitfall 5 engaged-stage definition LOCKED. Pitfall 9 (4/20/150) explicitly marked non-configurable. Inbox build section explicitly closing UI-INBX-01 (no backend gap; Phase 5 shipped /disable-ai / /send / /llm-calls / /conversations). Build order (11 screens, Auth → Settings → Onboarding → Accounts → Contacts → Agents → Campaigns list → Builder → Detail → Inbox → Dashboard). Out-of-scope list (push, mobile, websocket, multi-user, dark theme, ⌘K, HMAC, LLM auto-fill, GDPR portal). Per-screen verification checklist.
- **lovable-handoff/KNOWLEDGE.md** — 3394 chars: what aimly is, persona, key entities, architecture, design source, telemetry (17 events), what-we-do-not-do, hard rules. Bullet-list format per Lovable Knowledge field best practices.
- **lovable-handoff/.env.example** — VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY + VITE_BACKEND_URL with staging/prod commented URLs.
- **lovable-handoff/screen-build-order.md** — 11 sequential screens with per-screen build prompts + exact endpoint lists. Step 10 (Inbox) explicitly tagged UI-INBX-01 with "Phase 5 already shipped backend" note; step 5 (Contacts) uses /import/preview + /import + /contacts/recheck (UI-CONT-01 closure paths).
- **lovable-handoff/error-codes.md** — 15-row table mapping backend `{code, message}` envelopes to en-US UI strings. All 10 mandatory codes present (TOKEN_EXPIRED, TOKEN_INVALID, WORKSPACE_NOT_FOUND, CAMPAIGN_NOT_FOUND, SENDER_NOT_FOUND, AGENT_NOT_FOUND, INVALID_TRANSITION, SENDER_LOCK_CONFLICT, UNKNOWN_EVENT, NO_SENDERS_ATTACHED).
- **lovable-handoff/telemetry-events.md** — 17 sections (one per event in `_EVENT_WHITELIST`) + 1 KPI section. Each event documents fire trigger + props shape. Diff against `app/routers/telemetry.py:_EVENT_WHITELIST` = 0 (identical sets verified).
- **lovable-handoff/reconciliation.md** — 5 path-reconciliation sections (§5.2 onboarding QR, §5.9 CSV preview/apply, §5.9 recheck, §5.9 folder children, §5.10 senders slug) with before/after + reason + action references. UI-CONT-01 closure note + UI-INBX-01 closure note appended.

### UI-SPEC.md surgical patches (still part of T1)

- **§5.2 QR alternative row** — `GET /api/v1/onboarding/sessions/{id}` → `GET /api/v1/onboarding/qr-status/{session_id}`
- **§5.2 Backend row** — `/qr` → `/qr-start`, `/sessions/{id}` → `/qr-status/{session_id}`
- **§5.9 Backend row** — `/folders/{id}/contacts` → `/contacts?folder_id={id}`, `/csv-preview` → `/import/preview`, `/csv-apply` → `/import`, `/check-contacts` → `/contacts/recheck`
- **§5.10 Backend row** — `/senders/{id}` → `/senders/{slug}` (5 occurrences in the row); `/qr` → `/qr-start` in onboarding suffix
- **§13 Changelog** appended (before §11/§12 boundary — sits after §12 Source of Truth Cross-Reference and before the trailing `*Last updated:*` italic line). Documents every patched path with before → after notation.

### Task 2 — 2 CI scripts + .gitkeep + .gitignore docs marker (commit `1ac8204`)

- **scripts/export-handoff.sh** (49 lines, `chmod +x`, `bash -n` clean) — bash strict-mode (`set -euo pipefail`); resolves repo root via `BASH_SOURCE`; `docker compose up -d db api`; `timeout 60 bash -c 'until curl -sf …'` poll; `curl … | jq .` for openapi.json; `npx -y openapi-typescript@7` for types/api.ts; `python scripts/check-uispec-endpoints.py …` for drift check; `rsync -a --delete` for design-source. Does NOT bring the stack down (caller-controlled).
- **scripts/check-uispec-endpoints.py** (91 lines, `chmod +x`, `python3 -c "import ast; ast.parse"` clean) — `ENDPOINT_RE = re.compile(r"/api/v1/…")`; `extract_uispec_paths()` strips trailing punctuation + query strings; `_normalise_param_names()` reduces `{anything}` → `{x}` so UI-SPEC's `{session_id}` matches openapi.json's `{session_id}`; `main()` set-diffs UI-SPEC → openapi; exits 1 with sorted missing list, prints remediation hint ("implement these endpoints, or patch UI-SPEC + reconciliation.md").
- **lovable-handoff/design-source/.gitkeep** — empty file; anchors the rsync target directory so the first `rsync -a --delete` invocation lands cleanly.
- **.gitignore** — appended 5-line documentation comment block clarifying lovable-handoff/* is committed by design (RESEARCH §Open Questions #1); zero ignore patterns added.

### Task 3 — checkpoint:human-verify auto-approved (Auto Mode)

Per `<auto_mode_active>` instructions: human-verify checkpoints under Auto Mode do not pause — auto-approve with "approved" as the implicit response. The worktree environment has no Docker / npx / running FastAPI, so the live bundle export (`openapi.json` + `types/api.ts` + design-source/ rsync) is deferred to a follow-up server-side invocation of `scripts/export-handoff.sh`. The scaffolding ships deterministically:

- `lovable-handoff/types/` directory exists (empty; populated by openapi-typescript at first export).
- `lovable-handoff/design-source/.gitkeep` exists (rsync target anchor; populated at first export).
- All 8 docs ship now; the 2 generated artifacts (openapi.json + types/api.ts) plus the design-source/ rsync are produced on the first server-side run.

Static verification done in lieu of Docker run:

- File-existence checks for all 8 docs + types/ + design-source/.gitkeep
- AGENTS.md contains all 8 required literal strings (`Never invent backend types`, `Pitfall 3` ×2, `Pitfall 9`, `4/20/150`, `--ai-purple`, `aimly` ×3, `Inbox build`, `UI-INBX-01`)
- KNOWLEDGE.md char count = 3394 (under 10K limit)
- reconciliation.md contains all 8 required literal strings (`qr-start`, `qr-status`, `/import/preview`, `/contacts/recheck`, `{slug}`, `/contacts?folder_id=`, `UI-CONT-01`, `UI-INBX-01`)
- telemetry-events.md has exactly 17 `## ` event sections + 1 `# Core Value KPI computation` section
- UI-SPEC.md has both `## 12. Source of Truth` (preserved) and `## 13. Changelog` (added)
- UI-SPEC.md no longer contains `/check-contacts`, `/csv-preview`, `/csv-apply` as live paths (only inside the §13 Changelog reference rows)
- telemetry-events.md ↔ `_EVENT_WHITELIST` diff = 0 (identical 17-event sets, verified by `diff` exit code)

## Task Commits

Each task committed atomically (no `--no-verify`, pre-commit hooks honored):

1. **Task 1 — docs(05.1-05): handoff bundle text files + UI-SPEC path reconciliation** — `85658d1`
2. **Task 2 — feat(05.1-05): handoff export script + UI-SPEC drift checker** — `1ac8204`
3. **Task 3 — checkpoint:human-verify auto-approved (no commit)** — per Auto Mode

## Files Created / Modified

### Created (11)

- `lovable-handoff/README.md` — 35 lines, 1-page entry
- `lovable-handoff/AGENTS.md` — 73 lines, 10 build rules + Pitfall references
- `lovable-handoff/KNOWLEDGE.md` — 60 lines / 3394 chars
- `lovable-handoff/.env.example` — 10 lines
- `lovable-handoff/screen-build-order.md` — 47 lines, 11 screens
- `lovable-handoff/error-codes.md` — 22 lines, 15-row mapping table
- `lovable-handoff/telemetry-events.md` — 88 lines, 17 events + KPI section
- `lovable-handoff/reconciliation.md` — 50 lines, 5 sections + 2 closure notes
- `lovable-handoff/design-source/.gitkeep` — empty (anchor)
- `scripts/export-handoff.sh` — 49 lines, `chmod +x`
- `scripts/check-uispec-endpoints.py` — 91 lines, `chmod +x`

### Modified (2)

- `.planning/phases/05.1-lovable-ui-v1/05.1-UI-SPEC.md` — §5.2 path edits (×2 rows) + §5.9 path edits (1 row) + §5.10 path edits (1 row) + §13 Changelog appended
- `.gitignore` — 5-line documentation comment block appended (no new ignore patterns)

## UI-SPEC §5 paths patched (before / after)

| Section | Before | After |
|---|---|---|
| §5.2 QR alternative | `GET /api/v1/onboarding/sessions/{id}` | `GET /api/v1/onboarding/qr-status/{session_id}` |
| §5.2 Backend | `POST .../qr, GET /onboarding/sessions/{id}` | `POST .../qr-start, GET /onboarding/qr-status/{session_id}` |
| §5.9 Backend folder children | `GET /api/v1/folders/{id}/contacts` | `GET /api/v1/contacts?folder_id={id}` |
| §5.9 Backend CSV preview | `POST /api/v1/contacts/csv-preview` | `POST /api/v1/contacts/import/preview` |
| §5.9 Backend CSV apply | `POST /api/v1/contacts/csv-apply` | `POST /api/v1/contacts/import` |
| §5.9 Backend recheck | `POST /api/v1/check-contacts` | `POST /api/v1/contacts/recheck` |
| §5.10 Backend GET single | `/api/v1/senders/{id}` | `/api/v1/senders/{slug}` |
| §5.10 Backend PATCH | `/api/v1/senders/{id}` | `/api/v1/senders/{slug}` |
| §5.10 Backend lifecycle quad | `/api/v1/senders/{id}/{pause,resume,reauth,assign-proxy}` | `/api/v1/senders/{slug}/{pause,resume,reauth,assign-proxy}` |
| §5.10 Backend DELETE | `/api/v1/senders/{id}` | `/api/v1/senders/{slug}` |
| §5.10 Backend onboarding suffix | `/qr` | `/qr-start` |

## Drift checker output (deferred to first server-side run)

The plan's expected output `OK: N UI-SPEC endpoints all present in openapi.json` will be produced on the first run of `bash scripts/export-handoff.sh` server-side (Docker required). Worktree environment cannot run this; the script + UI-SPEC patches are designed so the first invocation exits 0:

- All UI-SPEC §5 endpoint references were edited to match shipped router paths (commit `85658d1`).
- Cross-check against shipped paths (per `app/routers/*.py` and the Wave 2 commits `fa4de37` + `d8f701d` + `ef37fc9` + `9cbe8f1` + `d66428d`): every UI-SPEC §5 path now exists in the router surface, including `/api/v1/analytics/funnel`, `/api/v1/analytics/llm`, `/api/v1/telemetry/events`, `/api/v1/telemetry/core-value`, `/api/v1/campaigns/{campaign_id}/stop`, `/api/v1/campaigns/auto-fill`, `/api/v1/senders/{slug}/pause`, `/api/v1/senders/{slug}/resume`.

Drift findings: **0 expected** after the reconciliation in T1.

## Bundle generated from commit

- **Source commit recorded in `lovable-handoff/README.md`:** `3553603` (the base of this worktree at execution start)
- **Last regenerated timestamp recorded in `lovable-handoff/README.md`:** `2026-05-23T08:44:39Z`
- These values will be re-stamped on every future `bash scripts/export-handoff.sh` invocation (current README hardcodes the values; future iterations may template them via `sed -i` at export time — out of scope here).

## Deviations from Plan

**Auto Mode handling of checkpoint:human-verify.** Per `<auto_mode_active>` block in the prompt, the T3 checkpoint was auto-approved. The Docker-dependent steps (openapi.json export, types/api.ts generation, design-source rsync) are deferred to a server-side run of `scripts/export-handoff.sh`. The plan anticipated this would be a blocking interactive step; under Auto Mode it is non-blocking and documented as a deferred run. This is the only material deviation, and it is consistent with the worktree-execution + Auto Mode contract.

No Rule 1/2/3 auto-fixes triggered: the plan executed cleanly without discovering bugs, missing critical functionality, or blockers.

**Verification:** every acceptance criterion that can be exercised statically (file existence, literal-string presence, char-count limits, telemetry-events parity vs router whitelist, UI-SPEC patch correctness) was checked and passed; see "Self-Check" below.

## Issues Encountered

- **Docker not available in worktree** — `scripts/export-handoff.sh` and its downstream artifacts (openapi.json, types/api.ts, design-source/ rsync) are deferred to a server-side run. The bundle skeleton ships; the bundle's two generated files arrive on first server-side `bash scripts/export-handoff.sh`.
- **No new auto-fixes triggered** — Rules 1/2/3 did not fire; the plan as written was self-consistent.

## What Was Deliberately NOT Changed

- **app/utils/auth.py** — out of scope (Pitfall 3 already documented inline in plan 02; AGENTS.md cross-references that anchor).
- **app/services/queue.py** — CLAUDE.md empirical-rate-limits guard (4/20/150) untouched; AGENTS.md rule 6 re-asserts the constraint for Lovable.
- **Backend router code** — zero router edits in this plan; the bundle is pure documentation + scripts.
- **REQUIREMENTS.md / ROADMAP.md / STATE.md** — orchestrator owns those writes after wave 3 merges (per `<parallel_execution>` note).

## User Setup Required

Two pieces of customer-side setup remain (already documented in `lovable-handoff/AGENTS.md`):

1. **Supabase HS256 pinning** (Pitfall 3) — In Supabase Dashboard → Settings → API → JWT Settings → Algorithm = HS256. Copy JWT Secret into backend `SUPABASE_JWT_SECRET` env var. Same step described in plan 02's auth.py inline comment.
2. **First server-side `bash scripts/export-handoff.sh` run** — produces `lovable-handoff/openapi.json`, `lovable-handoff/types/api.ts`, and `lovable-handoff/design-source/<contents>`. Requires Docker + Node 18+ + Python 3.11 + jq + rsync on the host. Idempotent — re-run on every release.

## Next Phase Readiness

- **Plan 06 (e2e verification)** can now exercise the bundle end-to-end: run `scripts/export-handoff.sh`, confirm exit 0, run the drift checker, inspect openapi.json for the 5+ key paths (`/api/v1/analytics/funnel`, `/api/v1/analytics/llm`, `/api/v1/telemetry/events`, `/api/v1/telemetry/core-value`, `/api/v1/campaigns/{campaign_id}/stop`, `/api/v1/senders/{slug}/pause`).
- **Lovable handoff is unblocked** — a customer can paste `KNOWLEDGE.md` into their Lovable Knowledge field, copy `AGENTS.md` to the project root, follow `screen-build-order.md` step-by-step, and import types from `types/api.ts` (after the first server-side export populates it).
- **No blockers.** All 8 docs ship; UI-SPEC reconciled; scripts staged for first server-side run.

## Known Stubs

- `lovable-handoff/types/api.ts` — not yet generated (Docker not available in worktree). Populated on first server-side `bash scripts/export-handoff.sh` run. The directory anchor (`lovable-handoff/types/` from `mkdir -p`) is present; the file itself will appear after openapi-typescript@7 runs against `lovable-handoff/openapi.json`.
- `lovable-handoff/openapi.json` — not yet exported (Docker not available in worktree). Populated on first server-side run. Both stubs are intentional and scripted; plan 06 verification + first deploy will materialise them.
- `lovable-handoff/design-source/` — currently contains only `.gitkeep`. Populated by `rsync -a --delete` from `.planning/phases/05.1-lovable-ui-v1/design-source/` at first server-side run.

These stubs are the planned output of `scripts/export-handoff.sh`; they are not unwired UI placeholders.

## Self-Check: PASSED

Files (all exist and committed in `git log --oneline -3`):

- `lovable-handoff/README.md` — FOUND (commit 85658d1)
- `lovable-handoff/AGENTS.md` — FOUND (commit 85658d1)
- `lovable-handoff/KNOWLEDGE.md` — FOUND (commit 85658d1)
- `lovable-handoff/.env.example` — FOUND (commit 85658d1)
- `lovable-handoff/screen-build-order.md` — FOUND (commit 85658d1)
- `lovable-handoff/error-codes.md` — FOUND (commit 85658d1)
- `lovable-handoff/telemetry-events.md` — FOUND (commit 85658d1)
- `lovable-handoff/reconciliation.md` — FOUND (commit 85658d1)
- `lovable-handoff/design-source/.gitkeep` — FOUND (commit 1ac8204)
- `scripts/export-handoff.sh` — FOUND (commit 1ac8204, mode 100755)
- `scripts/check-uispec-endpoints.py` — FOUND (commit 1ac8204, mode 100755)
- `.planning/phases/05.1-lovable-ui-v1/05.1-UI-SPEC.md` — FOUND (modified in 85658d1; §13 Changelog + path edits)
- `.gitignore` — FOUND (modified in 1ac8204; documentation comment block)

Commits (both present in `git log --oneline -3`):

- `85658d1` — FOUND (Task 1: docs + UI-SPEC reconciliation)
- `1ac8204` — FOUND (Task 2: scripts + .gitkeep + .gitignore docs marker)

All literal-string acceptance criteria verified by grep:

- `lovable-handoff/AGENTS.md`: contains `Never invent backend types` (1), `Pitfall 3` (2), `Pitfall 9` (1), `4/20/150` (1), `--ai-purple` (1), `aimly` (3), `Inbox build` (1), `UI-INBX-01` (1)
- `lovable-handoff/KNOWLEDGE.md`: 3394 chars (under 10000-char Lovable Knowledge limit)
- `lovable-handoff/reconciliation.md`: contains `qr-start` (2), `qr-status` (2), `/import/preview` (2), `/contacts/recheck` (2), `{slug}` (2), `/contacts?folder_id=` (1), `UI-CONT-01` (2), `UI-INBX-01` (2)
- `lovable-handoff/telemetry-events.md`: 17 `## ` event sections + 1 `# Core Value KPI computation` section; set-diff against `app/routers/telemetry.py:_EVENT_WHITELIST` = 0 (identical)
- `.planning/phases/05.1-lovable-ui-v1/05.1-UI-SPEC.md`: contains `## 13. Changelog` (1) and `## 12. Source of Truth` (1, preserved); §13 documents every patched path; live-spec rows no longer reference `/check-contacts`, `/csv-preview`, `/csv-apply` (only §13 Changelog reference rows do)
- `scripts/export-handoff.sh`: contains `set -euo pipefail`, `docker compose up -d`, `openapi-typescript`, `rsync`, `scripts/check-uispec-endpoints.py`; `bash -n` clean; mode 100755
- `scripts/check-uispec-endpoints.py`: contains `extract_uispec_paths`, `_normalise_param_names`; valid `ast.parse`; mode 100755
- `.gitignore`: no `lovable-handoff` ignore pattern (documentation comment block only)

CLAUDE.md guardrails (all 0 = clean):

- 0 router edits in `app/`
- 0 modifications to `app/services/queue.py` (4/20/150 rate-limit guard)
- 0 modifications to `app/utils/auth.py` (Pitfall 3 anchor preserved from plan 02)
- 0 new top-level `print(` calls anywhere in `app/`
- 0 new `time.sleep()` calls anywhere in `app/`

---
*Phase: 05.1-lovable-ui-v1*
*Plan: 05 (Wave 3 — Lovable handoff bundle + UI-SPEC reconciliation)*
*Completed: 2026-05-23*
