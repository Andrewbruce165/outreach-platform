---
phase: 05.1-lovable-ui-v1
created: 2026-05-23
source: .planning/phases/05-inbox-analytics/05-HUMAN-UAT.md
purpose: Map each of 7 outstanding Phase 5 UAT items to its 05.1 closure pathway (UI screen + backend endpoint + Lovable build step)
---

# Phase 05.1 — HUMAN-UAT Closure Mapping

Phase 5 left 7 items pending in `05-HUMAN-UAT.md` — they could not be verified without a UI. Phase 05.1 produces that UI (via Lovable, using the handoff bundle in `lovable-handoff/`). This document maps each item to:

1. The UI screen that exposes the behavior
2. The backend endpoint it exercises
3. The Lovable build step (from `lovable-handoff/screen-build-order.md`) that creates the surface
4. Status: **closed** (no further work) / **needs UI build** (gated on Lovable build completion) / **needs deployment** (CI/CD)

---

## Item 1 — Server-side pytest for all Phase 5 tests after deploy

**Original expected:** All ~94 Phase 5 tests pass — `pytest tests/test_phase5_*.py -x -q` on VPS. Covers migration_017 (11), inbox (14), manager_mode (5), send_takeover (6), bot_filter (12), analytics (12), analytics_correctness (8), llm_logger (11), llm_logger_no_block_on_error (3), llm_calls_endpoint (8), plus regression in migration_016 / campaign_router / ai_engine.

**05.1 pathway:**
- UI screen: n/a (CI / deployment concern)
- Backend endpoint: n/a
- Lovable build step: n/a
- Closure: run `pytest tests/test_phase5_*.py tests/test_phase5_1_*.py -x -q` on VPS after deploy. 05.1 plans 01-04 add new tests that must ALSO pass; plan 05 doesn't touch tests; plan 06 adds the Core Value E2E + phase sentinel suite. The new `tests/test_phase5_1_full_suite.py` runs first and catches package-level regressions before per-feature tests below it run.

**Status:** **needs deployment** — covered by `/gsd:verify-work` smoke after 05.1 merges.

---

## Item 2 — Apply migration 017 to fresh DB

**Original expected:** `\d+ conversations` shows status CHECK with 7 values incl `bot_ignored`; `\d+ llm_calls` has 15 cols + 2 indexes; 3 composite indexes present; `messages` table exists with `workspace_id` column.

**05.1 pathway:**
- UI screen: n/a
- Backend endpoint: n/a
- Lovable build step: n/a
- Closure: 05.1 migration 018 applies AFTER 017 (sequential). `tests/test_phase5_1_migration_018.py` exercises both migrations together; `tests/conftest.py` chains 012 → 013 → 014 → 015 → 016 → 017 → 018 on every fresh test DB. Fresh-DB smoke: `pytest tests/test_phase5_1_migration_018.py -x -q` confirms 017 + 018 both apply cleanly.

**Status:** **needs deployment** — same VPS smoke as item 1.

---

## Item 3 — Manager-mode auto-takeover from Lovable UI (D-04)

**Original expected:** Open inbox in Lovable, send message from manager UI on an active dialog → dialog flips to `status='manual'`, `ai_enabled=false`, `paused_reason='Manager sent message via UI'`; any pending queue items for that `recipient_phone` become `status='failed'`; manager message arrives in Telegram.

**05.1 pathway:**
- UI screen: **§5.7 Inbox — thread pane composer "Take over" button + first-manual-send warning**
- Backend endpoint: `POST /api/v1/conversations/{id}/disable-ai` AND `POST /api/v1/conversations/{id}/send` (both already shipped in Phase 5 P01)
- Lovable build step: `screen-build-order.md` step 10 (Inbox 3-pane). Composer warning text: "Sending now will turn AI off for this thread" (UI-SPEC §8.3).
- Closure: After Lovable builds Inbox screen, user signs in, opens an active conversation, clicks "Take over" or sends a manual message — backend behavior matches expected. Already tested in `tests/test_phase5_inbox_send_takeover.py` + `tests/test_phase5_manager_mode.py`.

**Status:** **needs UI build** — gated on Lovable Inbox screen completion.

---

## Item 4 — Proactive bot filter on live Telegram interaction

**Original expected:** Send Telegram message to a sender from a verified bot account (SpamBot id=178220800 or any @BotFather bot) → listener creates conversation `status='bot_ignored'`, `ai_enabled=false`, stores inbound message, `ai_engine.generate_response` NOT invoked; for 178220800 specifically delegate to `_handle_antispam_signal` (D-08 safety net) pauses sender lifecycle and cancels ALL queue items for that sender.

**05.1 pathway:**
- UI screen: **§5.7 Inbox — filter chip "Bot (ignored)" or status pill on conversation rows**
- Backend endpoint: `GET /api/v1/conversations?status=bot_ignored` (already shipped Phase 5; status enum extended in migration 017)
- Lovable build step: `screen-build-order.md` step 10 Inbox; the list pane filter chips include "Bot (ignored)" per UI-SPEC §5.7.
- Closure: Listener-side behavior is already shipped (Phase 5 P01 D-05/D-06/D-08). UI exposure = Inbox filter chip surfaces these conversations so the user can audit. Already tested in `tests/test_phase5_bot_filter.py` (12 cases).

**Status:** **needs UI build** — same gate as item 3.

---

## Item 5 — Analytics workspace endpoint correctness

**Original expected:** Hit `GET /api/v1/analytics/workspace` with auth_headers on a workspace with seeded sent/replied/lead/finished data → response `{sent, replied:{conversation_count, message_count}, leads, finishes}` reflects only that workspace's counts; `bot_ignored` conversations excluded; leads strict EQ (does NOT include finished).

**05.1 pathway:**
- UI screen: **§5.3 Dashboard KPI row** (cards) + **full-width Sankey funnel** (5-stage)
- Backend endpoint: `GET /api/v1/analytics/workspace` (Phase 5 — already shipped) PLUS new `GET /api/v1/analytics/funnel?scope=workspace` (Phase 05.1 plan 04 — adds engaged stage between replied and lead)
- Lovable build step: `screen-build-order.md` step 11 (Dashboard).
- Closure: Cards exposed by Phase 5 analytics; funnel exposed by 05.1 plan 04 (UI-DASH-01). Test coverage: `tests/test_phase5_analytics.py` + `tests/test_phase5_analytics_correctness.py` (Phase 5) + `tests/test_phase5_1_funnel.py` (Phase 05.1 — engaged-definition lock, monotonic non-increasing, bot_ignored excluded, cross-workspace 404 silent).

**Status:** **needs UI build** — Dashboard is the last screen in build order (step 11).

---

## Item 6 — End-to-end LLM call log on real OpenAI dialog

**Original expected:** Trigger AI response on a dialog → hit `GET /api/v1/conversations/{id}/llm-calls` → response shows 1 row per turn (or 2 if custom tools fired second OpenAI call); `prompt` JSONB has full `request_params`, `response_text` matches AI reply, `prompt_tokens`/`completion_tokens`/`total_tokens` populated, `latency_ms` ~= actual round-trip. Warmup-LLM calls must NOT appear (D-12 contract).

**05.1 pathway:**
- UI screen: **§5.7 Inbox — Thought Trace pane (right column, toggleable)** AND **§5.6 Campaign detail LLM trace tab**
- Backend endpoint: `GET /api/v1/conversations/{id}/llm-calls` (Phase 5 — already shipped) + new `GET /api/v1/analytics/llm?scope=campaign&id=X&since=7d` (Phase 05.1 plan 04 — UI-CAMPD-01)
- Lovable build step: Inbox = step 10; Campaign detail LLM trace tab = step 9.
- Closure: Per-call detail surface ships in Phase 5 (`tests/test_phase5_llm_calls_endpoint.py` + `tests/test_phase5_llm_logger.py`); aggregates surface ships in 05.1 plan 04 (`tests/test_phase5_1_llm_aggregates.py`). UI rendering = Lovable build.

**Status:** **needs UI build** — Inbox + Campaign detail tabs.

---

## Item 7 — Prompt-leak guard on live logs (T-05-03-PROMPT-LEAK)

**Original expected:** Inspect application logs `docker logs api 2>&1 | grep -i prompt` after a trigger of `generate_response` with system prompt containing distinctive text → distinctive prompt text NEVER appears in logs; only `conversation_id` + exception text on errors.

**05.1 pathway:**
- UI screen: n/a (logs concern)
- Backend endpoint: n/a
- Lovable build step: n/a
- Closure: Phase 5 P03 added the guard (`tests/test_phase5_llm_logger.py` prompt-leak regression). 05.1 plan 04 adds `logger.info("[telemetry] ingest workspace=%s event=%s event_id=%s", ...)` WITHOUT logging `body.props` — guard preserved (no PII or token leak through telemetry path). Static-grep verification in 05.1-04-SUMMARY.md confirmed `logger\.(info|warning|error|debug)\(.*props` returns 0 matches in `app/routers/telemetry.py`.

**Status:** **needs deployment** — VPS smoke: `docker logs api 2>&1 | grep -iE "prompt|api_key|jwt" | head -20` — confirm no leak. Verified by `tests/test_phase5_llm_logger.py` prompt-leak guard tests (regression-locked).

---

## Summary table

| # | Item | UI screen | Backend endpoint | Lovable step | Status |
|---|------|-----------|-------------------|--------------|--------|
| 1 | Phase 5 pytest on VPS | n/a | n/a | n/a | needs deployment |
| 2 | Migration 017 fresh DB | n/a | n/a | n/a | needs deployment |
| 3 | Manager-mode auto-takeover | §5.7 Inbox composer | POST /conversations/{id}/{disable-ai,send} | 10 | needs UI build |
| 4 | Proactive bot filter | §5.7 Inbox filter chip | GET /conversations?status=bot_ignored | 10 | needs UI build |
| 5 | Analytics workspace + Sankey | §5.3 Dashboard | GET /analytics/workspace + /analytics/funnel | 11 | needs UI build |
| 6 | LLM call log | §5.7 Inbox trace + §5.6 LLM trace tab | GET /conversations/{id}/llm-calls + /analytics/llm | 9, 10 | needs UI build |
| 7 | Prompt-leak guard | n/a (logs) | n/a | n/a | needs deployment |

## How to close after Lovable build

1. Customer's Lovable workspace ingests `lovable-handoff/` (AGENTS.md + KNOWLEDGE.md + types/api.ts + design-source/)
2. Lovable builds screens in the order in `screen-build-order.md`
3. After each screen smoke-tests against staging backend, mark the corresponding item in this table as `closed`
4. Items 1, 2, 7 are deployment-time — verify via `pytest -x -q` on VPS post-deploy + `docker logs api | grep prompt`
5. Final phase verification: `/gsd:verify-work 05.1` against this mapping document

## Cross-reference: which 05.1 plan closes which requirement

The 7 HUMAN-UAT items above are the user-side acceptance gate. The internal 05.1 requirements that enable them:

| 05.1 requirement | Closing plan | HUMAN-UAT item served |
|------------------|--------------|----------------------|
| UI-AUTH-01 (CORS regex) | 05.1-02 | preconditions for items 3-6 |
| UI-MIG-018 (telemetry_events + AI/Campaign widening) | 05.1-01 | item 2 (forward roll); items 5-6 (new aggregates) |
| UI-AGNT-01 (agent v2 fields) | 05.1-01 + 05.1-03 | indirect — item 6 (agent-scoped LLM aggregates) |
| UI-CAMPB-01 (campaign builder v2 fields) | 05.1-01 + 05.1-03 | indirect — item 5 (campaign-scoped funnel) |
| UI-CAMPL-01 (campaign stop alias + auto-fill stub) | 05.1-03 | n/a (UI build affordance only) |
| UI-ACCT-01 (sender pause/resume) | 05.1-03 | n/a (UI build affordance only) |
| UI-DASH-01 (analytics funnel) | 05.1-04 | item 5 (Sankey) |
| UI-CAMPD-01 (analytics LLM aggregates) | 05.1-04 | item 6 (LLM tab aggregates) |
| UI-TEL-01 (telemetry ingest) | 05.1-04 | none directly — telemetry is a new surface |
| UI-TEL-02 (Core Value KPI) | 05.1-04 | none directly — new KPI |
| UI-CORS (CORS regex docs) | 05.1-02 | precondition for items 3-6 |
| UI-HANDOFF (lovable-handoff bundle) | 05.1-05 | enabler for items 3-6 UI build |
| UI-INBX-01 (inbox closure note) | 05.1-05 | items 3, 4, 6 (inbox screen) |
| UI-CONT-01 (contacts path reconciliation) | 05.1-05 | none directly — UI-SPEC §5.9 path patches |
| Core-Value-E2E (E2E pytest) | 05.1-06 | none directly — closes the only remaining backend-testable item |

All 7 HUMAN-UAT items now have a documented closure pathway. Items 3-6 unblock when Lovable finishes building Inbox (step 10) + Dashboard (step 11); items 1, 2, 7 unblock at deployment time.
