---
status: partial
phase: 05.1-lovable-ui-v1
source: [05.1-VERIFICATION.md]
started: 2026-05-23T00:00:00Z
updated: 2026-05-23T00:00:00Z
---

## Current Test

[awaiting human testing — Lovable preview build + VPS deploy]

## Tests

### 1. Export handoff bundle (one-shot, needs Docker + Node 18+)
expected: `bash scripts/export-handoff.sh` exits 0, produces non-empty `lovable-handoff/openapi.json` (with `/analytics/funnel`, `/analytics/llm`, `/telemetry/events`, `/telemetry/core-value`, `/senders/{slug}/pause`, `/senders/{slug}/resume`, `/campaigns/{id}/stop`, `/campaigns/{id}/auto-fill`), `lovable-handoff/types/api.ts` (compiled by openapi-typescript@7), and populated `lovable-handoff/design-source/` after rsync from local design directory.
result: [pending]

### 2. Full pytest suite on VPS
expected: `docker compose exec api pytest tests/test_phase5_1_*.py -x -q` returns exit 0 — all 103 Phase 05.1 tests green. Includes migration 018 idempotency, ORM widening, agent/campaign/sender v2 routers, analytics funnel, LLM aggregates, telemetry ingest, Core Value KPI, Core Value E2E (<600s), phase sentinel. No regression in Phase 1-5 suites (`pytest tests/test_phase{1,2,3,4,5}_*.py -x -q`).
result: [pending]

### 3. Lovable UI build + Core Value walkthrough
expected: Ingest `lovable-handoff/` bundle into Lovable chat → build 11 screens per `screen-build-order.md` → deploy to `*.lovableproject.com` → smoke-test the full v1 flow against staging backend: signup (Supabase magic-link) → workspace created → TG-account onboarded → CSV contacts imported → agent created → campaign launched. `/telemetry/core-value` returns `time_to_first_campaign_seconds < 600` for that workspace. Closes HUMAN-UAT items 3-6 from parent Phase 5 HUMAN-UAT (manager takeover, bot filter, Sankey analytics, LLM call log) — see `05.1-HUMAN-UAT-MAPPING.md`.
result: [pending]

### 4. Supabase JWT settings — HS256 pin (D-14 Pitfall 3)
expected: In Supabase Dashboard → Settings → API → JWT Settings → Algorithm = `HS256`. Backend `app/utils/auth.py` decodes with `algorithms=["HS256"]` only — switching Supabase to ES256 will silently 401 every request. Verified after Lovable preview deploys and first authenticated request lands.
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
