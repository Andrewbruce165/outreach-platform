---
phase: 02.1-worker-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/queue.py
  - app/services/listener.py
  - app/services/warmup.py
  - app/services/rotation.py
  - tests/test_queue_workspace_id.py
  - tests/test_listener_workspace_id.py
  - tests/test_warmup_workspace_isolation.py
  - tests/test_rotation_workspace_id.py
autonomous: true
gap_closure: true
requirements:
  - TENT-01
must_haves:
  truths:
    - "queue.enqueue_message и enqueue_file принимают workspace_id и пишут message_queue с workspace_id (CR-01)"
    - "queue._send_item создаёт MessageLog и INSERT INTO conversations с workspace_id из sender.workspace_id (CR-01)"
    - "queue._fail_item создаёт MessageLog (failed) с workspace_id (CR-01)"
    - "listener.get_or_create_conversation INSERT с workspace_id из senders (CR-02)"
    - "warmup INSERT INTO warmup_sessions и warmup_messages содержат workspace_id (CR-04 issue 1)"
    - "warmup._get_active_pool партиционирован по workspace_id — пары формируются только внутри одного workspace (CR-04 issue 3)"
    - "warmup._send_via_telethon FloodWait UPDATE использует скобки (sender_a_id=:sid OR sender_b_id=:sid) AND status='active' (CR-04 issue 2)"
    - "rotation INSERT INTO context_contact_assignments содержит workspace_id (CR-03)"
    - "rotation._pick_best_sender фильтрует по s.workspace_id=:wid (CR-03)"
    - "rotation.get_or_assign_sender принимает workspace_id параметром"
  artifacts:
    - path: "app/services/queue.py"
      provides: "workspace_id propagation в enqueue_message/enqueue_file/_send_item/_fail_item/_upsert_conversation"
      contains: "workspace_id"
    - path: "app/services/listener.py"
      provides: "workspace_id в get_or_create_conversation INSERT (workspace_id из senders SELECT)"
      contains: "workspace_id"
    - path: "app/services/warmup.py"
      provides: "workspace_id в warmup_sessions/warmup_messages INSERT; partitioning по workspace; SQL precedence fix"
      contains: "workspace_id"
    - path: "app/services/rotation.py"
      provides: "workspace_id в context_contact_assignments INSERT; workspace guard в _pick_best_sender"
      contains: "workspace_id"
    - path: "tests/test_queue_workspace_id.py"
      provides: "Regression test: enqueue_message + _send_item не падает с NotNullViolation"
    - path: "tests/test_warmup_workspace_isolation.py"
      provides: "Regression test: _get_active_pool не возвращает sender'ов из разных workspaces в одной паре"
  key_links:
    - from: "app/services/queue.py::enqueue_message"
      to: "MessageQueue.workspace_id"
      via: "explicit param workspace_id"
      pattern: "workspace_id=workspace_id"
    - from: "app/services/queue.py::_send_item"
      to: "MessageLog.workspace_id + conversations INSERT"
      via: "sender.workspace_id"
      pattern: "workspace_id=sender.workspace_id"
    - from: "app/services/listener.py::get_or_create_conversation"
      to: "conversations INSERT"
      via: "SELECT s.workspace_id FROM senders WHERE id=:sender_id"
      pattern: "workspace_id"
    - from: "app/services/warmup.py::_create_new_sessions"
      to: "warmup_sessions partitioning"
      via: "GROUP BY workspace_id перед pair-shuffle"
      pattern: "GROUP BY.*workspace_id"
    - from: "app/services/rotation.py::get_or_assign_sender"
      to: "context_contact_assignments INSERT"
      via: "workspace_id param"
      pattern: "workspace_id"
---

<objective>
Закрыть BLOCKER findings CR-01, CR-02, CR-03, CR-04, CR-06 из 02-REVIEW.md — пробросить workspace_id во все INSERT/SELECT унаследованных worker'ов (queue, listener, warmup, rotation), партиционировать warmup pool по workspace_id (предотвратить cross-tenant pair leak) и исправить SQL precedence bug в warmup FloodWait UPDATE.

Purpose: Без этих фиксов первый успешный send/incoming/warmup-pair упадёт с NotNullViolation в multi-tenant БД (миграция 012 сделала workspace_id NOT NULL для messages_log, conversations, message_queue, warmup_messages, warmup_sessions, context_contact_assignments). Это блокер для Phase 4 (Campaigns) и для AI-listener'а уже сейчас.

Output: Все 4 файла worker'ов пишут с workspace_id. Регрессионные тесты на каждый воркер. Никаких NotNullViolation в multi-tenant сценариях.
</objective>

<execution_context>
@/Users/andrewbruce/Documents/outreach-platform/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tg-accounts-contacts/02-REVIEW.md
@.planning/phases/02-tg-accounts-contacts/02-VERIFICATION.md
@./CLAUDE.md

# Files to modify (read before editing)
@app/services/queue.py
@app/services/listener.py
@app/services/warmup.py
@app/services/rotation.py

# Reference: models with workspace_id NOT NULL
@app/models/__init__.py
@migrations/012_workspace.sql
@tests/conftest.py

<interfaces>
<!-- Key types from app/models/__init__.py (all workspace_id NOT NULL after migration 012) -->

```python
# Sender (line 73-99) — workspace_id NOT NULL, source of truth
class Sender(Base):
    id: UUID
    workspace_id: UUID  # NOT NULL — propagate to all derived rows
    slug: str
    role: str  # 'sender' | 'checker'
    auth_status: str  # 'ok' | 'session_expired' | ...
    lifecycle_status: str  # 'active' | 'warmup' | 'paused'
    rate_per_min: int
    rate_per_hour: int
    rate_per_day: int
    ai_context_id: UUID | None

# MessageQueue (line 169) — workspace_id NOT NULL
class MessageQueue(Base):
    workspace_id: UUID  # MUST be set on INSERT
    sender_id: UUID
    item_type: QueueItemType
    ...

# MessageLog (line 102) — workspace_id NOT NULL
class MessageLog(Base):
    workspace_id: UUID  # MUST be set on INSERT
    sender_id: UUID
    ...

# Conversation (line 219) — workspace_id NOT NULL (raw SQL INSERT)
# WarmupPool/WarmupSession/WarmupMessage — все workspace_id NOT NULL
# context_contact_assignments — workspace_id NOT NULL
```

Pattern for raw SQL INSERT with workspace propagation:
```python
# Step 1: get workspace_id from senders
sender_row = await db.execute(
    text("SELECT workspace_id FROM senders WHERE id = :sid"),
    {"sid": str(sender_id)}
).fetchone()
wid = str(sender_row.workspace_id)

# Step 2: pass to INSERT
await db.execute(
    text("""INSERT INTO conversations (workspace_id, sender_id, ...)
            VALUES (:wid, :sid, ...)"""),
    {"wid": wid, "sid": str(sender_id), ...}
)
```
</interfaces>

<existing_test_factories>
<!-- From tests/conftest.py (Phase 2 fixtures) -->

- test_workspace: async fixture creating Workspace row, yields workspace_id (UUID)
- test_sender_factory(workspace_id, role='sender'): creates Sender — returns Sender ORM instance
- test_checker(workspace_id): convenience checker fixture
- test_folder(workspace_id): folder fixture
- test_contacts_factory(folder_id, count=N): contacts fixture

Tests should use these fixtures + manually create message_queue rows for queue tests, warmup_pool/warmup_sessions for warmup tests, context_contact_assignments for rotation tests.
</existing_test_factories>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: queue.py + listener.py — workspace_id propagation</name>
  <files>app/services/queue.py, app/services/listener.py, tests/test_queue_workspace_id.py, tests/test_listener_workspace_id.py</files>
  <read_first>
    - app/services/queue.py (полностью — 894 строки; ключевые места: 433-442, 651-660, 685-714, 762-797, 800-837)
    - app/services/listener.py (ключевые места: 365-432 get_or_create_conversation, 446-475 save_message)
    - app/models/__init__.py (lines 73-260 — Sender/MessageQueue/MessageLog/Conversation; lines 169-216 MessageQueue NOT NULL workspace_id)
    - migrations/012_workspace.sql (lines 57-97 — подтверждение workspace_id NOT NULL на senders/messages_log/conversations/message_queue)
    - tests/conftest.py (Phase 2 фабрики: test_workspace, test_sender_factory)
    - tests/test_workspace_router.py (паттерн async-теста с db и фикстурами)
  </read_first>
  <behavior>
    Test 1 (queue): test_enqueue_message_writes_workspace_id — создать sender в test_workspace, вызвать enqueue_message(db, workspace_id=test_workspace.id, sender_id=sender.id, ...), затем SELECT message_queue WHERE id=:queue_id → row.workspace_id == test_workspace.id (НЕ None).
    Test 2 (queue): test_fail_item_writes_messagelog_with_workspace_id — создать item в message_queue, вызвать через мок _fail_item, проверить SELECT messages_log WHERE sender_id=:sid → row.workspace_id == sender.workspace_id.
    Test 3 (listener): test_get_or_create_conversation_writes_workspace_id — вызвать listener.get_or_create_conversation(sender_id=sender.id, ...), проверить SELECT conversations → row.workspace_id == sender.workspace_id.
    Test 4 (queue): test_upsert_conversation_writes_workspace_id — вызвать _upsert_conversation(sender, item, result), проверить conversations.workspace_id == sender.workspace_id.
    Test 5 (queue): test_enqueue_file_writes_workspace_id — аналогично Test 1 для enqueue_file.
  </behavior>
  <action>
**Шаг 1 — queue.py changes (precise edits):**

A) В сигнатуре enqueue_message (line 762) добавить `workspace_id` первым позиционным после db:
```python
async def enqueue_message(
    db: AsyncSession,
    workspace_id,  # ← NEW
    sender_id,
    sender_slug: str,
    ...
) -> dict:
    item = MessageQueue(
        workspace_id=workspace_id,  # ← NEW
        sender_id=sender_id,
        ...
    )
```

B) Аналогично enqueue_file (line 800) — добавить workspace_id и передать в MessageQueue(...).

C) В _send_item.__send_item_inner (line 370) после `r2 = await db.execute(select(Sender)...)` (line 380) у нас уже есть `sender: Sender` — sender.workspace_id доступен. Менять MessageLog(...) на line 434:
```python
log_entry = MessageLog(
    workspace_id=sender.workspace_id,  # ← NEW
    sender_id=sender.id,
    recipient_phone=item.recipient_phone,
    ...
)
```

D) В _upsert_conversation (line 665) — передать sender в SQL: добавить :wid в INSERT INTO conversations (line 685-690):
```python
r2 = await db.execute(
    text("""
        INSERT INTO conversations
            (workspace_id, sender_id, contact_phone, contact_name, contact_telegram_id, ai_enabled, ai_context_id)
        VALUES (:wid, :sid, :phone, :name, :tg_id, true, :ai_ctx)
        RETURNING id
    """),
    {
        "wid": str(sender.workspace_id),  # ← NEW
        "sid": str(sender.id),
        ...
    }
)
```

E) В _fail_item (line 622) при failure-path (line 651) MessageLog получает workspace_id из item.workspace_id (его мы только что записали через enqueue_message). НО: item.workspace_id у нас в ORM-модели — мы SELECT'или `item = MessageQueue` в __send_item_inner и пробросили его, поэтому в _fail_item у нас `item: MessageQueue` доступен. Менять (line 651-660):
```python
if new_status == QueueItemStatus.failed:
    log_entry = MessageLog(
        workspace_id=item.workspace_id,  # ← NEW
        sender_id=item.sender_id,
        ...
    )
```

F) Найти и обновить ВСЕХ caller'ов enqueue_message/enqueue_file в проекте. Запустить `grep -rn "enqueue_message\|enqueue_file" app/ tests/` и в каждом call-site добавить workspace_id первым параметром. Caller'ы будут в context routers (если есть) или в onboarding/senders test'ах. NB: на момент написания плана callers — преимущественно тесты (например test_queue_workspace_id.py будет первым); если найдётся production caller — пробросить workspace_id из AuthCtx.

**Шаг 2 — listener.py changes:**

A) В get_or_create_conversation (line 365): метод получает sender_id (str). Сначала SELECT workspace_id из senders. Изменить line 408-414:

```python
# Создаём новый
# Получаем workspace_id из senders (защита от NotNullViolation после миграции 012)
ws_row = await session.execute(
    text("SELECT workspace_id FROM senders WHERE id = :sid"),
    {"sid": sender_id}
)
ws_result = ws_row.fetchone()
if not ws_result:
    raise SQLAlchemyError(f"Sender {sender_id} not found — cannot create conversation")
workspace_id = str(ws_result[0])

result = await session.execute(
    text("""
        INSERT INTO conversations (workspace_id, sender_id, contact_phone, contact_name, contact_telegram_id, ai_enabled, ai_context_id)
        VALUES (:wid, :sender_id, :phone, :name, :tg_id, true, :ai_context_id)
        RETURNING id
    """),
    {"wid": workspace_id, "sender_id": sender_id, "phone": contact_phone, "name": contact_name, "tg_id": contact_telegram_id, "ai_context_id": ai_context_id}
)
```

NOTE: `messages` (line 449-460) — отдельная таблица, без workspace_id в миграции 012 (она НЕ перечислена в ALTER TABLE списке). Не трогать INSERT INTO messages — там workspace_id колонки НЕТ.

**Шаг 3 — тесты:**

Создать tests/test_queue_workspace_id.py:
```python
"""Regression: queue.py worker INSERTs включают workspace_id (CR-01)."""
import pytest
from sqlalchemy import text
from app.services.queue import enqueue_message, enqueue_file

pytestmark = pytest.mark.asyncio

async def test_enqueue_message_writes_workspace_id(db, test_workspace, test_sender_factory):
    sender = await test_sender_factory(workspace_id=test_workspace.id)
    result = await enqueue_message(
        db,
        workspace_id=test_workspace.id,
        sender_id=sender.id,
        sender_slug=sender.slug,
        recipient_phone="+79991234567",
        recipient_name="Test",
        message_text="hi",
    )
    row = (await db.execute(
        text("SELECT workspace_id FROM message_queue WHERE id = :id"),
        {"id": result["queue_id"]},
    )).fetchone()
    assert str(row[0]) == str(test_workspace.id)

async def test_enqueue_file_writes_workspace_id(db, test_workspace, test_sender_factory):
    sender = await test_sender_factory(workspace_id=test_workspace.id)
    result = await enqueue_file(
        db,
        workspace_id=test_workspace.id,
        sender_id=sender.id,
        sender_slug=sender.slug,
        recipient_phone="+79991234567",
        recipient_name="Test",
        file_url="http://example.com/f.pdf",
        file_name="f.pdf",
        caption=None,
    )
    row = (await db.execute(
        text("SELECT workspace_id FROM message_queue WHERE id = :id"),
        {"id": result["queue_id"]},
    )).fetchone()
    assert str(row[0]) == str(test_workspace.id)
```

Создать tests/test_listener_workspace_id.py:
```python
"""Regression: listener.get_or_create_conversation INSERT с workspace_id (CR-02)."""
import pytest
from sqlalchemy import text
from app.services.listener import TelegramListener

pytestmark = pytest.mark.asyncio

async def test_get_or_create_conversation_writes_workspace_id(db, test_workspace, test_sender_factory):
    sender = await test_sender_factory(workspace_id=test_workspace.id)
    listener = TelegramListener()
    conv = await listener.get_or_create_conversation(
        session=db,
        sender_id=str(sender.id),
        contact_phone="+79991234567",
        contact_name="Test",
        contact_telegram_id=12345,
    )
    row = (await db.execute(
        text("SELECT workspace_id FROM conversations WHERE id = :id"),
        {"id": conv["id"]},
    )).fetchone()
    assert str(row[0]) == str(test_workspace.id)
```
  </action>
  <verify>
    <automated>cd /Users/andrewbruce/Documents/outreach-platform &amp;&amp; python -m pytest tests/test_queue_workspace_id.py tests/test_listener_workspace_id.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "workspace_id" app/services/queue.py` >= 5
    - `grep -c "workspace_id" app/services/listener.py` >= 3
    - `grep -n "workspace_id=workspace_id" app/services/queue.py | wc -l` >= 1 (enqueue_message)
    - `grep -n "workspace_id=sender.workspace_id" app/services/queue.py | wc -l` >= 1 (_send_item MessageLog)
    - `grep -n "workspace_id=item.workspace_id" app/services/queue.py | wc -l` >= 1 (_fail_item)
    - `grep -E "INSERT INTO conversations.*workspace_id" app/services/listener.py` returns match (raw SQL inserts workspace_id first)
    - `python -c "from app.services.queue import enqueue_message; import inspect; sig = inspect.signature(enqueue_message); assert 'workspace_id' in sig.parameters"` succeeds
    - tests/test_queue_workspace_id.py exists, contains test_enqueue_message_writes_workspace_id + test_enqueue_file_writes_workspace_id
    - tests/test_listener_workspace_id.py exists, contains test_get_or_create_conversation_writes_workspace_id
  </acceptance_criteria>
  <done>
    Все 5 регрессионных тестов проходят. INSERT в message_queue, messages_log, conversations включают workspace_id (verified через SELECT после INSERT). Нет caller'ов enqueue_message/enqueue_file без workspace_id (grep подтверждает).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: warmup.py — workspace partitioning + SQL precedence fix</name>
  <files>app/services/warmup.py, tests/test_warmup_workspace_isolation.py</files>
  <read_first>
    - app/services/warmup.py (полностью — 561 строк; ключевые места: 163-189 _get_active_pool, 326-338 INSERT warmup_messages, 393-440 _create_new_sessions, 421-434 INSERT warmup_sessions, 527-534 FloodWait UPDATE)
    - app/models/__init__.py (lines 243-303 WarmupPool/WarmupSession/WarmupMessage; все workspace_id NOT NULL)
    - migrations/012_workspace.sql (lines 99-118 — warmup_* таблицы получили workspace_id NOT NULL)
    - tests/conftest.py
  </read_first>
  <behavior>
    Test 1: test_warmup_messages_insert_writes_workspace_id — два sender'а в одном workspace в warmup_pool → активна сессия → INSERT warmup_messages содержит workspace_id == sender.workspace_id.
    Test 2: test_warmup_sessions_insert_writes_workspace_id — после _create_new_sessions sессии содержат workspace_id == sender_a.workspace_id (= sender_b.workspace_id, т.к. partitioning).
    Test 3 (КРИТИЧНО — cross-tenant leak): test_warmup_pool_no_cross_tenant_pairs — создать sender'ов в двух разных workspaces в warmup_pool. Вызвать _create_new_sessions. Проверить: НИ ОДНА сессия не содержит sender_a.workspace_id != sender_b.workspace_id (SELECT pairs JOIN senders → equality assert).
    Test 4: test_floodwait_update_only_affects_active — создать ДВЕ warmup_sessions для одного sender'а: одну со status='active', другую со status='completed'. Имитировать FloodWait UPDATE (raw SQL вызов). Завершённая сессия не должна получить новый next_message_at — только active.
  </behavior>
  <action>
**Шаг 1 — _get_active_pool: вернуть workspace_id (line 163-189):**

```python
async def _get_active_pool(self, db: AsyncSession) -> list[dict]:
    """Активные участники пула с данными sender'а.

    Phase 02.1 (CR-04 issue 3): workspace_id возвращается, чтобы
    _create_new_sessions мог партиционировать пары — sender'ы из разных
    workspace'ов НЕ должны парироваться (cross-tenant leak).
    """
    result = await db.execute(text("""
        SELECT wp.sender_id, wp.workspace_id, wp.enrolled_at,
               s.slug, s.phone, s.session_string
        FROM warmup_pool wp
        JOIN senders s ON s.id = wp.sender_id AND s.workspace_id = wp.workspace_id
        WHERE wp.is_active = true
          AND s.lifecycle_status = 'active'
          AND s.auth_status = 'ok'
          AND s.role = 'sender'
    """))
    rows = result.fetchall()
    now = datetime.now(timezone.utc)
    return [
        {
            "sender_id":     str(r[0]),
            "workspace_id":  str(r[1]),  # ← NEW
            "enrolled_at":   r[2],
            "slug":          r[3],
            "phone":         r[4],
            "session_string": r[5],
            "enrolled_days": max(0, (now - r[2]).days),
        }
        for r in rows
    ]
```

**Шаг 2 — _create_new_sessions: партиционирование по workspace_id (line 393-440):**

```python
async def _create_new_sessions(self, db: AsyncSession):
    """Создать новые сессии для свободных пар в пуле.

    Phase 02.1 (CR-04 issue 3): пары формируются ВНУТРИ workspace_id —
    sender'ы из разных tenant'ов не должны общаться через warmup.
    """
    pool = await self._get_active_pool(db)
    if len(pool) < 2:
        return

    # Busy ids остаются global (sender может быть в активной сессии в своём workspace)
    busy_result = await db.execute(
        text("SELECT sender_a_id, sender_b_id FROM warmup_sessions WHERE status = 'active'")
    )
    busy_ids: set[str] = set()
    for row in busy_result.fetchall():
        busy_ids.add(str(row[0]))
        busy_ids.add(str(row[1]))

    available = [s for s in pool if s["sender_id"] not in busy_ids]
    if len(available) < 2:
        return

    # ──── CR-04 issue 3 FIX: партиционируем по workspace_id ────
    from itertools import groupby
    available_sorted = sorted(available, key=lambda s: s["workspace_id"])
    pairs = []
    for wsid, group_iter in groupby(available_sorted, key=lambda s: s["workspace_id"]):
        ws_group = list(group_iter)
        if len(ws_group) < 2:
            continue
        random.shuffle(ws_group)
        for i in range(0, len(ws_group) - 1, 2):
            pairs.append((ws_group[i], ws_group[i + 1]))

    for sender_a, sender_b in pairs:
        # Защита-в-глубину: оба sender'а — из одного workspace
        assert sender_a["workspace_id"] == sender_b["workspace_id"], \
            "Cross-tenant warmup pair attempted"
        topic  = random.choice(WARMUP_TOPICS)
        target = random.randint(4, 10)
        first_at = datetime.now(timezone.utc) + timedelta(minutes=random.randint(1, 5))

        await db.execute(
            text("""
                INSERT INTO warmup_sessions
                    (workspace_id, sender_a_id, sender_b_id, topic, target_messages, next_message_at)
                VALUES (:wid, :a, :b, :topic, :target, :first_at)
            """),
            {
                "wid":      sender_a["workspace_id"],  # ← NEW
                "a":        sender_a["sender_id"],
                "b":        sender_b["sender_id"],
                "topic":    topic,
                "target":   target,
                "first_at": first_at,
            }
        )
        logger.info(
            f"🔥 Новая warmup сессия (workspace={sender_a['workspace_id'][:8]}): "
            f"{sender_a['slug']} ↔ {sender_b['slug']} "
            f"(тема: «{topic}», {target} сообщений)"
        )

    await db.commit()
```

**Шаг 3 — _process_session.INSERT warmup_messages с workspace_id (line 326-338):**

Сначала загрузить workspace_id из sender'а — добавить `s.workspace_id` в SELECT line 248-254:

```python
result = await db.execute(
    text("""
        SELECT id, slug, phone, session_string, lifecycle_status, auth_status, workspace_id
        FROM senders WHERE id = ANY(:ids)
    """),
    {"ids": [from_id, to_id]}
)
senders_map = {
    str(r[0]): {
        "id": str(r[0]), "slug": r[1],
        "phone": r[2], "session_string": r[3],
        "lifecycle_status": r[4], "auth_status": r[5],
        "workspace_id": str(r[6]),  # ← NEW
        "is_eligible": (r[4] == "active" and r[5] == "ok"),
    }
    for r in result.fetchall()
}
```

Затем в INSERT warmup_messages (line 327-338) добавить workspace_id:

```python
await db.execute(
    text("""
        INSERT INTO warmup_messages (workspace_id, session_id, from_sender_id, to_sender_id, message_text)
        VALUES (:wid, :session_id, :from_id, :to_id, :text)
    """),
    {
        "wid":        from_sender["workspace_id"],  # ← NEW
        "session_id": session["id"],
        "from_id":    from_id,
        "to_id":      to_id,
        "text":       message_text,
    }
)
```

**Шаг 4 — SQL precedence fix (CR-04 issue 2; line 527-534):**

Обернуть OR в скобки. ВАЖНО: исправление меняет семантику с "active сессий sender_a + ВСЕ сессии sender_b" на "active сессий sender_a + active сессий sender_b" — это и есть правильное поведение.

```python
async with AsyncSessionLocal() as db:
    await db.execute(
        text("""
            UPDATE warmup_sessions
            SET next_message_at = :t, updated_at = NOW()
            WHERE (sender_a_id = :sid OR sender_b_id = :sid)
              AND status = 'active'
        """),
        {"t": retry_at, "sid": from_sender["id"]}
    )
    await db.commit()
```

**Шаг 5 — тесты:**

Создать tests/test_warmup_workspace_isolation.py:
```python
"""Regression: warmup INSERTs пишут workspace_id; pairs партиционированы (CR-04)."""
import pytest
from sqlalchemy import text
from app.services.warmup import WarmupWorker

pytestmark = pytest.mark.asyncio


async def _add_to_pool(db, sender_id, workspace_id):
    await db.execute(
        text("""
            INSERT INTO warmup_pool (workspace_id, sender_id, is_active, enrolled_at)
            VALUES (:wid, :sid, true, NOW() - INTERVAL '5 days')
        """),
        {"wid": str(workspace_id), "sid": str(sender_id)}
    )
    await db.commit()


async def test_warmup_pool_no_cross_tenant_pairs(db, test_workspace_factory, test_sender_factory):
    ws_a = await test_workspace_factory()
    ws_b = await test_workspace_factory()
    s_a1 = await test_sender_factory(workspace_id=ws_a.id)
    s_a2 = await test_sender_factory(workspace_id=ws_a.id)
    s_b1 = await test_sender_factory(workspace_id=ws_b.id)
    s_b2 = await test_sender_factory(workspace_id=ws_b.id)
    for s, w in [(s_a1, ws_a), (s_a2, ws_a), (s_b1, ws_b), (s_b2, ws_b)]:
        await _add_to_pool(db, s.id, w.id)

    worker = WarmupWorker()
    await worker._create_new_sessions(db)

    # Все созданные сессии должны быть intra-workspace
    rows = (await db.execute(text("""
        SELECT ws.workspace_id, sa.workspace_id as sa_wid, sb.workspace_id as sb_wid
        FROM warmup_sessions ws
        JOIN senders sa ON sa.id = ws.sender_a_id
        JOIN senders sb ON sb.id = ws.sender_b_id
    """))).fetchall()
    assert len(rows) > 0
    for row in rows:
        assert row.sa_wid == row.sb_wid, "Cross-tenant pair detected!"
        assert row.workspace_id == row.sa_wid


async def test_floodwait_update_only_affects_active(db, test_workspace, test_sender_factory):
    s = await test_sender_factory(workspace_id=test_workspace.id)
    s2 = await test_sender_factory(workspace_id=test_workspace.id)
    # Создаём 2 сессии: active + completed
    await db.execute(text("""
        INSERT INTO warmup_sessions (workspace_id, sender_a_id, sender_b_id, topic, target_messages, status, next_message_at)
        VALUES (:wid, :a, :b, 'test', 5, 'active', NOW() + INTERVAL '1 hour'),
               (:wid, :a, :b, 'test', 5, 'completed', NOW() - INTERVAL '1 day')
    """), {"wid": str(test_workspace.id), "a": str(s.id), "b": str(s2.id)})
    await db.commit()

    # Применяем fix: скобки + AND
    from datetime import datetime, timezone, timedelta
    retry_at = datetime.now(timezone.utc) + timedelta(hours=2)
    await db.execute(text("""
        UPDATE warmup_sessions
        SET next_message_at = :t, updated_at = NOW()
        WHERE (sender_a_id = :sid OR sender_b_id = :sid)
          AND status = 'active'
    """), {"t": retry_at, "sid": str(s.id)})
    await db.commit()

    rows = (await db.execute(text("""
        SELECT status, next_message_at FROM warmup_sessions
        WHERE sender_a_id = :sid ORDER BY status
    """), {"sid": str(s.id)})).fetchall()
    by_status = {r.status: r.next_message_at for r in rows}
    # Active обновилась
    assert abs((by_status['active'] - retry_at).total_seconds()) < 5
    # Completed НЕ обновилась (т.к. AND status='active' с правильной precedence)
    assert by_status['completed'] < datetime.now(timezone.utc)
```

NOTE: добавить фабрику `test_workspace_factory` в conftest.py если её нет — должна создавать N разных workspaces по требованию. Если уже есть `test_workspace` (single) — обернуть в фабрику-функцию.
  </action>
  <verify>
    <automated>cd /Users/andrewbruce/Documents/outreach-platform &amp;&amp; python -m pytest tests/test_warmup_workspace_isolation.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "workspace_id" app/services/warmup.py` >= 6
    - `grep -nE "INSERT INTO warmup_(sessions|messages)" app/services/warmup.py` показывает обе строки с :wid
    - `grep -nE "\(sender_a_id = :sid OR sender_b_id = :sid\)\s+AND" app/services/warmup.py` находит исправленный UPDATE
    - `grep -nE "groupby.*workspace_id|workspace_id.*groupby" app/services/warmup.py` находит partitioning loop
    - tests/test_warmup_workspace_isolation.py exists, содержит test_warmup_pool_no_cross_tenant_pairs + test_floodwait_update_only_affects_active
    - `python -c "from app.services.warmup import WarmupWorker; w = WarmupWorker(); print('ok')"` succeeds
  </acceptance_criteria>
  <done>
    Все 3 теста проходят. Cross-tenant warmup pairs невозможны (partitioning + assert). FloodWait UPDATE корректно бьёт только active сессии. INSERT в warmup_sessions/warmup_messages пишет workspace_id.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: rotation.py — workspace_id propagation + guard в _pick_best_sender</name>
  <files>app/services/rotation.py, tests/test_rotation_workspace_id.py</files>
  <read_first>
    - app/services/rotation.py (полностью — 165 строк)
    - app/models/__init__.py (model ContextContactAssignment — найти через grep, должен быть в файле)
    - migrations/012_workspace.sql (lines 127-132 — context_contact_assignments workspace_id NOT NULL)
    - migrations/007_context_contact_assignments.sql (исходная таблица)
    - tests/conftest.py
  </read_first>
  <behavior>
    Test 1: test_get_or_assign_sender_writes_workspace_id — создать AI context в workspace A, sender в workspace A с ai_context_id=ctx.id; вызвать get_or_assign_sender(db, ctx_id, '+79991234567', workspace_id=ws_a.id) → SELECT context_contact_assignments → row.workspace_id == ws_a.id.
    Test 2: test_pick_best_sender_workspace_isolation — sender_a из workspace A с ai_context_id=ctx_id; sender_b из workspace B с ai_context_id=ctx_id (теоретически возможно если ctx_id уязвим). _pick_best_sender(db, ctx_id, workspace_id=ws_a.id) → НИКОГДА не вернёт sender_b. Защита-в-глубину против inconsistency между AIContext.workspace_id и Sender.workspace_id.
  </behavior>
  <action>
**Шаг 1 — get_or_assign_sender: принять workspace_id (line 22-129):**

Сигнатура (line 22):
```python
async def get_or_assign_sender(
    db: AsyncSession,
    context_id: UUID,
    contact_phone: str,
    workspace_id: UUID,  # ← NEW
) -> Sender:
```

Все вызовы _pick_best_sender(db, context_id) заменить на _pick_best_sender(db, context_id, workspace_id):
- line 72 (replacement path)
- line 92 (initial path)

INSERT (line 96-103) — добавить workspace_id:
```python
await db.execute(
    text("""
        INSERT INTO context_contact_assignments (workspace_id, context_id, contact_phone, sender_id)
        VALUES (:wid, :ctx_id, :phone, :sid)
        ON CONFLICT (context_id, contact_phone) DO NOTHING
    """),
    {"wid": str(workspace_id), "ctx_id": ctx_str, "phone": contact_phone, "sid": str(best.id)},
)
```

NB: ON CONFLICT (context_id, contact_phone) — оставляем как есть, так как контракт уникальности на этих двух полях. Если уникальность была на тройке (workspace_id, context_id, contact_phone) — проверить через миграцию 007/012; если так — обновить ON CONFLICT.

SELECT в Step 1 (line 47-57) — добавить workspace guard:
```python
row = (await db.execute(
    text("""
        SELECT cca.sender_id,
               (s.lifecycle_status = 'active' AND s.auth_status = 'ok') AS is_eligible
        FROM context_contact_assignments cca
        JOIN senders s ON s.id = cca.sender_id
        WHERE cca.context_id = :ctx_id
          AND cca.contact_phone = :phone
          AND cca.workspace_id = :wid
    """),
    {"ctx_id": ctx_str, "phone": contact_phone, "wid": str(workspace_id)},
)).fetchone()
```

UPDATE (line 76-82) — добавить workspace_id guard:
```python
await db.execute(
    text("""
        UPDATE context_contact_assignments
        SET sender_id = :new_sid, updated_at = NOW()
        WHERE context_id = :ctx_id AND contact_phone = :phone AND workspace_id = :wid
    """),
    {"new_sid": str(new_sender.id), "ctx_id": ctx_str, "phone": contact_phone, "wid": str(workspace_id)},
)
```

Аналогично — последний SELECT re-read (line 107-113) — добавить AND workspace_id = :wid.

**Шаг 2 — _pick_best_sender: workspace_id фильтр (line 132-164):**

```python
async def _pick_best_sender(db: AsyncSession, context_id: UUID, workspace_id: UUID) -> Sender | None:
    """Phase 02.1 (CR-03): добавлен workspace guard — защита-в-глубину
    на случай inconsistency между AIContext.workspace_id и Sender.workspace_id.
    """
    row = (await db.execute(
        text("""
            SELECT s.id
            FROM senders s
            LEFT JOIN message_queue mq
                ON mq.sender_id = s.id
                AND mq.status = 'sent'
                AND mq.finished_at >= NOW() - INTERVAL '24 hours'
            WHERE s.ai_context_id = :ctx_id
              AND s.workspace_id = :wid
              AND s.lifecycle_status = 'active'
              AND s.auth_status = 'ok'
              AND s.role = 'sender'
            GROUP BY s.id, s.created_at
            ORDER BY COUNT(mq.id) ASC, s.created_at ASC
            LIMIT 1
        """),
        {"ctx_id": str(context_id), "wid": str(workspace_id)},
    )).fetchone()

    if row is None:
        return None

    result = await db.execute(select(Sender).where(Sender.id == row[0]))
    return result.scalar_one_or_none()
```

**Шаг 3 — обновить caller'ов get_or_assign_sender:**

```bash
grep -rn "get_or_assign_sender" app/ tests/ --include="*.py"
```

В каждом call-site добавить workspace_id (обычно из ctx.workspace_id, если call-site в router). Если call-site в listener — нужно загрузить workspace_id из senders по sender_id (см. Task 1 listener change).

**Шаг 4 — тест tests/test_rotation_workspace_id.py:**

```python
"""Regression: rotation INSERT/SELECT с workspace_id (CR-03)."""
import pytest
from sqlalchemy import text
from app.services.rotation import get_or_assign_sender, _pick_best_sender

pytestmark = pytest.mark.asyncio


async def test_get_or_assign_sender_writes_workspace_id(db, test_workspace, test_sender_factory, test_ai_context_factory):
    ctx = await test_ai_context_factory(workspace_id=test_workspace.id)
    sender = await test_sender_factory(workspace_id=test_workspace.id, ai_context_id=ctx.id)
    s = await get_or_assign_sender(db, ctx.id, "+79991234567", workspace_id=test_workspace.id)
    assert s.id == sender.id
    row = (await db.execute(
        text("""SELECT workspace_id FROM context_contact_assignments
                WHERE context_id = :cid AND contact_phone = :ph"""),
        {"cid": str(ctx.id), "ph": "+79991234567"},
    )).fetchone()
    assert str(row[0]) == str(test_workspace.id)


async def test_pick_best_sender_isolates_workspaces(db, test_workspace_factory, test_sender_factory, test_ai_context_factory):
    ws_a = await test_workspace_factory()
    ws_b = await test_workspace_factory()
    ctx = await test_ai_context_factory(workspace_id=ws_a.id)
    sender_a = await test_sender_factory(workspace_id=ws_a.id, ai_context_id=ctx.id)
    # sender_b в workspace B "по ошибке" привязан к ctx из workspace A —
    # rotation НЕ должна его возвращать
    sender_b = await test_sender_factory(workspace_id=ws_b.id, ai_context_id=ctx.id)
    result = await _pick_best_sender(db, ctx.id, workspace_id=ws_a.id)
    assert result is not None
    assert result.id == sender_a.id  # не sender_b
```

NB: test_ai_context_factory — добавить в conftest.py если нет (создаёт AIContext в workspace).
  </action>
  <verify>
    <automated>cd /Users/andrewbruce/Documents/outreach-platform &amp;&amp; python -m pytest tests/test_rotation_workspace_id.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "workspace_id" app/services/rotation.py` >= 7
    - `grep -E "INSERT INTO context_contact_assignments.*workspace_id" app/services/rotation.py` returns match
    - `grep -E "s\.workspace_id\s*=\s*:wid" app/services/rotation.py` returns match (in _pick_best_sender)
    - `python -c "from app.services.rotation import get_or_assign_sender; import inspect; sig = inspect.signature(get_or_assign_sender); assert 'workspace_id' in sig.parameters"` succeeds
    - tests/test_rotation_workspace_id.py exists, проходит
    - `grep -rn "get_or_assign_sender" app/ | grep -v "def get_or_assign_sender"` — все callers передают workspace_id (визуальная проверка) ИЛИ нет caller'ов (только тесты)
  </acceptance_criteria>
  <done>
    Тесты проходят. Сигнатура get_or_assign_sender принимает workspace_id. _pick_best_sender фильтрует по workspace_id (даже при inconsistency между AIContext и Sender workspace_id'ах). INSERT в context_contact_assignments содержит workspace_id.
  </done>
</task>

</tasks>

<verification>
**Финальная проверка плана 02.1-01:**

1. Запустить все 3 регрессионных набора тестов:
```bash
cd /Users/andrewbruce/Documents/outreach-platform
python -m pytest tests/test_queue_workspace_id.py tests/test_listener_workspace_id.py tests/test_warmup_workspace_isolation.py tests/test_rotation_workspace_id.py -x -v
```

2. Подтвердить отсутствие caller'ов без workspace_id:
```bash
grep -rn "enqueue_message\|enqueue_file\|get_or_assign_sender" app/ tests/ --include="*.py" | grep -v "def enqueue\|def get_or_assign\|workspace_id"
# expected: 0 строк или только закомментированные
```

3. Smoke: убедиться, что грубо не сломали queue/warmup workers:
```bash
python -c "from app.services.queue import queue_worker, enqueue_message, enqueue_file; from app.services.warmup import warmup_worker; from app.services.rotation import get_or_assign_sender; from app.services.listener import TelegramListener; print('imports ok')"
```

4. Существующие тесты Phase 2 не сломались:
```bash
python -m pytest tests/test_senders.py tests/test_contacts.py tests/test_folders.py tests/test_listener_reconcile.py -x
```
</verification>

<success_criteria>
- Все 9 acceptance criteria по 3 task'ам выполнены (grep + tests)
- CR-01 закрыт: queue.py все INSERT (message_queue, messages_log, conversations) пишут workspace_id
- CR-02 закрыт: listener.get_or_create_conversation пишет workspace_id из senders
- CR-03 закрыт: rotation.get_or_assign_sender принимает workspace_id; INSERT context_contact_assignments + SELECT _pick_best_sender используют workspace guard
- CR-04 issue 1 закрыт: warmup INSERT warmup_sessions/warmup_messages пишут workspace_id
- CR-04 issue 2 закрыт: SQL precedence в FloodWait UPDATE — скобки правильные
- CR-04 issue 3 закрыт: _create_new_sessions партиционирует pool по workspace_id, cross-tenant pairs невозможны
- CR-06 частично закрыт: workspace_id присутствует в INSERT/SELECT/UPDATE worker'ов (не RLS, но app-level guard добавлен)
- Phase 02 existing tests НЕ сломаны
- Никаких NotNullViolation в multi-tenant сценариях
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-worker-hardening/02.1-01-SUMMARY.md` summarising:
- Изменения в 4 файлах (queue.py, listener.py, warmup.py, rotation.py)
- Количество новых тестов и какие CR-XX они закрывают
- Любые caller'ы за пределами worker'ов, которые потребовали обновления
- Открытые TODO (например test_workspace_factory если добавлялась в conftest)
</output>
