---
phase: 02.1-worker-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/014_phase2_1_hardening.sql
  - app/models/__init__.py
  - app/routers/onboarding.py
  - app/services/onboarding_state.py
  - tests/test_onboarding_reauth.py
  - tests/test_migration_014.py
autonomous: true
gap_closure: true
requirements:
  - ONBD-01
  - SNDR-01
must_haves:
  truths:
    - "Reauth flow для existing sender обновляет session_string + auth_status='ok', НЕ создаёт второго sender'а в БД (CR-05)"
    - "Second reauth того же sender_slug проходит без IntegrityError (CR-05)"
    - "Sender.slug уникален в пределах workspace_id, но НЕ глобально — два workspace могут иметь sender со slug='sender-john' (WR-02)"
    - "Reauth с QR работает идентично reauth с SMS — обновляет существующего sender'а (CR-05)"
    - "Onboarding sessions имеют поле original_sender_id для маркировки reauth-flow"
  artifacts:
    - path: "migrations/014_phase2_1_hardening.sql"
      provides: "DROP global UNIQUE на senders.slug; CREATE UNIQUE INDEX (workspace_id, slug); ALTER onboarding_sessions ADD original_sender_id"
      contains: "IF NOT EXISTS"
    - path: "app/models/__init__.py"
      provides: "Sender.slug убран unique=True; OnboardingSession.original_sender_id добавлен"
      contains: "slug = Column"
    - path: "app/routers/onboarding.py"
      provides: "_refresh_sender_session helper; verify_code/verify_2fa/_wait_for_qr ветка UPDATE для reauth"
      contains: "_refresh_sender_session"
    - path: "app/services/onboarding_state.py"
      provides: "save_state принимает original_sender_id; load_state возвращает row с original_sender_id"
      contains: "original_sender_id"
    - path: "tests/test_onboarding_reauth.py"
      provides: "Reauth integration test — двойной reauth не падает с IntegrityError"
  key_links:
    - from: "app/routers/onboarding.py::reauth_start"
      to: "onboarding_sessions.original_sender_id"
      via: "save_state(... original_sender_id=sender.id)"
      pattern: "original_sender_id=sender\\.id"
    - from: "app/routers/onboarding.py::verify_code"
      to: "_refresh_sender_session OR _create_sender_from_session"
      via: "if session_row.original_sender_id: refresh ELSE create"
      pattern: "original_sender_id"
    - from: "migrations/014_phase2_1_hardening.sql"
      to: "senders unique constraint"
      via: "DROP CONSTRAINT senders_slug_key + CREATE UNIQUE INDEX (workspace_id, slug)"
      pattern: "idx_senders_workspace_slug"
---

<objective>
Закрыть BLOCKER CR-05 + WARNING WR-02: `senders.slug` сейчас globally UNIQUE → reauth существующего sender'а через `/reauth/{sender_slug}` падает с IntegrityError при попытке создать НОВОГО sender'а с тем же slug.

Решение в два шага:
1. **Миграция 014:** drop global UNIQUE constraint на `senders.slug`, добавить partial UNIQUE INDEX (workspace_id, slug). Также добавить `onboarding_sessions.original_sender_id` (nullable FK на senders).
2. **Reauth flow rewrite:** при `/reauth/{sender_slug}` сохранять `original_sender_id` в onboarding_sessions; в verify-code/verify-2fa/qr-flow ветка: если `original_sender_id IS NOT NULL` — вызывать `_refresh_sender_session` (UPDATE), иначе `_create_sender_from_session` (INSERT).

Purpose: Reauth — критичный UX-флоу для аккаунтов с истёкшей сессией (session_expired). Без этого фикса пользователь, у которого слетел Telegram-аккаунт, не может его восстановить через UI — нужно сначала удалить sender'а вручную из БД и онбордить заново, что неприемлемо.

Output: Working reauth flow + integration test покрывающий двойной reauth.
</objective>

<execution_context>
@/Users/andrewbruce/Documents/outreach-platform/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-tg-accounts-contacts/02-REVIEW.md
@.planning/phases/02-tg-accounts-contacts/02-VERIFICATION.md
@./CLAUDE.md

# Files to modify
@app/routers/onboarding.py
@app/services/onboarding_state.py
@app/models/__init__.py

# Pattern references
@migrations/013_phase2.sql
@migrations/012_workspace.sql
@tests/test_onboarding.py
@tests/test_onboarding_state.py

<interfaces>
<!-- Reauth call chain -->

```python
# Current (BROKEN — CR-05):
# 1. POST /reauth/{sender_slug}
#    sender = _load_sender_for_reauth(slug, workspace_id)
#    session_id = save_state(db, phone, ..., role=sender.role)
#    returns {session_id, status: "code_sent"}
#
# 2. POST /verify-code with session_id
#    new_sender = _create_sender_from_session(db, ctx, session_row, client)
#    ↑ THIS FAILS — INSERT with slug='sender-{telegram_id}' (same as existing) → IntegrityError
#    delete_session(...)
#    returns {sender_id, slug}

# Fixed (CR-05):
# 1. POST /reauth/{sender_slug}
#    sender = _load_sender_for_reauth(slug, workspace_id)
#    session_id = save_state(db, phone, ..., role=sender.role, original_sender_id=sender.id)  # ← NEW
#
# 2. POST /verify-code with session_id
#    if session_row.original_sender_id:
#        # REAUTH BRANCH
#        sender = await _load_existing_sender(db, session_row.original_sender_id, ctx.workspace_id)
#        await _refresh_sender_session(db, sender, client, session_row)
#    else:
#        # NORMAL ONBOARDING BRANCH
#        sender = await _create_sender_from_session(db, ctx, session_row, client)
#    delete_session(...)
```

OnboardingSession new field:
```python
class OnboardingSession(Base):
    ...
    original_sender_id = Column(UUID(as_uuid=True),
                                ForeignKey("senders.id", ondelete="CASCADE"),
                                nullable=True)  # NEW Phase 02.1 (CR-05)
```

Sender model change:
```python
# BEFORE (line 80):
slug = Column(String(50), unique=True, nullable=False, index=True)

# AFTER (WR-02):
slug = Column(String(50), nullable=False, index=True)  # uniqueness moved to (workspace_id, slug) UNIQUE INDEX
```
</interfaces>

<migration_pattern>
<!-- migrations/013_phase2.sql проверка идемпотентности и стиля -->

```sql
BEGIN;

-- Drop legacy global UNIQUE на slug
ALTER TABLE senders DROP CONSTRAINT IF EXISTS senders_slug_key;

-- Add partial UNIQUE (workspace_id, slug)
CREATE UNIQUE INDEX IF NOT EXISTS idx_senders_workspace_slug
    ON senders(workspace_id, slug);

-- Add original_sender_id to onboarding_sessions (CR-05)
ALTER TABLE onboarding_sessions
    ADD COLUMN IF NOT EXISTS original_sender_id UUID
        REFERENCES senders(id) ON DELETE CASCADE;

CREATE INDEX IF NOT EXISTS idx_onboarding_sessions_original_sender_id
    ON onboarding_sessions(original_sender_id)
    WHERE original_sender_id IS NOT NULL;

COMMIT;
```
</migration_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Миграция 014 + ORM updates</name>
  <files>migrations/014_phase2_1_hardening.sql, app/models/__init__.py, tests/test_migration_014.py</files>
  <read_first>
    - migrations/013_phase2.sql (паттерн idempotent BEGIN/COMMIT + IF NOT EXISTS)
    - migrations/012_workspace.sql (паттерн ALTER TABLE ADD COLUMN IF NOT EXISTS)
    - app/models/__init__.py (lines 73-99 Sender; найти OnboardingSession через grep)
    - tests/test_migration_013.py (паттерн миграционного теста)
  </read_first>
  <action>
**Шаг 1 — создать migrations/014_phase2_1_hardening.sql:**

```sql
-- migrations/014_phase2_1_hardening.sql
-- Phase 02.1: Multi-tenant Worker Hardening
-- Closes: CR-05 (reauth flow IntegrityError) + WR-02 (sender.slug global UNIQUE leak)
-- Changes:
--   1. DROP global UNIQUE on senders.slug → CREATE UNIQUE INDEX (workspace_id, slug)
--   2. ADD onboarding_sessions.original_sender_id (nullable FK on senders)
-- БД должна быть пустой или совместимой с idempotent изменениями.
-- Все операторы идемпотентны (IF NOT EXISTS / IF EXISTS).

BEGIN;

-- ── 1. Sender.slug: global UNIQUE → per-workspace UNIQUE (WR-02) ────────────
-- Legacy constraint name from initial schema: senders_slug_key.
-- В тестовой/чистой БД constraint может отсутствовать — IF EXISTS защищает.
ALTER TABLE senders DROP CONSTRAINT IF EXISTS senders_slug_key;

-- Также убираем возможный legacy unique index (на случай если schema создавалась без named constraint)
DROP INDEX IF EXISTS senders_slug_key;
DROP INDEX IF EXISTS idx_senders_slug;

-- Создаём per-workspace UNIQUE — теперь sender-john может существовать в каждом workspace
CREATE UNIQUE INDEX IF NOT EXISTS idx_senders_workspace_slug
    ON senders(workspace_id, slug);

-- ── 2. onboarding_sessions.original_sender_id (CR-05) ──────────────────────
-- Маркер reauth-flow: если NOT NULL → verify-code должен UPDATE'ить existing sender, не INSERT'ить.
ALTER TABLE onboarding_sessions
    ADD COLUMN IF NOT EXISTS original_sender_id UUID
        REFERENCES senders(id) ON DELETE CASCADE;

CREATE INDEX IF NOT EXISTS idx_onboarding_sessions_original_sender_id
    ON onboarding_sessions(original_sender_id)
    WHERE original_sender_id IS NOT NULL;

COMMIT;
```

**Шаг 2 — обновить app/models/__init__.py:**

A) Sender.slug (line 80) — убрать unique=True:
```python
slug = Column(String(50), nullable=False, index=True)  # Phase 02.1 (WR-02): uniqueness moved to (workspace_id, slug) UNIQUE INDEX
```

B) Найти класс OnboardingSession (через grep на `class OnboardingSession`) и добавить поле:
```python
class OnboardingSession(Base):
    __tablename__ = "onboarding_sessions"

    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    workspace_id = Column(UUID(as_uuid=True),
                          ForeignKey("workspaces.id", ondelete="CASCADE"),
                          nullable=False)
    phone = Column(String(20), nullable=False)
    phone_code_hash = Column(Text, nullable=False)
    encrypted_session_string = Column(Text, nullable=False)
    role = Column(String(20), nullable=False, server_default='sender')
    proxy = Column(JSONB, nullable=True)
    status = Column(String(20), nullable=False)
    # NEW Phase 02.1 (CR-05): reauth marker
    original_sender_id = Column(UUID(as_uuid=True),
                                ForeignKey("senders.id", ondelete="CASCADE"),
                                nullable=True)
    expires_at = Column(DateTime(timezone=True), nullable=False)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
```

**Шаг 3 — tests/test_migration_014.py:**

```python
"""Smoke test: migration 014 — slug per-workspace UNIQUE + onboarding_sessions.original_sender_id."""
import pytest
from pathlib import Path
from sqlalchemy import text

pytestmark = pytest.mark.asyncio


async def test_migration_014_applies_clean(raw_db_engine):
    """Применить 012 → 013 → 014 на пустой БД, проверить schema."""
    migrations_dir = Path("migrations")
    for mig in ("012_workspace.sql", "013_phase2.sql", "014_phase2_1_hardening.sql"):
        sql = (migrations_dir / mig).read_text()
        async with raw_db_engine.begin() as conn:
            await conn.exec_driver_sql(sql)

    # Verify: senders_slug_key constraint удалён
    async with raw_db_engine.connect() as conn:
        rows = (await conn.execute(text(
            "SELECT conname FROM pg_constraint WHERE conrelid = 'senders'::regclass AND conname = 'senders_slug_key'"
        ))).fetchall()
        assert len(rows) == 0, "senders_slug_key constraint должен быть удалён"

        # Verify: idx_senders_workspace_slug существует
        rows = (await conn.execute(text(
            "SELECT indexname FROM pg_indexes WHERE tablename = 'senders' AND indexname = 'idx_senders_workspace_slug'"
        ))).fetchall()
        assert len(rows) == 1, "idx_senders_workspace_slug должен существовать"

        # Verify: onboarding_sessions.original_sender_id колонка есть
        rows = (await conn.execute(text(
            """SELECT column_name FROM information_schema.columns
               WHERE table_name = 'onboarding_sessions' AND column_name = 'original_sender_id'"""
        ))).fetchall()
        assert len(rows) == 1


async def test_migration_014_idempotent(raw_db_engine):
    """Повторное применение 014 на той же БД не падает (IF NOT EXISTS / IF EXISTS)."""
    sql = Path("migrations/014_phase2_1_hardening.sql").read_text()
    async with raw_db_engine.begin() as conn:
        await conn.exec_driver_sql(sql)
        await conn.exec_driver_sql(sql)  # повторно — должно пройти


async def test_per_workspace_slug_uniqueness(db, test_workspace_factory, test_sender_factory):
    """Два workspace могут иметь sender со одинаковым slug."""
    ws_a = await test_workspace_factory()
    ws_b = await test_workspace_factory()
    s_a = await test_sender_factory(workspace_id=ws_a.id, slug="sender-shared")
    s_b = await test_sender_factory(workspace_id=ws_b.id, slug="sender-shared")
    assert s_a.id != s_b.id
    assert s_a.slug == s_b.slug == "sender-shared"
```

NB: raw_db_engine — fixture, аналогичная test_migration_013.py. Если её нет — посмотреть как сделано в test_migration_013.py и переиспользовать.
  </action>
  <verify>
    <automated>cd /Users/andrewbruce/Documents/outreach-platform &amp;&amp; python -m pytest tests/test_migration_014.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f migrations/014_phase2_1_hardening.sql` succeeds
    - `grep -c "IF NOT EXISTS\|IF EXISTS" migrations/014_phase2_1_hardening.sql` >= 4 (идемпотентность)
    - `grep "BEGIN;" migrations/014_phase2_1_hardening.sql` returns match
    - `grep "COMMIT;" migrations/014_phase2_1_hardening.sql` returns match
    - `grep "DROP CONSTRAINT IF EXISTS senders_slug_key" migrations/014_phase2_1_hardening.sql` returns match
    - `grep "idx_senders_workspace_slug" migrations/014_phase2_1_hardening.sql` returns match
    - `grep "original_sender_id" migrations/014_phase2_1_hardening.sql` returns match
    - `grep -n "slug = Column.*unique=True" app/models/__init__.py` returns 0 matches (unique=True убрано)
    - `grep -n "original_sender_id" app/models/__init__.py` returns >= 1 match (поле добавлено)
    - `python -c "from app.models import Sender, OnboardingSession; print(OnboardingSession.__table__.c.original_sender_id)"` succeeds
    - tests/test_migration_014.py проходит
  </acceptance_criteria>
  <done>
    Миграция 014 применяется идемпотентно, schema корректна (constraint удалён, UNIQUE INDEX создан, колонка добавлена). Sender ORM не имеет unique=True на slug. OnboardingSession ORM имеет original_sender_id поле.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reauth flow — _refresh_sender_session + ветка UPDATE в verify-code/verify-2fa/qr</name>
  <files>app/routers/onboarding.py, app/services/onboarding_state.py, tests/test_onboarding_reauth.py</files>
  <read_first>
    - app/routers/onboarding.py (полностью — особенно lines 200-260, 374-460, 461-514, 579-630, 681-811)
    - app/services/onboarding_state.py (полностью — функции save_state / load_state / update_status)
    - tests/test_onboarding.py (паттерн mock'а Telethon + integration test)
    - tests/test_onboarding_state.py (паттерн для save_state теста)
    - app/utils/auth.py (AuthCtx — для понимания ctx fields)
  </read_first>
  <behavior>
    Test 1: test_reauth_creates_session_with_original_sender_id — POST /reauth/{slug} → onboarding_sessions row.original_sender_id == sender.id.
    Test 2: test_reauth_verify_code_updates_existing_sender — реauth сессия → verify-code → SELECT senders → ровно 1 строка (НЕ 2); sender.session_string обновлён; sender.auth_status == 'ok'.
    Test 3 (КРИТИЧНО — bug repro): test_double_reauth_does_not_raise_integrity_error — два последовательных reauth того же sender_slug проходят без IntegrityError.
    Test 4: test_normal_onboarding_still_creates_sender — POST /start (NOT reauth) → verify-code → новый Sender создан (regression: не сломали обычный onboarding).
    Test 5: test_qr_reauth_updates_existing_sender — POST /reauth/qr/{slug} → _wait_for_qr → existing sender обновлён, новый НЕ создан.
  </behavior>
  <action>
**Шаг 1 — app/services/onboarding_state.py: save_state принимает original_sender_id.**

A) Найти save_state (через grep). Сигнатуру:
```python
async def save_state(
    db: AsyncSession,
    workspace_id: UUID,
    phone: str,
    phone_code_hash: str,
    session_string: str,
    role: str = "sender",
    proxy: Optional[dict] = None,
    original_sender_id: Optional[UUID] = None,  # ← NEW
) -> UUID:
    """..."""
    row = OnboardingSession(
        workspace_id=workspace_id,
        phone=phone,
        phone_code_hash=phone_code_hash,
        encrypted_session_string=encrypt_session(session_string),
        role=role,
        proxy=proxy,
        status="code_sent",
        original_sender_id=original_sender_id,  # ← NEW
        expires_at=datetime.now(timezone.utc) + timedelta(minutes=10),
    )
    db.add(row)
    ...
```

B) Если load_state возвращает только подмножество колонок — убедиться, что original_sender_id в SELECT'е (или возвращать ORM-row целиком). Скорее всего возвращает ORM-объект OnboardingSession — тогда original_sender_id доступен автоматически.

**Шаг 2 — app/routers/onboarding.py: новый helper _refresh_sender_session.**

Добавить рядом с _create_sender_from_session (около line 202):

```python
async def _load_existing_sender(
    db: AsyncSession,
    sender_id: UUID,
    workspace_id: UUID,
) -> Optional[Sender]:
    """Reauth helper — загрузить sender с workspace guard. Returns None если не найден."""
    result = await db.execute(
        select(Sender).where(
            Sender.id == sender_id,
            Sender.workspace_id == workspace_id,
        )
    )
    return result.scalars().first()


async def _refresh_sender_session(
    db: AsyncSession,
    sender: Sender,
    client: TelegramClient,
    session_row: OnboardingSession,
) -> Sender:
    """Phase 02.1 (CR-05) — reauth: UPDATE существующего sender'а вместо INSERT.

    Обновляем session_string + auth_status='ok'. Slug/name/role/workspace_id
    остаются неизменными. Proxy обновляется если она передана через onboarding.
    """
    sender.session_string = encrypt_session(client.session.save())
    sender.auth_status = "ok"
    # Proxy может смениться в reauth flow (например пользователь поменял proxy_id)
    if session_row.proxy is not None:
        sender.proxy = session_row.proxy
    await db.commit()
    await db.refresh(sender)
    logger.info(
        f"[onboarding] sender refreshed (reauth) slug={sender.slug} "
        f"workspace={str(sender.workspace_id)[:8]} sender_id={str(sender.id)[:8]}"
    )
    return sender
```

**Шаг 3 — app/routers/onboarding.py: ветка reauth в verify_code (line 447).**

Заменить line 447-453:
```python
    # CR-05: reauth branch
    if session_row.original_sender_id is not None:
        existing = await _load_existing_sender(
            db, session_row.original_sender_id, ctx.workspace_id
        )
        if existing is None:
            # Original sender удалён пока шёл reauth — не блокируем юзера, создаём новый
            logger.warning(
                f"[onboarding] reauth: original_sender_id={session_row.original_sender_id} "
                f"not found in workspace, falling back to create"
            )
            sender = await _create_sender_from_session(
                db, ctx, session_row, client, name=request.name
            )
        else:
            sender = await _refresh_sender_session(db, existing, client, session_row)
    else:
        sender = await _create_sender_from_session(
            db, ctx, session_row, client, name=request.name
        )
    await update_status(db, session_row.id, "completed")
    await _safe_disconnect(client)
    _drop_in_process(session_row.id)
    await delete_session(db, session_row.id)
    return {
        "status": "success",
        "sender_id": str(sender.id),
        "slug": sender.slug,
    }
```

**Шаг 4 — app/routers/onboarding.py: аналогичная ветка в verify_2fa (line 502).**

Заменить:
```python
    if session_row.original_sender_id is not None:
        existing = await _load_existing_sender(
            db, session_row.original_sender_id, ctx.workspace_id
        )
        if existing is None:
            sender = await _create_sender_from_session(
                db, ctx, session_row, client, name=request.name
            )
        else:
            sender = await _refresh_sender_session(db, existing, client, session_row)
    else:
        sender = await _create_sender_from_session(
            db, ctx, session_row, client, name=request.name
        )
    await update_status(db, session_row.id, "completed")
    await _safe_disconnect(client)
    _drop_in_process(session_row.id)
    await delete_session(db, session_row.id)
    return {...}
```

**Шаг 5 — app/routers/onboarding.py: ветка в _wait_for_qr (line 599-617).**

```python
async def _wait_for_qr(
    session_id: UUID,
    client: TelegramClient,
    workspace_id: UUID,
    qr_login,
):
    """Background task — waits for the user to scan the QR (D-17 QR caveat)."""
    try:
        try:
            await qr_login.wait(timeout=120)
        except SessionPasswordNeededError:
            async with AsyncSessionLocal() as db:
                await update_status(
                    db,
                    session_id,
                    "awaiting_2fa",
                    encrypted_session_string=encrypt_session(client.session.save()),
                )
            return

        async with AsyncSessionLocal() as db:
            row = (
                await db.execute(
                    select(OnboardingSession).where(
                        OnboardingSession.id == session_id,
                    )
                )
            ).scalars().first()
            if row is None:
                return

            class _Ctx:
                def __init__(self, wid: UUID):
                    self.workspace_id = wid
            ctx = _Ctx(workspace_id)

            # CR-05 reauth branch (QR)
            if row.original_sender_id is not None:
                existing = await _load_existing_sender(db, row.original_sender_id, workspace_id)
                if existing is None:
                    await _create_sender_from_session(db, ctx, row, client)
                else:
                    await _refresh_sender_session(db, existing, client, row)
            else:
                await _create_sender_from_session(db, ctx, row, client)

            await update_status(db, session_id, "completed")
            await delete_session(db, session_id)
    except Exception as e:  # noqa: BLE001
        logger.error(f"[onboarding] _wait_for_qr error: {e}", exc_info=True)
        async with AsyncSessionLocal() as db:
            try:
                await update_status(db, session_id, "failed")
            except Exception:  # noqa: BLE001
                pass
    finally:
        _drop_in_process(session_id)
        await _safe_disconnect(client)
```

**Шаг 6 — app/routers/onboarding.py: reauth_start передаёт original_sender_id (line 703-758).**

В reauth_start (line 743-758) изменить save_state вызов:
```python
    session_string = client.session.save()
    session_id = await save_state(
        db,
        workspace_id=ctx.workspace_id,
        phone=phone,
        phone_code_hash=sent_code.phone_code_hash,
        session_string=session_string,
        role=sender.role,
        proxy=sender.proxy,
        original_sender_id=sender.id,  # ← NEW
    )
```

Аналогично в reauth_qr (line 791-799):
```python
    session_id = await save_state(
        db,
        workspace_id=ctx.workspace_id,
        phone=sender.phone or "",
        phone_code_hash="",
        session_string=session_string,
        role=sender.role,
        proxy=sender.proxy,
        original_sender_id=sender.id,  # ← NEW
    )
```

**Шаг 7 — tests/test_onboarding_reauth.py:**

```python
"""Phase 02.1 (CR-05): reauth flow — UPDATE existing sender, не создаёт второго."""
import pytest
from unittest.mock import AsyncMock, MagicMock, patch
from sqlalchemy import text
from app.services.onboarding_state import save_state, load_state

pytestmark = pytest.mark.asyncio


async def test_save_state_persists_original_sender_id(db, test_workspace, test_sender_factory):
    sender = await test_sender_factory(workspace_id=test_workspace.id)
    session_id = await save_state(
        db,
        workspace_id=test_workspace.id,
        phone="+79001234567",
        phone_code_hash="hash123",
        session_string="dummy_session_str",
        role="sender",
        proxy=None,
        original_sender_id=sender.id,
    )
    row = await load_state(db, session_id, test_workspace.id)
    assert row is not None
    assert row.original_sender_id == sender.id


async def test_reauth_branch_updates_existing_sender(db, test_workspace, test_sender_factory):
    """Прямой тест helper'а _refresh_sender_session — UPDATE а не INSERT."""
    from app.routers.onboarding import _refresh_sender_session, _load_existing_sender
    from app.models import OnboardingSession

    sender = await test_sender_factory(workspace_id=test_workspace.id)
    original_session = sender.session_string
    original_id = sender.id

    # Mock TelegramClient.session.save()
    mock_client = MagicMock()
    mock_client.session.save = MagicMock(return_value="new_session_string_xyz")

    # Создаём reauth-OnboardingSession row
    row = OnboardingSession(
        workspace_id=test_workspace.id,
        phone=sender.phone,
        phone_code_hash="",
        encrypted_session_string="dummy",
        role=sender.role,
        proxy=None,
        status="code_sent",
        original_sender_id=sender.id,
        expires_at=__import__('datetime').datetime.now(__import__('datetime').timezone.utc) + __import__('datetime').timedelta(minutes=10),
    )
    db.add(row)
    await db.commit()

    existing = await _load_existing_sender(db, sender.id, test_workspace.id)
    assert existing.id == original_id
    refreshed = await _refresh_sender_session(db, existing, mock_client, row)

    # ID того же sender'а (UPDATE а не INSERT)
    assert refreshed.id == original_id
    # session_string обновлён (encrypted, не равен исходному)
    assert refreshed.session_string != original_session
    # auth_status вернулся в 'ok'
    assert refreshed.auth_status == "ok"

    # В БД ровно один sender со slug=sender.slug
    count = (await db.execute(text(
        "SELECT COUNT(*) FROM senders WHERE slug = :s AND workspace_id = :w"
    ), {"s": sender.slug, "w": str(test_workspace.id)})).scalar()
    assert count == 1, f"Ожидался 1 sender, найдено {count} (CR-05 regression!)"


async def test_double_refresh_does_not_break(db, test_workspace, test_sender_factory):
    """Двойной reauth подряд — не падает с IntegrityError."""
    from app.routers.onboarding import _refresh_sender_session, _load_existing_sender
    from app.models import OnboardingSession

    sender = await test_sender_factory(workspace_id=test_workspace.id)

    mock_client = MagicMock()
    mock_client.session.save = MagicMock(side_effect=["session_v1", "session_v2"])

    for i in range(2):
        row = OnboardingSession(
            workspace_id=test_workspace.id,
            phone=sender.phone,
            phone_code_hash=f"hash_v{i}",
            encrypted_session_string="dummy",
            role=sender.role,
            proxy=None,
            status="code_sent",
            original_sender_id=sender.id,
            expires_at=__import__('datetime').datetime.now(__import__('datetime').timezone.utc) + __import__('datetime').timedelta(minutes=10),
        )
        db.add(row)
        await db.commit()
        existing = await _load_existing_sender(db, sender.id, test_workspace.id)
        await _refresh_sender_session(db, existing, mock_client, row)

    # Не должно быть IntegrityError. Проверяем: всё ещё 1 sender.
    count = (await db.execute(text(
        "SELECT COUNT(*) FROM senders WHERE id = :sid"
    ), {"sid": str(sender.id)})).scalar()
    assert count == 1
```

NB: Для теста test_normal_onboarding_still_creates_sender — он покрывается существующими tests/test_onboarding.py через mock'нутый Telethon-flow. Если existing test полагается на slug=unique globally, нужно его перепроверить и адаптировать.
  </action>
  <verify>
    <automated>cd /Users/andrewbruce/Documents/outreach-platform &amp;&amp; python -m pytest tests/test_onboarding_reauth.py tests/test_onboarding.py tests/test_onboarding_state.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_refresh_sender_session" app/routers/onboarding.py | wc -l` >= 4 (def + 3 caller branches: verify_code, verify_2fa, _wait_for_qr)
    - `grep -n "_load_existing_sender" app/routers/onboarding.py | wc -l` >= 4
    - `grep -n "original_sender_id" app/routers/onboarding.py | wc -l` >= 5 (saving + 3 branches + helper)
    - `grep -n "original_sender_id" app/services/onboarding_state.py | wc -l` >= 2
    - `python -c "from app.services.onboarding_state import save_state; import inspect; sig = inspect.signature(save_state); assert 'original_sender_id' in sig.parameters"` succeeds
    - tests/test_onboarding_reauth.py содержит test_save_state_persists_original_sender_id + test_reauth_branch_updates_existing_sender + test_double_refresh_does_not_break
    - Регрессия: tests/test_onboarding.py всё ещё проходит (existing onboarding не сломан)
    - Регрессия: tests/test_onboarding_state.py всё ещё проходит
  </acceptance_criteria>
  <done>
    Все 3 новых теста + existing onboarding тесты проходят. _refresh_sender_session использует UPDATE (не INSERT). Reauth flow корректно различает new onboarding (original_sender_id IS NULL) и reauth (NOT NULL).
  </done>
</task>

</tasks>

<verification>
**Финальная проверка плана 02.1-02:**

1. Полный pytest сессии test_onboarding + test_migration:
```bash
cd /Users/andrewbruce/Documents/outreach-platform
python -m pytest tests/test_onboarding_reauth.py tests/test_migration_014.py tests/test_onboarding.py tests/test_onboarding_state.py tests/test_senders.py -x -v
```

2. Smoke: imports не сломаны:
```bash
python -c "from app.routers.onboarding import _refresh_sender_session, _load_existing_sender; from app.services.onboarding_state import save_state, load_state; from app.models import OnboardingSession, Sender; print('ok')"
```

3. ORM/Schema sync — Sender.slug больше не глобально unique:
```bash
python -c "
from app.models import Sender
col = Sender.__table__.c.slug
assert col.unique is None or col.unique is False, 'Sender.slug should NOT have unique=True (per WR-02)'
print('slug uniqueness moved to (workspace_id, slug) UNIQUE INDEX — ok')
"
```

4. Миграция применяется в чистой БД (если есть testcontainer / docker compose):
```bash
# Если есть docker compose с testdb:
docker compose exec db psql -U postgres -d testdb -c "BEGIN; \i migrations/014_phase2_1_hardening.sql; ROLLBACK;"
```
</verification>

<success_criteria>
- CR-05 закрыт: reauth flow для existing sender обновляет session_string + auth_status='ok' через _refresh_sender_session (UPDATE), а не INSERT'ит нового
- WR-02 закрыт: миграция 014 переводит UNIQUE на slug в per-workspace (workspace_id, slug); ORM Sender.slug без unique=True
- onboarding_sessions имеет колонку original_sender_id (nullable FK)
- save_state принимает original_sender_id; reauth_start/reauth_qr передают sender.id
- Двойной reauth работает без IntegrityError (тест-репро)
- Регрессия: обычный onboarding (POST /start → verify-code → create) всё ещё работает
- Все 8 acceptance criteria task'ов выполнены
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-worker-hardening/02.1-02-SUMMARY.md` summarising:
- Миграция 014: список изменений schema (slug + onboarding_sessions)
- Reauth helper: _refresh_sender_session signature + 3 ветки caller'ов
- Тесты: новые + проверенные existing
- Любые edge cases (например: что делать если original_sender удалён пока шёл reauth)
- Открытые TODO (например test_workspace_factory если добавлялась)
</output>
