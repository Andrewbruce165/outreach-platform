---
phase: 15-account-warmup-via-inter-account-ai-chat
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - app/services/warmup.py
autonomous: true
requirements: [WARM-03, WARM-06, WARM-10, WARM-12, WARM-13, WARM-14]
must_haves:
  truths:
    - "A workspace with warmup_settings.enabled=false (or no row) produces zero new warmup sessions on a tick"
    - "Empty/absent warmup_settings resolve to the 24 RU WARMUP_TOPICS + WARMUP_SYSTEM_PROMPT (behaviour unchanged when unconfigured)"
    - "A sender that is spam_limited/frozen or has a future restricted_until is excluded from warmup pool selection"
    - "Warmup sends stay on the direct-Telethon path and never route through message_queue (campaign rate-limits untouched)"
  artifacts:
    - path: "app/services/warmup.py"
      provides: "enabled-gated pool selection, content-default resolver, restriction-skip clause"
      contains: "warmup_settings"
  key_links:
    - from: "app/services/warmup.py::_get_active_pool"
      to: "warmup_settings.enabled"
      via: "JOIN/filter dropping disabled workspaces"
      pattern: "warmup_settings"
    - from: "app/services/warmup.py::_get_active_pool"
      to: "senders.restriction_status / restricted_until"
      via: "RESV-05 restriction-skip clause"
      pattern: "restriction_status = 'none'"
    - from: "app/services/warmup.py::_generate_message"
      to: "warmup_settings.topics / system_prompt"
      via: "content resolver COALESCing to code defaults"
      pattern: "WARMUP_TOPICS"
---

<objective>
Make the warmup ENGINE honor the per-workspace master flag, read per-workspace content with safe code-default fallback, and stop warming restricted/frozen accounts — without touching the protected queue path. Turns the RED WARM-06/10/14 tests green; WARM-03/12/13 are verified-no-regression invariants.

Purpose: D-06 (start/stop = DB flag, worker stays a global singleton), D-10 (per-workspace content, default = current 24 RU topics + prompt), D-14 (close the security hole: pool selection ignores restriction_status today). D-03/D-12/D-13 are confirmed-intact: warmup already bypasses message_queue, combine-with-campaign is allowed, no auto-enroll on onboarding.
Output: an engine that skips disabled workspaces, resolves content-or-default, and excludes restricted senders.
</objective>

<execution_context>
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/workflows/execute-plan.md
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-account-warmup-via-inter-account-ai-chat/15-CONTEXT.md
@.planning/phases/15-account-warmup-via-inter-account-ai-chat/15-RESEARCH.md

<interfaces>
<!-- Verified live during planning. -->

app/services/warmup.py — class WarmupWorker (line 81):
  - LEVEL_CONFIG (line 33, 5 levels 5→120/day — DO NOT change, D-09).
  - WARMUP_TOPICS (line 41, the 24 RU topics) and WARMUP_SYSTEM_PROMPT (line 68) — the code defaults to COALESCE to.
  - _tick (line 133): if working hours → _process_due_sessions(db) + _create_new_sessions(db).
  - _is_working_hours (line 144, 09–20 МСК — DO NOT change, D-08).
  - _get_active_pool (line 165): SELECT wp.sender_id, wp.workspace_id, wp.enrolled_at, s.slug, s.phone, s.session_string FROM warmup_pool wp JOIN senders s ON s.id=wp.sender_id AND s.workspace_id=wp.workspace_id WHERE wp.is_active=true AND s.lifecycle_status='active' AND s.auth_status='ok' AND s.role='sender'. Currently NO restriction filter (the D-14 hole).
  - _process_session (line 264): builds from_sender/to_sender with "is_eligible": (status=='active' and auth=='ok') at line 290; guard at line 302. Add restriction to is_eligible here too.
  - _create_new_sessions (line 445): partitions pairs by workspace_id, asserts cross-tenant invariant at ~498 (DO NOT remove). topic = random.choice(WARMUP_TOPICS) at line 500.
  - _generate_message (line 531): system content = WARMUP_SYSTEM_PROMPT.format(topic=topic) at line 540.
  - _send_via_telethon (line 571): direct client.send_message, handles FloodWait at ~610 (DO NOT break — D-03 path, CLAUDE.md guard).

warmup_settings table (from Plan 01): workspace_id PK, enabled BOOL DEFAULT FALSE, topics JSONB DEFAULT '[]', system_prompt TEXT NULL, language TEXT DEFAULT 'ru', tone TEXT NULL.

RESV-05 restriction-skip clause to copy (contact_check_worker.py:259-268):
  AND restriction_status = 'none'
  AND lifecycle_status <> 'paused'
  AND (restricted_until IS NULL OR restricted_until <= NOW())
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enabled-gate + restriction-skip in pool selection</name>
  <files>app/services/warmup.py, tests/test_warmup_worker.py</files>
  <read_first>
    - app/services/warmup.py (lines 165-200 _get_active_pool; lines 264-310 _process_session is_eligible; lines 445-505 _create_new_sessions cross-tenant assertion)
    - app/services/contact_check_worker.py (lines 259-268 RESV-05 clause)
    - tests/test_warmup_worker.py (RED tests test_disabled_workspace_skipped, test_restricted_sender_excluded)
  </read_first>
  <behavior>
    - test_disabled_workspace_skipped: a workspace whose warmup_settings.enabled is false OR has no row → _get_active_pool excludes its senders → _create_new_sessions creates 0 sessions for it.
    - test_restricted_sender_excluded: a sender with restriction_status='spam_limited' OR restricted_until in the future → excluded from _get_active_pool.
  </behavior>
  <action>
    In `_get_active_pool` (line ~178), extend the query:
    1. D-06 enabled gate — JOIN the settings and require enabled, treating "no row" as OFF:
       `LEFT JOIN warmup_settings ws ON ws.workspace_id = wp.workspace_id` and add `AND COALESCE(ws.enabled, false) = true` to the WHERE.
    2. D-14 restriction skip — add to the WHERE (RESV-05 pattern):
       `AND s.restriction_status = 'none' AND (s.restricted_until IS NULL OR s.restricted_until <= NOW())`.
       (lifecycle is already gated to 'active', which excludes 'paused'.)
    Then in `_process_session` `is_eligible` (line ~290), add restriction to the per-side eligibility so an account restricted mid-session also stops: include `r[?]=='none'` for restriction_status (extend the SELECT that feeds is_eligible to also fetch restriction_status / restricted_until and AND them in). Do NOT remove the cross-tenant assertion at ~498. Do NOT change LEVEL_CONFIG or _is_working_hours.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_warmup_worker.py::test_disabled_workspace_skipped tests/test_warmup_worker.py::test_restricted_sender_excluded -x</automated>
  </verify>
  <acceptance_criteria>
    - `_get_active_pool` query contains "warmup_settings" and "COALESCE(ws.enabled, false) = true" (grep).
    - `_get_active_pool` query contains "restriction_status = 'none'" and "restricted_until IS NULL OR" (grep).
    - The cross-tenant assertion line at ~498 still present (grep "Cross-tenant warmup pair").
    - LEVEL_CONFIG and _is_working_hours bodies unchanged (grep their key constants: 09 <= ... < 20).
    - Both named tests exit 0.
  </acceptance_criteria>
  <done>Disabled workspaces and restricted senders are excluded from warmup selection; combine-with-campaign and cross-tenant guards intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-workspace content resolver with code-default fallback</name>
  <files>app/services/warmup.py, tests/test_warmup_worker.py</files>
  <read_first>
    - app/services/warmup.py (lines 41 WARMUP_TOPICS, 68 WARMUP_SYSTEM_PROMPT, 445-505 _create_new_sessions topic pick, 531-545 _generate_message system prompt)
    - tests/test_warmup_worker.py (RED test test_content_defaults_when_empty)
    - .planning/phases/15-account-warmup-via-inter-account-ai-chat/15-RESEARCH.md (Code Examples: _get_warmup_content COALESCE)
  </read_first>
  <behavior>
    - test_content_defaults_when_empty: a workspace with no warmup_settings row (and one with empty topics / NULL system_prompt) resolves to the 24 RU WARMUP_TOPICS and WARMUP_SYSTEM_PROMPT — behaviour byte-identical to today when unconfigured.
  </behavior>
  <action>
    Add an async resolver to WarmupWorker:
    ```python
    async def _get_warmup_content(self, db, workspace_id: str) -> tuple[list[str], str]:
        row = (await db.execute(text(
            "SELECT topics, system_prompt FROM warmup_settings WHERE workspace_id = :wid"
        ), {"wid": workspace_id})).fetchone()
        topics = (row[0] if row and row[0] else None) or WARMUP_TOPICS
        prompt = (row[1] if row and row[1] else None) or WARMUP_SYSTEM_PROMPT
        return topics, prompt
    ```
    Wire it where content is chosen: in `_create_new_sessions`, resolve topics per workspace (the loop is already partitioned by workspace_id — call the resolver once per workspace group and `random.choice(topics)` from the resolved list instead of the module-level WARMUP_TOPICS). Thread the resolved system_prompt to `_generate_message` (pass it as an argument, defaulting to the resolved prompt) so `_generate_message` uses the per-workspace prompt instead of the hardcoded WARMUP_SYSTEM_PROMPT. Keep the existing `.format(topic=topic)` call shape. Empty list / NULL → falls back to the constants (the resolver guarantees this). No change to LEVEL_CONFIG, send path, or FloodWait handling.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_warmup_worker.py::test_content_defaults_when_empty -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/warmup.py` contains `async def _get_warmup_content` returning a `(topics, prompt)` tuple that COALESCEs to WARMUP_TOPICS / WARMUP_SYSTEM_PROMPT (grep both constant names inside/near the resolver).
    - `_create_new_sessions` calls `_get_warmup_content` (grep) and topic selection uses the resolved list.
    - `_generate_message` uses a resolved/passed prompt (no longer references the module-level WARMUP_SYSTEM_PROMPT directly when a per-workspace prompt is provided) — verify by grep that the call site threads the prompt.
    - test_content_defaults_when_empty exits 0.
  </acceptance_criteria>
  <done>Content is per-workspace with safe code-default fallback; unconfigured behaviour is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Verify-no-regression invariants — queue bypass (D-03), combine (D-12), no auto-enroll (D-13)</name>
  <files>tests/test_warmup_worker.py</files>
  <read_first>
    - app/services/warmup.py (_send_via_telethon line 571-615 — direct send + FloodWait)
    - app/services/queue.py (confirm warmup does not appear; protected constants)
    - app/routers/onboarding.py (confirm onboarding does not INSERT into warmup_pool)
  </read_first>
  <action>
    This task ADDS NO new behaviour — it proves three locked invariants hold and records them, so the gate can trust them:
    - WARM-03 (D-03): `_send_via_telethon` calls `client.send_message` directly and never enqueues into `message_queue`; the protected queue.py constants (MIN/MAX_SEND_INTERVAL, 4/20/150, MAX_NEW_CONTACTS_PER_HOUR=15) are untouched by this phase.
    - WARM-12 (D-12): there is no code that auto-pauses warmup when a sender is in an active campaign (no such branch exists; combine is allowed by omission).
    - WARM-13 (D-13): onboarding does NOT insert the new sender into warmup_pool (no auto-enroll). Enroll happens only via the explicit POST /pool/{sender_id} endpoint.
    Add/confirm a lightweight regression assertion: extend tests/test_warmup_worker.py with `test_warmup_send_path_bypasses_queue` that asserts (via inspect.getsource of _send_via_telethon) the token "message_queue" is ABSENT and "send_message" is PRESENT (introspection guard, Phase 13 style). If a richer behavioural test is cheap, prefer it; otherwise the introspection guard is acceptable for an invariant.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_warmup_worker.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.getsource(WarmupWorker._send_via_telethon)` contains "send_message" and does NOT contain "message_queue" (proven by the new test_warmup_send_path_bypasses_queue passing).
    - `grep -n "warmup_pool" app/routers/onboarding.py` returns 0 matches (no auto-enroll) — record in SUMMARY.
    - `git diff app/services/queue.py` is empty for this plan (protected constants untouched).
    - Full tests/test_warmup_worker.py exits 0.
  </acceptance_criteria>
  <done>D-03/D-12/D-13 invariants verified and guarded; queue.py untouched.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_warmup_worker.py -x` — all green.
- Full suite green (786+ baseline + now-green worker tests).
- `git diff app/services/queue.py` empty (protected path untouched).
- Cross-tenant pairing assertion still present in _create_new_sessions.
</verification>

<success_criteria>
- WARM-06/10/14 tests green; WARM-03/12/13 invariants guarded.
- Disabled workspaces and restricted senders excluded; content resolves with safe defaults.
- LEVEL_CONFIG, _is_working_hours, FloodWait, and queue.py all untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/15-account-warmup-via-inter-account-ai-chat/15-03-SUMMARY.md` recording: the exact enabled-gate + restriction clauses added, the content-resolver wiring, and the onboarding no-auto-enroll confirmation.
</output>
