---
phase: 15-account-warmup-via-inter-account-ai-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/037_warmup_settings.sql
  - app/models/__init__.py
  - tests/conftest.py
  - tests/test_warmup_isolation.py
  - tests/test_warmup_router.py
  - tests/test_warmup_worker.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [WARM-01, WARM-02, WARM-04, WARM-05, WARM-06, WARM-10, WARM-14]
must_haves:
  truths:
    - "migrations/037_warmup_settings.sql applies idempotently and creates warmup_settings keyed by workspace_id, enabled DEFAULT FALSE, with no live-workspace seed"
    - "tests/test_warmup_isolation.py, test_warmup_router.py, test_warmup_worker.py exist and FAIL (RED) because the implementation behaviours do not exist yet"
    - "REQUIREMENTS.md contains a WARM-01..15 derived block following the RESV/PACE/NDLG convention"
  artifacts:
    - path: "migrations/037_warmup_settings.sql"
      provides: "warmup_settings table (workspace_id PK, enabled DEFAULT FALSE, topics JSONB, system_prompt TEXT, language, tone, timestamps)"
      contains: "CREATE TABLE IF NOT EXISTS warmup_settings"
    - path: "app/models/__init__.py"
      provides: "WarmupSettings ORM model"
      contains: "class WarmupSettings"
    - path: "tests/test_warmup_isolation.py"
      provides: "RED guards for WARM-01/02/04 (internal detection, no-DB-write/no-AI, source-introspection wiring)"
      min_lines: 60
    - path: "tests/test_warmup_router.py"
      provides: "RED guards for WARM-05 (workspace scoping + response-shape preservation)"
      min_lines: 40
    - path: "tests/test_warmup_worker.py"
      provides: "RED guards for WARM-06/10/14 (disabled-workspace skip, content defaults, restriction skip)"
      min_lines: 40
    - path: ".planning/REQUIREMENTS.md"
      provides: "WARM-01..15 derived requirements block"
      contains: "WARM-01"
  key_links:
    - from: "tests/conftest.py"
      to: "migrations/037_warmup_settings.sql"
      via: "_build_outreach_schema migration apply list"
      pattern: "037_warmup_settings"
    - from: "app/database.py::_apply_migrations"
      to: "migrations/037_warmup_settings.sql"
      via: "auto-apply on api start (lexical order)"
      pattern: "037"
---

<objective>
Lay the Wave-0 foundation for Phase 15: the persistent `warmup_settings` table (migration 037 + ORM), and the three RED test files that later waves turn green. This is the goal-backward anchor — the isolation guarantee (WARM-01/02/04) is provable only because these tests exist first and fail.

Purpose: Per 15-VALIDATION.md, build the isolation/router/worker test stubs RED before any implementation. Per research §Storage Recommendation, create the `warmup_settings` table that the worker (Plan 03) and router (Plan 04) read.
Output: migration 037, `WarmupSettings` ORM, conftest wiring, three RED test files, and the WARM-01..15 block in REQUIREMENTS.md.
</objective>

<execution_context>
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/workflows/execute-plan.md
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-account-warmup-via-inter-account-ai-chat/15-CONTEXT.md
@.planning/phases/15-account-warmup-via-inter-account-ai-chat/15-RESEARCH.md
@.planning/phases/15-account-warmup-via-inter-account-ai-chat/15-VALIDATION.md

<interfaces>
<!-- Verified live during planning — use these directly, no exploration needed. -->

Migration is the highest existing 036; next free is 037. Migrations are raw-SQL ONLY, idempotent, auto-applied on api start via app/database.py::_apply_migrations behind pg_advisory_lock, tracked in schema_migrations. Never Alembic.

Existing warmup tables already carry workspace_id (mig 012). senders columns verified in prod:
  workspace_id UUID NOT NULL, role VARCHAR(20) CHECK IN ('sender','checker'),
  auth_status VARCHAR(30) DEFAULT 'ok', lifecycle_status VARCHAR(20) CHECK IN ('active','warmup','paused'),
  telegram_id BIGINT, restriction_status TEXT NOT NULL DEFAULT 'none' CHECK IN ('none','spam_limited','frozen'),
  restricted_until TIMESTAMPTZ.
  NOTE: senders.is_active does NOT exist (dropped mig 013). warmup_pool.is_active DOES exist (separate enroll-toggle column).

ORM models live in app/models/__init__.py (package). Base is defined there. Existing: class Workspace (line 31), class WorkspaceApiKey, class WarmupPool (line 335), warmup_sessions/warmup_messages models. Add WarmupSettings here.

conftest schema builder: tests/conftest.py::_build_outreach_schema applies a hardcoded list of migration files (around lines 146-178) so raw-SQL tests see the columns. Mig 032 is applied via an `if file.exists()` guard. New migration 037 must be appended to the apply list the same way (or via an exists-guard) so warmup_settings exists in the test DB.

Test framework: pytest + pytest-asyncio (pytestmark = pytest.mark.asyncio). Fixtures available: async_db_session, test_workspace, test_sender_factory (kwargs: role, slug, lifecycle_status, auth_status, restriction_status, restricted_until, telegram_id, workspace_id...), async_client, valid_supabase_jwt / es256_supabase_jwt factories.

REQUIREMENTS.md derived-block convention (see the RESV/PACE/NDLG sections near the end): a `### <Title> (Phase NN — derived this phase, see NN-CONTEXT.md decisions)` header, then `- [ ] **WARM-NN**: ...(D-NN)` bullets, then add rows to the Traceability table + bump the date footer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 037 + WarmupSettings ORM</name>
  <files>migrations/037_warmup_settings.sql, app/models/__init__.py</files>
  <read_first>
    - migrations/036_checker_probe_burn.sql (idempotency style reference)
    - migrations/037_warmup_settings.sql (will create)
    - app/models/__init__.py (Base, Workspace at line 31, WarmupPool at line 335 — add WarmupSettings nearby)
    - app/database.py (confirm _apply_migrations picks up migrations/*.sql lexically)
  </read_first>
  <action>
    Create `migrations/037_warmup_settings.sql` — idempotent, wrapped in BEGIN/COMMIT:
    ```sql
    CREATE TABLE IF NOT EXISTS warmup_settings (
        workspace_id   UUID PRIMARY KEY REFERENCES workspaces(id) ON DELETE CASCADE,
        enabled        BOOLEAN NOT NULL DEFAULT FALSE,
        topics         JSONB   NOT NULL DEFAULT '[]'::jsonb,
        system_prompt  TEXT,
        language       TEXT    NOT NULL DEFAULT 'ru',
        tone           TEXT,
        created_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        updated_at     TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );
    ```
    LOCKED DECISION (explicit opt-in, research Open Question 3): `enabled` DEFAULT FALSE and the migration MUST NOT seed the existing live workspace to `true` — NO `INSERT ... ON CONFLICT` seed row at all. This is a behaviour change: warmup stops until the user flips the master toggle on in the new tab. Add a top-of-file SQL comment stating this explicitly.
    Then add `class WarmupSettings(Base)` to `app/models/__init__.py` (place near WarmupPool, ~line 335) mapping all columns above (workspace_id as primary_key, enabled Boolean server_default text('false'), topics JSONB server_default text("'[]'::jsonb"), system_prompt/tone nullable Text, language Text server_default text("'ru'"), created_at/updated_at TIMESTAMPTZ server_default func.now()). __tablename__ = "warmup_settings".
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api python -c "import app.models; print(app.models.WarmupSettings.__tablename__)"</automated>
  </verify>
  <acceptance_criteria>
    - `migrations/037_warmup_settings.sql` contains "CREATE TABLE IF NOT EXISTS warmup_settings" AND "enabled        BOOLEAN NOT NULL DEFAULT FALSE" (grep both).
    - File contains NO `INSERT INTO warmup_settings` (grep -c returns 0) — no live-workspace seed.
    - File contains a comment mentioning explicit opt-in / no seed.
    - `app/models/__init__.py` contains `class WarmupSettings` with `__tablename__ = "warmup_settings"` (grep).
    - The python import command above prints `warmup_settings` and exits 0.
  </acceptance_criteria>
  <done>warmup_settings migration + ORM exist; idempotent; default-FALSE; no seed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire migration 037 into conftest schema builder</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (lines ~146-216: _build_outreach_schema migration apply list, the mig-032 exists-guard pattern, the warmup_sessions ALTER block)
    - migrations/037_warmup_settings.sql (from Task 1)
  </read_first>
  <action>
    In `tests/conftest.py::_build_outreach_schema`, after the existing mig-032 `if _mig_032.exists()` guard block, add an analogous exists-guarded apply for 037:
    ```python
    _mig_037 = PROJECT_ROOT / "migrations" / "037_warmup_settings.sql"
    if _mig_037.exists():
        await asyncpg_conn.execute(_mig_037.read_text())
    ```
    Use the exists-guard form (not the unconditional tuple) so the test DB gets `warmup_settings`. Do NOT touch the DSN guard (lines 46-77) or any other fixture.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api python -c "import asyncio,asyncpg,os; print('conftest wiring present')" && grep -c "037_warmup_settings" tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` contains "037_warmup_settings" (grep -c returns >= 1).
    - The added block uses an `.exists()` guard (grep for `_mig_037` and `.exists()`).
    - DSN guard region (lines ~46-77) is unchanged.
  </acceptance_criteria>
  <done>warmup_settings table is created in the ephemeral test DB during _build_outreach_schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED test stubs — isolation, router, worker</name>
  <files>tests/test_warmup_isolation.py, tests/test_warmup_router.py, tests/test_warmup_worker.py</files>
  <read_first>
    - tests/test_queue_even_pacing.py (lines ~290-309: _assert_pacing_predicate_wired getsource-introspection pattern to mirror)
    - tests/test_phase5_analytics_correctness.py (line ~388: test_internal_warmup_conversation_excluded — keep-green reference; do NOT modify)
    - app/services/listener.py (class TelegramListener line 132; handle_incoming_message line 605; handle_outgoing_message line 1123)
    - app/services/warmup.py (class WarmupWorker line 81; _get_active_pool line 165; WARMUP_TOPICS line 41; WARMUP_SYSTEM_PROMPT line 68)
    - tests/conftest.py (async_db_session, test_workspace, test_sender_factory signatures)
    - .planning/phases/15-account-warmup-via-inter-account-ai-chat/15-VALIDATION.md (exact test names per req)
  </read_first>
  <behavior>
    These tests MUST be RED (fail) at the end of this plan — they assert behaviours Plans 02/03/04 will implement. Use the EXACT test names from 15-VALIDATION.md so the per-req map resolves.

    tests/test_warmup_isolation.py:
    - test_internal_detected_by_workspace_telegram_id (WARM-01): given two senders in the same workspace, the listener treats a message whose counterparty telegram_id ∈ that workspace's senders as internal — independent of warmup_pool membership and independent of phone. Assert via the new listener helper `_get_workspace_sender_tg_ids(workspace_id)` returning a set containing the other sender's telegram_id (helper does not exist yet → RED).
    - test_internal_inbound_no_dbwrite_no_ai (WARM-02): construct a fake inbound event whose sender.id is a known workspace-sender telegram_id; patch get_me, schedule_ai_response (AsyncMock); call handle_incoming_message; assert schedule_ai_response NOT called AND zero rows added to conversations/messages for that pair. RED until the short-circuit exists.
    - test_shortcircuit_wired (WARM-04): inspect.getsource(TelegramListener.handle_incoming_message) AND handle_outgoing_message both contain the marker token "_get_workspace_sender_tg_ids". RED now.

    tests/test_warmup_router.py:
    - test_pool_workspace_scoped (WARM-05): a sender in workspace A is invisible to a GET /api/v1/warmup/pool authenticated as workspace B (and vice versa). Use async_client + a JWT factory. RED until the router is workspace-scoped (currently verify_api_key, global).
    - test_response_shapes_preserved (WARM-05): GET /pool returns objects whose keys are a SUPERSET of {id, slug, name, phone, in_pool, warmup_active, enrolled_at, enrolled_days, level, sent_today} (NOTE: assert against the corrected key set — `is_active` is being removed; assert it is NOT required) and INCLUDE the new D-11 keys restriction_status, restricted_until. RED until rewrite.

    tests/test_warmup_worker.py:
    - test_disabled_workspace_skipped (WARM-06): a workspace whose warmup_settings.enabled=false (or no row) produces zero new warmup_sessions on a tick. RED until the enabled gate exists.
    - test_content_defaults_when_empty (WARM-10): a workspace with no warmup_settings row (or empty topics / NULL system_prompt) resolves to the 24 RU WARMUP_TOPICS + WARMUP_SYSTEM_PROMPT via the new content-resolver helper. RED until the helper exists.
    - test_restricted_sender_excluded (WARM-14): a sender with restriction_status='spam_limited' (or restricted_until in the future) is excluded from _get_active_pool selection. RED until the clause is added.
  </behavior>
  <action>
    Create the three files with the test functions named exactly as above. Put `pytestmark = pytest.mark.asyncio` at module top. For behaviours whose target symbol does not exist yet (e.g. `_get_workspace_sender_tg_ids`), import inside the test body (deferred import) so `pytest --collect-only` stays clean, and let the AttributeError/assertion drive RED — mirror the deferred-in-body import pattern from tests/test_queue_even_pacing.py and Phase 08 Wave-0. Reuse async_db_session/test_workspace/test_sender_factory fixtures. For the fake-inbound-event helper, build a small local helper that fabricates an object with `.id`, `.first_name`, `.last_name`, `.bot=False` for the Telegram sender and patches event.client.get_me / event.get_chat / schedule_ai_response with AsyncMock — do NOT connect to real Telegram. Do NOT modify test_phase5_analytics_correctness.py (it must stay green as the WARM-02 read-side guard).
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_warmup_isolation.py tests/test_warmup_router.py tests/test_warmup_worker.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist; each contains its exact test names from 15-VALIDATION.md (grep each name).
    - `pytest --collect-only` for the trio exits 0 (collects cleanly, no import errors).
    - Running the trio (without --collect-only) shows the new tests FAILING/ERRORing (RED) — capture and record the count in the SUMMARY. (They go green in Plans 02/03/04.)
    - tests/test_phase5_analytics_correctness.py is unmodified (git diff shows no change to that file).
  </acceptance_criteria>
  <done>Three RED test files exist with the canonical names; collection is clean; the pre-existing analytics test is untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Add WARM-01..15 derived block to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (RESV/PACE/NDLG derived blocks near the end + the Traceability table + the dated footer)
    - .planning/phases/15-account-warmup-via-inter-account-ai-chat/15-CONTEXT.md (the WARM-01..15 wording in <decisions>)
  </read_first>
  <action>
    Add a new section after the Phase 14 RESV block, titled exactly:
    `### Account Warmup via Inter-Account AI Chat (Phase 15 — derived this phase, see 15-CONTEXT.md decisions)`
    Then 15 `- [ ] **WARM-NN**: <text> (D-NN)` bullets copied verbatim from 15-CONTEXT.md <decisions> WARM-01..15. Add 15 rows `| WARM-NN | Phase 15 | Pending |` to the Traceability table. Append a footer line `*2026-06-29 — derived WARM-01..15 (Account Warmup via Inter-Account AI Chat) during Phase 15 planning*`.
  </action>
  <verify>
    <automated>grep -c "WARM-" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains the exact section header with "(Phase 15 — derived this phase, see 15-CONTEXT.md decisions)".
    - All of WARM-01 through WARM-15 appear as `**WARM-NN**` bullets (grep each).
    - 15 new `| WARM-NN | Phase 15 | Pending |` rows exist in the Traceability table.
    - Footer date line added.
  </acceptance_criteria>
  <done>WARM-01..15 are documented in REQUIREMENTS.md per the established convention.</done>
</task>

</tasks>

<verification>
- `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api python -c "import app.models; print(app.models.WarmupSettings.__tablename__)"` prints `warmup_settings`.
- The three new test files collect cleanly and FAIL (RED) — expected; they go green in later waves.
- Full suite (excluding the new RED files) stays at its GREEN baseline (786 passed) — run `pytest --ignore=tests/test_warmup_isolation.py --ignore=tests/test_warmup_router.py --ignore=tests/test_warmup_worker.py` to confirm no regression from the conftest/migration/ORM changes.
- migration 037 re-applies idempotently (the migration-test DB round-trip in conftest).
</verification>

<success_criteria>
- warmup_settings table + ORM exist, idempotent, default-FALSE enabled, no live-workspace seed.
- conftest builds warmup_settings in the test DB.
- Three RED test files with canonical names exist and fail for the right reasons.
- WARM-01..15 block added to REQUIREMENTS.md.
- No regression in the existing 786-test baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/15-account-warmup-via-inter-account-ai-chat/15-01-SUMMARY.md` recording: the RED test count (for the trio), confirmation of default-FALSE/no-seed, and the conftest wiring.
</output>
