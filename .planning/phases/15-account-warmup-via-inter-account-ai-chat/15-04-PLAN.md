---
phase: 15-account-warmup-via-inter-account-ai-chat
plan: 04
type: execute
wave: 3
depends_on: ["15-01", "15-03"]
files_modified:
  - app/routers/warmup.py
  - lovable-handoff/openapi.json
autonomous: true
requirements: [WARM-05, WARM-07, WARM-08, WARM-09, WARM-11, WARM-10, WARM-06]
must_haves:
  truths:
    - "Every /api/v1/warmup endpoint is workspace-scoped via auth_dep/AuthCtx; a sender/session in workspace A is invisible to workspace B"
    - "All existing response keys of the 8 endpoints are preserved; new keys (restriction_status, restricted_until, warmup reason) are ADDED not renamed"
    - "The /pool query no longer references the dropped senders.is_active column"
    - "GET/PUT /settings expose the master enabled toggle + per-workspace content with resolved defaults"
  artifacts:
    - path: "app/routers/warmup.py"
      provides: "auth_dep-scoped router, is_active fix, settings + master-toggle endpoints, enriched per-account status"
      contains: "auth_dep"
  key_links:
    - from: "app/routers/warmup.py"
      to: "app.utils.auth.auth_dep / AuthCtx"
      via: "Depends(auth_dep) on every endpoint + WHERE workspace_id = :wid"
      pattern: "Depends\\(auth_dep\\)"
    - from: "app/routers/warmup.py PUT /settings"
      to: "warmup_settings table"
      via: "INSERT ... ON CONFLICT (workspace_id) DO UPDATE"
      pattern: "ON CONFLICT"
    - from: "app/routers/warmup.py GET /pool"
      to: "senders.restriction_status / restricted_until"
      via: "enriched per-account status (D-11)"
      pattern: "restriction_status"
---

<objective>
Productize the warmup API: rewrite all 8 endpoints from legacy `verify_api_key` onto `auth_dep`/`AuthCtx` workspace-scoping, fix the latent `senders.is_active` bug, enrich per-account status with restriction reason (D-11), and add the master-toggle + content `settings` endpoints (D-06/D-07/D-10). Regenerate the openapi handoff. Turns the RED WARM-05 router tests green.

Purpose: D-05 (workspace-scope every query, preserve response shapes for the Lovable-generated frontend), D-11 (show WHY an account isn't warming — derive from restriction_status/restricted_until + last sent_at, NO new error column per the locked decision), D-06/D-07/D-10 (master toggle + content settings backing the approved UI-SPEC). The UI tab itself is built in the sibling repo and human-verified separately (15-VALIDATION manual-only).
Output: a workspace-scoped warmup router with settings endpoints and enriched status, plus regenerated openapi.json.
</objective>

<execution_context>
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/workflows/execute-plan.md
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-account-warmup-via-inter-account-ai-chat/15-CONTEXT.md
@.planning/phases/15-account-warmup-via-inter-account-ai-chat/15-RESEARCH.md
@.planning/phases/15-account-warmup-via-inter-account-ai-chat/15-UI-SPEC.md

<interfaces>
<!-- Verified live during planning. -->

app/utils/auth.py: class AuthCtx(BaseModel) (line 170) with fields workspace_id: UUID, user_id, source, role. async def auth_dep(...) (line 179) → AuthCtx. Supports Supabase JWT (ES256/JWKS + HS256) AND X-Workspace-Key API key (so n8n integrations keep working). Import: `from app.utils.auth import AuthCtx, auth_dep`. Scoped pattern reference: app/routers/senders.py header + _validate_workspace_owns_* (404 if sender not in ctx.workspace_id).

app/routers/warmup.py — CURRENTLY on `from app.routers.auth import verify_api_key` with `_: str = Depends(verify_api_key)` on all 8 endpoints (lines 36/91/132/164/194/244/304/347). The GET /pool SELECT references `s.is_active` (line 48) and maps it to response key "is_active" (line 74) — THIS COLUMN WAS DROPPED (mig 013); the endpoint would 500 in prod. warmup_pool.is_active is a DIFFERENT, existing column (the per-account enroll toggle, aliased `warmup_active`) — keep it.

The 8 endpoints + response keys to PRESERVE verbatim (from 15-RESEARCH §Don't-break-the-response-shape):
  GET  /pool           → senders[]: id, slug, name, phone, in_pool, warmup_active, enrolled_at, enrolled_days, level, sent_today  [REMOVE is_active key; ADD restriction_status, restricted_until, warmup_reason]
  POST /pool/{sender_id} (201)      → status, sender_id, slug  [ADD workspace-ownership 404]
  DELETE /pool/{sender_id} (204)    → (no body)  [ADD workspace-ownership 404]
  PATCH /pool/{sender_id}/toggle    → sender_id, warmup_active
  GET  /stats          → active_accounts, active_sessions, messages_today, sessions_completed_today, accounts[]:{slug,name,sent_today,enrolled_days,level}
  GET  /sessions?status= → sessions[]:{id, topic, status, messages_sent, target_messages, next_message_at, created_at, updated_at, sender_a, sender_b, progress_pct}
  GET  /sessions/{id}  → id, topic, status, messages_sent, target_messages, progress_pct, next_message_at, created_at, updated_at, sender_a{slug,name}, sender_b{slug,name}
  GET  /sessions/{id}/messages → total, messages[]:{id, text, sent_at, from_slug, from_name, direction}

warmup_pool / warmup_sessions / warmup_messages all carry workspace_id (mig 012) — filter directly. warmup_settings (from Plan 01): workspace_id PK, enabled, topics JSONB, system_prompt, language, tone.

LOCKED DECISION (D-11 "why not warming"): DERIVE the reason from senders.restriction_status / restricted_until + last warmup_messages.sent_at. NO new error column, NO change to _send_via_telethon. The warmup_reason field is computed in the /pool response (e.g. spam_limited → reason text incl. restricted_until; frozen → frozen text; else null).

openapi regeneration: rebuild the API container first, then export via the lovable-handoff flow (same as Phase 10/11/14) — do NOT hand-edit openapi.json. The frontend tab lives in sibling repo aimly-tg-outreach and is NOT built here (manual UAT per 15-VALIDATION).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Workspace-scope the 8 endpoints + fix is_active + enrich /pool status</name>
  <files>app/routers/warmup.py, tests/test_warmup_router.py</files>
  <read_first>
    - app/routers/warmup.py (all 8 endpoints; GET /pool SELECT at 33-86; POST/DELETE/toggle at 87-189; /stats at 191-238; /sessions at 240-356)
    - app/utils/auth.py (AuthCtx line 170, auth_dep line 179)
    - app/routers/senders.py (workspace-scoped header + _validate_workspace_owns_* 404 pattern)
    - tests/test_warmup_router.py (RED tests test_pool_workspace_scoped, test_response_shapes_preserved)
  </read_first>
  <behavior>
    - test_pool_workspace_scoped: a sender in workspace A does not appear in GET /pool authed as workspace B; POST/DELETE/toggle on a sender not owned by ctx.workspace_id → 404.
    - test_response_shapes_preserved: GET /pool objects contain the preserved key set (NOT requiring is_active) PLUS restriction_status, restricted_until.
  </behavior>
  <action>
    Replace `from app.routers.auth import verify_api_key` with `from app.utils.auth import AuthCtx, auth_dep`. On EVERY endpoint, replace `_: str = Depends(verify_api_key)` with `ctx: AuthCtx = Depends(auth_dep)`.
    For each query, add workspace scope: `AND s.workspace_id = :wid` (or JOIN-through to senders.workspace_id for warmup_sessions/warmup_messages which also carry workspace_id — use their own `workspace_id = :wid`), binding `{"wid": str(ctx.workspace_id)}`.
    GET /pool: REMOVE `s.is_active` from the SELECT and the `"is_active"` key from the response dict (the column is dropped). Keep `wp.is_active AS warmup_active` (different column). ADD to the SELECT `s.restriction_status, s.restricted_until` and a LEFT JOIN LATERAL / subquery for the last `warmup_messages.sent_at` for that sender; compute and add response keys `restriction_status`, `restricted_until`, and `warmup_reason` (DERIVED per the locked D-11 decision: spam_limited/frozen → human reason text; else null). Do not add an error column anywhere.
    POST/DELETE/toggle /pool/{sender_id}: before mutating, verify the target sender belongs to ctx.workspace_id (SELECT 1 ... WHERE id=:sid AND workspace_id=:wid); 404 if not — mirror senders.py _validate_workspace_owns_*.
    /stats and /sessions*: scope every count/select by workspace_id. Preserve ALL existing response keys verbatim (only ADD).
    Async/await, text(), logging. Russian for any user-facing message strings.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_warmup_router.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/warmup.py` has NO remaining `verify_api_key` reference (grep -c == 0) and every endpoint has `Depends(auth_dep)` (grep count == number of endpoints).
    - The GET /pool SELECT contains NO `s.is_active` (grep == 0) but DOES contain `wp.is_active` aliased `warmup_active` and the new `restriction_status`, `restricted_until` (grep).
    - GET /pool response dict contains keys `restriction_status`, `restricted_until`, `warmup_reason` and does NOT contain `is_active`.
    - No `_send_via_telethon` change and no new error column anywhere (grep — locked D-11).
    - test_pool_workspace_scoped and test_response_shapes_preserved exit 0.
  </acceptance_criteria>
  <done>All 8 endpoints workspace-scoped; is_active bug fixed; per-account status enriched with restriction reason; shapes preserved.</done>
</task>

<task type="auto">
  <name>Task 2: GET/PUT /settings — master toggle + per-workspace content</name>
  <files>app/routers/warmup.py, tests/test_warmup_router.py</files>
  <read_first>
    - app/routers/warmup.py (post Task 1 — auth_dep imports in place)
    - app/services/warmup.py (WARMUP_TOPICS line 41, WARMUP_SYSTEM_PROMPT line 68 — defaults to surface in GET)
    - .planning/phases/15-account-warmup-via-inter-account-ai-chat/15-UI-SPEC.md (Copywriting: master CTA, settings save copy)
    - .planning/phases/15-account-warmup-via-inter-account-ai-chat/15-RESEARCH.md (Code Examples: PUT /settings ON CONFLICT upsert)
  </read_first>
  <action>
    Add two workspace-scoped endpoints to the warmup router:
    `GET /api/v1/warmup/settings` (ctx: AuthCtx = Depends(auth_dep)): SELECT from warmup_settings WHERE workspace_id=:wid; return `{enabled, topics, system_prompt, language, tone}` with resolved defaults surfaced (when topics empty → return the 24 WARMUP_TOPICS; when system_prompt NULL → return WARMUP_SYSTEM_PROMPT) so the UI shows what is actually in effect. When no row exists → return defaults with `enabled: false`.
    `PUT /api/v1/warmup/settings` (body: a pydantic WarmupSettingsUpdate with optional enabled, topics, system_prompt, language, tone): upsert via
    `INSERT INTO warmup_settings (workspace_id, enabled, topics, system_prompt, language, tone, updated_at) VALUES (:wid, :enabled, :topics, :prompt, :lang, :tone, NOW()) ON CONFLICT (workspace_id) DO UPDATE SET enabled=EXCLUDED.enabled, topics=EXCLUDED.topics, system_prompt=EXCLUDED.system_prompt, language=EXCLUDED.language, tone=EXCLUDED.tone, updated_at=NOW()`.
    The `enabled` field IS the master toggle (D-06/D-07). Return `{"status": "saved"}` plus the resolved settings. Define the pydantic request/response schemas in this router file (or app/schemas if that's the convention — check senders.py). await db.commit(). Async/await, logging.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_warmup_router.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/warmup.py` contains `@router.get("/settings")` and `@router.put("/settings")`, both with `Depends(auth_dep)` (grep).
    - PUT handler contains `ON CONFLICT (workspace_id) DO UPDATE` (grep).
    - GET /settings returns the 24-topic default when topics is empty and the default prompt when NULL (assert in a router test — add `test_settings_get_resolves_defaults` if not already covered).
    - PUT with enabled=true persists and GET reflects it (assert via async_client in a test).
    - Full tests/test_warmup_router.py exits 0.
  </acceptance_criteria>
  <done>Master toggle + content settings endpoints exist, workspace-scoped, with default-resolving GET and idempotent upsert PUT.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate openapi handoff</name>
  <files>lovable-handoff/openapi.json</files>
  <read_first>
    - lovable-handoff/openapi.json (current — will be regenerated, do not hand-edit)
    - CLAUDE.md §Lovable-фронт quirks + the export-handoff flow used in Phase 10/11/14 (check for an export script under lovable-handoff/ or scripts/)
  </read_first>
  <action>
    Rebuild the API container so the new routes are live (`docker compose up -d --build api`), then run the project's export-handoff flow to regenerate `lovable-handoff/openapi.json` from the running app (same mechanism Phase 10/11/14 used — do NOT hand-edit the spec). The new `/warmup/settings` GET/PUT and the enriched `/pool` shape must appear in the regenerated spec. The frontend (sibling repo) is NOT modified here — it is generated/built separately and human-verified per 15-VALIDATION manual-only checklist. Do NOT commit anything in the sibling repo from this plan.
  </action>
  <verify>
    <automated>grep -c "/warmup/settings" lovable-handoff/openapi.json</automated>
  </verify>
  <acceptance_criteria>
    - `lovable-handoff/openapi.json` contains the path `/warmup/settings` (grep -c >= 1).
    - The `/warmup/pool` response schema in openapi.json includes `restriction_status` and does NOT include `is_active` (grep).
    - The spec was generated (not hand-edited) — note the generation command in the SUMMARY.
  </acceptance_criteria>
  <done>openapi.json reflects the new settings endpoints and enriched pool shape for the Lovable frontend.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_warmup_router.py -x` — all green (WARM-05 + settings).
- Full suite green (786+ baseline + now-green router tests).
- `grep verify_api_key app/routers/warmup.py` → 0 matches.
- `grep "s.is_active" app/routers/warmup.py` → 0 matches.
- openapi.json contains /warmup/settings and the enriched pool shape.
</verification>

<success_criteria>
- WARM-05/07/08/09/11 satisfied; settings (WARM-06/10) exposed via API.
- All 8 endpoints workspace-scoped, response shapes preserved (+additions only).
- is_active latent bug fixed; D-11 reason derived (no new error column).
- openapi handoff regenerated.
</success_criteria>

<output>
After completion, create `.planning/phases/15-account-warmup-via-inter-account-ai-chat/15-04-SUMMARY.md` recording: the auth_dep migration, the is_active removal, the derived warmup_reason logic, the settings endpoint shapes, and the openapi regeneration command. Note the manual-UAT item (frontend tab) is verified separately per 15-VALIDATION.
</output>
