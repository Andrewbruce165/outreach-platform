---
phase: 04-campaigns
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - migrations/016_phase4.sql
  - app/models/__init__.py
  - app/schemas/__init__.py
  - app/routers/campaigns.py
  - app/routers/agents.py
  - app/routers/folders.py
  - app/routers/senders.py
  - app/main.py
  - tests/conftest.py
  - tests/test_migration_016.py
  - tests/test_campaigns_model.py
  - tests/test_campaign_router.py
  - tests/test_sender_lock.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CAMP-01, CAMP-02, CAMP-03, CAMP-04, CAMP-07, CAMP-08, CAMP-14]
requirements_addressed: [CAMP-01, CAMP-02, CAMP-03, CAMP-04, CAMP-07, CAMP-08, CAMP-14]
gap_closure: false

must_haves:
  truths:
    - "Миграция 016_phase4.sql применяется идемпотентно и создаёт campaigns / campaign_senders / campaign_contact_assignments + добавляет conversations.campaign_id NULLable + message_queue.campaign_id NULLable + DROP context_contact_assignments + extends conversations.status CHECK"
    - "POST /api/v1/campaigns создаёт кампанию с валидацией agent_id / folder_id / senders[] (все workspace-scoped); 201 на success, 422 на invalid timezone, 409 на duplicate name, 404 на чужой workspace agent/folder/sender"
    - "GET /api/v1/campaigns / GET /{id} возвращают список или одну кампанию workspace со computed `is_exhausted: bool` и `attached_senders[]` (с флагом `locked_by_campaign_id` для conflicting senders)"
    - "POST /api/v1/campaigns/{id}/start блокирует с 409 если хотя бы один из campaign_senders уже в running кампании этого workspace; в conflict response — список `[{sender_id, campaign_id, campaign_name}]`"
    - "DELETE /api/v1/campaigns/{id} возвращает 409 на status='running', 204 на draft/paused/done (hard delete с FK cascade)"
    - "DELETE /api/v1/agents/{id} возвращает 409 если агент в running кампании; реальный campaign_count в GET /agents (не хардкод 0); DELETE /api/v1/folders/{id} аналогично; DELETE/PATCH lifecycle_status sender аналогично"
    - "REQUIREMENTS.md CAMP-14 переписано на «3 отдельных webhook URL (lead/handoff/finish), любой может быть NULL» (per D-13)"
  artifacts:
    - path: "migrations/016_phase4.sql"
      provides: "Все DDL для Phase 4 — campaigns, campaign_senders, campaign_contact_assignments, conversations.campaign_id, message_queue.campaign_id, DROP context_contact_assignments, CHECK constraints, indexes"
      contains: "CREATE TABLE IF NOT EXISTS campaigns, CREATE TABLE IF NOT EXISTS campaign_senders, CREATE TABLE IF NOT EXISTS campaign_contact_assignments, ALTER TABLE conversations ADD COLUMN IF NOT EXISTS campaign_id, ALTER TABLE message_queue ADD COLUMN IF NOT EXISTS campaign_id, DROP TABLE IF EXISTS context_contact_assignments"
    - path: "app/models/__init__.py"
      provides: "ORM Campaign, CampaignSender, CampaignContactAssignment; +Conversation.campaign_id; +MessageQueue.campaign_id; -ContextContactAssignment"
      contains: "class Campaign(Base), class CampaignSender(Base), class CampaignContactAssignment(Base)"
    - path: "app/routers/campaigns.py"
      provides: "CRUD endpoints + lifecycle endpoints (/start /pause /resume /finish) + /duplicate под AuthDep"
      contains: "@router.post(\"\"), @router.get(\"\"), @router.get(\"/{campaign_id}\"), @router.patch(\"/{campaign_id}\"), @router.delete(\"/{campaign_id}\"), @router.post(\"/{campaign_id}/start\"), @router.post(\"/{campaign_id}/pause\"), @router.post(\"/{campaign_id}/resume\"), @router.post(\"/{campaign_id}/finish\"), @router.post(\"/{campaign_id}/duplicate\")"
    - path: "app/schemas/__init__.py"
      provides: "Pydantic CampaignCreate, CampaignUpdate, CampaignResponse, CampaignListResponse, ToolSpec"
      contains: "class CampaignCreate(BaseModel), class CampaignResponse(BaseModel), class ToolSpec(BaseModel)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated CAMP-14 from «один webhook URL» to «3 отдельных URL (lead/handoff/finish), любой NULL»"
      contains: "CAMP-14.*3 отдельных webhook URL"
  key_links:
    - from: "app/main.py"
      to: "app/routers/campaigns.py"
      via: "app.include_router(campaigns.router)"
      pattern: "include_router\\(campaigns\\.router\\)"
    - from: "app/routers/agents.py DELETE handler"
      to: "campaigns table (status='running' check)"
      via: "SELECT 1 FROM campaigns WHERE agent_id=:aid AND status='running'"
      pattern: "campaigns.*WHERE agent_id"
    - from: "app/routers/folders.py DELETE handler"
      to: "campaigns table (status='running' check)"
      via: "SELECT 1 FROM campaigns WHERE folder_id=:fid AND status='running'"
      pattern: "campaigns.*WHERE folder_id"
    - from: "app/routers/senders.py DELETE / PATCH lifecycle_status"
      to: "campaign_senders + campaigns table"
      via: "SELECT 1 FROM campaign_senders cs JOIN campaigns c ON c.id=cs.campaign_id WHERE cs.sender_id=:sid AND c.status='running'"
      pattern: "campaign_senders.*JOIN campaigns"
---

<objective>
Создать миграцию 016_phase4.sql со всем DDL для Phase 4 (campaigns, campaign_senders, campaign_contact_assignments, conversations.campaign_id, message_queue.campaign_id, DROP context_contact_assignments, CHECK constraints на conversations.status). Создать ORM-модели и Pydantic-схемы. Создать `/api/v1/campaigns` роутер с полным CRUD + lifecycle endpoints (start/pause/resume/finish/duplicate) + sender lock check. Закрыть 4 TODO(phase-4) метки в agents/folders/senders роутерах (block delete при active campaign + реальный campaign_count). Обновить REQUIREMENTS.md CAMP-14 на 3 webhook URL.

Purpose: Фундаментальный план Phase 4 — все остальные планы (04-03 schedule, 04-04 queue+worker, 04-05 signals) опираются на ORM `Campaign` и таблицу. Migration 016 — единственный файл DDL во всей фазе, остальные планы расширяют его (если нужно) но не создают новые миграции.

Output: Migration 016, ORM модели, схемы, роутер, регистрация в main.py, закрытие 4 TODO меток, обновлённый REQUIREMENTS.md, ~3 файла тестов (test_migration_016, test_campaigns_model, test_campaign_router, test_sender_lock).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-campaigns/04-CONTEXT.md
@.planning/phases/04-campaigns/04-RESEARCH.md
@.planning/phases/04-campaigns/04-VALIDATION.md
@.planning/phases/04-campaigns/04-01-AUDIT.md
@migrations/015_phase3.sql
@app/models/__init__.py
@app/schemas/__init__.py
@app/routers/agents.py
@app/routers/folders.py
@app/routers/senders.py
@app/main.py
@app/utils/auth.py
@tests/conftest.py

<interfaces>
<!-- Key types from existing codebase that this plan extends/depends on -->

From app/utils/auth.py:
```python
class AuthCtx:
    workspace_id: UUID
    user_id: str | None
    source: Literal['jwt', 'api_key']
    role: str | None

async def auth_dep(...) -> AuthCtx: ...
```

From app/models/__init__.py (relevant existing models):
```python
class AIContext(Base):
    __tablename__ = "ai_contexts"
    id: UUID
    workspace_id: UUID FK CASCADE
    name: str  # UNIQUE(workspace_id, name) after Phase 3
    # ... (system_prompt, rules, tone_of_voice, faq, company_info, product_info)

class Folder(Base):
    __tablename__ = "folders"
    id: UUID
    workspace_id: UUID FK CASCADE
    name: str  # UNIQUE(workspace_id, name)

class Sender(Base):
    __tablename__ = "senders"
    id: UUID
    workspace_id: UUID FK CASCADE
    auth_status: str  # 'ok' / 'session_expired' / etc.
    lifecycle_status: str  # 'active' / 'warmup' / 'paused'
    # ... (rate_per_min, rate_per_hour, rate_per_day, proxy fields)

class Conversation(Base):
    __tablename__ = "conversations"
    id: UUID
    workspace_id: UUID FK
    ai_context_id: UUID FK SET NULL
    status: str  # String(20), CURRENT values: 'active','manual','paused' — extends to 'lead','handoff','finished'
    ai_enabled: bool

class MessageQueue(Base):
    __tablename__ = "message_queue"
    id: UUID
    workspace_id: UUID FK
    sender_id: UUID FK
    recipient_phone: str
    message_text: str
    scheduled_at: datetime
    status: str  # 'pending','processing','sent','failed'

class ContextContactAssignment(Base):  # DROPPED in this plan
    __tablename__ = "context_contact_assignments"
```

From app/schemas/__init__.py (existing patterns):
```python
class AgentCreate(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    name: constr(min_length=1, max_length=100)
    # ...

# Phase 3 partial-PATCH pattern:
class AgentUpdate(BaseModel):
    name: Optional[constr(...)] = None
    # ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — create test stubs + extend conftest.py + write migration 016 + apply</name>
  <files>
    tests/test_migration_016.py,
    tests/test_campaigns_model.py,
    tests/test_campaign_router.py,
    tests/test_sender_lock.py,
    tests/conftest.py,
    migrations/016_phase4.sql
  </files>
  <read_first>
    - migrations/015_phase3.sql (полностью — паттерн idempotent миграции, BEGIN/COMMIT, IF NOT EXISTS / IF EXISTS)
    - migrations/014_phase2_1_hardening.sql (для понимания паттерна conversations CHECK constraint если был)
    - tests/conftest.py (полностью — фикстуры `test_workspace`, `test_sender_factory`, `test_folder`, `test_agent_factory`, migration runner ~line 62 — где применяются 012-015)
    - .planning/phases/04-campaigns/04-CONTEXT.md `<canonical_refs>` lines 244-259 (cписок файлов которые меняем)
    - .planning/phases/04-campaigns/04-RESEARCH.md `## Code Examples` — Migration 016 skeleton (lines ~866-955)
    - .planning/phases/04-campaigns/04-RESEARCH.md `## Common Pitfalls` Pitfall 2 (ALTER TYPE ADD VALUE — почему оставляем conversations.status как String + CHECK, не ENUM)
    - .planning/phases/04-campaigns/04-01-AUDIT.md (Section 4 для recovered webhook_functions shape baseline, Section 6 Q1 NULLable resolution)
    - app/models/__init__.py (полностью — для понимания текущих моделей которые расширяем)
    - app/database.py (для понимания `AsyncSessionLocal`, `engine` который применяет миграции в conftest)
  </read_first>
  <action>
**Шаг 1. Создать 4 test stubs файла:**

Каждый файл — заголовок-комментарий + `import pytest` + `pytestmark = pytest.mark.asyncio` + по 5-10 pytest.skip stubs. Это unblock'ит pytest collection и зафиксирует test API ДО реализации.

`tests/test_migration_016.py` — stubs для:
- `test_migration_016_idempotent` (apply twice — no error)
- `test_campaigns_table_exists` (columns id, workspace_id, agent_id, folder_id, name, status, timezone, work_hour_start, work_hour_end, work_days_mask, start_date, stop_date, message_template, lead_webhook_url, handoff_webhook_url, finish_webhook_url, lead_trigger_hint, handoff_trigger_hint, finish_trigger_hint, tools, created_at, updated_at)
- `test_campaigns_status_check_constraint` (only 'draft'/'running'/'paused'/'done' accepted)
- `test_campaigns_work_hours_check` (start < end, 0-24 range)
- `test_campaigns_work_days_check` (1-127 range)
- `test_campaign_senders_table_exists` (PK campaign_id+sender_id, workspace_id NOT NULL)
- `test_campaign_contact_assignments_table_exists` (UNIQUE campaign_id+contact_phone)
- `test_context_contact_assignments_dropped`
- `test_conversations_campaign_id_added_nullable`
- `test_conversations_status_check_extended` (adds 'lead','handoff','finished' к 'active','manual','paused')
- `test_message_queue_campaign_id_added_nullable_per_audit_q1`
- `test_queue_campaign_id_set_null_on_campaign_delete`

`tests/test_campaigns_model.py` — stubs для:
- `test_create_campaign` (CAMP-01)
- `test_create_with_other_workspace_agent_404` (CAMP-02 workspace isolation)
- `test_create_with_other_workspace_folder_404` (CAMP-03)
- `test_create_with_other_workspace_sender_404` (CAMP-04 Q4 from AUDIT.md)
- `test_default_timezone_is_europe_moscow`
- `test_default_work_hours_9_to_20`
- `test_default_work_days_mask_31`
- `test_create_status_default_draft`
- `test_create_duplicate_name_409`
- `test_create_invalid_timezone_422`

`tests/test_campaign_router.py` — stubs для:
- `test_list_campaigns_workspace_isolated`
- `test_get_campaign_returns_is_exhausted_computed`
- `test_get_campaign_returns_attached_senders_with_locked_flag` (Q4)
- `test_patch_campaign_partial_update`
- `test_delete_running_409`
- `test_delete_draft_204_hard_delete`
- `test_delete_done_204_hard_delete`
- `test_delete_done_keeps_queue_history_via_set_null` (Q1)
- `test_lifecycle_draft_to_running_via_start`
- `test_lifecycle_running_to_paused`
- `test_lifecycle_paused_to_running_via_resume`
- `test_lifecycle_running_to_done_via_finish`
- `test_lifecycle_done_terminal_no_back`
- `test_lifecycle_draft_to_done_forbidden`
- `test_start_requires_at_least_one_sender_422`
- `test_duplicate_endpoint_copies_row_and_senders_not_queue_assignments` (Q2)
- `test_block_delete_agent_when_running_campaign_409` (closes Phase 3 D-09 TODO)
- `test_block_delete_folder_when_running_campaign_409` (closes Phase 2 D-06 TODO)
- `test_block_delete_sender_when_running_campaign_409`
- `test_block_patch_sender_lifecycle_to_paused_when_running_campaign_409`
- `test_agent_campaign_count_real_not_hardcoded_zero` (closes Phase 3 D-10 TODO)

`tests/test_sender_lock.py` — stubs для:
- `test_start_409_when_sender_in_other_running_campaign` (CAMP-04 main)
- `test_409_conflict_response_has_sender_id_campaign_id_campaign_name_list`
- `test_resume_re_checks_sender_lock` (D-04 paused→running re-check)
- `test_two_draft_campaigns_can_share_senders` (only running blocks)
- `test_done_campaign_does_not_block_sender_reuse`

Структура каждого stub:
```python
import pytest

pytestmark = pytest.mark.asyncio


async def test_create_campaign(async_client, valid_supabase_jwt, test_workspace, test_agent_factory, test_folder, test_sender_factory):
    pytest.skip("Wave 0 stub — Task 2 implements")
```

**Шаг 2. Расширить `tests/conftest.py`:**

Добавить НОВЫЕ фикстуры (НЕ перезаписать существующие — append):

```python
@pytest_asyncio.fixture
async def test_campaign_factory(async_db_session, test_workspace, test_agent_factory, test_folder):
    """Factory создаёт draft campaign в workspace. Можно вызвать несколько раз для разных имён."""
    async def _factory(
        name: str = "Test Campaign",
        agent_id=None,
        folder_id=None,
        message_template: str = "Hello {{name}}!",
        timezone: str = "Europe/Moscow",
        work_hour_start: int = 9,
        work_hour_end: int = 20,
        work_days_mask: int = 31,
        start_date=None,
        stop_date=None,
        lead_webhook_url: str | None = None,
        handoff_webhook_url: str | None = None,
        finish_webhook_url: str | None = None,
        lead_trigger_hint: str | None = None,
        handoff_trigger_hint: str | None = None,
        finish_trigger_hint: str | None = None,
        tools: list | None = None,
        status: str = "draft",
    ):
        # INSERT через async_db_session
        # Возвращает dict campaign row
        ...
    return _factory


@pytest_asyncio.fixture
async def test_running_campaign_factory(test_campaign_factory, test_sender_factory):
    """Factory создаёт running campaign с N senders attached."""
    async def _factory(name: str = "Running Test", sender_count: int = 1, **kwargs):
        campaign = await test_campaign_factory(name=name, status="running", **kwargs)
        senders = [await test_sender_factory() for _ in range(sender_count)]
        # INSERT в campaign_senders
        # ...
        return campaign, senders
    return _factory


@pytest_asyncio.fixture
async def attach_sender_to_campaign(async_db_session):
    """Прицепить sender к существующей кампании."""
    async def _attach(campaign_id, sender_id, workspace_id):
        await async_db_session.execute(
            text("INSERT INTO campaign_senders (campaign_id, sender_id, workspace_id) VALUES (:cid, :sid, :wid) ON CONFLICT DO NOTHING"),
            {"cid": str(campaign_id), "sid": str(sender_id), "wid": str(workspace_id)},
        )
        await async_db_session.commit()
    return _attach
```

И в migration runner блоке (`conftest.py:~62` где `sql_015 = ... .read_text()` + `await conn.exec_driver_sql(sql_015)`) — **добавить применение `016_phase4.sql`**:
```python
sql_016 = (PROJECT_ROOT / "migrations" / "016_phase4.sql").read_text()
await conn.exec_driver_sql(sql_016)
```

**Шаг 3. Написать `migrations/016_phase4.sql`:**

Полный idempotent DDL. Точная структура (адаптировано из RESEARCH.md Code Examples Пример 4 + AUDIT.md Q1 NULLable + Q2 duplicate need):

```sql
-- Phase 4: Campaigns — модель + связи + DROP context_contact_assignments
-- Idempotent (IF NOT EXISTS / IF EXISTS)

BEGIN;

-- ── 1. campaigns table (D-01..D-15) ──────────────────────────────────────
CREATE TABLE IF NOT EXISTS campaigns (
    id                   UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    workspace_id         UUID NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
    agent_id             UUID NOT NULL REFERENCES ai_contexts(id) ON DELETE RESTRICT,
    folder_id            UUID NOT NULL REFERENCES folders(id) ON DELETE RESTRICT,
    name                 VARCHAR(150) NOT NULL,
    description          TEXT,
    status               VARCHAR(20) NOT NULL DEFAULT 'draft',
    timezone             TEXT NOT NULL DEFAULT 'Europe/Moscow',
    work_hour_start      INT NOT NULL DEFAULT 9,
    work_hour_end        INT NOT NULL DEFAULT 20,
    work_days_mask       INT NOT NULL DEFAULT 31,
    start_date           TIMESTAMPTZ,
    stop_date            TIMESTAMPTZ,
    message_template     TEXT NOT NULL DEFAULT '',
    lead_webhook_url     TEXT,
    handoff_webhook_url  TEXT,
    finish_webhook_url   TEXT,
    lead_trigger_hint    TEXT,
    handoff_trigger_hint TEXT,
    finish_trigger_hint  TEXT,
    tools                JSONB NOT NULL DEFAULT '[]'::jsonb,
    created_at           TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at           TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT campaigns_status_check CHECK (status IN ('draft','running','paused','done')),
    CONSTRAINT campaigns_work_hours_check CHECK (work_hour_start >= 0 AND work_hour_end <= 24 AND work_hour_start < work_hour_end),
    CONSTRAINT campaigns_work_days_check CHECK (work_days_mask BETWEEN 1 AND 127)
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_campaigns_workspace_name ON campaigns(workspace_id, name);
CREATE INDEX IF NOT EXISTS idx_campaigns_status_running ON campaigns(workspace_id, status) WHERE status = 'running';
CREATE INDEX IF NOT EXISTS idx_campaigns_agent_id ON campaigns(agent_id);
CREATE INDEX IF NOT EXISTS idx_campaigns_folder_id ON campaigns(folder_id);

-- ── 2. campaign_senders through-table (D-03) ────────────────────────────
CREATE TABLE IF NOT EXISTS campaign_senders (
    campaign_id  UUID NOT NULL REFERENCES campaigns(id) ON DELETE CASCADE,
    sender_id    UUID NOT NULL REFERENCES senders(id) ON DELETE CASCADE,
    workspace_id UUID NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
    added_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (campaign_id, sender_id)
);
CREATE INDEX IF NOT EXISTS idx_campaign_senders_sender ON campaign_senders(sender_id);
CREATE INDEX IF NOT EXISTS idx_campaign_senders_workspace ON campaign_senders(workspace_id);

-- ── 3. campaign_contact_assignments (D-06) — replaces context_contact_assignments
DROP TABLE IF EXISTS context_contact_assignments;

CREATE TABLE IF NOT EXISTS campaign_contact_assignments (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    workspace_id  UUID NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
    campaign_id   UUID NOT NULL REFERENCES campaigns(id) ON DELETE CASCADE,
    contact_phone VARCHAR(20) NOT NULL,
    sender_id     UUID NOT NULL REFERENCES senders(id) ON DELETE CASCADE,
    created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE UNIQUE INDEX IF NOT EXISTS idx_cca_campaign_phone ON campaign_contact_assignments(campaign_id, contact_phone);
CREATE INDEX IF NOT EXISTS idx_cca_sender ON campaign_contact_assignments(sender_id);
CREATE INDEX IF NOT EXISTS idx_cca_workspace ON campaign_contact_assignments(workspace_id);

-- ── 4. conversations.campaign_id (D-05) + status CHECK extension (C-13) ──
ALTER TABLE conversations
    ADD COLUMN IF NOT EXISTS campaign_id UUID REFERENCES campaigns(id) ON DELETE SET NULL;

CREATE INDEX IF NOT EXISTS idx_conversations_campaign_id ON conversations(campaign_id) WHERE campaign_id IS NOT NULL;

-- Extend conversations.status CHECK constraint — drop old + add extended
ALTER TABLE conversations DROP CONSTRAINT IF EXISTS conversations_status_check;
ALTER TABLE conversations ADD CONSTRAINT conversations_status_check
    CHECK (status IN ('active','manual','paused','lead','handoff','finished'));

-- ── 5. message_queue.campaign_id (D-16 ОВЕРРАЙДНУТ через AUDIT.md Q1 на NULLable) ──
ALTER TABLE message_queue
    ADD COLUMN IF NOT EXISTS campaign_id UUID REFERENCES campaigns(id) ON DELETE SET NULL;

-- Composite index для эффективных queue-tick'ов (per-campaign filter)
CREATE INDEX IF NOT EXISTS idx_message_queue_workspace_campaign_status_scheduled
    ON message_queue(workspace_id, campaign_id, status, scheduled_at);

COMMIT;
```

**Шаг 4. Применить миграцию локально через docker:**

В action отсутствует docker exec — этот шаг человеком/CI выполнится. В test_migration_016.py тесты применят 016 через тот же runner что conftest.py использует для 012-015.

**Note about migration_template hardcoded DEFAULT '':**
Это технически не валидно для CAMP-09 (CampaignEnqueueWorker нужен реальный message_template чтобы render_template сработал). На API-уровне (Pydantic CampaignCreate.message_template: constr(min_length=1)) валидируем что юзер ОБЯЗАН указать template. DB DEFAULT '' позволяет идемпотентно применить миграцию без backfill (БД чистая per Phase 1 D-01) и не блокирует тесты которые создают draft кампанию без явного template.
  </action>
  <verify>
    <automated>test -f migrations/016_phase4.sql &amp;&amp; test -f tests/test_migration_016.py &amp;&amp; test -f tests/test_campaigns_model.py &amp;&amp; test -f tests/test_campaign_router.py &amp;&amp; test -f tests/test_sender_lock.py &amp;&amp; pytest tests/test_migration_016.py tests/test_campaigns_model.py tests/test_campaign_router.py tests/test_sender_lock.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All 4 test stub files exist: `for f in tests/test_migration_016.py tests/test_campaigns_model.py tests/test_campaign_router.py tests/test_sender_lock.py; do test -f "$f" || exit 1; done`
    - migrations/016_phase4.sql exists with BEGIN/COMMIT: `grep -E "^BEGIN;" migrations/016_phase4.sql && grep -E "^COMMIT;" migrations/016_phase4.sql`
    - Migration creates 3 new tables: `grep -cE "CREATE TABLE IF NOT EXISTS (campaigns|campaign_senders|campaign_contact_assignments)" migrations/016_phase4.sql` returns 3
    - Migration drops context_contact_assignments: `grep -E "DROP TABLE IF EXISTS context_contact_assignments" migrations/016_phase4.sql`
    - Migration adds conversations.campaign_id NULLable: `grep -E "ALTER TABLE conversations.*ADD COLUMN IF NOT EXISTS campaign_id UUID" migrations/016_phase4.sql`
    - Migration adds message_queue.campaign_id NULLable per Q1 (NO `NOT NULL` after campaign_id type): `grep -E "ALTER TABLE message_queue.*ADD COLUMN IF NOT EXISTS campaign_id UUID REFERENCES campaigns\(id\) ON DELETE SET NULL" migrations/016_phase4.sql && ! grep -E "message_queue ADD COLUMN.*campaign_id UUID NOT NULL" migrations/016_phase4.sql`
    - Status CHECK extends with lead/handoff/finished: `grep -E "CHECK.*lead.*handoff.*finished" migrations/016_phase4.sql`
    - 3 CHECK constraints on campaigns: `grep -cE "CONSTRAINT campaigns_(status|work_hours|work_days)_check" migrations/016_phase4.sql` returns 3
    - Indexes present: `grep -cE "CREATE (UNIQUE )?INDEX IF NOT EXISTS idx_" migrations/016_phase4.sql` returns >= 8
    - 3 webhook URL columns + 3 trigger_hint columns: `grep -cE "(lead|handoff|finish)_(webhook_url|trigger_hint)" migrations/016_phase4.sql` returns 6
    - conftest.py has test_campaign_factory fixture: `grep -E "def test_campaign_factory" tests/conftest.py`
    - conftest.py applies migration 016: `grep -E "016_phase4\.sql" tests/conftest.py`
    - Test stubs collect without error: `pytest tests/test_migration_016.py tests/test_campaigns_model.py tests/test_campaign_router.py tests/test_sender_lock.py --collect-only -q`
  </acceptance_criteria>
  <done>
    Тестовые stubs созданы (минимум 40 тестов суммарно по 4 файлам через pytest --collect-only), conftest.py расширен фабриками + migration runner применяет 016, migrations/016_phase4.sql создан со всем DDL (3 новые таблицы, DROP старой, +2 колонки в существующих, CHECK extension на conversations.status, 8+ индексов), все acceptance grep'ы проходят, pytest collection не падает.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ORM models + Pydantic schemas + /api/v1/campaigns router (CRUD + lifecycle + sender lock + duplicate)</name>
  <files>
    app/models/__init__.py,
    app/schemas/__init__.py,
    app/routers/campaigns.py,
    app/main.py
  </files>
  <behavior>
    - POST /api/v1/campaigns с валидным payload (agent_id, folder_id, sender_ids[], name, message_template) возвращает 201 + CampaignResponse {id, status='draft', workspace_id=ctx.workspace_id, ...}
    - POST с agent_id из другого workspace → 404 {code: 'AGENT_NOT_FOUND'}
    - POST с folder_id из другого workspace → 404 {code: 'FOLDER_NOT_FOUND'}
    - POST с любым sender_id из другого workspace → 404 {code: 'SENDER_NOT_FOUND'} (Q4 workspace-isolation)
    - POST с timezone='Invalid/Zone' → 422 {code: 'INVALID_TIMEZONE'}
    - POST с дублирующимся именем в workspace → 409 {code: 'CAMPAIGN_NAME_DUPLICATE'}
    - GET /api/v1/campaigns возвращает только campaigns ctx.workspace_id
    - GET /api/v1/campaigns/{id} возвращает CampaignResponse с computed `is_exhausted: bool` и `attached_senders: [{sender_id, locked_by_campaign_id: UUID | None}]`
    - GET /{id} из другого workspace → 404
    - PATCH /{id} partial-update работает на draft и paused (на running запрещены некоторые поля, например agent_id / folder_id — block 409 с пояснением)
    - DELETE /{id} running → 409 {code: 'CAMPAIGN_RUNNING'}
    - DELETE /{id} draft/paused/done → 204 (hard delete, FK cascade срабатывает: campaign_senders/cca CASCADE, conversations/message_queue SET NULL)
    - POST /{id}/start: draft|paused → running, проверяет sender lock (если sender в другой running кампании этого workspace → 409 с conflicts списком), требует ≥1 sender attached
    - POST /{id}/pause: running → paused
    - POST /{id}/resume: paused → running, ре-чекает sender lock
    - POST /{id}/finish: running|paused → done (terminal)
    - POST /{id}/start на done → 409 {code: 'INVALID_TRANSITION'}
    - POST /{id}/duplicate: копирует campaigns row + campaign_senders, имя `"{name} (copy)"` или `"{name} (copy N)"` если конфликт, status='draft', НЕ копирует queue/assignments
  </behavior>
  <read_first>
    - app/routers/agents.py (полностью — Phase 3 workspace-scoped CRUD pattern, duplicate-name retry-on-IntegrityError ~lines 255+, DELETE handler ~line 246)
    - app/routers/folders.py (полностью — get_or_create_by_name, DELETE с 409 на occupancy)
    - app/utils/auth.py (полностью — AuthDep, AuthCtx)
    - app/main.py (полностью — паттерн include_router)
    - app/models/__init__.py (полностью — паттерн UUID PK, server_default timestamps, SQLEnum vs String+CHECK, relationship())
    - app/schemas/__init__.py (полностью — паттерн ConfigDict(from_attributes=True), partial PATCH с Optional полями)
    - .planning/phases/04-campaigns/04-CONTEXT.md `<decisions>` D-01..D-07 (модель + связи + lifecycle + sender lock + delete семантика)
    - .planning/phases/04-campaigns/04-RESEARCH.md `## Architecture Patterns` Pattern 1 (workspace-scoped CRUD), Pattern 5 (sender lock через FOR UPDATE)
    - .planning/phases/04-campaigns/04-RESEARCH.md `## Code Examples` Пример 1 (CRUD endpoint skeleton)
    - .planning/phases/04-campaigns/04-RESEARCH.md `## Common Pitfalls` Pitfall 4 (TOCTOU race в /start — рекомендация simple acceptable race для v1)
    - .planning/phases/04-campaigns/04-RESEARCH.md `## Open Questions` Q2 (duplicate), Q4 (workspace isolation)
    - .planning/phases/04-campaigns/04-01-AUDIT.md (Section 6 — все Q resolutions)
    - migrations/016_phase4.sql (уже создана в Task 1)
    - tests/test_campaigns_model.py, tests/test_campaign_router.py, tests/test_sender_lock.py (stubs из Task 1 — то что нужно imlement; снять pytest.skip когда тест проходит)
  </read_first>
  <action>
**Шаг 1. ORM-модели в `app/models/__init__.py`:**

В конце файла (после существующих классов) ДОБАВИТЬ:

```python
class Campaign(Base):
    __tablename__ = "campaigns"

    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    workspace_id = Column(UUID(as_uuid=True), ForeignKey("workspaces.id", ondelete="CASCADE"), nullable=False)
    agent_id = Column(UUID(as_uuid=True), ForeignKey("ai_contexts.id", ondelete="RESTRICT"), nullable=False)
    folder_id = Column(UUID(as_uuid=True), ForeignKey("folders.id", ondelete="RESTRICT"), nullable=False)
    name = Column(String(150), nullable=False)
    description = Column(Text, nullable=True)
    status = Column(String(20), nullable=False, default="draft")  # draft/running/paused/done — CHECK enforced in DB
    timezone = Column(Text, nullable=False, default="Europe/Moscow")
    work_hour_start = Column(Integer, nullable=False, default=9)
    work_hour_end = Column(Integer, nullable=False, default=20)
    work_days_mask = Column(Integer, nullable=False, default=31)  # Mo-Fri
    start_date = Column(DateTime(timezone=True), nullable=True)
    stop_date = Column(DateTime(timezone=True), nullable=True)
    message_template = Column(Text, nullable=False, server_default="")
    lead_webhook_url = Column(Text, nullable=True)
    handoff_webhook_url = Column(Text, nullable=True)
    finish_webhook_url = Column(Text, nullable=True)
    lead_trigger_hint = Column(Text, nullable=True)
    handoff_trigger_hint = Column(Text, nullable=True)
    finish_trigger_hint = Column(Text, nullable=True)
    tools = Column(JSONB, nullable=False, server_default=text("'[]'::jsonb"))
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())

    workspace = relationship("Workspace", backref="campaigns")
    agent = relationship("AIContext", backref="campaigns")
    folder = relationship("Folder", backref="campaigns")
    senders = relationship("CampaignSender", back_populates="campaign", cascade="all, delete-orphan")


class CampaignSender(Base):
    __tablename__ = "campaign_senders"

    campaign_id = Column(UUID(as_uuid=True), ForeignKey("campaigns.id", ondelete="CASCADE"), primary_key=True)
    sender_id = Column(UUID(as_uuid=True), ForeignKey("senders.id", ondelete="CASCADE"), primary_key=True)
    workspace_id = Column(UUID(as_uuid=True), ForeignKey("workspaces.id", ondelete="CASCADE"), nullable=False)
    added_at = Column(DateTime(timezone=True), server_default=func.now())

    campaign = relationship("Campaign", back_populates="senders")
    sender = relationship("Sender")


class CampaignContactAssignment(Base):
    __tablename__ = "campaign_contact_assignments"

    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    workspace_id = Column(UUID(as_uuid=True), ForeignKey("workspaces.id", ondelete="CASCADE"), nullable=False)
    campaign_id = Column(UUID(as_uuid=True), ForeignKey("campaigns.id", ondelete="CASCADE"), nullable=False)
    contact_phone = Column(String(20), nullable=False)
    sender_id = Column(UUID(as_uuid=True), ForeignKey("senders.id", ondelete="CASCADE"), nullable=False)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
```

**ВАЖНО:** Удалить класс `ContextContactAssignment` (строки ~330-349) — таблица дропнута в миграции 016 (D-06).

В `Conversation` (строка ~217-238) ДОБАВИТЬ:
```python
campaign_id = Column(UUID(as_uuid=True), ForeignKey("campaigns.id", ondelete="SET NULL"), nullable=True)
```

В `MessageQueue` (строки ~167-214) ДОБАВИТЬ:
```python
campaign_id = Column(UUID(as_uuid=True), ForeignKey("campaigns.id", ondelete="SET NULL"), nullable=True)
```

**Шаг 2. Pydantic-схемы в `app/schemas/__init__.py`:**

В конце файла ДОБАВИТЬ:

```python
class ToolSpec(BaseModel):
    """Phase 4 C-10 — Pydantic-валидация shape для campaigns.tools JSONB."""
    model_config = ConfigDict(from_attributes=True)
    name: constr(pattern=r"^[a-zA-Z_][a-zA-Z0-9_]*$", max_length=64)
    description: constr(max_length=1024)
    parameters: dict  # raw OpenAI function-call JSON schema
    webhook_url: HttpUrl
    webhook_method: Literal["POST", "GET"] = "POST"


class CampaignSenderAttach(BaseModel):
    """Краткая read-only форма sender в attached_senders."""
    sender_id: UUID
    locked_by_campaign_id: UUID | None = None
    locked_by_campaign_name: str | None = None


class CampaignCreate(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    name: constr(min_length=1, max_length=150)
    description: str | None = None
    agent_id: UUID
    folder_id: UUID
    sender_ids: list[UUID] = Field(default_factory=list)  # may be empty at create; required for start
    message_template: constr(min_length=1)
    timezone: str = "Europe/Moscow"
    work_hour_start: int = Field(default=9, ge=0, le=23)
    work_hour_end: int = Field(default=20, ge=1, le=24)
    work_days_mask: int = Field(default=31, ge=1, le=127)
    start_date: datetime | None = None
    stop_date: datetime | None = None
    lead_webhook_url: HttpUrl | None = None
    handoff_webhook_url: HttpUrl | None = None
    finish_webhook_url: HttpUrl | None = None
    lead_trigger_hint: str | None = None
    handoff_trigger_hint: str | None = None
    finish_trigger_hint: str | None = None
    tools: list[ToolSpec] = Field(default_factory=list)

    @model_validator(mode="after")
    def check_work_hours(self):
        if self.work_hour_start >= self.work_hour_end:
            raise ValueError("work_hour_start must be less than work_hour_end")
        return self


class CampaignUpdate(BaseModel):
    """Partial PATCH — все поля Optional."""
    name: Optional[constr(min_length=1, max_length=150)] = None
    description: Optional[str] = None
    agent_id: Optional[UUID] = None
    folder_id: Optional[UUID] = None
    message_template: Optional[constr(min_length=1)] = None
    timezone: Optional[str] = None
    work_hour_start: Optional[int] = Field(default=None, ge=0, le=23)
    work_hour_end: Optional[int] = Field(default=None, ge=1, le=24)
    work_days_mask: Optional[int] = Field(default=None, ge=1, le=127)
    start_date: Optional[datetime] = None
    stop_date: Optional[datetime] = None
    lead_webhook_url: Optional[HttpUrl] = None
    handoff_webhook_url: Optional[HttpUrl] = None
    finish_webhook_url: Optional[HttpUrl] = None
    lead_trigger_hint: Optional[str] = None
    handoff_trigger_hint: Optional[str] = None
    finish_trigger_hint: Optional[str] = None
    tools: Optional[list[ToolSpec]] = None


class CampaignResponse(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: UUID
    workspace_id: UUID
    name: str
    description: str | None = None
    agent_id: UUID
    folder_id: UUID
    status: str
    timezone: str
    work_hour_start: int
    work_hour_end: int
    work_days_mask: int
    start_date: datetime | None = None
    stop_date: datetime | None = None
    message_template: str
    lead_webhook_url: str | None = None
    handoff_webhook_url: str | None = None
    finish_webhook_url: str | None = None
    lead_trigger_hint: str | None = None
    handoff_trigger_hint: str | None = None
    finish_trigger_hint: str | None = None
    tools: list[dict]
    attached_senders: list[CampaignSenderAttach] = Field(default_factory=list)
    is_exhausted: bool = False  # computed at read-time
    created_at: datetime
    updated_at: datetime


class CampaignListResponse(BaseModel):
    items: list[CampaignResponse]
    total: int
```

**Шаг 3. Роутер `app/routers/campaigns.py`:**

Новый файл. Структура (skeleton, executor наполняет тело):

```python
import logging
import zoneinfo
from typing import Optional
from uuid import UUID

from fastapi import APIRouter, Depends, HTTPException, Query, status
from sqlalchemy import select, text, func
from sqlalchemy.exc import IntegrityError
from sqlalchemy.ext.asyncio import AsyncSession

from app.database import get_db
from app.models import (
    Campaign, CampaignSender, CampaignContactAssignment,
    AIContext, Folder, Sender, Conversation, MessageQueue,
)
from app.schemas import (
    CampaignCreate, CampaignUpdate, CampaignResponse,
    CampaignListResponse, CampaignSenderAttach,
)
from app.utils.auth import AuthCtx, auth_dep

logger = logging.getLogger(__name__)

router = APIRouter(prefix="/api/v1/campaigns", tags=["campaigns"])


# ── Helpers ────────────────────────────────────────────────────────────
async def _load_campaign(db: AsyncSession, ctx: AuthCtx, campaign_id: UUID) -> Campaign:
    """Workspace-scoped fetch by id; 404 if not found or wrong workspace.

    # TODO(v2-rls): replaced by RLS policy app.workspace_id
    """
    res = await db.execute(
        select(Campaign).where(
            Campaign.id == campaign_id,
            Campaign.workspace_id == ctx.workspace_id,
        )
    )
    c = res.scalars().first()
    if c is None:
        raise HTTPException(404, {"code": "CAMPAIGN_NOT_FOUND", "message": "Campaign not found"})
    return c


async def _validate_workspace_owns_agent(db, ctx, agent_id) -> None:
    """404 если агент не принадлежит workspace."""
    ...


async def _validate_workspace_owns_folder(db, ctx, folder_id) -> None: ...
async def _validate_workspace_owns_senders(db, ctx, sender_ids: list[UUID]) -> None: ...


async def _compute_is_exhausted(db: AsyncSession, campaign_id: UUID, folder_id: UUID) -> bool:
    """is_exhausted = (folder contacts with tg_status='registered' all assigned) AND (no pending/processing in queue)."""
    unassigned = await db.execute(text("""
        SELECT COUNT(*) FROM contacts c
        WHERE c.folder_id = :fid
          AND c.tg_status = 'registered'
          AND NOT EXISTS (
              SELECT 1 FROM campaign_contact_assignments cca
              WHERE cca.campaign_id = :cid AND cca.contact_phone = c.phone
          )
    """), {"fid": str(folder_id), "cid": str(campaign_id)})
    unassigned_count = unassigned.scalar() or 0

    pending = await db.execute(text("""
        SELECT COUNT(*) FROM message_queue
        WHERE campaign_id = :cid AND status IN ('pending','processing')
    """), {"cid": str(campaign_id)})
    pending_count = pending.scalar() or 0

    return unassigned_count == 0 and pending_count == 0


async def _build_attached_senders(db: AsyncSession, ctx: AuthCtx, campaign_id: UUID) -> list[CampaignSenderAttach]:
    """SELECT campaign_senders + JOIN running campaigns to derive locked_by_campaign_*."""
    rows = await db.execute(text("""
        SELECT cs.sender_id,
               (SELECT c.id FROM campaign_senders cs2
                  JOIN campaigns c ON c.id = cs2.campaign_id
                  WHERE cs2.sender_id = cs.sender_id
                    AND c.status = 'running'
                    AND c.id != :cid
                    AND c.workspace_id = :wid
                  LIMIT 1) AS locked_by_id,
               (SELECT c.name FROM campaign_senders cs2
                  JOIN campaigns c ON c.id = cs2.campaign_id
                  WHERE cs2.sender_id = cs.sender_id
                    AND c.status = 'running'
                    AND c.id != :cid
                    AND c.workspace_id = :wid
                  LIMIT 1) AS locked_by_name
        FROM campaign_senders cs
        WHERE cs.campaign_id = :cid
    """), {"cid": str(campaign_id), "wid": str(ctx.workspace_id)})

    return [
        CampaignSenderAttach(
            sender_id=r[0],
            locked_by_campaign_id=r[1],
            locked_by_campaign_name=r[2],
        )
        for r in rows.fetchall()
    ]


async def _campaign_to_response(db, ctx, campaign: Campaign) -> CampaignResponse:
    attached = await _build_attached_senders(db, ctx, campaign.id)
    is_exhausted = await _compute_is_exhausted(db, campaign.id, campaign.folder_id)
    return CampaignResponse(
        **{k: getattr(campaign, k) for k in CampaignResponse.model_fields if k not in ("attached_senders", "is_exhausted")},
        attached_senders=attached,
        is_exhausted=is_exhausted,
    )


# ── Endpoints ──────────────────────────────────────────────────────────

@router.post("", response_model=CampaignResponse, status_code=201)
async def create_campaign(payload: CampaignCreate, ctx: AuthCtx = Depends(auth_dep), db: AsyncSession = Depends(get_db)):
    # 1. Validate timezone
    try:
        zoneinfo.ZoneInfo(payload.timezone)
    except (zoneinfo.ZoneInfoNotFoundError, Exception):
        raise HTTPException(422, {"code": "INVALID_TIMEZONE", "message": f"Unknown IANA timezone '{payload.timezone}'"})

    # 2. Workspace-scoped owns checks
    await _validate_workspace_owns_agent(db, ctx, payload.agent_id)
    await _validate_workspace_owns_folder(db, ctx, payload.folder_id)
    if payload.sender_ids:
        await _validate_workspace_owns_senders(db, ctx, payload.sender_ids)

    # 3. Duplicate name check
    existing = (await db.execute(
        select(Campaign).where(Campaign.workspace_id == ctx.workspace_id, Campaign.name == payload.name.strip())
    )).scalars().first()
    if existing:
        raise HTTPException(409, {"code": "CAMPAIGN_NAME_DUPLICATE", "message": f"Campaign '{payload.name}' already exists"})

    # 4. Insert
    camp = Campaign(
        workspace_id=ctx.workspace_id,
        name=payload.name.strip(),
        description=payload.description,
        agent_id=payload.agent_id,
        folder_id=payload.folder_id,
        message_template=payload.message_template,
        timezone=payload.timezone,
        work_hour_start=payload.work_hour_start,
        work_hour_end=payload.work_hour_end,
        work_days_mask=payload.work_days_mask,
        start_date=payload.start_date,
        stop_date=payload.stop_date,
        lead_webhook_url=str(payload.lead_webhook_url) if payload.lead_webhook_url else None,
        handoff_webhook_url=str(payload.handoff_webhook_url) if payload.handoff_webhook_url else None,
        finish_webhook_url=str(payload.finish_webhook_url) if payload.finish_webhook_url else None,
        lead_trigger_hint=payload.lead_trigger_hint,
        handoff_trigger_hint=payload.handoff_trigger_hint,
        finish_trigger_hint=payload.finish_trigger_hint,
        tools=[t.model_dump(mode="json") for t in payload.tools],
        status="draft",
    )
    db.add(camp)
    await db.flush()  # generate id

    for sid in payload.sender_ids:
        db.add(CampaignSender(campaign_id=camp.id, sender_id=sid, workspace_id=ctx.workspace_id))

    await db.commit()
    await db.refresh(camp)
    return await _campaign_to_response(db, ctx, camp)


@router.get("", response_model=CampaignListResponse)
async def list_campaigns(
    status: Optional[str] = Query(default=None),
    ctx: AuthCtx = Depends(auth_dep),
    db: AsyncSession = Depends(get_db),
):
    # TODO(v2-rls): replaced by RLS policy app.workspace_id
    q = select(Campaign).where(Campaign.workspace_id == ctx.workspace_id)
    if status:
        q = q.where(Campaign.status == status)
    rows = (await db.execute(q.order_by(Campaign.created_at.desc()))).scalars().all()
    items = [await _campaign_to_response(db, ctx, c) for c in rows]
    return CampaignListResponse(items=items, total=len(items))


@router.get("/{campaign_id}", response_model=CampaignResponse)
async def get_campaign(campaign_id: UUID, ctx: AuthCtx = Depends(auth_dep), db: AsyncSession = Depends(get_db)):
    c = await _load_campaign(db, ctx, campaign_id)
    return await _campaign_to_response(db, ctx, c)


@router.patch("/{campaign_id}", response_model=CampaignResponse)
async def patch_campaign(campaign_id: UUID, payload: CampaignUpdate, ctx: AuthCtx = Depends(auth_dep), db: AsyncSession = Depends(get_db)):
    c = await _load_campaign(db, ctx, campaign_id)
    update_data = payload.model_dump(exclude_unset=True)

    # Block changing agent_id / folder_id on running campaign
    if c.status == "running":
        forbidden = {"agent_id", "folder_id"}
        present = forbidden & set(update_data.keys())
        if present:
            raise HTTPException(409, {"code": "CAMPAIGN_RUNNING_IMMUTABLE_FIELDS", "fields": list(present)})

    # Validate timezone if changed
    if "timezone" in update_data:
        try:
            zoneinfo.ZoneInfo(update_data["timezone"])
        except Exception:
            raise HTTPException(422, {"code": "INVALID_TIMEZONE", "message": f"Unknown IANA timezone '{update_data['timezone']}'"})

    # If agent_id / folder_id changing — workspace check
    if "agent_id" in update_data:
        await _validate_workspace_owns_agent(db, ctx, update_data["agent_id"])
    if "folder_id" in update_data:
        await _validate_workspace_owns_folder(db, ctx, update_data["folder_id"])

    # Convert HttpUrl → str
    for k in ("lead_webhook_url", "handoff_webhook_url", "finish_webhook_url"):
        if k in update_data and update_data[k] is not None:
            update_data[k] = str(update_data[k])

    if "tools" in update_data and update_data["tools"] is not None:
        update_data["tools"] = [t.model_dump(mode="json") if hasattr(t, "model_dump") else t for t in update_data["tools"]]

    for k, v in update_data.items():
        setattr(c, k, v)

    try:
        await db.commit()
    except IntegrityError as e:
        await db.rollback()
        if "idx_campaigns_workspace_name" in str(e.orig):
            raise HTTPException(409, {"code": "CAMPAIGN_NAME_DUPLICATE"})
        raise
    await db.refresh(c)
    return await _campaign_to_response(db, ctx, c)


@router.delete("/{campaign_id}", status_code=204)
async def delete_campaign(campaign_id: UUID, ctx: AuthCtx = Depends(auth_dep), db: AsyncSession = Depends(get_db)):
    c = await _load_campaign(db, ctx, campaign_id)
    if c.status == "running":
        raise HTTPException(409, {"code": "CAMPAIGN_RUNNING", "message": "Stop campaign before deleting"})
    await db.delete(c)
    await db.commit()
    return


# ── Lifecycle ──────────────────────────────────────────────────────────

async def _check_sender_lock(db, ctx, campaign_id) -> list:
    """Returns list of conflict tuples [(sender_id, other_campaign_id, other_campaign_name), ...]."""
    rows = await db.execute(text("""
        SELECT cs.sender_id, c.id, c.name
        FROM campaign_senders cs
        JOIN campaigns c ON c.id = cs.campaign_id
        WHERE cs.sender_id IN (SELECT sender_id FROM campaign_senders WHERE campaign_id = :cid)
          AND c.status = 'running'
          AND c.id != :cid
          AND c.workspace_id = :wid
    """), {"cid": str(campaign_id), "wid": str(ctx.workspace_id)})
    return rows.fetchall()


@router.post("/{campaign_id}/start", response_model=CampaignResponse)
async def start_campaign(campaign_id: UUID, ctx: AuthCtx = Depends(auth_dep), db: AsyncSession = Depends(get_db)):
    c = await _load_campaign(db, ctx, campaign_id)
    if c.status not in ("draft", "paused"):
        raise HTTPException(409, {"code": "INVALID_TRANSITION", "from": c.status, "to": "running"})

    # ≥1 sender required
    sender_count = (await db.execute(
        select(func.count()).select_from(CampaignSender).where(CampaignSender.campaign_id == c.id)
    )).scalar()
    if sender_count == 0:
        raise HTTPException(422, {"code": "NO_SENDERS_ATTACHED", "message": "Attach at least one sender before starting"})

    # Sender lock check
    conflicts = await _check_sender_lock(db, ctx, c.id)
    if conflicts:
        raise HTTPException(409, {
            "code": "SENDER_LOCK_CONFLICT",
            "conflicts": [{"sender_id": str(r[0]), "campaign_id": str(r[1]), "campaign_name": r[2]} for r in conflicts],
        })

    c.status = "running"
    await db.commit()
    await db.refresh(c)
    return await _campaign_to_response(db, ctx, c)


@router.post("/{campaign_id}/pause", response_model=CampaignResponse)
async def pause_campaign(campaign_id: UUID, ctx: AuthCtx = Depends(auth_dep), db: AsyncSession = Depends(get_db)):
    c = await _load_campaign(db, ctx, campaign_id)
    if c.status != "running":
        raise HTTPException(409, {"code": "INVALID_TRANSITION", "from": c.status, "to": "paused"})
    c.status = "paused"
    await db.commit()
    await db.refresh(c)
    return await _campaign_to_response(db, ctx, c)


@router.post("/{campaign_id}/resume", response_model=CampaignResponse)
async def resume_campaign(campaign_id: UUID, ctx: AuthCtx = Depends(auth_dep), db: AsyncSession = Depends(get_db)):
    c = await _load_campaign(db, ctx, campaign_id)
    if c.status != "paused":
        raise HTTPException(409, {"code": "INVALID_TRANSITION", "from": c.status, "to": "running"})
    conflicts = await _check_sender_lock(db, ctx, c.id)
    if conflicts:
        raise HTTPException(409, {
            "code": "SENDER_LOCK_CONFLICT",
            "conflicts": [{"sender_id": str(r[0]), "campaign_id": str(r[1]), "campaign_name": r[2]} for r in conflicts],
        })
    c.status = "running"
    await db.commit()
    await db.refresh(c)
    return await _campaign_to_response(db, ctx, c)


@router.post("/{campaign_id}/finish", response_model=CampaignResponse)
async def finish_campaign(campaign_id: UUID, ctx: AuthCtx = Depends(auth_dep), db: AsyncSession = Depends(get_db)):
    c = await _load_campaign(db, ctx, campaign_id)
    if c.status not in ("running", "paused"):
        raise HTTPException(409, {"code": "INVALID_TRANSITION", "from": c.status, "to": "done"})
    c.status = "done"
    await db.commit()
    await db.refresh(c)
    return await _campaign_to_response(db, ctx, c)


@router.post("/{campaign_id}/duplicate", response_model=CampaignResponse, status_code=201)
async def duplicate_campaign(campaign_id: UUID, ctx: AuthCtx = Depends(auth_dep), db: AsyncSession = Depends(get_db)):
    """Q2: copy row + campaign_senders, NOT queue items, NOT cca."""
    c = await _load_campaign(db, ctx, campaign_id)

    # Find free name "{name} (copy)" / "{name} (copy 2)" ...
    base = f"{c.name} (copy)"
    candidate = base
    i = 1
    while True:
        existing = (await db.execute(
            select(Campaign).where(Campaign.workspace_id == ctx.workspace_id, Campaign.name == candidate)
        )).scalars().first()
        if not existing:
            break
        i += 1
        candidate = f"{c.name} (copy {i})"

    new_c = Campaign(
        workspace_id=ctx.workspace_id,
        name=candidate,
        description=c.description,
        agent_id=c.agent_id,
        folder_id=c.folder_id,
        message_template=c.message_template,
        timezone=c.timezone,
        work_hour_start=c.work_hour_start,
        work_hour_end=c.work_hour_end,
        work_days_mask=c.work_days_mask,
        start_date=c.start_date,
        stop_date=c.stop_date,
        lead_webhook_url=c.lead_webhook_url,
        handoff_webhook_url=c.handoff_webhook_url,
        finish_webhook_url=c.finish_webhook_url,
        lead_trigger_hint=c.lead_trigger_hint,
        handoff_trigger_hint=c.handoff_trigger_hint,
        finish_trigger_hint=c.finish_trigger_hint,
        tools=c.tools,
        status="draft",
    )
    db.add(new_c)
    await db.flush()

    # Copy campaign_senders
    sender_rows = (await db.execute(select(CampaignSender).where(CampaignSender.campaign_id == c.id))).scalars().all()
    for s in sender_rows:
        db.add(CampaignSender(campaign_id=new_c.id, sender_id=s.sender_id, workspace_id=ctx.workspace_id))

    await db.commit()
    await db.refresh(new_c)
    return await _campaign_to_response(db, ctx, new_c)
```

**Шаг 4. Регистрация в `app/main.py`:**

Найти существующие `app.include_router(...)` блоки. Добавить:
```python
from app.routers import campaigns as campaigns_router
app.include_router(campaigns_router.router)
```

**Шаг 5. Снять pytest.skip с stubs в Task 1**, заменить на реальные test bodies. (Эта работа происходит итеративно — executor пишет implementation, прогоняет тест, фиксит ошибки.)
  </action>
  <verify>
    <automated>pytest tests/test_campaigns_model.py tests/test_campaign_router.py tests/test_sender_lock.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Campaign / CampaignSender / CampaignContactAssignment ORM в models: `grep -cE "class (Campaign|CampaignSender|CampaignContactAssignment)\(Base\):" app/models/__init__.py` returns 3
    - ContextContactAssignment removed from models: `! grep -E "class ContextContactAssignment" app/models/__init__.py`
    - Conversation has campaign_id: `grep -E "campaign_id = Column\(UUID.*ondelete=\"SET NULL\"" app/models/__init__.py` (in Conversation block)
    - MessageQueue has campaign_id: same pattern check
    - ToolSpec schema present: `grep -E "class ToolSpec\(BaseModel\)" app/schemas/__init__.py`
    - CampaignCreate / Update / Response present: `grep -cE "class Campaign(Create|Update|Response|ListResponse|SenderAttach)\(BaseModel\)" app/schemas/__init__.py` returns 5
    - campaigns.py router exists with all endpoints: `for ep in '"" GET' '"" POST' "/{campaign_id} GET" "/{campaign_id} PATCH" "/{campaign_id} DELETE" "/start" "/pause" "/resume" "/finish" "/duplicate"; do grep -E "router.(get|post|patch|delete)" app/routers/campaigns.py; done` returns matches
    - Router registered in main: `grep -E "include_router\(campaigns" app/main.py`
    - Tests pass: `pytest tests/test_campaigns_model.py tests/test_campaign_router.py tests/test_sender_lock.py -x` exits 0
  </acceptance_criteria>
  <done>
    ORM модели Campaign/CampaignSender/CampaignContactAssignment созданы, ContextContactAssignment удалён, Conversation/MessageQueue расширены campaign_id. Pydantic схемы (ToolSpec, CampaignCreate/Update/Response/List, CampaignSenderAttach) в schemas. Роутер `/api/v1/campaigns` с 10 endpoints (CRUD + 5 lifecycle + duplicate) под AuthDep + workspace-scope. Регистрация в main.py. Тесты test_campaigns_model.py + test_campaign_router.py + test_sender_lock.py зелёные.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close TODO(phase-4) markers in agents/folders/senders routers + update REQUIREMENTS.md CAMP-14</name>
  <files>
    app/routers/agents.py,
    app/routers/folders.py,
    app/routers/senders.py,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - app/routers/agents.py (полностью — текущая структура; точки: `campaign_count: 0` ~line 64, DELETE handler ~line 246 c TODO(phase-4))
    - app/routers/folders.py (полностью — DELETE handler ~line 248 c TODO(phase-4))
    - app/routers/senders.py (полностью — DELETE handler + PATCH lifecycle_status — добавляем check active campaign)
    - app/models/__init__.py (новые модели Campaign / CampaignSender из Task 2 — для импорта)
    - .planning/REQUIREMENTS.md (полностью — CAMP-14 строка которая должна быть переписана)
    - .planning/phases/04-campaigns/04-CONTEXT.md `<decisions>` D-13 (3 webhook URL обоснование)
    - .planning/phases/04-campaigns/04-01-AUDIT.md Section 1 (точные file:line меток TODO)
    - tests/test_campaign_router.py (тесты на `test_block_delete_agent_when_running_campaign_409`, `test_block_delete_folder_when_running_campaign_409`, `test_block_delete_sender_when_running_campaign_409`, `test_block_patch_sender_lifecycle_to_paused_when_running_campaign_409`, `test_agent_campaign_count_real_not_hardcoded_zero`)
  </read_first>
  <action>
**Шаг 1. `app/routers/agents.py` — реальный campaign_count + block DELETE:**

Найти строку ~64 с `campaign_count=0` хардкодом. Заменить на subquery:

В Phase 3 `list_agents`:
```python
# WAS (Phase 3 D-10 stub):
# campaign_count=0  # TODO(phase-4): real COUNT

# NOW:
campaign_count_row = (await db.execute(text("""
    SELECT a.id AS agent_id, COUNT(c.id) AS cnt
    FROM ai_contexts a
    LEFT JOIN campaigns c ON c.agent_id = a.id AND c.workspace_id = a.workspace_id
    WHERE a.workspace_id = :wid
    GROUP BY a.id
"""), {"wid": str(ctx.workspace_id)})).fetchall()
counts = {r[0]: r[1] for r in campaign_count_row}
# ...
# При сборке AgentResponse:
campaign_count=counts.get(agent.id, 0)
```

В `get_agent` (single) аналогично — отдельный SELECT COUNT для этого агента.

В DELETE handler (~line 246):
```python
@router.delete("/{agent_id}", status_code=204)
async def delete_agent(agent_id: UUID, ctx: AuthCtx = Depends(auth_dep), db: AsyncSession = Depends(get_db)):
    agent = await _load_agent(db, ctx, agent_id)

    # Phase 4 D-09 close: block DELETE if active (running) campaign references this agent
    active = (await db.execute(text("""
        SELECT id, name FROM campaigns
        WHERE agent_id = :aid AND workspace_id = :wid AND status = 'running'
    """), {"aid": str(agent_id), "wid": str(ctx.workspace_id)})).fetchall()
    if active:
        raise HTTPException(409, {
            "code": "AGENT_USED_BY_RUNNING_CAMPAIGN",
            "message": "Cannot delete agent — used by running campaign(s)",
            "campaigns": [{"id": str(r[0]), "name": r[1]} for r in active],
        })

    await db.delete(agent)
    await db.commit()
    return
```

Удалить `# TODO(phase-4)` метку — закрыто.

**Шаг 2. `app/routers/folders.py` — block DELETE при active campaign:**

Найти строку ~248 с TODO(phase-4). DELETE handler:
```python
@router.delete("/{folder_id}", status_code=204)
async def delete_folder(folder_id: UUID, force: bool = False, ctx: AuthCtx = Depends(auth_dep), db: AsyncSession = Depends(get_db)):
    folder = await _load_folder(db, ctx, folder_id)

    # Phase 4 close TODO: block delete если есть active campaign
    active = (await db.execute(text("""
        SELECT id, name FROM campaigns
        WHERE folder_id = :fid AND workspace_id = :wid AND status = 'running'
    """), {"fid": str(folder_id), "wid": str(ctx.workspace_id)})).fetchall()
    if active:
        raise HTTPException(409, {
            "code": "FOLDER_USED_BY_RUNNING_CAMPAIGN",
            "message": "Cannot delete folder — used by running campaign(s)",
            "campaigns": [{"id": str(r[0]), "name": r[1]} for r in active],
        })

    # Existing contact_count check stays (Phase 2 D-06)
    # ...
```

Удалить `# TODO(phase-4)` метку.

**Шаг 3. `app/routers/senders.py` — block DELETE/PATCH lifecycle_status при active campaign:**

Добавить helper:
```python
async def _check_sender_not_in_running_campaign(db, ctx, sender_id) -> None:
    """Phase 4 close: sender нельзя удалить/паузить если он в running кампании."""
    active = (await db.execute(text("""
        SELECT c.id, c.name
        FROM campaign_senders cs
        JOIN campaigns c ON c.id = cs.campaign_id
        WHERE cs.sender_id = :sid AND c.workspace_id = :wid AND c.status = 'running'
    """), {"sid": str(sender_id), "wid": str(ctx.workspace_id)})).fetchall()
    if active:
        raise HTTPException(409, {
            "code": "SENDER_USED_BY_RUNNING_CAMPAIGN",
            "message": "Sender is attached to running campaign(s) — pause/stop them first",
            "campaigns": [{"id": str(r[0]), "name": r[1]} for r in active],
        })
```

Вызвать в DELETE handler перед `await db.delete(sender)`:
```python
@router.delete("/{sender_id}", status_code=204)
async def delete_sender(sender_id: UUID, ctx: AuthCtx = Depends(auth_dep), db: AsyncSession = Depends(get_db)):
    sender = await _load_sender(db, ctx, sender_id)
    await _check_sender_not_in_running_campaign(db, ctx, sender_id)
    await db.delete(sender)
    await db.commit()
```

В PATCH lifecycle_status handler (если есть, или в общем PATCH когда меняется lifecycle_status на 'paused' или 'warmup' — НЕ active):
```python
if payload.lifecycle_status and payload.lifecycle_status != "active":
    await _check_sender_not_in_running_campaign(db, ctx, sender_id)
```

**Шаг 4. Обновить `.planning/REQUIREMENTS.md` CAMP-14:**

Найти строку в REQUIREMENTS.md (раздел "Campaigns (Phase 4)"):
```
- [ ] **CAMP-14**: Webhook кампании — 3 отдельных URL на типы событий: `lead_webhook_url`, `handoff_webhook_url`, `finish_webhook_url`...
```

Если этот текст уже там — это OK (REQUIREMENTS.md уже обновлен ранее, см. строка 73 в REQUIREMENTS.md). Если же там старый текст «один webhook на кампанию» — переписать на:

```markdown
- [ ] **CAMP-14**: Webhook кампании — 3 отдельных URL на типы событий: `lead_webhook_url`, `handoff_webhook_url`, `finish_webhook_url`. Любой может быть NULL (тогда событие не вызывает webhook, но `conversation.status` всё равно обновляется). Pre-Phase-4 формулировка «один webhook на кампанию» обновлена по итогам discuss-phase (D-13) — клиент предпочёл явное разделение endpoint'ов под разные интеграции
```

Также проверить и при необходимости обновить раздел `## Traceability` — CAMP-14 должна остаться Pending Phase 4.

**Шаг 5. Verify TODO(phase-4) labels closed:**

Запустить:
```
grep -nrE "TODO\(phase-4\)" app/routers/agents.py app/routers/folders.py app/routers/senders.py
```

Этот grep должен вернуть пусто (или только not-applicable метки которые НЕ для этого плана — но AUDIT.md Section 1 указывает что эти 4 метки закрываются в Plan 04-02).
  </action>
  <verify>
    <automated>pytest tests/test_campaign_router.py::test_block_delete_agent_when_running_campaign_409 tests/test_campaign_router.py::test_block_delete_folder_when_running_campaign_409 tests/test_campaign_router.py::test_block_delete_sender_when_running_campaign_409 tests/test_campaign_router.py::test_agent_campaign_count_real_not_hardcoded_zero -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md CAMP-14 lists 3 webhook URL: `grep -E "CAMP-14.*3 отдельных webhook URL.*lead_webhook_url.*handoff_webhook_url.*finish_webhook_url" .planning/REQUIREMENTS.md` (or similar match)
    - agents.py block delete on running campaign: `grep -E "AGENT_USED_BY_RUNNING_CAMPAIGN" app/routers/agents.py`
    - folders.py block delete: `grep -E "FOLDER_USED_BY_RUNNING_CAMPAIGN" app/routers/folders.py`
    - senders.py helper + DELETE block + PATCH lifecycle block: `grep -E "SENDER_USED_BY_RUNNING_CAMPAIGN" app/routers/senders.py`
    - agents.py campaign_count is dynamic SELECT, not hardcoded 0: `grep -E "SELECT.*COUNT.*FROM campaigns" app/routers/agents.py && ! grep -E "campaign_count\s*=\s*0" app/routers/agents.py`
    - 4 TODO(phase-4) labels closed (none remaining in these 3 routers): `! grep -nE "TODO\(phase-4\)" app/routers/agents.py app/routers/folders.py app/routers/senders.py`
    - Tests pass: `pytest tests/test_campaign_router.py::test_block_delete_agent_when_running_campaign_409 tests/test_campaign_router.py::test_block_delete_folder_when_running_campaign_409 tests/test_campaign_router.py::test_block_delete_sender_when_running_campaign_409 tests/test_campaign_router.py::test_agent_campaign_count_real_not_hardcoded_zero -x`
  </acceptance_criteria>
  <done>
    Все 4 TODO(phase-4) метки в agents/folders/senders роутерах закрыты, agents.py показывает реальный campaign_count из COUNT(*) FROM campaigns, REQUIREMENTS.md CAMP-14 финально обновлен на 3 webhook URL, 4 теста в test_campaign_router.py зелёные.
  </done>
</task>

</tasks>

<verification>
- Migration 016 идемпотентна — применяется повторно без error (`pytest tests/test_migration_016.py::test_migration_016_idempotent -x`)
- Все 3 новые таблицы существуют, FK правильно ссылаются (workspace_id CASCADE, agent_id RESTRICT, folder_id RESTRICT, sender CASCADE)
- conversations.campaign_id NULLable + status CHECK расширен
- message_queue.campaign_id NULLable (per AUDIT.md Q1)
- context_contact_assignments DROPPED
- /api/v1/campaigns роутер зарегистрирован в main.py — `curl localhost:8000/openapi.json | jq '.paths | keys | map(select(startswith("/api/v1/campaigns")))'` показывает 10 endpoints
- 4 TODO(phase-4) метки закрыты в agents/folders/senders
- REQUIREMENTS.md CAMP-14 обновлён на 3 webhook URL
- Full pytest suite зелёный после Plan 04-02 завершения
</verification>

<success_criteria>
- [x] migrations/016_phase4.sql применяется идемпотентно
- [x] ORM модели Campaign/CampaignSender/CampaignContactAssignment созданы, ContextContactAssignment удалён
- [x] Conversation/MessageQueue расширены campaign_id NULLable
- [x] Pydantic schemas CampaignCreate/Update/Response/List + ToolSpec созданы
- [x] /api/v1/campaigns CRUD + 5 lifecycle endpoints + /duplicate работают
- [x] Sender lock check возвращает 409 с conflicts list при /start и /resume
- [x] 4 TODO(phase-4) метки закрыты (agents.py campaign_count + 3 DELETE/PATCH блокировки)
- [x] REQUIREMENTS.md CAMP-14 обновлён на 3 webhook URL
- [x] CAMP-01, CAMP-02, CAMP-03, CAMP-04, CAMP-07, CAMP-08, CAMP-14 closable through this plan
</success_criteria>

<output>
After completion, create `.planning/phases/04-campaigns/04-02-SUMMARY.md` with:
- Какие модели/схемы/endpoints созданы (списком).
- Migration 016 ключевые DDL (3 таблицы, 2 ALTER, DROP старой, CHECK расширение).
- Список закрытых TODO(phase-4) с file:line.
- Обновление CAMP-14 в REQUIREMENTS.md.
- Acceptance test results (X/Y тестов зелёные).
</output>
</content>
</invoke>