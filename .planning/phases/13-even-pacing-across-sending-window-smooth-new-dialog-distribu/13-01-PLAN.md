---
phase: 13-even-pacing-across-sending-window-smooth-new-dialog-distribu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_queue_even_pacing.py
autonomous: true
requirements: [PACE-01, PACE-02, PACE-03, PACE-04, PACE-05, PACE-06, PACE-07]
must_haves:
  truths:
    - "A new test file tests/test_queue_even_pacing.py exists and is collected by pytest (no import/collection errors)"
    - "Tests for PACE-01..07 are present as RED stubs (import the not-yet-existing helper / behaviour inside the test body so --collect-only stays clean)"
    - "Helpers from tests/test_queue_new_dialog_limit.py are reused (_insert_pending_item, _seed_sent_dialog, _set_cap, _item_status, _run_worker_capturing_picked) so the new file mirrors the proven Phase 12 pattern"
    - "Each test maps 1:1 to the PACE-01..07 → Test Map in 13-RESEARCH.md §Validation Architecture"
  artifacts:
    - path: "tests/test_queue_even_pacing.py"
      provides: "RED test scaffold covering PACE-01..07 via the Phase 12 test analog"
      contains: "def test_window_elapsed_fraction"
  key_links:
    - from: "tests/test_queue_even_pacing.py"
      to: "app/services/queue.py::_process_next_for_sender candidate SELECT"
      via: "worker._process_next_for_sender(sid) with _check_rate_limits mocked True"
      pattern: "_process_next_for_sender"
    - from: "tests/test_queue_even_pacing.py"
      to: "app/services/queue.py::_window_elapsed_fraction (to be created in 13-02)"
      via: "import inside test body (deferred so collection passes)"
      pattern: "_window_elapsed_fraction"
---

<objective>
Create the Wave 0 RED test scaffold for Phase 13 even-pacing — a new `tests/test_queue_even_pacing.py` that encodes the expected behaviour of PACE-01..07 BEFORE any production code exists. This satisfies the Nyquist rule: every implementation task in plan 13-02 has a concrete `<automated>` command that exists from the start.

Purpose: lock the contract (expected-by-now eligibility, window-start counter, structural interval-floor, jitter, follow-up bypass, PROTECTED-constants intact) in executable form so 13-02 implements against tests, not prose.
Output: one new test file with 7 tests mapped 1:1 to PACE-01..07, reusing the Phase 12 test helpers verbatim. Tests are RED now (helper / predicate not yet implemented) and turn GREEN in 13-02.
</objective>

<execution_context>
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/workflows/execute-plan.md
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-CONTEXT.md
@.planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-RESEARCH.md
@tests/test_queue_new_dialog_limit.py
@app/services/queue.py

<interfaces>
<!-- Reuse these EXACT helpers from tests/test_queue_new_dialog_limit.py (Phase 12). Do NOT re-invent. -->

From tests/test_queue_new_dialog_limit.py:
- `async def _insert_pending_item(db, *, workspace_id, sender_id, campaign_id, recipient_phone, scheduled_at_offset_minutes=-1) -> str`  # pending item, scheduled_at <= NOW()
- `async def _seed_sent_dialog(db, *, workspace_id, sender_id, campaign_id, recipient_phone)`  # status='sent', finished_at=NOW() explicit (factory leaves it NULL)
- `async def _set_cap(db, *, campaign_id, cap)`  # UPDATE campaigns.max_new_dialogs_per_day
- `async def _item_status(db, qid) -> str | None`
- `def _run_worker_capturing_picked(worker)`  # mocks _check_rate_limits→True, _get_long_pause_seconds→None, _send_item→capture id; returns (captured, cm_rate, cm_pause, cm_send)

From conftest fixtures (already present, used by Phase 12 tests):
- `async_db_session`
- `test_running_campaign_factory(sender_count, work_hour_start, work_hour_end, work_days_mask)` -> (camp_dict, senders); camp_dict has keys `id`, `workspace_id`

To-be-created in 13-02 (import INSIDE the test body so --collect-only passes now):
- `app.services.queue._window_elapsed_fraction(*, campaign_tz, work_hour_start, work_hour_end, now=None) -> tuple[datetime, float]`
- `app.services.queue.PACE_JITTER_LOW`, `app.services.queue.PACE_JITTER_HIGH`
</interfaces>
</context>

<threat_model>
ASVS L1 — Phase 13 adds no new external input, API, DB column, or UI; the surface is queue-internal logic reading existing campaign config. Test-scaffold-specific risks and mitigations:

| Threat | Vector | Mitigation in this plan |
|--------|--------|-------------------------|
| Division-by-zero / degenerate window in the helper under test | `work_hour_end == work_hour_start` (zero-width window) reaching the elapsed-fraction math | PACE-02 test includes a zero-width / `start==end` input asserting no `ZeroDivisionError` (helper must guard `width or 24`); test encodes the expectation so 13-02 cannot ship the bug |
| `max_new_dialogs_per_day = 0` driving `expected_now = 0` | a 0-limit campaign blocking everything or erroring | PACE-03/PACE-05 tests cover limit boundaries (cap reached / under cap) so a 0 or tiny limit degrades to "nothing picked", never a crash |
| SQL injection via window/expected values | pacing values reaching SQL as interpolated strings | PACE-03 test drives the REAL `_process_next_for_sender` SELECT; the implementation contract (asserted in 13-02 introspection) requires `:window_start_utc` / `:expected_now` BIND PARAMS — the test exercises the bound path so a string-interpolated variant would be caught by behaviour drift |
| Correctness/DoS from a new subquery breaking `SKIP LOCKED` | refactor that drops `FOR UPDATE OF mq SKIP LOCKED` or `LIMIT 8` | PACE-03 includes a source-introspection assertion that the SELECT source still contains `FOR UPDATE OF mq SKIP LOCKED` and `LIMIT 8` (mirrors `test_check_rate_limits_untouched`) |

No secrets, no auth surface, no PII in test fixtures beyond synthetic phone numbers.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold tests/test_queue_even_pacing.py with the pure-helper unit tests (PACE-01, PACE-02)</name>
  <files>tests/test_queue_even_pacing.py</files>
  <read_first>
    - tests/test_queue_new_dialog_limit.py (copy helper functions + imports + pytestmark verbatim)
    - app/services/queue.py:39-69 (PROTECTED rate-const block — names to assert intact)
    - app/services/queue.py:73-111 (_campaign_in_working_window — the helper the new one is modelled on; injectable `now` signature)
    - .planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-RESEARCH.md §Validation Architecture (Determinism recipe + PACE→Test Map) and §Architecture Pattern 1 (helper sketch + landmines)
    - .planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-CONTEXT.md (D-01, D-02, D-05, D-06, D-08)
  </read_first>
  <action>
    Create new file `tests/test_queue_even_pacing.py`. Copy the imports, `pytestmark = pytest.mark.asyncio`, and the helper functions (`_insert_pending_item`, `_seed_sent_dialog`, `_set_cap`, `_item_status`, `_run_worker_capturing_picked`) from `tests/test_queue_new_dialog_limit.py` verbatim. Add `import inspect` and `from app.services import queue as queue_module`.

    Write `test_protected_constants_intact` (PACE-01): assert `queue_module.MIN_SEND_INTERVAL == 20`, `queue_module.MAX_SEND_INTERVAL == 55`, `queue_module.SEND_INTERVAL_FATIGUE == 0.5`, `queue_module.LONG_PAUSE_EVERY_MIN == 12`, `queue_module.LONG_PAUSE_EVERY_MAX == 25`, `queue_module.LONG_PAUSE_MIN_SECS == 180`, `queue_module.LONG_PAUSE_MAX_SECS == 600`, `queue_module.MAX_NEW_CONTACTS_PER_HOUR == 15`. Then assert the new jitter constants exist and are sane: `queue_module.PACE_JITTER_LOW` and `queue_module.PACE_JITTER_HIGH` are floats with `0 < PACE_JITTER_LOW < 1 < PACE_JITTER_HIGH` (import them inside the test so collection passes before 13-02).

    Write `test_window_elapsed_fraction` (PACE-02): import `_window_elapsed_fraction` INSIDE the test body. Drive it with a fixed injectable `now` across the determinism cases from RESEARCH: (a) `now` exactly at window start → fraction == 0.0 and `window_start_utc` equals that instant; (b) mid-window (e.g. tz `Europe/Moscow`, work_hour_start=9, work_hour_end=20, `now` at local 14:30) → fraction strictly between 0 and 1, close to expected `(14.5-9)/11`; (c) `now` just before close → fraction near 1.0 and `<= 1.0`; (d) `now` before window start (frac would be negative) → clamped to 0.0; (e) `now` after close (frac would be >1) → saturates at 1.0; (f) zero-width / degenerate `work_hour_start == work_hour_end` → no `ZeroDivisionError`, fraction in `[0,1]`. Use `datetime(..., tzinfo=timezone.utc)` values; assert with `pytest.approx` where fractional.

    Mark this Task's tests RED-expected: they will fail at import (`_window_elapsed_fraction` / `PACE_JITTER_*` not yet defined) — that is the correct Wave-0 state. Do NOT add the production helper here.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_queue_even_pacing.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only` lists `test_protected_constants_intact` and `test_window_elapsed_fraction` with NO collection/import errors (deferred imports are inside test bodies).
    - Running the two tests (without 13-02) fails inside the body with a NameError/ImportError on `_window_elapsed_fraction` / `PACE_JITTER_*` — i.e. genuinely RED, not skipped.
    - Helper functions are byte-identical copies of the Phase 12 originals (no behavioural edits).
    - `test_window_elapsed_fraction` passes an injectable `now` (no wall-clock dependence) and includes the zero-width-window case.
  </acceptance_criteria>
  <done>tests/test_queue_even_pacing.py exists, collects cleanly, and contains RED unit tests for PACE-01 (constants intact + jitter consts) and PACE-02 (elapsed-fraction across boundary/midnight/degenerate inputs).</done>
</task>

<task type="auto">
  <name>Task 2: Add the integration RED tests for the pacing predicate (PACE-03..07)</name>
  <files>tests/test_queue_even_pacing.py</files>
  <read_first>
    - tests/test_queue_new_dialog_limit.py (the three integration tests are the direct template for seed→run→assert-picked shape)
    - app/services/queue.py:300-339 (Phase 12 candidate SELECT — the predicate the pacing condition extends; LIMIT 8 / FOR UPDATE OF mq SKIP LOCKED to assert preserved)
    - .planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-RESEARCH.md §Validation Architecture (Determinism recipe items 1-3 — fraction-robust scenario design) and Pitfall 1 / Pitfall 4 / Pitfall 5
    - .planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-CONTEXT.md (D-03, D-04, D-05, D-06, D-07, D-10)
  </read_first>
  <action>
    Append five tests to `tests/test_queue_even_pacing.py`, all using the `async_db_session` + `test_running_campaign_factory` fixtures and `_run_worker_capturing_picked`, choosing fraction-robust window bounds per the RESEARCH determinism recipe (`work_hour_start=0, work_hour_end=24, work_days_mask=127` so the wall-clock fraction lands in `(0,1]` deterministically for the assertion direction).

    `test_pacing_gate` (PACE-03): set a high cap (so Phase 12 cap is NOT the binding constraint) and a small `max_new_dialogs_per_day` driving `expected_now`. Seed enough `_seed_sent_dialog` rows since window-start so `count_opened >= expected_now` → assert a fresh never-contacted new-dialog `_insert_pending_item` is NOT in `captured["picked"]` and stays `pending` ("over expected ⇒ blocked"). Add a complementary case: `count_opened == 0` with a wide-elapsed window and large limit so `expected_now >= 1` → assert the new-dialog item IS picked ("under expected ⇒ allowed"). Add a source-introspection assertion (Pitfall 5): `inspect.getsource(QueueWorker._process_next_for_sender)` still contains `"FOR UPDATE OF mq SKIP LOCKED"` and `"LIMIT 8"`.

    `test_pace_counter_window_start` (PACE-04): assert the two counters diverge per D-06. Seed one `sent` row with `finished_at` set BEFORE today's window start but still inside trailing-24h (insert with an explicit older `finished_at`, e.g. NOW() - INTERVAL chosen so it predates window-start in the `0..24` setup is impossible — instead use a narrow window like work_hour_start set to the current UTC hour so window-start is "minutes ago", then seed a row finished 2h ago). Assert: the trailing-24h cap counter (mirror via `_count_in_window_sent`-style query) counts that row, but the pace numerator (`finished_at >= :window_start_utc`) does NOT — so a new dialog remains eligible by pace even though the cap counter is non-zero. Drive `_process_next_for_sender` and assert the new-dialog item IS picked.

    `test_interval_floor` (PACE-05): narrow window + high `max_new_dialogs_per_day` (limit physically cannot fit at the base floor). Seed `count_opened` to a value where `expected_now` is small → assert at most the already-allowed quantity is picked and NO crash occurs (no exception raised from `_process_next_for_sender`); document in a comment that the base 20–55s interval (untouched) is the binding floor and the structural clamp means the limit is simply not reached (D-03/D-10) — there is NO numeric `max()` to assert.

    `test_catchup_no_burst` (PACE-06): catch-up scenario — `count_opened = 0`, large `expected_now` (many eligible). Insert TWO never-contacted new-dialog pending items. Run `_process_next_for_sender` ONCE → assert `len(captured["picked"]) <= 1` (the worker sends exactly one item per call; LIMIT 8 is a candidate pool, not a multi-fire). Add a source-introspection assertion that `inspect.getsource(QueueWorker._process_next_for_sender)` references `random.uniform` with the jitter constants (jitter present in source) — defer the import of the constant names inside the test body.

    `test_followup_bypasses_pacing` (PACE-07): pacing would block (seed `count_opened >= expected_now` as in PACE-03), but the pending item is a follow-up (`_seed_sent_dialog` a prior `sent` to the same phone, then `_insert_pending_item` to that phone). Assert the follow-up IS in `captured["picked"]` and transitions to `processing`. Add an introspection guard: `inspect.getsource(QueueWorker._check_rate_limits)` does NOT contain `window_start` / `expected_now` / `PACE_JITTER` (pacing must not live in the per-tick gate, D-07).
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_queue_even_pacing.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only` lists all seven tests (two from Task 1 + five here) with zero collection errors.
    - Running the integration tests against the CURRENT (pre-13-02) queue.py fails (RED) because the pacing predicate / helper does not exist yet — assertions reach the not-yet-present behaviour, not a skip.
    - Each test name matches the PACE→Test Map in 13-RESEARCH.md (`test_pacing_gate`, `test_pace_counter_window_start`, `test_interval_floor`, `test_catchup_no_burst`, `test_followup_bypasses_pacing`).
    - PACE-03 and PACE-06 include the `FOR UPDATE OF mq SKIP LOCKED` / `LIMIT 8` / `random.uniform` source-introspection assertions.
    - PACE-07 includes the `_check_rate_limits` introspection guard (no pacing refs).
    - No watch-mode flags; the test-overlay command form is used (never bare `docker compose run --rm api pytest`).
  </acceptance_criteria>
  <done>All seven PACE-01..07 tests are present in tests/test_queue_even_pacing.py, collect cleanly, and are RED against the current code — ready for 13-02 to turn GREEN.</done>
</task>

</tasks>

<verification>
- `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_queue_even_pacing.py --collect-only -q` → 7 tests collected, 0 errors.
- `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_queue_even_pacing.py -q` → all 7 RED (fail on missing helper/predicate), none skipped.
- `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_queue_new_dialog_limit.py tests/test_queue_per_campaign_hours.py -q` → still GREEN (no regression from adding the new file).
- NEVER run pytest without the test-overlay. NEVER `docker compose down -v`.
</verification>

<success_criteria>
- New file tests/test_queue_even_pacing.py exists with 7 tests mapped 1:1 to PACE-01..07.
- Phase 12 helpers reused verbatim; deferred imports keep `--collect-only` clean.
- Tests are genuinely RED (reach not-yet-implemented behaviour) and respect PROTECTED-constant guards via introspection.
- Pre-existing queue test files remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-01-SUMMARY.md`
</output>
