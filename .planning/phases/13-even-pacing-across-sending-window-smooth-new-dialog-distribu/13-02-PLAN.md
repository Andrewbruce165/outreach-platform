---
phase: 13-even-pacing-across-sending-window-smooth-new-dialog-distribu
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - app/services/queue.py
autonomous: true
requirements: [PACE-01, PACE-02, PACE-03, PACE-04, PACE-05, PACE-06, PACE-07]
must_haves:
  truths:
    - "D-05: a new cold dialog is eligible iff new dialogs opened since the start of today's window are below the expected-by-now count = max_new_dialogs_per_day × elapsed_fraction × jitter"
    - "D-06: the pacing numerator counts new dialogs opened from the start of TODAY's campaign window (finished_at >= :window_start_utc), NOT trailing-24h — distinct from the Phase 12 cap counter"
    - "D-01: the denominator is the RAW window width (work_hour_end − work_hour_start), with no long-pause subtraction"
    - "D-10: the target interval is an additional gate ON TOP of the existing per-send interval — base 20–55s interval, fatigue factor and long pauses stay untouched; pacing is a separate predicate for new-dialog items only and follow-ups never see it"
    - "D-08: jitter is applied to expected_now via random.uniform so new-dialog openings don't form a machine grid"
    - "D-03: when the window cannot fit the limit at the base floor, the limit is simply not reached (safety over volume) — no special-casing, no crash"
    - "D-07: each sender paces independently; pacing lives in the per-item SELECT, never in _check_rate_limits (which would block follow-ups)"
    - "D-02: work_days_mask does not enter the pacing denominator — the daily limit is fixed; the mask only gates whether we send on a given day (existing working-window logic)"
    - "D-04: catch-up exists but is bounded by the existing gates (base 20–55s per-send, 4/min, 15 new contacts/hour, MAX_NEW_CONTACTS_PER_HOUR) — no separate burst mechanism is added; the existing gates ARE the catch-up speed ceiling"
    - "D-09: pacing is auto-derived with no new config surface — no DB columns, API fields, UI, or openapi regeneration; all logic lives inside queue.py"
    - "PROTECTED constants (MIN/MAX_SEND_INTERVAL, SEND_INTERVAL_FATIGUE, LONG_PAUSE_*, MAX_NEW_CONTACTS_PER_HOUR) and the base interval/fatigue/long-pause logic are unchanged"
  artifacts:
    - path: "app/services/queue.py"
      provides: "PACE_JITTER constants, _window_elapsed_fraction helper, and the expected-by-now pacing predicate inside _process_next_for_sender"
      contains: "_window_elapsed_fraction"
  key_links:
    - from: "app/services/queue.py::_process_next_for_sender"
      to: "app/services/queue.py::_window_elapsed_fraction"
      via: "Python call computing (window_start_utc, elapsed_fraction) then expected_now bind param"
      pattern: "_window_elapsed_fraction"
    - from: "app/services/queue.py::_process_next_for_sender candidate SELECT"
      to: "message_queue (count since window start)"
      via: "correlated COUNT(DISTINCT recipient_phone) subquery with :window_start_utc and :expected_now bind params, beside the Phase 12 cap"
      pattern: ":window_start_utc"
---

<objective>
Implement Phase 13 even pacing entirely inside `app/services/queue.py`: add jitter constants, a pure `_window_elapsed_fraction` helper, and an "expected-by-now" pacing predicate in the `_process_next_for_sender` candidate SELECT — turning the 13-01 RED tests GREEN. New cold dialogs are released evenly across the campaign's daily window instead of front-loaded, while follow-ups, the base 20–55s interval, fatigue, long pauses, and the Phase 12 cap stay untouched (D-01..D-10, CLAUDE.md guard).

Purpose: smooth the daily new-dialog limit (Phase 12 input) across the active window so a sender doesn't burn its whole allowance in the first hour — the explicit user goal of Phase 13.
Output: a single-file change (no migration, no API, no UI per D-09) that satisfies PACE-01..07.
</objective>

<execution_context>
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/workflows/execute-plan.md
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-CONTEXT.md
@.planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-RESEARCH.md
@.planning/phases/12-per-campaign-daily-new-dialog-limit-max-new-dialogs-per-day/12-CONTEXT.md
@app/services/queue.py
@tests/test_queue_even_pacing.py

<interfaces>
<!-- Contracts the 13-01 tests already expect. Implement EXACTLY these names/signatures. -->

To create in app/services/queue.py:
- module constants `PACE_JITTER_LOW: float`, `PACE_JITTER_HIGH: float` (e.g. 0.75 / 1.25) appended to the rate-config block (queue.py:39-69), changing NO existing line.
- `def _window_elapsed_fraction(*, campaign_tz: str, work_hour_start: int, work_hour_end: int, now: Optional[datetime] = None) -> tuple[datetime, float]`
  returns `(window_start_utc, elapsed_fraction)` where `elapsed_fraction` is clamped to `[0.0, 1.0]`; RAW window width (D-01); injectable `now` (mirrors `_campaign_in_working_window`).

Existing contracts that MUST be preserved unchanged:
- `_campaign_in_working_window(*, campaign_tz, work_hour_start, work_hour_end, work_days_mask, now=None) -> bool` (queue.py:73-111) — clone its zoneinfo/astimezone idiom; do not modify it.
- The Phase 12 candidate SELECT in `_process_next_for_sender` (queue.py:300-339) — `LIMIT 8`, `FOR UPDATE OF mq SKIP LOCKED`, the `(follow-up EXISTS … OR new-dialog-under-cap …)` clause, and the Python post-filter working-window re-check (queue.py:344-356).
- `_check_rate_limits` (queue.py:385-528) — base 20–55s interval + fatigue + 4/20/150 + 15/h — ZERO edits (D-07/D-10).
</interfaces>
</context>

<threat_model>
ASVS L1 — queue-internal logic reading existing campaign config; no new external input / API / DB / UI. Enumerated surface + mitigations (all realised by tasks in this plan):

| Threat | Vector | Mitigation (task) |
|--------|--------|-------------------|
| Division-by-zero on window width | `work_hour_end == work_hour_start` (zero-width) reaching `elapsed / (width*3600)` | Task 1: `width_h = (work_hour_end - work_hour_start) % 24 or 24` guard → never 0; PACE-02 zero-width test asserts no `ZeroDivisionError` |
| Negative / >1 elapsed fraction → underflow or overshoot | boundary instant, DST spring-forward, `now` before window-start | Task 1: clamp result to `[0.0, 1.0]` → degrades to `expected≈0` (conservative, no new dialogs) or saturates at full limit; PACE-02 boundary cases |
| `max_new_dialogs_per_day = 0` (or tiny) → expected_now = 0 | a 0-limit campaign | Task 2: `count < :expected_now` with `expected_now=0` simply yields no new dialogs picked — no error; PACE-05 covers narrow-window/limit-not-reached |
| SQL injection / wrong-type bind | window_start / expected reaching SQL | Task 2: pass values STRICTLY as bind params `:window_start_utc` (a tz-aware datetime) and `:expected_now` (a float) via the existing `text(...)` + params dict idiom — NEVER f-string interpolate into the SQL; mirrors the Phase 12 bound-param pattern |
| Lock escalation / breaking SKIP LOCKED via the new subquery | adding a correlated read subquery that escalates row locks or drops `FOR UPDATE OF mq SKIP LOCKED` | Task 2: keep the correlated-subquery shape (no CTE — Pitfall 5); `FOR UPDATE OF mq` locks only `mq`, read-only subqueries against `message_queue` under READ COMMITTED acquire no row locks; PACE-03 source-introspection asserts `FOR UPDATE OF mq SKIP LOCKED` + `LIMIT 8` remain |
| Benign double-open race between parallel workers | two workers both see `count < expected` | Accepted (same posture as Phase 12): at worst ~1 extra new dialog per tick, self-correcting next tick — documented in source comment, not a defect |

No secrets logged; no PII added to logs (pacing adds no new logging of recipient data).
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PACE_JITTER constants + the _window_elapsed_fraction pure helper</name>
  <files>app/services/queue.py</files>
  <behavior>
    - test_protected_constants_intact (PACE-01): all PROTECTED constants unchanged; PACE_JITTER_LOW/HIGH present, 0 < LOW < 1 < HIGH.
    - test_window_elapsed_fraction (PACE-02): fraction == 0 at window start; ~ (elapsed/width) mid-window; <= 1 near close; clamped to 0 when now < start; saturates at 1 when now > close; no ZeroDivisionError on zero-width window; injectable now.
  </behavior>
  <read_first>
    - app/services/queue.py:39-69 (rate-config block — append after line 69 ONLY; change no existing line)
    - app/services/queue.py:73-111 (_campaign_in_working_window — clone the zoneinfo.ZoneInfo(...).astimezone() idiom and the invalid-tz WARNING guard style)
    - .planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-RESEARCH.md §Architecture Pattern 1 (helper sketch + landmines) and §Code Examples (constant placement)
    - tests/test_queue_even_pacing.py (the PACE-01/PACE-02 expectations to satisfy)
    - .planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-CONTEXT.md (D-01, D-02, D-05, D-06)
  </read_first>
  <action>
    Append two module constants to the rate-config block immediately after queue.py:69 (the `QUEUE_TICK_BATCH` / closing rule line region), in a new `# ── Even-pacing config (Phase 13) ───` sub-block: `PACE_JITTER_LOW = 0.75` and `PACE_JITTER_HIGH = 1.25` with a comment citing D-08 (±25% spread on the expected-by-now count to de-grid openings). Change NO existing constant.

    Add a new pure function `_window_elapsed_fraction` next to `_campaign_in_working_window` (place it directly after that function, before `class QueueWorker`). Signature exactly: `def _window_elapsed_fraction(*, campaign_tz, work_hour_start, work_hour_end, now=None) -> tuple[datetime, float]`. Logic: default `now = datetime.now(timezone.utc)`; resolve `tz = zoneinfo.ZoneInfo(campaign_tz)` reusing the same try/except WARNING guard as `_campaign_in_working_window` (on invalid tz return `(now, 0.0)` so pacing conservatively blocks — never crash); `local = now.astimezone(tz)`; compute `width_h = (work_hour_end - work_hour_start) % 24 or 24` (RAW window, D-01; guards zero-width); set `start_local = local.replace(hour=work_hour_start, minute=0, second=0, microsecond=0)`; defensive post-midnight branch `if work_hour_end <= work_hour_start and local.hour < work_hour_end: start_local -= timedelta(days=1)` (non-wrap is the supported case per RESEARCH Open Question 1 — this branch is defensive only); `window_start_utc = start_local.astimezone(timezone.utc)`; `frac = (now - window_start_utc).total_seconds() / (width_h * 3600)`; return `(window_start_utc, max(0.0, min(1.0, frac)))`. Write a docstring citing D-01 (raw width) and the clamp rationale (Pitfall 2). Do NOT subtract long pauses (D-01). Do NOT import anything new — `zoneinfo`, `datetime`, `timedelta`, `timezone`, `random` are already imported at the top of the module.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_queue_even_pacing.py::test_protected_constants_intact tests/test_queue_even_pacing.py::test_window_elapsed_fraction -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_protected_constants_intact` and `test_window_elapsed_fraction` PASS (were RED in 13-01).
    - `inspect.getsource` shows the rate-config block (queue.py:39-69) PROTECTED lines unchanged — only an appended pacing sub-block.
    - `_window_elapsed_fraction` is a module-level pure function with injectable `now`, returns `(datetime, float)`, fraction always in `[0.0, 1.0]`, and raises no exception on zero-width or invalid-tz input.
    - `_campaign_in_working_window` source is byte-identical to before (not edited).
  </acceptance_criteria>
  <done>PACE_JITTER_LOW/HIGH exist in the rate-config block and _window_elapsed_fraction computes the clamped raw-window elapsed fraction with injectable now; PACE-01 and PACE-02 are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the expected-by-now pacing predicate to the _process_next_for_sender SELECT</name>
  <files>app/services/queue.py</files>
  <behavior>
    - test_pacing_gate (PACE-03): over-expected new dialog NOT picked / under-expected picked; SELECT still has FOR UPDATE OF mq SKIP LOCKED + LIMIT 8.
    - test_pace_counter_window_start (PACE-04): pre-window sent row counts toward 24h cap but NOT toward today's pace ⇒ new dialog still eligible.
    - test_interval_floor (PACE-05): narrow window + high limit ⇒ no crash, limit not reached (structural clamp, no numeric max()).
    - test_catchup_no_burst (PACE-06): one call picks <= 1 new dialog; jitter (random.uniform with PACE_JITTER_*) present in source.
    - test_followup_bypasses_pacing (PACE-07): follow-up picked while a new dialog would be paced; _check_rate_limits has no pacing refs.
  </behavior>
  <read_first>
    - app/services/queue.py:288-356 (the second SELECT block in _process_next_for_sender: the Phase 12 predicate at :317-333, the bind dict at :338, and the Python post-filter loop :344-356)
    - app/services/queue.py (newly added _window_elapsed_fraction + PACE_JITTER_* from Task 1)
    - .planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-RESEARCH.md §Architecture Pattern 2 (SQL shape + locking note), Pattern 3 (structural clamp — NO numeric max()), Pattern 4 (jitter on expected_now), Pitfalls 1 & 5
    - tests/test_queue_even_pacing.py (PACE-03..07 expectations)
    - .planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-CONTEXT.md (D-03, D-05, D-06, D-07, D-08, D-10)
  </read_first>
  <action>
    In `_process_next_for_sender`, BEFORE issuing the second SELECT, the worker already does not know per-campaign window bounds for the pacing math — so first fetch the candidate's campaign window. Keep the existing SELECT's JOIN to `campaigns c` which already exposes `c.timezone`, `c.work_hour_start`, `c.work_hour_end`. Because the pacing `expected_now` is a per-(sender,campaign) Python-computed bind, compute it for THIS sender's active campaign: read the running campaign row for this sender's pending items (the existing SELECT is per-sender across possibly one campaign; in practice a sender's queued items belong to its attached campaign). Add a small pre-query SELECT (one row) fetching `c.id, c.timezone, c.work_hour_start, c.work_hour_end, c.max_new_dialogs_per_day` for the next eligible pending item's campaign (ORDER BY mq.priority DESC, mq.created_at ASC LIMIT 1, same WHERE base as the main SELECT minus the predicate) so the window math targets the right campaign. If no row, `return` (nothing to do). Then call `window_start_utc, frac = _window_elapsed_fraction(campaign_tz=..., work_hour_start=..., work_hour_end=..., now=now_utc)` and compute `expected_now = max_new_dialogs_per_day * frac * random.uniform(PACE_JITTER_LOW, PACE_JITTER_HIGH)` (D-08 jitter, fresh each call).

    Extend the main candidate SELECT's new-dialog branch (queue.py:326-332) by ANDing the Phase 13 pacing subquery beside the Phase 12 cap, per RESEARCH Pattern 2: add `AND (SELECT COUNT(DISTINCT paced.recipient_phone) FROM message_queue paced WHERE paced.sender_id = mq.sender_id AND paced.campaign_id = mq.campaign_id AND paced.status = 'sent' AND paced.finished_at >= :window_start_utc) < :expected_now`. The follow-up `EXISTS prior sent` branch is left UNCHANGED so follow-ups bypass pacing entirely (D-07/D-10/PACE-07). Add `:window_start_utc` and `:expected_now` to the params dict at queue.py:338 — STRICTLY as bind params, never string-interpolated (threat: SQL injection). Preserve `ORDER BY mq.priority DESC, mq.created_at ASC`, `LIMIT 8`, `FOR UPDATE OF mq SKIP LOCKED`, and the Python post-filter loop byte-for-byte (Pitfall 5). Do NOT convert to a CTE.

    Do NOT add any numeric `max(target, base)` (Pattern 3 — the clamp is structural: the base interval gate in `_check_rate_limits` is untouched and remains the floor; the predicate is the ceiling). Do NOT touch `_check_rate_limits` (D-07/PACE-07). Add an inline comment block citing D-05/D-06/D-08/D-10, noting the two-counter distinction (window-start pace vs trailing-24h cap, Pitfall 1) and the accepted benign double-open race (READ COMMITTED, same posture as Phase 12). Note in the comment that the single pre-query targets the campaign of the next eligible item; if a sender ever spans multiple running campaigns the pace uses that item's campaign window (acceptable — per-(sender,campaign) isolation already holds from Phase 12).
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_queue_even_pacing.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All seven PACE-01..07 tests in tests/test_queue_even_pacing.py PASS.
    - `inspect.getsource(QueueWorker._process_next_for_sender)` contains `:window_start_utc`, `:expected_now`, `FOR UPDATE OF mq SKIP LOCKED`, `LIMIT 8`, and a `random.uniform(PACE_JITTER_LOW, PACE_JITTER_HIGH)` call; contains NO standalone numeric `max(` clamp for the interval.
    - `inspect.getsource(QueueWorker._check_rate_limits)` contains none of: `window_start`, `expected_now`, `PACE_JITTER` (pacing absent from the per-tick gate).
    - The Phase 12 cap predicate (`< c.max_new_dialogs_per_day` with `NOW() - INTERVAL '24 hours'`) and the follow-up `EXISTS prior sent` branch are still present and unchanged.
    - `expected_now` and `window_start_utc` reach SQL only via the params dict (no f-string interpolation in the `text(...)` literal).
  </acceptance_criteria>
  <done>The expected-by-now pacing predicate gates new cold dialogs by today's-window elapsed fraction × jitter, beside the untouched Phase 12 cap and follow-up bypass; PACE-03..07 are GREEN and the protected base gate / SKIP LOCKED / LIMIT 8 invariants hold.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression + protected-invariant confirmation</name>
  <files>app/services/queue.py</files>
  <read_first>
    - tests/test_queue_new_dialog_limit.py (Phase 12 cap tests must remain GREEN — the shared SELECT was extended, not replaced)
    - tests/test_queue_per_campaign_hours.py (Phase 4 working-window tests — re-check loop preserved)
    - app/services/queue.py (final state after Tasks 1-2)
    - .planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-RESEARCH.md §Validation Architecture (Sampling Rate — per-wave full suite)
  </read_first>
  <action>
    Run the full test suite via the test-overlay to confirm no regression in the shared `_process_next_for_sender` SELECT (Phase 4 working-window, Phase 9 failover, Phase 12 cap) and that the new pacing tests pass alongside everything else. If any pre-existing queue test regressed, fix the integration in queue.py (do NOT weaken the new tests, do NOT touch PROTECTED constants or the base interval logic) and re-run. Make no changes beyond queue.py. Confirm the file is the ONLY modified source file (D-09: no migration, no model, no API, no UI, no openapi regen).
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Full suite GREEN (baseline is GREEN per MEMORY; exit code 0).
    - `tests/test_queue_new_dialog_limit.py`, `tests/test_queue_per_campaign_hours.py`, and any failover tests pass unchanged.
    - `git status --porcelain` shows only `app/services/queue.py` and `tests/test_queue_even_pacing.py` (from 13-01) as changed under app/ and tests/ — no migration file, no model/schema/router/openapi change (D-09).
    - `test_check_rate_limits_untouched`-style guards (Phase 12 + new PACE-01/PACE-07) all pass.
  </acceptance_criteria>
  <done>The full suite is green; pacing is added with zero regression and zero changes outside queue.py, honouring the PROTECTED-constant and D-09 single-file guards.</done>
</task>

</tasks>

<verification>
- Per task: `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_queue_even_pacing.py -x -q` (+ tests/test_queue_new_dialog_limit.py tests/test_queue_per_campaign_hours.py for SELECT-regression).
- Per wave (this plan): full suite `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest -q`.
- NEVER run pytest without the test-overlay (conftest guard DROP SCHEMA → prod). NEVER `docker compose down -v` (wipes prod postgres_data volume).
- Source-introspection guards confirm PROTECTED constants + base interval/fatigue/long-pause logic + `_check_rate_limits` are untouched (D-10/D-07/CLAUDE.md guard).
</verification>

<success_criteria>
- New cold dialogs are gated by an expected-by-now predicate (D-05) counted from today's window start (D-06) over the raw window width (D-01), with jitter (D-08), as an additional gate on top of the untouched base interval (D-10), in the per-item SELECT only (D-07).
- Follow-ups, the base 20–55s interval, fatigue, long pauses, the Phase 12 cap, `LIMIT 8`, and `FOR UPDATE OF mq SKIP LOCKED` are all preserved.
- Change confined to app/services/queue.py (D-09); full suite green; PACE-01..07 GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/13-even-pacing-across-sending-window-smooth-new-dialog-distribu/13-02-SUMMARY.md`
</output>
