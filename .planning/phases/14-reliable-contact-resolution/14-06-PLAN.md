---
phase: 14-reliable-contact-resolution
plan: 06
type: execute
wave: 6
depends_on: ["14-05"]
files_modified:
  - .planning/notes/checker-pool-throttle-spike.md
autonomous: false
requirements: [RESV-01, RESV-02]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "A written findings note answers three questions with evidence: (1) is phone-resolve dead pool-wide RIGHT NOW (resolvePhone+importContacts hit-rate across several non-checker accounts vs the 49 known-live control set), (2) does @username resolution still work where phone-resolve fails, (3) is it OUR drain rate that triggers instant burst-throttle (is the 14-02 burst-cap/cooldown actually on the live resolve path or bypassed)"
    - "The spike is READ-ONLY against prod data: it resolves the 49 control numbers (real registered Barter rows) and probes non-checker accounts WITHOUT writing contacts/contacts_cache rows; any address-book import is cleaned up; the 49 control rows stay tg_status='registered'"
    - "The note ends with an explicit GO / NO-GO recommendation on re-activating the parked checkers + draining the 14k, surfaced at a blocking human-verify gate"
    - "Re-activating the parked checkers and draining the 14k are explicitly DEFERRED to a follow-up gated by this spike's GO verdict — this plan performs NEITHER"
  artifacts:
    - path: ".planning/notes/checker-pool-throttle-spike.md"
      provides: "Diagnostic findings + per-account hit-rates + @username viability + rate-trigger analysis + GO/NO-GO recommendation"
      contains: "GO"
  key_links:
    - from: "diagnostic spike script (scratchpad)"
      to: ".planning/notes/checker-pool-throttle-spike.md"
      via: "hit-rate evidence written to the note, no prod-data mutation"
      pattern: "hit.rate|GO|NO-GO"
    - from: "findings note GO/NO-GO"
      to: "checkpoint:human-verify gate"
      via: "user reads the note and decides re-activation"
      pattern: "human-verify"
---

<objective>
Close Gap B from the 14-04 live-smoke failure: two FRESH "healthy" accounts flooded instantly at 0% mobile, which calls the whole Phase-14 premise (D-01: a healthy checker pool can resolve phones) into question. Run a CONTROLLED, READ-ONLY diagnostic spike — NOT a re-activation, NOT a 14k drain — to answer whether phone-resolve is dead pool-wide right now, whether @username resolution still works where phone-resolve fails, and whether it is OUR drain rate that triggers the instant burst-throttle. Write the evidence to a findings note with an explicit GO/NO-GO recommendation, and surface it at a blocking human-verify gate.

This runs AFTER 14-05 (the flood-aware worker fix) so the spike interprets the worker's behaviour against the corrected finalization logic, and so that if a GO is later given, the worker that drains is the fixed one.

Purpose: The 14-04 smoke proved we cannot blindly re-activate. Before anyone re-activates or drains, we need evidence on what is actually throttled and at what rate — gathered without poisoning prod data.
Output: `.planning/notes/checker-pool-throttle-spike.md` with per-account hit-rates, @username viability, rate-trigger analysis, and a GO/NO-GO call; a human-verify decision gate.
</objective>

<execution_context>
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/workflows/execute-plan.md
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-reliable-contact-resolution/14-CONTEXT.md
@.planning/notes/checker-false-negatives.md
@.planning/phases/14-reliable-contact-resolution/control-set-known-live.txt
@.planning/phases/14-reliable-contact-resolution/14-05-PLAN.md

<interfaces>
<!-- Prior art and exact API entry points — DO NOT re-explore. -->

Prior-art scratch scripts (the diagnostic pattern to reuse, in scratchpad/):
  - diag_resolve.py — resolves a single number across multiple accounts via both
    ResolvePhoneRequest and ImportContactsRequest; this is how the original diagnosis
    proved method-symmetry (importContacts does NOT recover where resolvePhone is empty).
  - known_live_probe.py — runs the 49 known-live control set through one account and
    reports the live hit-rate (calibration baseline: 48/49, first false-negative at pos 49).

app/services/checker.py (read-only entry points to call from the spike):
  - resolve_phone_with_fallback(client, phone) -> {"is_registered": bool, "telegram_id": int|None}
    — ResolvePhone then importContacts fallback with MANDATORY DeleteContacts cleanup
    (the address book is cleaned in a finally). Use this for the phone-resolve probe so
    the spike does not leave imported contacts behind.
  - CheckerService.probe_control(checker_slug, encrypted_session, phones, proxy) ->
    {"checked": int, "results": [{"phone", "is_registered"}…]} — LIVE-only ResolvePhone,
    NEVER reads _lookup_cache, NEVER writes _save_cache, NEVER mutates contacts rows.
    This is the read-only-safe path for the control-set probe.
  - CheckerService.check_usernames(...) / ResolveUsernameRequest — the @username path
    (D-02: the only API way to confirm a privacy-hidden account). For a small sample of
    control numbers that ALSO have a known @username, resolve the username to see if it
    succeeds where phone-resolve failed.

Control set: 49 known-live numbers in
  .planning/phases/14-reliable-contact-resolution/control-set-known-live.txt
  AND shipped at app/data/control_set_known_live.txt (runtime copy). These are real
  tg_status='registered' rows in the Barter folder (folder_id 4ecdde17-f454-4a1b-b4ba-732fd6b9449f)
  — the probe must NOT roll them back.

Accounts to probe (read-only resolve only — do NOT change role/auth_status/lifecycle):
  - The 2 parked checkers: sender-7979031303, sender-8364639216 (role='checker',
    auth_status='restricted', restriction_status='none', lifecycle_status='paused').
  - SEVERAL non-checker sender accounts (role='sender', auth_status='ok') — to compare
    hit-rates pool-wide. diag_resolve.py used sender-8017533134 (RU) + sender-7979031303.
    Read session_string + proxy from the senders table; do NOT activate anything.

Burst-cap / pacing knobs (verify they apply on the live path):
  - get_settings().contact_check_burst_cap (default 30), contact_check_pace_low/high
    (2.0/3.5s), contact_check_cooldown_seconds (900), contact_check_daily_cap (400).
  - Question 3 = confirm the worker's live resolve actually paces 2-3s and caps the
    burst (the 14-04 smoke flooded at 20-30 — was the cap honoured or did something fire
    faster?). probe_control already paces via contact_check_pace_low/high.

Project op-note (CRITICAL for any SQL): `docker exec <c> psql … <<'SQL'` WITHOUT `-i`
  is a SILENT no-op (the 14-04 rollback hit this). Use `psql -c "…"` or `docker exec -i`.
  This spike should be READ-ONLY; if any prod row is touched, the plan documents the
  cleanup and uses the -i / -c form.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read-only diagnostic spike — phone-resolve, @username, rate-trigger</name>
  <read_first>
    - .planning/notes/checker-false-negatives.md (full — esp. §"Два режима троттла" calibration and §"Часть 2" the failure to explain)
    - .planning/phases/14-reliable-contact-resolution/control-set-known-live.txt (the 49 control numbers — ground truth)
    - app/services/checker.py (resolve_phone_with_fallback, probe_control, check_usernames — the read-only entry points)
    - scratchpad/diag_resolve.py and scratchpad/known_live_probe.py if present (prior-art pattern to reuse)
    - CLAUDE.md §"docker exec heredoc needs -i" / §"Семантика checker'а" (op-safety + privacy false-negative caveat)
  </read_first>
  <files>(scratchpad spike script + read-only prod resolve — NO repo file writes except the note in Task 2; NO contacts/contacts_cache mutation)</files>
  <action>
    Write a one-off async spike script in the scratchpad dir (NOT committed to app/) that, for each target account (the 2 parked checkers + several role='sender' accounts), connects via its decrypted session_string + proxy and resolves the SAME small ordered slice of the 49 control numbers using the READ-ONLY `probe_control` path (no cache read/write, no contacts mutation) AND, separately, `resolve_phone_with_fallback` (which self-cleans its address book) — recording per-account: count resolved live, count empty, and WHERE in the sequence the first empty appears (the burst-onset position, calibration said ~45-50; the smoke saw collapse at 20-30). Answer the three questions:
    (1) PHONE-RESOLVE POOL-WIDE: compare hit-rates across the non-checker accounts vs the parked checkers on the control set — is every account at ~0% (pool-wide contacts-API throttle) or do the fresh non-checker accounts still resolve ~48/49 (then the parked checkers specifically are degraded, not the pool)?
    (2) @USERNAME VIABILITY: for a small sample of control numbers that have a known @username, call `check_usernames` and record whether the username resolves where the phone did not (D-02 — the only confirm path for privacy-hidden accounts).
    (3) OUR-RATE-TRIGGERS-THROTTLE: instrument the spike to pace at `contact_check_pace_low/high` (2-3s) and cap the burst at `contact_check_burst_cap` — record the hit-rate-vs-position curve and whether collapse happens BELOW the cap (meaning the cap is too high / something on the live worker path bypasses it). Cross-check: read whether the worker's live resolve path (`_tick` → check_phones → resolve_phone_with_fallback) actually applies the pace/cap by inspecting `app/services/contact_check_worker.py` + `checker.py` (the pace sleep is between phones; the cap is the batch LIMIT) — note any gap where the live path could fire faster than the calibrated onset.
    READ-ONLY invariant: use `probe_control` (never mutates contacts), and if `resolve_phone_with_fallback` imports a contact it cleans it up in its own finally. Do NOT write contacts/contacts_cache. Do NOT change any senders row (no role/auth/lifecycle UPDATE). If any SQL is needed, use `psql -c` or `docker exec -i` (heredoc-without-`-i` is a silent no-op per the op-note). After the run, re-verify the 49 control rows are still `tg_status='registered'` (read-only SELECT). Capture the raw per-account numbers for the Task-2 note.
  </action>
  <verify>
    <automated>docker exec outreach-platform-db psql -U outreach_user -d outreach_platform -tAc "SELECT COUNT(*) FROM contacts WHERE folder_id='4ecdde17-f454-4a1b-b4ba-732fd6b9449f' AND tg_status='registered'" | grep -qx 49</automated>
  </verify>
  <acceptance_criteria>
    - The 49 control numbers are STILL tg_status='registered' after the spike (verify command exits 0 — read-only invariant held)
    - Per-account hit-rates on the control set captured for ≥2 non-checker accounts + the 2 parked checkers
    - @username resolution attempted on a sample where phone-resolve failed; result recorded
    - The hit-rate-vs-position curve captured (where the first empty appears per account) to judge whether collapse is below the burst-cap
    - No contacts/contacts_cache rows written by the spike; no senders row activated/changed
  </acceptance_criteria>
  <done>Evidence gathered read-only on phone-resolve pool-wide health, @username viability, and rate-trigger — without poisoning prod data.</done>
</task>

<task type="auto">
  <name>Task 2: Write the findings note + GO/NO-GO recommendation</name>
  <read_first>
    - .planning/notes/checker-false-negatives.md (the existing diagnosis — match its structure/tone; this new note is its Часть-3 companion)
    - the raw per-account numbers captured in Task 1
  </read_first>
  <files>.planning/notes/checker-pool-throttle-spike.md</files>
  <action>
    Write `.planning/notes/checker-pool-throttle-spike.md` recording: the three questions and their answers with the per-account hit-rate table, the position-of-first-empty curve, the @username viability result, and the rate-trigger analysis (does collapse happen below the burst-cap → is the cap mis-set or the live path bypassing it). Conclude with an explicit, bolded **GO / NO-GO** recommendation on re-activating the 2 parked checkers + draining the 14k, with the conditions a GO would require (e.g. "GO only if non-checker accounts resolve ≥X% AND collapse onset > cap AND a long cooldown/lower cap applied"; "NO-GO if phone-resolve is dead pool-wide → pivot to @username-only resolve or longer rest"). State plainly that re-activation and the 14k drain are DEFERRED to a follow-up gated by this verdict — this plan performs neither. Link back to checker-false-negatives.md §"Часть 2".
  </action>
  <verify>
    <automated>grep -qi -E "GO|NO-GO" /root/apps/aimly/tg-outreach/.planning/notes/checker-pool-throttle-spike.md && grep -qi "username" /root/apps/aimly/tg-outreach/.planning/notes/checker-pool-throttle-spike.md</automated>
  </verify>
  <acceptance_criteria>
    - Note answers all three questions with the captured per-account evidence (hit-rate table + position curve)
    - Note contains an explicit GO or NO-GO recommendation with stated conditions
    - Note states re-activation + 14k drain are deferred to a verdict-gated follow-up
    - Note links to checker-false-negatives.md
  </acceptance_criteria>
  <done>A written, evidence-backed findings note with a clear re-activation GO/NO-GO call.</done>
</task>

<task type="checkpoint:human-verify" id="14-06-T3" gate="blocking">
  <name>Task 3: Re-activation GO/NO-GO decision (human-verify)</name>
  <what-built>
    A read-only diagnostic spike (Task 1) probed the 49 known-live control set across the 2 parked checkers and several non-checker accounts, tested @username resolution where phone-resolve failed, and measured whether collapse happens below the burst-cap — without writing any contacts/contacts_cache rows or activating any account. The findings + an explicit GO/NO-GO recommendation are written to `.planning/notes/checker-pool-throttle-spike.md` (Task 2). The 14-05 flood-aware worker fix is already merged, so if a GO is given the draining worker is the corrected one.
  </what-built>
  <how-to-verify>
    1. Read `.planning/notes/checker-pool-throttle-spike.md` in full — the per-account hit-rate table, the position-of-first-empty curve, the @username result, and the rate-trigger analysis.
    2. Decide: is phone-resolve dead POOL-WIDE (every account ~0% on known-live) → NO-GO on phone-resolve, consider @username-only / long rest; OR do the non-checker accounts still resolve ~48/49 (then the parked checkers were specifically burned, fresh resolvers viable) → conditional GO with a lower cap / longer cooldown?
    3. Confirm the spike left prod clean: the 49 control rows still `registered`, 0 new contacts_cache rows, no account re-activated.
    4. Decide the follow-up: GO (plan a guarded re-activation + staged drain as a NEW gap-closure plan, NOT here) or NO-GO (pivot — e.g. @username resolution, longer rest, or new fresh accounts).
  </how-to-verify>
  <resume-signal>Type "GO" to authorise a SEPARATE follow-up plan for guarded re-activation + staged 14k drain; or "NO-GO" with the chosen pivot (e.g. @username-only resolve, longer cooldown, fresh accounts). Either way, re-activation and the 14k drain are NOT performed in this plan.</resume-signal>
</task>

</tasks>

<verification>
- Spike ran read-only; the 49 control rows still registered; no contacts_cache rows written; no account activated.
- Findings note answers all three questions with per-account evidence and a GO/NO-GO call.
- Human-verify gate reached with the note as the decision artifact.
- Re-activation + 14k drain explicitly deferred to a verdict-gated follow-up.
</verification>

<success_criteria>
- The Phase-14 premise (healthy pool can resolve phones) is tested with evidence, not assumed — directly addressing the 14-04 failure that two fresh accounts flooded at 0%.
- A clear GO/NO-GO recommendation lets the user choose the next move without risking another poisoning run.
- RESV-04 (14k re-check) and full re-activation remain DEFERRED — NOT claimed complete by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/14-reliable-contact-resolution/14-06-SUMMARY.md`
</output>
