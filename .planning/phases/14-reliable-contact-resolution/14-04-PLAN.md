---
phase: 14-reliable-contact-resolution
plan: 04
type: execute
wave: 4
depends_on: ["14-02", "14-03"]
files_modified:
  - CLAUDE.md
  - .planning/notes/checker-false-negatives.md
autonomous: false
requirements: [RESV-04, RESV-07]
user_setup: []

must_haves:
  truths:
    - "the 49 known-live control numbers are confirmed still tg_status='registered' and are NOT rolled back; the 699 Barter contaminated rows are confirmed pending"
    - "the two healthy parked checkers are activated ONLY after cap/probe/rotation/selection code is merged, and a live control-probe confirms ~48/49 resolve before the pool drains 14k"
    - "the CLAUDE.md checker-semantics section no longer claims the checker was healthy on 2026-06-23 and records the shadow-ban diagnosis"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Corrected §Семантика checker'а (is_registered) with shadow-ban diagnosis"
      contains: "shadow"
  key_links:
    - from: "senders rows (sender-7979031303, sender-8364639216)"
      to: "active rotation"
      via: "UPDATE auth_status='ok', lifecycle_status='active' after code merge"
      pattern: "lifecycle_status"
---

<objective>
Bring the hardened pipeline online safely and finish the docs. This plan runs LAST (D-03 activation gate): verify the contaminated-data state is correct, ACTIVATE the two parked healthy checkers only now that cap/probe/rotation/selection are in code, smoke the live control-probe before letting the pool drain 14k contacts (RESV-04/D-08), and correct the now-false checker-semantics docs (RESV-07).

Purpose: Activating the checkers before the guards existed would let the uncapped worker grab them and re-poison data — this plan is the gate that prevents that and proves the end-to-end re-check works on a healthy resolver.
Output: verified DB state, two activated checkers, a passing live control-probe smoke, corrected CLAUDE.md + note.
</objective>

<execution_context>
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/workflows/execute-plan.md
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-reliable-contact-resolution/14-CONTEXT.md
@.planning/phases/14-reliable-contact-resolution/14-RESEARCH.md
@.planning/phases/14-reliable-contact-resolution/14-PATTERNS.md
@.planning/notes/checker-false-negatives.md
@.planning/phases/14-reliable-contact-resolution/control-set-known-live.txt

<interfaces>
<!-- DB state confirmed by planner 2026-06-26 (verify again — state shifts daily): -->
<!-- contacts: 14484 pending / 53 registered / 5 stray not_registered -->
<!-- Barter folder 4ecdde17-f454-4a1b-b4ba-732fd6b9449f: 699 pending + 49 registered (the 49 = control set, KEEP registered) -->
<!-- senders: sender-7979031303 (checker, auth=restricted, restr=none, paused) -->
<!--          sender-8364639216 (checker, auth=restricted, restr=none, paused)  ← the two to ACTIVATE -->
<!--          sender-8428118140 (checker, auth=restricted, restr=spam_limited, paused) ← STAYS parked (real shadow-ban) -->

Activation (run ONLY after Plans 02+03 merged & GREEN):
```sql
UPDATE senders SET auth_status='ok', lifecycle_status='active'
WHERE slug IN ('sender-7979031303','sender-8364639216')
  AND restriction_status='none';   -- guard: never activate the broken one
```
Rollback (if a checker misbehaves): UPDATE senders SET role='sender', auth_status='ok', lifecycle_status='active' WHERE slug=...

Docs target (RESEARCH correction): the section lives in `/root/apps/aimly/tg-outreach/CLAUDE.md` §"Семантика checker'а (is_registered)" — NOT /root/CLAUDE.md. Keep the privacy-false-negative caveat (still TRUE); correct the "healthy 2026-06-23" claim.
</interfaces>
</context>

<threat_model>
ASVS L1. Threats mitigated by THIS plan:
- **Re-poisoning via premature activation (data-integrity, the core failure mode):** activating the parked checkers before the cap/probe/selection guards exist would let the uncapped worker grab them and reproduce the 15-20× under-count. Mitigation = depends_on [14-02, 14-03] (waves enforce ordering) + the activation task runs after a GREEN full-suite gate + the activation SQL guards `restriction_status='none'` so the real shadow-banned checker (sender-8428118140) is never activated.
- **Rolling back the known-live control set (data loss):** the 49 control numbers are `registered` in Barter; rolling them to pending would corrupt the probe's ground truth. Mitigation = the pre-activation verification task COUNTs the 49 vs 699 and asserts the 49 stay registered before any further data action.
- **Account-safety regression on first live drain:** activating both checkers and immediately draining 14k risks a fresh shadow-ban if the cap is mis-set. Mitigation = the human-verify checkpoint runs a small live control-probe smoke (~48/49 expected) BEFORE approving the full drain, and monitors sender_restriction_events.
- **Stale/false docs (operational integrity):** the CLAUDE.md section asserting the checker was healthy misleads future operators. Mitigation = RESV-07 correction.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pre-activation DB verification (contaminated-data state)</name>
  <read_first>
    - .planning/phases/14-reliable-contact-resolution/14-PATTERNS.md §"Pre-plan DB verification" (the three checks)
    - .planning/notes/checker-false-negatives.md §"Часть 1" (what was done manually)
    - .planning/phases/14-reliable-contact-resolution/control-set-known-live.txt (the 49 to protect)
  </read_first>
  <files>(read-only DB queries — no file writes; record findings in the SUMMARY)</files>
  <action>
    Run read-only SQL against prod `outreach-platform-db` (psql -U outreach_user -d outreach_platform) and record exact counts in the plan SUMMARY: (1) `SELECT tg_status, COUNT(*) FROM contacts GROUP BY tg_status` — confirm the bulk is pending. (2) `SELECT tg_status, COUNT(*) FROM contacts WHERE folder_id='4ecdde17-f454-4a1b-b4ba-732fd6b9449f' GROUP BY tg_status` — confirm the 699 contaminated are already `pending` and the 49 control numbers are `registered` (do NOT roll these 49 back — they are the probe ground truth). (3) `SELECT slug, role, auth_status, restriction_status, lifecycle_status FROM senders WHERE role='checker'` — confirm sender-7979031303 + sender-8364639216 are restriction_status='none'/paused (ready to activate) and sender-8428118140 stays spam_limited/paused. If the 699 are NOT pending (state drifted), explicitly decide and document a rollback scope that excludes the 49 control numbers before proceeding — do NOT blanket-roll the Barter folder. No code or data write in this task; verification only.
  </action>
  <verify>
    <automated>docker exec outreach-platform-db psql -U outreach_user -d outreach_platform -tAc "SELECT COUNT(*) FROM contacts WHERE folder_id='4ecdde17-f454-4a1b-b4ba-732fd6b9449f' AND tg_status='registered'" | grep -qx 49</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records the three count queries' results verbatim
    - the 49 Barter registered control numbers confirmed present (verify command exits 0)
    - the 699 Barter contaminated confirmed tg_status='pending' (or a control-set-excluding rollback scope documented)
    - sender-8428118140 confirmed still restriction_status='spam_limited' (stays parked)
  </acceptance_criteria>
  <done>Contaminated-data state verified; control set protected; no premature data mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Activate the two parked healthy checkers (D-03 gate)</name>
  <read_first>
    - .planning/phases/14-reliable-contact-resolution/14-CONTEXT.md §D-03 (activation conditions + rollback)
    - .planning/phases/14-reliable-contact-resolution/14-RESEARCH.md §"Runtime State Inventory" (activation row)
    - app/services/contact_check_worker.py (confirm Plan-02 selection gate + Plan-03 probe are merged before activating)
  </read_first>
  <files>(prod DB UPDATE — no repo file writes; record the SQL + result in SUMMARY)</files>
  <action>
    PRECONDITION (assert before running): Plans 02 and 03 are merged and the full test suite is GREEN (the cap, probe, rotation, and selection-fix are in code). Only then run the activation UPDATE against prod: `UPDATE senders SET auth_status='ok', lifecycle_status='active' WHERE slug IN ('sender-7979031303','sender-8364639216') AND restriction_status='none'` — the `restriction_status='none'` guard ensures the real shadow-banned sender-8428118140 (spam_limited) is never activated even by accident. Do NOT change `role` (they stay `role='checker'`). After the UPDATE, confirm exactly 2 rows changed and re-SELECT the two checkers to confirm auth_status='ok'/lifecycle_status='active'/restriction_status='none'. The worker will pick them up on its next tick (api already running). Record the rollback statement in the SUMMARY (role='sender', auth_status='ok', lifecycle_status='active').
  </action>
  <verify>
    <automated>docker exec outreach-platform-db psql -U outreach_user -d outreach_platform -tAc "SELECT COUNT(*) FROM senders WHERE slug IN ('sender-7979031303','sender-8364639216') AND auth_status='ok' AND lifecycle_status='active' AND role='checker'" | grep -qx 2</automated>
  </verify>
  <acceptance_criteria>
    - exactly 2 senders activated (verify command exits 0)
    - sender-8428118140 remains restriction_status='spam_limited' and is NOT active (re-SELECT in SUMMARY)
    - role unchanged ('checker') on the activated pair
    - rollback SQL recorded in SUMMARY
  </acceptance_criteria>
  <done>Two healthy checkers active in the pool; broken checker untouched; activation gated behind merged guards.</done>
</task>

<task type="auto">
  <name>Task 3: RESV-07 docs correction</name>
  <read_first>
    - CLAUDE.md §"Семантика checker'а (is_registered)" (the section to correct — currently claims healthy 2026-06-23)
    - .planning/notes/checker-false-negatives.md (the diagnosis to cite; already written — extend if calibration detail is missing)
    - .planning/phases/14-reliable-contact-resolution/14-PATTERNS.md §"Docs (RESV-07)"
  </read_first>
  <files>CLAUDE.md (the PROJECT file /root/apps/aimly/tg-outreach/CLAUDE.md — NOT /root/CLAUDE.md), .planning/notes/checker-false-negatives.md</files>
  <action>
    In `/root/apps/aimly/tg-outreach/CLAUDE.md` §"Семантика checker'а (is_registered)": correct the now-FALSE claim that checker `sender-8428118140` was healthy/not-broken on 2026-06-23 — replace with the shadow-ban diagnosis (volume-driven contacts-API throttle, false-negatives 15-20×, link to `.planning/notes/checker-false-negatives.md`). KEEP the privacy-false-negative caveat (`is_registered=false` = "not resolvable by phone by a stranger account", privacy-hidden numbers false-negative) — it is still TRUE and also lives verbatim in checker.py:15-42. Add a one-line pointer to the Phase-14 mechanism (health-probe + confidence/source + restriction-gated selection) so operators know false negatives are now re-checked, not trusted. In `.planning/notes/checker-false-negatives.md`, confirm the diagnosis + two-throttle-mode calibration + Часть-1 manual steps are recorded (extend only if a gap exists — the note is already written).
  </action>
  <verify>
    <automated>grep -q -i "shadow" /root/apps/aimly/tg-outreach/CLAUDE.md && ! grep -q "checker .sender-8428118140. .* здоров" /root/apps/aimly/tg-outreach/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md §"Семантика checker'а" no longer states the checker was healthy on 2026-06-23
    - CLAUDE.md retains the privacy-false-negative caveat
    - CLAUDE.md links to .planning/notes/checker-false-negatives.md and mentions the Phase-14 probe/confidence mechanism
    - .planning/notes/checker-false-negatives.md records diagnosis + calibration + Часть-1
  </acceptance_criteria>
  <done>Docs reflect the shadow-ban diagnosis and the new safety mechanism; privacy caveat preserved.</done>
</task>

<task type="checkpoint:human-verify" id="14-04-T4" gate="blocking">
  <name>Task 4: Live control-probe smoke + first-drain monitoring (human-verify)</name>
  <what-built>
    The full hardened pipeline is live: restriction-gated selection (Plan 02), burst/daily cap + cooldown (Plan 02), control-probe with ≥2-miss detect + suspect rollback + confidence (Plan 03), importContacts fallback with cleanup (Plan 03), and two activated healthy checkers (Task 2). 14484 pending contacts (mobiles first) are now eligible for re-check via the same queue (RESV-04/D-08).
  </what-built>
  <how-to-verify>
    1. Run a LIVE control-probe smoke against an activated checker (e.g. `sender-8364639216`) using the 49 numbers in `.planning/phases/14-reliable-contact-resolution/control-set-known-live.txt` — expect ~48/49 to resolve live (calibration baseline). A score far below ~48 means the account is throttled — do NOT proceed to a full drain; investigate.
    2. Watch the first ~30-60 min of live resolution: `docker logs outreach-platform-db | grep -i contact` and tail api logs for `ContactCheckWorker` lines — confirm mobiles (+79…) drain first and registered-rate climbs toward the true ~26% overall / ~50% mobile (NOT the broken 2.5%).
    3. Check `SELECT s.slug, e.event_type, e.restricted_until, e.created_at FROM sender_restriction_events e JOIN senders s ON s.id=e.sender_id WHERE e.created_at > NOW()-INTERVAL '2 hours' ORDER BY e.created_at DESC` — if a checker got auto-flagged `spam_limited`, confirm it left rotation (the probe working as designed) rather than continuing to emit false negatives.
    4. Confirm no checker hit a hard shadow-ban (hit-rate did not collapse to <1%).
  </how-to-verify>
  <resume-signal>Type "approved" once the live probe shows ~48/49 and the registered-rate is climbing toward true live-share without a shadow-ban; or describe the issue (e.g. probe score low, false-negative rate still high) to trigger gap closure.</resume-signal>
</task>

</tasks>

<verification>
- Pre-activation counts recorded; 49 control numbers still registered; 699 pending.
- Exactly 2 checkers activated; broken checker still parked.
- CLAUDE.md corrected; privacy caveat kept.
- Live control-probe ~48/49; registered-rate climbing toward true live-share; no shadow-ban (human-verified).
</verification>

<success_criteria>
- RESV-04: 2110+699 contaminated + 14k pending re-checked via the same pool queue, mobiles first, by healthy activated resolvers.
- RESV-07: docs corrected; diagnosis + calibration recorded.
- Success Criteria 3 (end-to-end without shadow-ban) human-verified on the live pool.
</success_criteria>

<output>
After completion, create `.planning/phases/14-reliable-contact-resolution/14-04-SUMMARY.md`
</output>
