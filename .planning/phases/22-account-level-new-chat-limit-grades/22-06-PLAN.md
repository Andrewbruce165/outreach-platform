---
phase: 22-account-level-new-chat-limit-grades
plan: 06
type: execute
wave: 3
depends_on: ["22-03", "22-04", "22-05"]
files_modified:
  - migrations/059_drop_dead_limit_columns.sql
  - app/models/__init__.py
  - app/schemas/__init__.py
  - app/routers/campaigns.py
  - tests/conftest.py
  - tests/test_send_campaign.py
autonomous: true
requirements: [D-07, D-04]

must_haves:
  truths:
    - "campaigns.max_new_dialogs_per_day is dropped from the DB, ORM, schemas, and router — nothing references it (D-07)"
    - "senders.rate_per_day is dropped from the DB and ORM (D-04 final removal)"
    - "The campaign create/update/response API works with no max_new_dialogs_per_day field and no dialog-limit validation (D-07)"
    - "No app/ source references max_new_dialogs_per_day or rate_per_day after this plan"
  artifacts:
    - migrations/059_drop_dead_limit_columns.sql
  key_links:
    - "this plan runs LAST (Wave 3) after 22-03/04/05 stopped reading both columns — the DROP cannot crash the boot-time worker (RESEARCH Pitfall 6)"
    - "ORM removal + migration DROP ship together so create_all (test schema) and the DB stay consistent"
---

<objective>
Retire the two superseded throttle columns now that no code reads them: drop `campaigns.max_new_dialogs_per_day` (D-07) and `senders.rate_per_day` (D-04) from the database and ORM, clean the campaign API surface, and prove no residual references remain.

Purpose: This is the terminal cleanup wave. It depends on 22-03 (queue stopped reading both), 22-04 (sender API stopped reading rate_per_day), and 22-05 (warmup uses the ladder) so the DROP is safe at boot-time auto-apply (RESEARCH Pitfall 6).
Output: migration 059 dropping both columns, ORM/schema/campaign-router cleanup, a conftest note, a grep gate proving zero references, and green campaign tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-CONTEXT.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 059 DROP + ORM column removal</name>
  <read_first>
    - app/models/__init__.py (line 123 rate_per_day on Sender; line ~776 Campaign.max_new_dialogs_per_day + its comment)
    - migrations/050_lower_new_dialog_cap.sql (prior migration that set the default — context for the DROP)
    - 22-RESEARCH.md §"Migrations" + Pitfall 6 (DROP ordering vs boot auto-apply)
  </read_first>
  <action>
    Create `migrations/059_drop_dead_limit_columns.sql` (idempotent, BEGIN/COMMIT, 038-style header noting this must land after the Phase-22 code that stopped reading these columns):
    - `ALTER TABLE campaigns DROP COLUMN IF EXISTS max_new_dialogs_per_day;`
    - `ALTER TABLE senders DROP COLUMN IF EXISTS rate_per_day;`
    Both `IF EXISTS` so re-run is a no-op.

    In `app/models/__init__.py`:
    - Remove `rate_per_day = Column(Integer, nullable=False, server_default='150')` (line 123) from Sender.
    - Remove `max_new_dialogs_per_day = Column(...)` (line ~776) from Campaign (and its now-stale comment referencing the default-10 / migration 050 history).
    Because conftest builds the test schema from ORM create_all, removing these ORM columns means the test DB no longer has them — consistent with the migration DROP.
  </action>
  <verify>
    <automated>grep -c "DROP COLUMN IF EXISTS" migrations/059_drop_dead_limit_columns.sql; grep -c "rate_per_day\|max_new_dialogs_per_day" app/models/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - migrations/059_drop_dead_limit_columns.sql contains `DROP COLUMN IF EXISTS max_new_dialogs_per_day` and `DROP COLUMN IF EXISTS rate_per_day`
    - `grep -c "rate_per_day" app/models/__init__.py` returns 0
    - `grep -c "max_new_dialogs_per_day" app/models/__init__.py` returns 0
    - app/models/__init__.py still contains `rate_per_min` and `rate_per_hour`
  </acceptance_criteria>
  <done>Both dead columns are dropped from DB and ORM via an idempotent migration.</done>
</task>

<task type="auto">
  <name>Task 2: Campaign schema + router cleanup (D-07)</name>
  <read_first>
    - app/schemas/__init__.py (CampaignCreate.max_new_dialogs_per_day ~799-803, CampaignUpdate ~876, CampaignResponse ~940)
    - app/routers/campaigns.py (lines 62-63 DIALOG_LIMIT_SOFT_CAP/HARD_CAP; 70- _validate_max_new_dialogs; references at 85, 97, 375, 464, 520, 654-655)
    - 22-RESEARCH.md §"Current Code" #5 and #6 (exact reference map)
  </read_first>
  <action>
    Schemas (`app/schemas/__init__.py`): remove the `max_new_dialogs_per_day` field from `CampaignCreate`, `CampaignUpdate`, and `CampaignResponse` (all three spots).
    Router (`app/routers/campaigns.py`): remove `DIALOG_LIMIT_SOFT_CAP` (62), `DIALOG_LIMIT_HARD_CAP` (63), the `_validate_max_new_dialogs` helper (70-), and every reference at lines 85, 97, 375, 464, 520, 654-655 (validation calls, assignments to the model, response serialization of the field). Ensure campaign create/update/response and any warning-collection still work without the dialog-limit path — remove only the dialog-limit-specific lines, leave other campaign validation intact.
  </action>
  <verify>
    <automated>grep -rc "max_new_dialogs_per_day\|_validate_max_new_dialogs\|DIALOG_LIMIT" app/routers/campaigns.py app/schemas/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "max_new_dialogs_per_day" app/schemas/__init__.py` returns 0
    - `grep -c "max_new_dialogs_per_day" app/routers/campaigns.py` returns 0
    - `grep -c "_validate_max_new_dialogs\|DIALOG_LIMIT" app/routers/campaigns.py` returns 0
    - app/routers/campaigns.py still defines the campaign create/update/response routes
  </acceptance_criteria>
  <done>The campaign API no longer exposes or validates a per-campaign dialog cap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: conftest note + campaign tests + repo-wide grep gate</name>
  <read_first>
    - tests/conftest.py (lines 225-298 — migration blocks; 059 is a DROP so create_all just won't build the columns — usually no conftest block needed, but add a short comment documenting that)
    - tests/test_send_campaign.py (existing campaign create/enqueue tests to adjust for the removed field)
  </read_first>
  <behavior>
    - Campaign create/update succeeds with no max_new_dialogs_per_day in the payload or response.
    - Enqueue + queue pick still work end-to-end with the account budget (no per-campaign cap).
    - Repo-wide: no app/ source references either dropped column.
  </behavior>
  <action>
    Add a brief comment in `tests/conftest.py` near the migration blocks noting that migration 059 is a pure DROP handled implicitly by create_all (the ORM no longer declares the columns, so the test schema inherits the dropped state — no exists-guarded block needed; RESEARCH Pitfall 1 note).
    Update `tests/test_send_campaign.py` (and any other campaign test that sets/asserts max_new_dialogs_per_day) to remove the field and assert campaigns work without it.
    Add a grep-gate assertion (a small test or a verify command) that `grep -rn "max_new_dialogs_per_day" app/` and `grep -rn "rate_per_day" app/` both return nothing (excluding this plan's comments — use `grep -rn --include=*.py "max_new_dialogs_per_day\|rate_per_day" app/` and expect empty).
  </action>
  <verify>
    <automated>test -z "$(grep -rn --include=*.py 'max_new_dialogs_per_day\|rate_per_day' app/)" && docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_send_campaign.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn --include=*.py "max_new_dialogs_per_day" app/` returns nothing
    - `grep -rn --include=*.py "rate_per_day" app/` returns nothing
    - tests/test_send_campaign.py no longer sets max_new_dialogs_per_day
    - `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_send_campaign.py -x` exits 0
  </acceptance_criteria>
  <done>Campaign tests pass without the dropped field and a repo-wide grep confirms zero residual references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration 059 auto-apply → prod boot | DROP must not crash a still-referencing worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-16 | Denial of Service | boot-time DROP vs residual reference | high | mitigate | Wave-3 ordering (after 22-03/04/05) + Task 3 grep gate guarantees no code reads the columns before they are dropped (RESEARCH Pitfall 6) |
| T-22-17 | Denial of Service | non-idempotent DROP | low | mitigate | `DROP COLUMN IF EXISTS` re-run-safe |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase |
</threat_model>

<verification>
- `grep -rn --include=*.py "max_new_dialogs_per_day\|rate_per_day" app/` returns nothing
- `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_send_campaign.py -x` green
</verification>

<success_criteria>
Both superseded throttle columns are fully retired from DB, ORM, schemas, and routers with zero residual references and green campaign tests.
</success_criteria>

<output>
Create `.planning/phases/22-account-level-new-chat-limit-grades/22-06-SUMMARY.md` when done
</output>
