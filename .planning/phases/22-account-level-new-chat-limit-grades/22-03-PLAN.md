---
phase: 22-account-level-new-chat-limit-grades
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - app/services/queue.py
  - tests/test_queue_new_dialog_limit.py
  - tests/test_queue_even_pacing.py
  - tests/test_send.py
autonomous: true
requirements: [D-01, D-05, D-06, D-13, D-03, D-04]

must_haves:
  truths:
    - "The new-dialog cap counts DISTINCT recipient_phone sender-wide (any campaign), not per-campaign (D-01/D-06/D-13)"
    - "A phone the sender already sent status='sent' to in ANY campaign is treated as a known peer (follow-up), spending no budget in a different campaign (D-13)"
    - "The cap RHS and the Phase-13 pace numerator use the account grade budget (budget_for_level from the workspace ladder), not campaigns.max_new_dialogs_per_day (D-01/D-05)"
    - "The Phase-13 campaign working window (timezone/hours) is preserved for pacing (D-05)"
    - "_check_rate_limits no longer reads or gates on rate_per_day; rate_per_min, rate_per_hour, MAX_NEW_CONTACTS_PER_HOUR and the interval floor are untouched (D-04)"
  artifacts:
    - app/services/queue.py
  key_links:
    - "expected_now numerator swaps c.max_new_dialogs_per_day → account budget; the CAST(:expected_now AS DOUBLE PRECISION) bind MUST stay (untyped bind truncates to bigint, blocking all new dialogs)"
    - "all three subqueries (EXISTS/cap/pace) drop every opened.campaign_id = mq.campaign_id filter and become sender-wide; LIMIT 8 + FOR UPDATE OF mq SKIP LOCKED + s.restriction_status <> 'spam_limited' gate preserved"
---

<objective>
Move the daily new-dialog gate from per-campaign to per-account: rewrite the three predicate subqueries in `_process_next_for_sender` to be sender-wide, drive the cap and pace by the account grade budget, and remove the now-defunct `rate_per_day` gate from `_check_rate_limits`.

Purpose: This is the enforcement core of the phase (D-01/D-05/D-06/D-13 + the D-04 backend gate removal). The column `campaigns.max_new_dialogs_per_day` still physically exists after this plan (dropped in 22-06); this plan simply stops reading it and reads the account budget instead — safe intermediate state.
Output: A rewritten queue pick path that spends one sender-wide daily budget derived from the grade ladder, plus extended queue/pacing/rate tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-CONTEXT.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-RESEARCH.md
@app/services/grade_ladder.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Account-budget + sender-wide rewrite of _process_next_for_sender</name>
  <read_first>
    - app/services/queue.py (lines 359-529 — the full _process_next_for_sender: pre-query 402-420, expected_now 438-440, main pick SELECT 442-529 with EXISTS 494-499, cap COUNT 506-511, pace COUNT 513-518, spam_limited gate 505, LIMIT 8 / FOR UPDATE at 521-522)
    - app/services/grade_ladder.py (load_ladder, budget_for_level, resolve_ladder)
    - 22-RESEARCH.md §"Current Code — Verified Integration Points" #1 (exact line map + Pitfall 3 sender-wide semantics + the DOUBLE PRECISION cast note)
  </read_first>
  <behavior>
    - Sender-wide dedup: a phone the sender sent status='sent' to in campaign A counts as known when the same phone is queued in campaign B → serviced via the EXISTS branch, budget NOT spent.
    - Account budget: with grade level 1 (default budget 5), a sender opens at most 5 new distinct phones per trailing-24h ACROSS all its campaigns combined (not 5 per campaign).
    - Pacing: expected_now uses the account budget as the daily numerator while the campaign working window (tz/hours) is still taken from the next-item's campaign.
  </behavior>
  <action>
    Resolve the account grade budget once per tick in Python (mirrors how expected_now is a Python-computed bind). After the existing pre-query fetches `camp_row` (lines 402-420), also fetch this sender's `current_level` and `workspace_id`, load the workspace ladder via `grade_ladder.load_ladder(db, workspace_id)`, and compute `account_budget = grade_ladder.budget_for_level(ladder, current_level)`. Remove `c.max_new_dialogs_per_day AS c_cap` from the pre-query SELECT (line 407) — it is no longer the source of truth.

    Change the `expected_now` computation (lines 438-440) to use `account_budget` as the numerator instead of `camp_row.c_cap`: `expected_now = account_budget * frac * random.uniform(PACE_JITTER_LOW, PACE_JITTER_HIGH)`. KEEP the `CAST(:expected_now AS DOUBLE PRECISION)` in the SELECT and the explanatory comment (an untyped bind is inferred bigint and truncates a fractional expected_now to 0, silently blocking all new dialogs).

    Rewrite the three subqueries in the main pick SELECT (lines 494-518) to be sender-wide (drop EVERY `campaign_id` filter, key on sender_id + recipient_phone):
    - EXISTS / follow-up branch: `EXISTS (SELECT 1 FROM message_queue prior WHERE prior.sender_id = mq.sender_id AND prior.recipient_phone = mq.recipient_phone AND prior.status = 'sent')` — remove `prior.campaign_id = mq.campaign_id`, add `prior.sender_id = mq.sender_id` (RESEARCH Pitfall 3: this is a genuine semantic change, not a token swap).
    - Cap COUNT: `(SELECT COUNT(DISTINCT opened.recipient_phone) FROM message_queue opened WHERE opened.sender_id = mq.sender_id AND opened.status = 'sent' AND opened.finished_at >= NOW() - INTERVAL '24 hours') < CAST(:account_budget AS INTEGER)` — remove the `opened.campaign_id = mq.campaign_id` line, replace RHS `c.max_new_dialogs_per_day` with the `:account_budget` bind.
    - Pace COUNT: `(SELECT COUNT(DISTINCT paced.recipient_phone) FROM message_queue paced WHERE paced.sender_id = mq.sender_id AND paced.status = 'sent' AND paced.finished_at >= :window_start_utc) < CAST(:expected_now AS DOUBLE PRECISION)` — remove the `paced.campaign_id = mq.campaign_id` line.
    Preserve the `s.restriction_status <> 'spam_limited'` new-dialog gate (line 505), `LIMIT 8`, `FOR UPDATE OF mq SKIP LOCKED`, `ORDER BY mq.priority DESC, mq.created_at ASC` (D-06 FIFO — no new campaign-priority logic), and the benign double-open race posture (do NOT add heavier locking). Pass `account_budget` as a bind in the params dict. Update the block comments to cite Phase 22 D-01/D-05/D-06/D-13 and the trailing-24h shared window (D-03).
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_queue_new_dialog_limit.py tests/test_queue_even_pacing.py -x</automated>
  </verify>
  <acceptance_criteria>
    - app/services/queue.py contains `prior.sender_id = mq.sender_id` in the EXISTS subquery
    - `grep -c "opened.campaign_id = mq.campaign_id" app/services/queue.py` returns 0
    - `grep -c "paced.campaign_id = mq.campaign_id" app/services/queue.py` returns 0
    - `grep -c "c.max_new_dialogs_per_day" app/services/queue.py` returns 0
    - app/services/queue.py contains `:account_budget` and still contains `CAST(:expected_now AS DOUBLE PRECISION)`
    - app/services/queue.py still contains `FOR UPDATE OF mq SKIP LOCKED` and `restriction_status <> 'spam_limited'`
  </acceptance_criteria>
  <done>New-dialog cap + pacing are account-wide, budget-driven, and sender-wide-deduped while preserving the working window, FIFO order, lock semantics, and PeerFlood gate.</done>
</task>

<task type="auto">
  <name>Task 2: Remove rate_per_day gate from _check_rate_limits (D-04 backend)</name>
  <read_first>
    - app/services/queue.py (lines 566-705 — _check_rate_limits: rate_per_day SELECT at 581, max_per_day at 618, the daily-cap block 671-687; min/hour blocks 620-651; MAX_NEW_CONTACTS_PER_HOUR at 664; interval floor 689-705)
    - 22-RESEARCH.md §"Current Code" #2 (exact removal map + the protected checks that MUST stay)
  </read_first>
  <action>
    In `_check_rate_limits`, remove exactly the three `rate_per_day` touch points (D-04):
    1. Drop `rate_per_day` from the sender-row SELECT column list (line 581) — keep `rate_per_min, rate_per_hour` and the lifecycle/auth/restriction columns.
    2. Delete `max_per_day = sender_row.rate_per_day` (line 618).
    3. Delete the entire "Messages sent in last 24 hours (daily cap)" block: the COUNT query (671-680) and the `if msgs_today >= max_per_day: ... return False` gate (682-687).
    Do NOT touch: the per-minute check (620-633), per-hour check (635-651), the unique-contacts `MAX_NEW_CONTACTS_PER_HOUR` check (653-669), the `frozen` skip (609-614), spam_limited handling (601-608), or the randomised interval/fatigue floor (689-705) — these are the protected structural gates (CLAUDE.md guard: never touch rate_per_min/hour/interval/FloodWait). Update the docstring (lines 567-571) to reflect that rate limits are now per-minute/per-hour only.
  </action>
  <verify>
    <automated>grep -c "rate_per_day\|max_per_day" app/services/queue.py; docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_send.py -k rate -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rate_per_day" app/services/queue.py` returns 0
    - `grep -c "max_per_day" app/services/queue.py` returns 0
    - app/services/queue.py still contains `max_per_min` and `max_per_hour` and `MAX_NEW_CONTACTS_PER_HOUR`
    - app/services/queue.py still contains the fatigue/interval block (`SEND_INTERVAL_FATIGUE`)
    - `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_send.py -k rate -x` exits 0
  </acceptance_criteria>
  <done>The daily-message cap is gone from the backend gate; per-minute/hour/unique-contact/interval protections remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend queue/pacing/rate tests for account-wide behavior</name>
  <read_first>
    - tests/test_queue_new_dialog_limit.py (existing per-campaign cap tests + helpers to extend)
    - tests/test_queue_even_pacing.py (existing pacing tests; helpers are copied verbatim between the two files per 22-VALIDATION.md — extend both consistently)
    - tests/test_send.py (existing rate-limit tests)
    - app/services/grade_ladder.py (to set/derive the account budget in test setup)
  </read_first>
  <behavior>
    - `test_...sender_wide...`: sender sends to phone P in campaign A (status='sent'); the same phone queued in campaign B is serviced as a follow-up (budget not spent). (D-13)
    - account budget cap counts across campaigns: with budget 5, 3 new phones in campaign A + 2 new in campaign B = budget exhausted; a 6th new phone in either campaign is blocked. (D-01/D-06)
    - pacing numerator = account budget: expected_now scales with the grade budget, campaign window preserved. (D-05)
    - rate: with no daily cap, a sender can exceed the old 150/day as long as per-minute/hour hold; per-minute and per-hour still gate. (D-04)
  </behavior>
  <action>
    Extend `tests/test_queue_new_dialog_limit.py` with a `sender_wide` dedup case and an account-budget-across-campaigns case (seed a sender with 2 running campaigns; assert the shared budget). Extend `tests/test_queue_even_pacing.py` with a case asserting expected_now uses the account budget as the numerator while the campaign window is unchanged (keep the shared helper in sync). Extend `tests/test_send.py` (or its rate test) to assert removal of the daily cap: sends above 150 in 24h are NOT blocked by a daily gate, while per-minute/per-hour still block. Set the account budget in setup via a sender_grade_settings row or by setting current_level and relying on code-defaults.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_queue_new_dialog_limit.py tests/test_queue_even_pacing.py tests/test_send.py -k "rate or sender_wide or budget or pacing" -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_queue_new_dialog_limit.py contains a test whose name includes `sender_wide`
    - tests/test_queue_new_dialog_limit.py asserts the budget is shared across two campaigns of one sender
    - tests/test_send.py asserts no daily-message cap blocks sends (per-minute/hour still gate)
    - The targeted pytest command above exits 0
  </acceptance_criteria>
  <done>Sender-wide dedup, account budget, budget-driven pacing, and daily-cap removal are proven green via test-overlay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| queue worker → message_queue pick | budget/window values must be binds, never interpolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-07 | Tampering | account_budget / window binds in pick SELECT | high | mitigate | Pass `:account_budget` / `:window_start_utc` / `:expected_now` strictly as bind params (Phase 13 rule); never f-string interpolate |
| T-22-08 | Elevation of Privilege | cross-sender dedup leakage | medium | mitigate | Sender-wide dedup keys on `prior.sender_id = mq.sender_id` (RESEARCH Pitfall 3) — a phone contacted by ANOTHER sender does not mark it known for this sender |
| T-22-09 | Denial of Service | benign double-open race | low | accept | Documented Phase 12/13 posture: ~1 extra new dialog per tick, self-correcting; heavier locking would serialize senders |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase |
</threat_model>

<verification>
- Targeted queue/pacing/rate tests green via test-overlay
- `grep -rn "c.max_new_dialogs_per_day\|rate_per_day\|max_per_day" app/services/queue.py` returns nothing
</verification>

<success_criteria>
One sender-wide daily budget (from the grade ladder) gates all new dialogs across a sender's campaigns and warmup share; the daily-message cap is removed from the backend without disturbing the protected interval/rate floors.
</success_criteria>

<output>
Create `.planning/phases/22-account-level-new-chat-limit-grades/22-03-SUMMARY.md` when done
</output>
