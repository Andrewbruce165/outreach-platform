---
phase: 22-account-level-new-chat-limit-grades
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - app/routers/grade_settings.py
  - app/services/grade_progression.py
  - app/main.py
  - tests/test_grade_settings.py
  - tests/test_grade_progression.py
autonomous: true
requirements: [D-16, D-14, D-17]

must_haves:
  truths:
    - "GET /api/v1/sender-grade-settings returns the workspace ladder, resolving to code-defaults 5/30, 9/30, 13 when no row exists (D-16)"
    - "PUT /api/v1/sender-grade-settings idempotently upserts the 3-level ladder, workspace-scoped, with green-corridor warnings for out-of-recommended values (D-16)"
    - "A background sweep advances senders past their step_days: current_level += 1 and level_updated_at = NOW() when NOW() - level_updated_at >= step_days(current_level) (D-14)"
    - "A sender at level 3 never advances (D-17)"
  artifacts:
    - app/routers/grade_settings.py
    - app/services/grade_progression.py
  key_links:
    - "grade_progression sweep reads step_days from the per-workspace ladder via grade_ladder.load_ladder — not a code constant"
    - "settings endpoints are workspace-scoped via auth_dep exactly like warmup settings (no cross-tenant read/write)"
---

<objective>
Expose the per-workspace grade ladder over a GET/PUT API cloned from the warmup-settings pattern, and add a background worker that auto-advances each sender's grade level once its configured step-days elapse.

Purpose: D-16 (configurable ladder) and D-14/D-17 (auto-progression, permanent at level 3) are the "grade grows automatically" half of the phase. This plan owns the two brand-new files plus main.py wiring, so it runs in parallel with the queue/warmup/sender plans (no shared source file).
Output: `app/routers/grade_settings.py`, `app/services/grade_progression.py`, main.py registration + worker lifecycle, and green tests for defaults, upsert, cross-tenant isolation, and progression/stop-at-3.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-CONTEXT.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-RESEARCH.md
@app/services/grade_ladder.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET/PUT /sender-grade-settings router + green-corridor validation</name>
  <read_first>
    - app/routers/warmup.py (lines 491-594 — WarmupSettingsUpdate, _resolve_settings, GET /settings, PUT /settings with INSERT ... ON CONFLICT (workspace_id) DO UPDATE; clone this exactly)
    - app/routers/senders.py (lines 60-73, 179-220 — RATE_SOFT_CAP/RATE_HARD_CAP + _validate_rate_limits WarningItem soft/hard pattern for the green corridor)
    - app/services/grade_ladder.py (LADDER_DEFAULTS, resolve_ladder, load_ladder from 22-01)
    - app/main.py (lines 194-196 — app.include_router registration site)
    - app/utils/auth.py (AuthCtx, auth_dep — workspace scoping)
  </read_first>
  <action>
    Create `app/routers/grade_settings.py` with `router = APIRouter(prefix="/api/v1", tags=["grade-settings"])`. Mirror warmup.py's settings endpoints:
    - Pydantic body `GradeLadderUpdate` with the fixed 3-level fields: `level1_chats_per_day: int = Field(5, ge=1, le=100)`, `level1_step_days: int = Field(30, ge=1, le=365)`, `level2_chats_per_day: int = Field(9, ge=1, le=100)`, `level2_step_days: int = Field(30, ge=1, le=365)`, `level3_chats_per_day: int = Field(13, ge=1, le=100)`. No level3 step (D-17). Fixed 3 levels — no add/remove (D-16).
    - `SOFT_CAP_CHATS = 13` and `SOFT_STEP_DAYS_MIN = 30` green-corridor constants. A `_validate_ladder(body) -> list[WarningItem]` that appends a WarningItem when any chats_per_day exceeds SOFT_CAP_CHATS or any step_days is below SOFT_STEP_DAYS_MIN, worded in the tone of senders.py rate warnings (Russian, e.g. "Уровень N: X чатов/сутки превышает рекомендованный зелёный коридор (≤13)"). Return 200 + warnings[] for soft breaches; Pydantic `le=100` handles the hard cap.
    - `GET /sender-grade-settings`: `SELECT level1_chats_per_day, level1_step_days, level2_chats_per_day, level2_step_days, level3_chats_per_day FROM sender_grade_settings WHERE workspace_id = :wid` scoped by `ctx.workspace_id`; no row → return the resolved code-defaults (5/30, 9/30, 13) shaped as `{levels: [{level, chats_per_day, step_days}], recommended: {...}}`.
    - `PUT /sender-grade-settings`: idempotent `INSERT INTO sender_grade_settings (...) VALUES (...) ON CONFLICT (workspace_id) DO UPDATE SET ... , updated_at = NOW()` with bind params only; commit; return `{status: "saved", settings: <resolved>, warnings: [...]}`.
    Register the router in `app/main.py` next to the other `app.include_router(...)` calls (after senders.router, ~line 196): `app.include_router(grade_settings.router)` and add `from app.routers import grade_settings` to the router imports.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('app/routers/grade_settings.py').read())" && grep -n "ON CONFLICT (workspace_id) DO UPDATE" app/routers/grade_settings.py && grep -n "grade_settings.router" app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - app/routers/grade_settings.py contains `prefix="/api/v1"` and route paths `/sender-grade-settings`
    - app/routers/grade_settings.py contains `ON CONFLICT (workspace_id) DO UPDATE`
    - app/routers/grade_settings.py contains `ctx.workspace_id` (workspace-scoped) and `auth_dep`
    - app/routers/grade_settings.py contains `def _validate_ladder` returning WarningItem list
    - app/main.py contains `app.include_router(grade_settings.router)`
  </acceptance_criteria>
  <done>Workspace ladder is readable/writable via a warmup-settings-style API with code-defaults and green-corridor warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Grade auto-progression sweep worker</name>
  <read_first>
    - app/services/warmup.py (lines 84-133 — WarmupWorker start/stop/_run/TICK_INTERVAL asyncio-task lifecycle to mirror)
    - app/main.py (lines 53-88 — lifespan: worker .start() on startup, await .stop() on shutdown)
    - app/services/grade_ladder.py (load_ladder, step_days_for_level)
    - app/database.py (AsyncSessionLocal usage pattern)
  </read_first>
  <action>
    Create `app/services/grade_progression.py` modeling the WarmupWorker lifecycle (class `GradeProgressionWorker` with `start()`, async `stop()`, async `_run()` loop, `TICK_INTERVAL = 3600` seconds — hourly is ample for a day-scale ladder). On each tick, open an AsyncSessionLocal and run a single set-based sweep that advances at most one level per sender per tick, reading step-days from each sender's workspace ladder:
    - Use a SQL UPDATE joined to a per-workspace resolved-ladder subquery so step-days come from sender_grade_settings (falling back to code-defaults 30/30 via COALESCE against absent rows). Shape:
      `UPDATE senders s SET current_level = s.current_level + 1, level_updated_at = NOW() FROM (SELECT w.id AS wid, COALESCE(g.level1_step_days, 30) AS s1, COALESCE(g.level2_step_days, 30) AS s2 FROM workspaces w LEFT JOIN sender_grade_settings g ON g.workspace_id = w.id) L WHERE s.workspace_id = L.wid AND s.current_level < 3 AND NOW() - s.level_updated_at >= make_interval(days => CASE s.current_level WHEN 1 THEN L.s1 WHEN 2 THEN L.s2 END);`
    - current_level = 3 is excluded by `s.current_level < 3` (D-17 permanent top level). A stalled account catches up one level per tick (accept eventual catch-up per RESEARCH Pattern 3; or loop the UPDATE until rowcount 0 — document the choice).
    - Instantiate module-level `grade_progression_worker = GradeProgressionWorker()`.
    Wire into `app/main.py` lifespan: add `from app.services.grade_progression import grade_progression_worker`, call `grade_progression_worker.start()` in startup (next to the other worker starts) with a log line, and `await grade_progression_worker.stop()` in shutdown (next to the other stops).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('app/services/grade_progression.py').read())" && grep -n "grade_progression_worker.start()" app/main.py && grep -n "current_level < 3" app/services/grade_progression.py</automated>
  </verify>
  <acceptance_criteria>
    - app/services/grade_progression.py contains `class GradeProgressionWorker` with `def start` and `async def stop`
    - app/services/grade_progression.py contains `current_level = s.current_level + 1` and `current_level < 3`
    - app/services/grade_progression.py reads step-days from `sender_grade_settings` (COALESCE fallback)
    - app/main.py contains `grade_progression_worker.start()` and `await grade_progression_worker.stop()`
  </acceptance_criteria>
  <done>A durable hourly sweep advances grades per the workspace ladder and stops at level 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests — ladder GET/PUT defaults + cross-tenant + progression/stop-at-3</name>
  <read_first>
    - tests/test_warmup_router.py (the settings GET/PUT + code-defaults + auth test structure to mirror)
    - tests/test_grade_foundation.py (from 22-01 — async DB fixture + insert helpers)
    - app/services/grade_progression.py (the sweep to invoke directly in a test)
  </read_first>
  <behavior>
    - GET /sender-grade-settings with no row → levels resolve to (5,30),(9,30),(13). (D-16)
    - PUT then GET round-trips custom values; PUT with chats_per_day=20 returns a green-corridor warning but 200. (D-16)
    - A workspace A cannot read/write workspace B's ladder (auth_dep scoping). (V4)
    - Progression: a sender with level_updated_at older than step_days advances +1; a sender inside the window does not; a sender at level 3 never advances even far past any interval. (D-14/D-17)
  </behavior>
  <action>
    Create `tests/test_grade_settings.py` (mirror test_warmup_router.py) covering the GET/PUT defaults, custom round-trip, green-corridor warning, and cross-tenant isolation behaviors.
    Create `tests/test_grade_progression.py` that seeds senders with controlled level_updated_at timestamps (e.g. NOW() - INTERVAL '31 days' at level 1), runs the sweep's UPDATE (invoke the worker's tick body or the SQL directly against the test session), and asserts: due level-1 sender → level 2 with reset level_updated_at; in-window sender unchanged; level-3 sender unchanged. Use the sampling command from 22-VALIDATION.md.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_grade_settings.py tests/test_grade_progression.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_grade_settings.py exists and asserts default budgets 5/9/13 on an absent row
    - tests/test_grade_settings.py asserts a cross-tenant read/write is rejected
    - tests/test_grade_progression.py asserts a level-3 sender never advances
    - `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_grade_settings.py tests/test_grade_progression.py -x` exits 0
  </acceptance_criteria>
  <done>Ladder API and auto-progression are covered by green integration tests including the D-17 stop-at-3 and cross-tenant guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET/PUT /sender-grade-settings | Untrusted workspace token; must not touch another tenant's ladder |
| client → ladder values | Untrusted ints must stay in bounds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-04 | Information Disclosure / Tampering | GET/PUT /sender-grade-settings | high | mitigate | Every query scoped by `ctx.workspace_id` from auth_dep; upsert keyed on the caller's workspace_id (mirrors warmup settings) |
| T-22-05 | Tampering | ladder int inputs | medium | mitigate | Pydantic `ge=1, le=100` (chats) / `ge=1, le=365` (step days) hard bounds + green-corridor soft warnings |
| T-22-06 | Tampering | progression sweep SQL | low | mitigate | Bind params / set-based UPDATE only; no user input reaches the sweep |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase |
</threat_model>

<verification>
- `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_grade_settings.py tests/test_grade_progression.py -x` green
- App imports cleanly with the new router + worker registered
</verification>

<success_criteria>
Workspace ladder is configurable via API with green-corridor guidance and cross-tenant isolation; grades auto-advance per the ladder and freeze at level 3.
</success_criteria>

<output>
Create `.planning/phases/22-account-level-new-chat-limit-grades/22-02-SUMMARY.md` when done
</output>
