---
phase: 22-account-level-new-chat-limit-grades
plan: 07
type: execute
wave: 4
depends_on: ["22-02", "22-04", "22-06"]
files_modified:
  - lovable-handoff/openapi.json
  - lovable-handoff/types/api.ts
  - .planning/phases/22-account-level-new-chat-limit-grades/22-FRONTEND-HANDOFF.md
autonomous: false
requirements: [D-11, D-12]
user_setup: []

must_haves:
  truths:
    - "lovable-handoff/openapi.json reflects the new GET/PUT /sender-grade-settings, PATCH /senders/{slug}/grade, and the extended SenderResponse (current_level/level_updated_at/remaining_daily_budget) (D-12)"
    - "openapi.json no longer references max_new_dialogs_per_day or rate_per_day/per_day (D-04/D-07/D-12)"
    - "A frontend handoff note tells the sibling aimly-tg-outreach repo what to build: ladder editor, per-card grade + remaining budget, override control (D-11)"
  artifacts:
    - lovable-handoff/openapi.json
    - .planning/phases/22-account-level-new-chat-limit-grades/22-FRONTEND-HANDOFF.md
  key_links:
    - "openapi.json is regenerated from the running api via scripts/export-handoff.sh — never hand-edited"
    - "frontend work lives in the sibling repo /root/apps/aimly/aimly-tg-outreach (independent origin) — this plan only produces the contract + handoff note, no UI-SPEC.md exists (user chose to proceed without one)"
---

<objective>
Publish the API contract for the grade feature and hand off the UI work to the sibling frontend repo, then confirm the end-to-end grade behavior with a human check.

Purpose: D-12 (regenerate openapi.json + types) and D-11 (frontend: ladder editor, grade display, override control). The backend is fully in place after Waves 1-3; this plan makes the contract consumable by Lovable and records exactly what the sibling repo must build. Frontend UI is a lightweight cross-repo follow-up (no UI-SPEC.md — user proceeded without one; VALIDATION marks these manual-only).
Output: regenerated openapi.json + types, a frontend handoff note, and a human-verify checkpoint covering the three UI-visible behaviors.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-CONTEXT.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-VALIDATION.md
@scripts/export-handoff.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate openapi.json + types (D-12)</name>
  <read_first>
    - scripts/export-handoff.sh (the canonical regeneration flow: rebuild api, wait for /openapi.json, dump + jq, sanity-check title == Outreach Platform / aimly, generate types/api.ts via openapi-typescript)
    - lovable-handoff/openapi.json (current contract — to diff against after regen)
    - CLAUDE.md §"Git & Deploy" (api rebuild: docker compose up -d --build api)
  </read_first>
  <action>
    Rebuild the api container so the new routers/schemas are live (`docker compose up -d --build api`), then run `scripts/export-handoff.sh` to regenerate `lovable-handoff/openapi.json` and `lovable-handoff/types/api.ts` from the running FastAPI. Do NOT hand-edit openapi.json. After regen, confirm the diff shows: the two new `/sender-grade-settings` operations, the `/senders/{slug}/grade` PATCH, the `current_level`/`level_updated_at`/`remaining_daily_budget` fields on SenderResponse, and the disappearance of `max_new_dialogs_per_day` and the sender `per_day` rate field. If the script cannot reach a running api in this environment, capture the exact failure in the SUMMARY and fall back to documenting the required manual regen command for the deploy host.
  </action>
  <verify>
    <automated>grep -c "sender-grade-settings" lovable-handoff/openapi.json; grep -c "max_new_dialogs_per_day" lovable-handoff/openapi.json</automated>
  </verify>
  <acceptance_criteria>
    - lovable-handoff/openapi.json contains `sender-grade-settings`
    - lovable-handoff/openapi.json contains `/senders/{slug}/grade`
    - lovable-handoff/openapi.json contains `current_level` and `remaining_daily_budget`
    - `grep -c "max_new_dialogs_per_day" lovable-handoff/openapi.json` returns 0
  </acceptance_criteria>
  <done>The handoff contract reflects the grade endpoints/fields and drops the retired ones.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend handoff note for the sibling repo (D-11)</name>
  <read_first>
    - lovable-handoff/README.md (handoff doc conventions)
    - lovable-handoff/openapi.json (the regenerated contract to reference)
    - CLAUDE.md §"Фронтенд (отдельный репо)" (sibling repo /root/apps/aimly/aimly-tg-outreach, independent origin)
  </read_first>
  <action>
    Create `.planning/phases/22-account-level-new-chat-limit-grades/22-FRONTEND-HANDOFF.md` describing the three UI deliverables for the sibling `aimly-tg-outreach` repo (D-11), each mapped to the regenerated contract:
    1. Workspace grade-ladder editor (3 fixed rows: chats/day + days-to-next; level 3 has no step) bound to GET/PUT `/api/v1/sender-grade-settings`; show green-corridor warnings from the response `warnings[]` in the same style as the existing rate-limit warnings.
    2. Per-account card: display `current_level`, `level_updated_at`, and `remaining_daily_budget` from the extended SenderResponse; optional progress-to-next-level indicator.
    3. Manual grade-override control on the card → PATCH `/api/v1/senders/{slug}/grade` with `{current_level}` (1..3); note that it resets the progression timer (D-15).
    Also note the removals to purge from the frontend: the campaign `max_new_dialogs_per_day` form field and the sender daily-message (`per_day`) rate editor. Additionally flag the sender-list "TODAY x/150" usage column: it currently renders `sent_today/rate_per_day` (the `SenderResponse.sent_today` numerator was computed specifically so `{sent_today}/{rate_per_day}` never desyncs — see app/routers/senders.py sent_today logic and app/schemas/__init__.py). With the daily-message cap removed (D-04), that denominator no longer exists — instruct the frontend to either drop the "TODAY x/150" column entirely or repurpose it as a plain "messages sent today" counter (numerator only, no `/150` denominator). State explicitly that no UI-SPEC.md exists for this phase (user chose to proceed without one) and that this note + the regenerated openapi.json ARE the contract.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-account-level-new-chat-limit-grades/22-FRONTEND-HANDOFF.md && grep -c "sender-grade-settings\|current_level\|/grade" .planning/phases/22-account-level-new-chat-limit-grades/22-FRONTEND-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - 22-FRONTEND-HANDOFF.md exists and references `sender-grade-settings`, `/senders/{slug}/grade`, and `current_level`
    - 22-FRONTEND-HANDOFF.md lists the campaign `max_new_dialogs_per_day` and sender `per_day` fields to remove from the UI
    - 22-FRONTEND-HANDOFF.md notes no UI-SPEC.md exists for this phase
  </acceptance_criteria>
  <done>The sibling repo has a precise, contract-linked list of what to build and remove.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify grade feature end-to-end (UI + backend)</name>
  <action>Pause for human verification of the grade feature across the sibling frontend and the backend test set — see how-to-verify for the exact steps. This is a cross-repo UI checkpoint (VALIDATION marks the UI behaviors manual-only); the executor performs no code changes in this task.</action>
  <what-built>Account-level grade limits end-to-end: workspace ladder API, per-sender grade + remaining budget on the card, manual override, shared budget across outreach + warmup, and removal of the per-campaign dialog cap and the daily-message cap. Frontend contract + handoff published (UI itself is built in the sibling repo).</what-built>
  <how-to-verify>
    1. Open the workspace grade-ladder editor (once the sibling frontend consumes the new contract) — confirm 3 rows with chats/day + days-to-next, defaults 5/30, 9/30, 13, and a green-corridor warning when you enter e.g. 20 chats/day.
    2. Open a sender card — confirm it shows the current grade/level, level-updated timestamp, and remaining daily new-chat budget.
    3. Use the override control to set the grade to level 2 — confirm the card reflects level 2 and that the progression timer reset (level_updated_at is now).
    4. Confirm the campaign form no longer has a "max new dialogs per day" field and the sender editor no longer has a daily-message-limit field.
    5. Backend sanity: run the Phase-22 targeted test set via the test-overlay (test_grade_settings, test_grade_progression, test_senders -k "rate or grade or override", test_queue_new_dialog_limit, test_queue_even_pacing, test_warmup_worker -k "pair or reserve", test_send_campaign) and confirm green; diff against a clean-tree run of the same subset (baseline is RED on the full suite — targeted only, per 22-VALIDATION.md).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handoff artifact → sibling repo | contract only; no secrets embedded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-18 | Information Disclosure | openapi.json export | low | mitigate | export-handoff.sh sanity-checks the title is Outreach Platform / aimly (guards against dumping a neighbouring FastAPI); no secrets in the schema |
| T-22-19 | Spoofing | wrong-app openapi dump | low | mitigate | Script aborts if info.title mismatches |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | export-handoff uses `npx openapi-typescript` — a pinned dev tool, not an app dependency; no runtime package added |
</threat_model>

<verification>
- openapi.json contains the grade endpoints/fields and no retired fields
- Human verifies the three UI behaviors + targeted backend test set green
</verification>

<success_criteria>
The grade feature's contract is published and consumable, the sibling repo has an actionable handoff, and a human confirms the ladder editor, grade display, override, and field removals work end-to-end.
</success_criteria>

<output>
Create `.planning/phases/22-account-level-new-chat-limit-grades/22-07-SUMMARY.md` when done
</output>
