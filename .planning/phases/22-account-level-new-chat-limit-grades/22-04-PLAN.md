---
phase: 22-account-level-new-chat-limit-grades
plan: 04
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - app/routers/senders.py
  - app/schemas/__init__.py
  - tests/test_senders.py
autonomous: true
requirements: [D-04, D-12, D-15]

must_haves:
  truths:
    - "The sender API no longer exposes, validates, or accepts rate_per_day (per_day) anywhere (D-04)"
    - "SenderResponse carries current_level and level_updated_at, and a remaining daily new-chat budget on the list endpoint (D-12)"
    - "PATCH /senders/{slug}/grade sets current_level and resets level_updated_at = NOW() in one operation, workspace-scoped (D-15)"
    - "Manual override to an out-of-range level (not 1..3) is rejected (D-15)"
  artifacts:
    - app/routers/senders.py
    - app/schemas/__init__.py
  key_links:
    - "manual override writes the SAME two fields as auto-progression (current_level + level_updated_at=NOW()); no separate frozen flag (D-15)"
    - "grade PATCH is guarded by the existing _assert_workspace_owns_sender / auth_dep so a tenant cannot re-grade a sender it does not own"
---

<objective>
Clean `rate_per_day` out of the sender API surface (D-04 UI/API half), surface the account grade on `SenderResponse` (D-12), and add a workspace-scoped manual grade-override PATCH that resets the progression timer (D-15).

Purpose: These are the sender-facing API changes. The ORM `senders.rate_per_day` column stays until 22-06 (final drop); this plan only stops the API from reading/exposing it, and adds the grade read + override write. Runs in parallel with the queue/warmup/settings plans (only file touched by another plan is app/schemas/__init__.py, whose Campaign-section edits land later in 22-06 — different wave).
Output: rate_per_day removed from schemas + router; SenderResponse extended with grade fields; a dedicated grade PATCH sub-route; extended test_senders.py.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-CONTEXT.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-RESEARCH.md
@app/services/grade_ladder.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove rate_per_day from schemas + sender router surface (D-04)</name>
  <read_first>
    - app/schemas/__init__.py (lines 75-79 RateLimits.per_day; 90-104 SenderCreate.rate_per_day at 104; 107-121 SenderUpdate.rate_per_day at 115; 124-180 SenderResponse.rate_limits at 152)
    - app/routers/senders.py (lines 71-73 RATE_HARD_CAP/RATE_SOFT_CAP; 158-162 RateLimits construction in _sender_to_response; 179-220 _validate_rate_limits; 540-541 + 628-629 callers; 560-561 + 647-648 assignments)
    - 22-RESEARCH.md §"Current Code" #5 and #6 (exact removal map; note rate_per_min/hour MUST stay)
  </read_first>
  <action>
    Schemas (`app/schemas/__init__.py`), remove ONLY the day component (keep per_minute/per_hour everywhere):
    - `RateLimits`: delete `per_day: int = 150` (line 79).
    - `SenderCreate`: delete `rate_per_day: Optional[int] = Field(None, ge=1, le=300)` (line 104).
    - `SenderUpdate`: delete `rate_per_day: Optional[int] = Field(None, ge=1, le=300)` (line 115).
    Do NOT touch the Campaign schemas in this file (they are 22-06's).

    Router (`app/routers/senders.py`):
    - `RATE_HARD_CAP` (line 71) and `RATE_SOFT_CAP` (line 73): remove the `"rate_per_day": ...` key from each dict, leaving `rate_per_min`/`rate_per_hour`.
    - `_validate_rate_limits` (179-220): drop the `rate_per_day` parameter and every reference to it inside (the dict entry / cap lookups at lines ~182, 193).
    - Its callers at lines 540-541 (create) and 628-629 (PATCH): drop the `rate_per_day=...` argument.
    - Assignments `sender.rate_per_day = request.rate_per_day` at 560-561 and 647-648: delete them.
    - In `_sender_to_response` (line 158-162): construct `RateLimits(per_minute=sender.rate_per_min, per_hour=sender.rate_per_hour)` — drop `per_day=sender.rate_per_day`.
    Leave `sender.rate_per_day` untouched in the ORM (dropped in 22-06); simply stop reading/writing it through the API.
  </action>
  <verify>
    <automated>grep -c "rate_per_day\|per_day" app/routers/senders.py; grep -n "per_day" app/schemas/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rate_per_day" app/routers/senders.py` returns 0
    - app/routers/senders.py RATE_HARD_CAP line no longer contains `rate_per_day`
    - app/schemas/__init__.py has no `per_day` inside the RateLimits class and no `rate_per_day` in SenderCreate/SenderUpdate
    - app/routers/senders.py still contains `rate_per_min` and `rate_per_hour` in RATE_SOFT_CAP
  </acceptance_criteria>
  <done>rate_per_day is gone from every sender API path (schemas, caps, validation, response, assignments); per-minute/per-hour untouched.</done>
</task>

<task type="auto">
  <name>Task 2: SenderResponse grade fields + PATCH /senders/{slug}/grade override (D-12/D-15)</name>
  <read_first>
    - app/schemas/__init__.py (lines 124-180 SenderResponse — where to add fields; ConfigDict(from_attributes=True) at 126)
    - app/routers/senders.py (lines 125-176 _sender_to_response builder; line 618 PATCH /senders/{slug} + the _assert_workspace_owns_sender / auth_dep ownership pattern used there)
    - app/services/grade_ladder.py (budget_for_level, load_ladder for the remaining-budget computation)
    - 22-RESEARCH.md Open Question 3 (recommendation: dedicated PATCH sub-route to keep the timer-reset semantics explicit)
  </read_first>
  <action>
    Schemas — extend `SenderResponse` (after the rate_limits field, ~line 152) with:
    - `current_level: int = 1`
    - `level_updated_at: Optional[datetime] = None`
    - `remaining_daily_budget: Optional[int] = None`  # new-chat budget minus distinct new dialogs opened in trailing 24h; None where not computed
    Add a `GradeOverrideRequest` model with `current_level: int = Field(..., ge=1, le=3)` (D-15 bounds; D-16 fixed 3 levels).

    Router (`app/routers/senders.py`):
    - In `_sender_to_response`, populate `current_level=getattr(sender, "current_level", 1) or 1` and `level_updated_at=getattr(sender, "level_updated_at", None)`. Add an optional `remaining_daily_budget: Optional[int] = None` parameter and pass it through; compute it only on the list endpoint (budget_for_level(load_ladder(...), current_level) minus a `SELECT COUNT(DISTINCT recipient_phone) ... status='sent' AND finished_at >= NOW() - INTERVAL '24 hours'`, clamped at 0). Single-sender paths report None (same convention as sent_today=0).
    - Add a dedicated route `@router.patch("/senders/{slug}/grade")` accepting `GradeOverrideRequest`, guarded by the same auth_dep + `_assert_workspace_owns_sender` used by the existing PATCH at line 618. It runs `UPDATE senders SET current_level = :lvl, level_updated_at = NOW() WHERE id = :sid` (D-15: identical write to auto-progression, resets the timer — no separate frozen flag) then returns the refreshed SenderResponse. Bind params only.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('app/routers/senders.py').read())" && grep -n "current_level" app/schemas/__init__.py && grep -n "senders/{slug}/grade" app/routers/senders.py</automated>
  </verify>
  <acceptance_criteria>
    - app/schemas/__init__.py SenderResponse contains `current_level` and `level_updated_at` and `remaining_daily_budget`
    - app/schemas/__init__.py contains `class GradeOverrideRequest` with `ge=1, le=3`
    - app/routers/senders.py contains `@router.patch("/senders/{slug}/grade")`
    - app/routers/senders.py grade route contains `level_updated_at = NOW()` and `_assert_workspace_owns_sender`
  </acceptance_criteria>
  <done>The sender card can read its grade + remaining budget and override the grade via a workspace-scoped PATCH that resets the timer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend test_senders.py — rate removal + grade fields + override</name>
  <read_first>
    - tests/test_senders.py (existing sender CRUD + rate-limit tests, fixture patterns)
    - app/routers/senders.py (the new grade route + response builder)
  </read_first>
  <behavior>
    - `..._rate...`: creating/updating a sender no longer accepts rate_per_day; SenderResponse has no per_day; per_minute/per_hour still validated. (D-04)
    - `..._grade...`: GET/list sender exposes current_level and level_updated_at. (D-12)
    - `..._override...`: PATCH /senders/{slug}/grade to level 2 sets current_level=2 and bumps level_updated_at; a subsequent read reflects it; the timer reset means auto-progression restarts from the new baseline. (D-15)
    - override to level 4 → 422; a sender in another workspace → 403/404. (D-15/V4)
  </behavior>
  <action>
    Extend `tests/test_senders.py` with: a rate-removal case (rate_per_day rejected/ignored, response has no per_day, per_minute/per_hour intact), a grade-field-exposure case, an override happy path (level set + timer reset), an out-of-range override rejection, and a cross-tenant override rejection. Use `-k grade`, `-k override`, `-k rate` selectable names per 22-VALIDATION.md.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_senders.py -k "rate or grade or override" -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_senders.py contains a test matching `-k grade` and one matching `-k override`
    - tests/test_senders.py asserts a level-4 override is rejected
    - tests/test_senders.py asserts a cross-tenant grade override is rejected
    - `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_senders.py -k "rate or grade or override" -x` exits 0
  </acceptance_criteria>
  <done>Sender API changes for D-04/D-12/D-15 are proven green including bounds and cross-tenant guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PATCH /senders/{slug}/grade | Untrusted token must own the sender; level must be in range |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-10 | Elevation of Privilege | PATCH /senders/{slug}/grade | high | mitigate | auth_dep + `_assert_workspace_owns_sender` gate; UPDATE keyed on the owned sender id |
| T-22-11 | Tampering | current_level override value | medium | mitigate | Pydantic `ge=1, le=3` on GradeOverrideRequest + DB CHECK from mig 056 |
| T-22-12 | Tampering | override / remaining-budget SQL | low | mitigate | Bind params only; no interpolation |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase |
</threat_model>

<verification>
- `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_senders.py -k "rate or grade or override" -x` green
- `grep -rn "rate_per_day" app/routers/senders.py` returns nothing
</verification>

<success_criteria>
rate_per_day is fully removed from the sender API; the grade and remaining budget are visible; a workspace-scoped, range-checked manual override resets the progression timer.
</success_criteria>

<output>
Create `.planning/phases/22-account-level-new-chat-limit-grades/22-04-SUMMARY.md` when done
</output>
