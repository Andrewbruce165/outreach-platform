---
phase: 22-account-level-new-chat-limit-grades
plan: 05
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - app/services/warmup.py
  - tests/test_warmup_worker.py
autonomous: true
requirements: [D-08, D-09, D-03]

must_haves:
  truths:
    - "Creating a warmup session for a NEW pair (never warmed together) charges the initiator's account new-chat budget and records the pair in sender_first_contacts (D-08)"
    - "Creating a session for a KNOWN pair (already in sender_first_contacts) spends no budget (D-08)"
    - "Warmup only opens a new pair when the initiator has remaining budget after reserving outreach: remaining = account_budget - spent_in_trailing_24h - pending cold openers (D-09)"
    - "The initiator (charged account, writes first) is the older/more-warmed account of the pair (D-08 discretion)"
    - "The shared budget uses the trailing-24h window in both queue and warmup (D-03, RESEARCH Pitfall 4)"
  artifacts:
    - app/services/warmup.py
  key_links:
    - "new-pair classification reads sender_first_contacts with canonical LEAST/GREATEST ordering matching migration 057"
    - "the reserve reads the same trailing-24h sender-wide DISTINCT-recipient count the queue cap uses, so outreach is never starved by warmup"
---

<objective>
Make warmup pairing spend the shared account new-chat budget only for genuinely new pairs, behind an outreach-priority reserve, so warmup competes for whatever budget outreach does not need today.

Purpose: D-08 (new-pair-only budget via sender_first_contacts) and D-09 (outreach priority reserve) are the warmup half of the shared-budget model (D-03). Only file touched is app/services/warmup.py — fully parallel with the other Wave-2 plans.
Output: `_create_new_sessions` classifies pairs known/new, charges + records new pairs, respects the reserve, orders the initiator as the older/warmed account, and a trailing-24h shared window; extended warmup tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-CONTEXT.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-RESEARCH.md
@app/services/grade_ladder.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New-pair classification + charge + registry insert in _create_new_sessions</name>
  <read_first>
    - app/services/warmup.py (lines 541-629 — _create_new_sessions full-mesh; active_pairs frozenset dedup 566-572; combinations loop 587-591; INSERT warmup_sessions 607-622)
    - app/services/warmup.py (lines 167-227 _get_active_pool — enrolled_at/enrolled_days per pool member; lines 229-239 _count_sent_today which uses CURRENT_DATE — the window to standardize)
    - app/services/grade_ladder.py (load_ladder, budget_for_level)
    - migrations/057_sender_first_contacts.sql (canonical LEAST/GREATEST pair ordering + PK)
    - 22-RESEARCH.md §"Current Code" #3 (initiator determination + window-mismatch Pitfall 4)
  </read_first>
  <behavior>
    - Pair (A,B) present in sender_first_contacts → treated as known → session created with NO budget charge (current free behavior).
    - Pair (A,B) absent → new → only created if the initiator's remaining budget >= 1; on creation, insert the canonical (LEAST,GREATEST) pair into sender_first_contacts.
    - Initiator = the older/more-warmed account (larger enrolled_days, tie-break by enrolled_at); it becomes sender_a so it writes first.
  </behavior>
  <action>
    In `_create_new_sessions`, after computing the candidate `pairs` list (line 591) and before creating sessions, classify each pair against the registry: load existing pairs for the workspace once — `SELECT sender_a_id, sender_b_id FROM sender_first_contacts` — into a `set[frozenset]` (same shape as active_pairs). For each candidate pair:
    - If `frozenset({a_id, b_id})` is in the known set → KNOWN: create the session as today, charge nothing.
    - Else → NEW: determine the initiator as the account with the greater `enrolled_days` (tie-break: earlier `enrolled_at`); reorder the pair so the initiator is `sender_a` (so `_process_session`'s NEW-session `else` branch makes sender_a write first — RESEARCH §3). Compute the initiator's remaining budget (Task 2 reserve). Only if remaining >= 1: create the session AND `INSERT INTO sender_first_contacts (sender_a_id, sender_b_id, first_contact_at) VALUES (LEAST(:a,:b), GREATEST(:a,:b), NOW()) ON CONFLICT DO NOTHING`. If remaining < 1, skip creating this new pair this tick (it becomes eligible once outreach frees budget).
    Preserve the existing workspace partitioning + cross-tenant assertion (596-599) and the active_pairs skip (589-590). Bind params only.
  </action>
  <verify>
    <automated>grep -n "sender_first_contacts" app/services/warmup.py && docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_warmup_worker.py -k pair -x</automated>
  </verify>
  <acceptance_criteria>
    - app/services/warmup.py reads `sender_first_contacts` into a known-pairs set
    - app/services/warmup.py contains `INSERT INTO sender_first_contacts` with `LEAST(` / `GREATEST(` and `ON CONFLICT DO NOTHING`
    - app/services/warmup.py chooses the initiator by `enrolled_days` and orders it as sender_a
    - `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_warmup_worker.py -k pair -x` exits 0
  </acceptance_criteria>
  <done>New warmup pairs charge the older/warmed initiator and are recorded; known pairs stay free.</done>
</task>

<task type="auto">
  <name>Task 2: Outreach-priority reserve on the shared trailing-24h budget (D-09)</name>
  <read_first>
    - app/services/warmup.py (lines 229-239 _count_sent_today — CURRENT_DATE window to replace with trailing-24h for the shared budget)
    - app/services/queue.py (the trailing-24h cap COUNT rewritten in 22-03 — the count shape to match: DISTINCT recipient_phone, status='sent', finished_at >= NOW() - INTERVAL '24 hours')
    - app/services/grade_ladder.py (budget_for_level, load_ladder)
  </read_first>
  <action>
    Add a helper in warmup.py (e.g. `async def _remaining_new_chat_budget(db, sender_id, workspace_id, current_level) -> int`) computing the initiator's remaining shared new-chat budget on the trailing-24h window (RESEARCH Pitfall 4 — standardize on trailing-24h, NOT CURRENT_DATE):
    - `account_budget = budget_for_level(await load_ladder(db, workspace_id), current_level)`.
    - `spent` = `SELECT COUNT(DISTINCT recipient_phone) FROM message_queue WHERE sender_id = :sid AND status='sent' AND finished_at >= NOW() - INTERVAL '24 hours'` (matches the 22-03 queue cap so both workers agree on "spent today").
    - `pending` = `SELECT COUNT(DISTINCT recipient_phone) FROM message_queue WHERE sender_id = :sid AND status='pending' AND campaign_id IS NOT NULL AND NOT EXISTS (prior sent to that phone by this sender)` — the reserved cold openers still queued today (outreach priority, D-09). Keep this a bounded count (cap at account_budget) so a huge backlog does not force negative math.
    - `remaining = max(0, account_budget - spent - pending)`.
    Fetch each pool member's `current_level` in `_get_active_pool` (add `s.current_level` to its SELECT + returned dict) so the initiator's level is available without an extra round-trip. Call `_remaining_new_chat_budget` from Task 1's NEW-pair branch.
  </action>
  <verify>
    <automated>grep -n "INTERVAL '24 hours'" app/services/warmup.py && grep -n "_remaining_new_chat_budget\|current_level" app/services/warmup.py && docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_warmup_worker.py -k reserve -x</automated>
  </verify>
  <acceptance_criteria>
    - app/services/warmup.py contains `_remaining_new_chat_budget` computing `account_budget - spent - pending`
    - app/services/warmup.py uses `INTERVAL '24 hours'` (trailing-24h) for the shared-budget spent count
    - app/services/warmup.py `_get_active_pool` SELECT includes `s.current_level`
    - `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_warmup_worker.py -k reserve -x` exits 0
  </acceptance_criteria>
  <done>Warmup only takes budget outreach does not need, computed on the same trailing-24h window the queue uses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Warmup budget/reserve/backfill tests</name>
  <read_first>
    - tests/test_warmup_worker.py (existing worker tests + DB fixtures + how sessions/pool are seeded)
    - tests/test_grade_foundation.py (from 22-01 — sender_first_contacts backfill idempotency helper reference)
  </read_first>
  <behavior>
    - New pair with budget available → session created + registry row inserted; charged initiator = older/warmed account = sender_a.
    - Known pair (already in registry) → session created, no new registry row, no budget consumed.
    - Reserve: a sender with account_budget spent/reserved by outreach (pending openers) → warmup creates no new pair for it (remaining 0); once reserve drops, a new pair opens.
    - Backfill idempotency: a pre-existing warmed pair (from warmup_sessions/messages backfill) is classified KNOWN, not new.
  </behavior>
  <action>
    Extend `tests/test_warmup_worker.py` with `-k pair` cases (new vs known classification, initiator = older account as sender_a, registry insert) and `-k reserve` cases (outreach pending/spent starves warmup, then frees it). Reuse the 22-01 backfill assertion to confirm a backfilled pair is treated as known. Seed pool members with distinct enrolled_at to control the initiator choice.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_warmup_worker.py -k "pair or reserve" -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_warmup_worker.py has cases matching `-k pair` and `-k reserve`
    - tests/test_warmup_worker.py asserts a known/backfilled pair is not charged
    - tests/test_warmup_worker.py asserts the initiator is the older account and is sender_a
    - `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_warmup_worker.py -k "pair or reserve" -x` exits 0
  </acceptance_criteria>
  <done>D-08/D-09 behavior — new-pair charge, known-pair free, reserve priority, initiator ordering, backfill-known — proven green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| warmup worker → sender_first_contacts / message_queue | internal; must not cross workspace when pairing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-13 | Information Disclosure | cross-tenant warmup pair via registry | high | mitigate | Preserve `_get_active_pool` workspace partitioning + the cross-tenant assertion; registry entries are same-workspace by construction |
| T-22-14 | Denial of Service | outreach starved by warmup | medium | mitigate | Outreach-priority reserve subtracts spent + pending before warmup can take a slot (D-09) |
| T-22-15 | Tampering | budget/reserve SQL | low | mitigate | Bind params only; trailing-24h window matches the queue cap (no window drift) |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase |
</threat_model>

<verification>
- `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_warmup_worker.py -k "pair or reserve" -x` green
- warmup and queue agree on the trailing-24h spent window (grep confirms `INTERVAL '24 hours'` in both)
</verification>

<success_criteria>
Warmup spends the shared account budget only for new pairs behind an outreach reserve, records pairs so repeats are free, and never starves outreach.
</success_criteria>

<output>
Create `.planning/phases/22-account-level-new-chat-limit-grades/22-05-SUMMARY.md` when done
</output>
