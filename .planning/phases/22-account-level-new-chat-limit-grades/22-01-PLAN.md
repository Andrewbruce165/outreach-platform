---
phase: 22-account-level-new-chat-limit-grades
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/056_sender_grade_columns.sql
  - migrations/057_sender_first_contacts.sql
  - migrations/058_sender_grade_settings.sql
  - app/models/__init__.py
  - app/services/grade_ladder.py
  - tests/conftest.py
  - tests/test_grade_foundation.py
autonomous: true
requirements: [D-14, D-16, D-08, D-10]

must_haves:
  truths:
    - "A fresh test DB built by conftest has senders.current_level and senders.level_updated_at columns (D-14)"
    - "Existing sender rows get level_updated_at backfilled to created_at (D-10/D-14)"
    - "sender_first_contacts table exists and is idempotently backfilled from prior warmup pairs so already-warmed pairs are NOT counted as new (D-08)"
    - "sender_grade_settings table exists with per-workspace 3-level ladder columns (D-16)"
    - "grade_ladder.py resolves code-defaults 5/30, 9/30, 13 when no workspace row exists (D-16)"
  artifacts:
    - migrations/056_sender_grade_columns.sql
    - migrations/057_sender_first_contacts.sql
    - migrations/058_sender_grade_settings.sql
    - app/services/grade_ladder.py
  key_links:
    - "ORM Sender.current_level / level_updated_at carry server_default (create_all builds fresh/test schema from ORM, not migration — project-orm-default-vs-server-default-drift)"
    - "conftest exists-guarded blocks apply the SQL-only backfills (056 level_updated_at=created_at, 057 pair backfill) — conftest does NOT glob migrations"
---

<objective>
Lay the durable data-model foundation for account-level grade limits: add grade storage columns to senders, the new-warmup-pair registry, and the per-workspace grade ladder settings table, plus a shared ladder-resolution helper that every downstream feature plan imports.

Purpose: Isolate all additive schema + ORM + shared-helper work into one atomic foundation so the Wave-2 feature plans (queue rewrite, settings API, warmup budget, sender API) build on a consistent schema without touching each other's files. No behavior changes yet — nothing reads these columns until Wave 2.
Output: 3 idempotent migrations, additive ORM models/columns with mandatory server_default, `app/services/grade_ladder.py` resolver, conftest exists-guarded blocks, and a foundation test proving the schema + backfill are correct.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-CONTEXT.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-RESEARCH.md
@.planning/phases/22-account-level-new-chat-limit-grades/22-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 056 + ORM grade columns on Sender</name>
  <read_first>
    - migrations/038_warmup_settings.sql (idempotent migration header/BEGIN-COMMIT pattern)
    - migrations/052_sender_tg_premium.sql (recent ADD COLUMN idempotent example)
    - app/models/__init__.py (lines 74-152 — Sender model; lines 120-124 show rate_per_min/hour/day + created_at with server_default)
    - CLAUDE.md "Миграции" + "ORM default= vs server_default=" memory note in this file's context
  </read_first>
  <action>
    Create `migrations/056_sender_grade_columns.sql` (idempotent, BEGIN/COMMIT, header comment in the 038 style noting auto-apply + fail-fast). Statements:
    1. `ALTER TABLE senders ADD COLUMN IF NOT EXISTS current_level INT NOT NULL DEFAULT 1;`
    2. `ALTER TABLE senders ADD COLUMN IF NOT EXISTS level_updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW();`
    3. Backfill existing rows so the grade timer starts at account creation (D-10/D-14): `UPDATE senders SET level_updated_at = created_at WHERE created_at IS NOT NULL AND level_updated_at > created_at;` (only touches rows where the new DEFAULT NOW() overshot created_at; idempotent/re-runnable).
    4. Add a guarded CHECK for the 1..3 range using a DO block that swallows duplicate_object:
       `DO $$ BEGIN ALTER TABLE senders ADD CONSTRAINT senders_current_level_range CHECK (current_level BETWEEN 1 AND 3); EXCEPTION WHEN duplicate_object THEN NULL; END $$;`
    Do NOT drop rate_per_day here — that removal ships in plan 22-06 after all readers are rewritten (RESEARCH Pitfall 6).

    In `app/models/__init__.py` Sender model, immediately after the `created_at` column (line ~124), ADD:
    - `current_level = Column(Integer, nullable=False, server_default='1')  # Phase 22 D-14: account grade level 1..3`
    - `level_updated_at = Column(DateTime(timezone=True), nullable=False, server_default=func.now())  # Phase 22 D-14: grade timer; backfilled = created_at (mig 056)`
    Both MUST carry server_default (project-orm-default-vs-server-default-drift: create_all ignores Python-side default=; raw INSERTs in _insert_sender_raw / bulk import omit these). Leave rate_per_day (line 123) in place for now.
  </action>
  <verify>
    <automated>grep -n "current_level" app/models/__init__.py && grep -n "level_updated_at" app/models/__init__.py && grep -c "IF NOT EXISTS" migrations/056_sender_grade_columns.sql</automated>
  </verify>
  <acceptance_criteria>
    - migrations/056_sender_grade_columns.sql contains `ADD COLUMN IF NOT EXISTS current_level`
    - migrations/056_sender_grade_columns.sql contains `ADD COLUMN IF NOT EXISTS level_updated_at`
    - migrations/056_sender_grade_columns.sql contains `level_updated_at = created_at`
    - migrations/056_sender_grade_columns.sql contains `duplicate_object`
    - app/models/__init__.py contains `current_level = Column(Integer, nullable=False, server_default='1')`
    - app/models/__init__.py contains `level_updated_at = Column(DateTime(timezone=True), nullable=False, server_default=func.now())`
    - app/models/__init__.py still contains `rate_per_day = Column(Integer` (not removed in this plan)
  </acceptance_criteria>
  <done>senders table gains current_level + level_updated_at (backfilled to created_at) via idempotent migration; ORM mirrors both with server_default.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 057 (sender_first_contacts + backfill) + 058 (sender_grade_settings) + ORM models + grade_ladder helper</name>
  <read_first>
    - migrations/038_warmup_settings.sql (workspace-PK table + code-default pattern)
    - app/models/__init__.py (lines 383-453 — WarmupSession/WarmupMessage/WarmupSettings models; use WarmupSettings as the template for the new settings ORM model)
    - app/services/warmup.py (lines 541-629 — _create_new_sessions; the backfill source is warmup_messages {from_sender_id,to_sender_id} / warmup_sessions {sender_a_id,sender_b_id})
    - app/routers/warmup.py (lines 506-523 — _resolve_settings code-default shape to mirror)
  </read_first>
  <action>
    Create `migrations/057_sender_first_contacts.sql` (idempotent):
    - `CREATE TABLE IF NOT EXISTS sender_first_contacts (sender_a_id UUID NOT NULL REFERENCES senders(id) ON DELETE CASCADE, sender_b_id UUID NOT NULL REFERENCES senders(id) ON DELETE CASCADE, first_contact_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), PRIMARY KEY (sender_a_id, sender_b_id));` Store the pair with a canonical order (sender_a_id < sender_b_id) so an unordered pair maps to exactly one row — document this invariant in a header comment and enforce it in the backfill with `LEAST(...)`/`GREATEST(...)`.
    - Idempotent backfill from prior warmup activity so already-warmed pairs are NOT charged as new later (D-08): `INSERT INTO sender_first_contacts (sender_a_id, sender_b_id, first_contact_at) SELECT LEAST(sender_a_id, sender_b_id), GREATEST(sender_a_id, sender_b_id), MIN(created_at) FROM warmup_sessions WHERE sender_a_id IS NOT NULL AND sender_b_id IS NOT NULL GROUP BY 1,2 ON CONFLICT DO NOTHING;` Add a second backfill from warmup_messages ({from_sender_id,to_sender_id}) with the same LEAST/GREATEST canonicalization and `ON CONFLICT DO NOTHING`.

    Create `migrations/058_sender_grade_settings.sql` (idempotent, mirror 038 header):
    - `CREATE TABLE IF NOT EXISTS sender_grade_settings (workspace_id UUID PRIMARY KEY REFERENCES workspaces(id) ON DELETE CASCADE, level1_chats_per_day INT NOT NULL DEFAULT 5, level1_step_days INT NOT NULL DEFAULT 30, level2_chats_per_day INT NOT NULL DEFAULT 9, level2_step_days INT NOT NULL DEFAULT 30, level3_chats_per_day INT NOT NULL DEFAULT 13, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());` Fixed 3 levels (D-16); no level3 step (D-17: level 3 is permanent). Seed nothing (absent row = code-defaults).

    In `app/models/__init__.py`, ADD two ORM models mirroring the migrations (near WarmupSettings, ~line 453):
    - `class SenderFirstContact(Base)` → `__tablename__ = "sender_first_contacts"`, composite PK (sender_a_id, sender_b_id) both UUID FK to senders.id ondelete CASCADE, first_contact_at with server_default=func.now().
    - `class SenderGradeSettings(Base)` → `__tablename__ = "sender_grade_settings"`, workspace_id UUID PK FK to workspaces.id ondelete CASCADE, and the five INT columns each with the matching server_default ('5','30','9','30','13'), created_at/updated_at with server_default=func.now(). Every NOT NULL column MUST carry server_default.

    Create `app/services/grade_ladder.py` — the single shared ladder resolver imported by queue, warmup, senders router, and the settings router:
    - Module constant `LADDER_DEFAULTS = [(5, 30), (9, 30), (13, None)]  # (chats_per_day, step_days) for levels 1,2,3; level 3 step None (D-17)`.
    - `def resolve_ladder(row) -> list[tuple[int, int | None]]:` given a sender_grade_settings row (or None), return a 3-element list of (chats_per_day, step_days); None row → LADDER_DEFAULTS.
    - `def budget_for_level(ladder, level: int) -> int:` return ladder[level-1][0] (clamp level to 1..3).
    - `def step_days_for_level(ladder, level: int) -> int | None:` return ladder[level-1][1].
    - `async def load_ladder(db, workspace_id) -> list:` `SELECT level1_chats_per_day, level1_step_days, level2_chats_per_day, level2_step_days, level3_chats_per_day FROM sender_grade_settings WHERE workspace_id = :wid` then `resolve_ladder`; missing row → LADDER_DEFAULTS. Use bind params only.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('app/services/grade_ladder.py').read())" && grep -c "ON CONFLICT DO NOTHING" migrations/057_sender_first_contacts.sql && grep -n "sender_grade_settings" app/models/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - migrations/057_sender_first_contacts.sql contains `CREATE TABLE IF NOT EXISTS sender_first_contacts`
    - migrations/057_sender_first_contacts.sql contains `LEAST(` and `GREATEST(` and at least two `ON CONFLICT DO NOTHING`
    - migrations/058_sender_grade_settings.sql contains `level1_chats_per_day INT NOT NULL DEFAULT 5` and `level3_chats_per_day INT NOT NULL DEFAULT 13`
    - app/models/__init__.py contains `__tablename__ = "sender_first_contacts"` and `__tablename__ = "sender_grade_settings"`
    - app/services/grade_ladder.py contains `LADDER_DEFAULTS = [(5, 30), (9, 30), (13, None)]`
    - app/services/grade_ladder.py contains `def budget_for_level` and `async def load_ladder`
  </acceptance_criteria>
  <done>Registry + ladder-settings tables exist with idempotent backfill; ORM models added; grade_ladder.py resolves budget/step from a workspace row or code-defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: conftest SQL-only blocks + foundation test</name>
  <read_first>
    - tests/conftest.py (lines 225-298 — the exists-guarded _mig_045/046/053/054/055 blocks + the post-create_all ALTER defaults; this is the exact pattern to extend)
    - tests/test_warmup_worker.py (async fixture usage for DB-backed tests)
  </read_first>
  <behavior>
    - Test: after schema build, `SELECT current_level, level_updated_at FROM senders` succeeds and a freshly inserted sender defaults current_level=1.
    - Test: sender_first_contacts backfill is idempotent — inserting a warmup_sessions row for a pair, running the 057 backfill twice, yields exactly one sender_first_contacts row for that pair (already-warmed pair not double-counted).
    - Test: load_ladder returns (5,9,13) budgets for a workspace with no sender_grade_settings row (code-defaults).
  </behavior>
  <action>
    In `tests/conftest.py`, after the `_mig_055` block (~line 268) and BEFORE the post-create_all ALTER section, append exists-guarded blocks for the SQL-only operations this phase's tests depend on (the ORM create_all already builds the new tables/columns, so ONLY the SQL-only backfills need explicit application):
    - `_mig_056` block: apply `migrations/056_sender_grade_columns.sql` if it exists (its level_updated_at=created_at backfill + CHECK are SQL-only; columns come from create_all).
    - `_mig_057` block: apply `migrations/057_sender_first_contacts.sql` if it exists (the warmup-pair backfill is SQL-only). Copy the exact `if _mig.exists(): await asyncpg_conn.execute(_mig.read_text())` shape with a header comment citing RESEARCH Pitfall 1 (conftest does NOT glob).
    Do NOT add a block for 058 (pure CREATE TABLE, built by create_all).

    Create `tests/test_grade_foundation.py` implementing the three behaviors above using the existing async DB fixtures (mirror how test_warmup_worker.py acquires a session). Use `pytest.mark.asyncio` if that is the file convention.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_grade_foundation.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py contains `056_sender_grade_columns.sql` and `057_sender_first_contacts.sql`
    - tests/test_grade_foundation.py exists and references `sender_first_contacts` and `load_ladder`
    - `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_grade_foundation.py -x` exits 0
  </acceptance_criteria>
  <done>Test DB matches prod schema for the new columns/tables; idempotent backfill and code-default ladder proven green via test-overlay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration auto-apply → prod boot | A malformed/non-idempotent migration fails api startup (fail-fast) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Denial of Service | migrations 056/057/058 auto-apply | medium | mitigate | Every statement idempotent (IF NOT EXISTS / DO$$ duplicate_object / ON CONFLICT DO NOTHING); re-run-safe on drift so a partial apply cannot wedge boot |
| T-22-02 | Tampering | sender_first_contacts backfill | low | mitigate | Canonical LEAST/GREATEST pair ordering + PK dedup prevents a warmed pair being recorded twice or counted as new |
| T-22-03 | Information Disclosure | sender_grade_settings.workspace_id | low | accept | workspace-scoped FK; no cross-tenant read path introduced in this plan (API added in 22-02) |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (RESEARCH: no new dependencies) |
</threat_model>

<verification>
- `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_grade_foundation.py -x` green
- `grep -rn "max_new_dialogs_per_day\|rate_per_day" app/models/__init__.py` still shows both (removals deferred to 22-06)
</verification>

<success_criteria>
Schema foundation is in place: grade columns backfilled, registry + ladder tables created, shared resolver returns code-defaults, and the test-overlay confirms the fresh-DB schema matches prod.
</success_criteria>

<output>
Create `.planning/phases/22-account-level-new-chat-limit-grades/22-01-SUMMARY.md` when done
</output>
