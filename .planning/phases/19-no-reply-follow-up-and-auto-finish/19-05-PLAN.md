---
phase: 19-no-reply-follow-up-and-auto-finish
plan: 05
type: execute
wave: 4
depends_on: [19-02, 19-04]
files_modified:
  - lovable-handoff/openapi.json
  - /root/apps/aimly/aimly-tg-outreach (campaign create/edit form — Follow Up block)
autonomous: false
requirements: [NORP-13]
must_haves:
  truths:
    - "D-08/D-12: the campaign create/edit form exposes an Enable Follow Up toggle (default OFF) plus ping interval (hours, 4–168), max pings (1–5), and auto-finish hours (24–720), following the existing recontact / max_new_dialogs_per_day settings pattern"
    - "The regenerated lovable-handoff/openapi.json includes the 4 follow-up fields on the campaign create/update/response schemas"
    - "no_reply appears as a visible conversation status in inbox filters (via the existing status mechanism — D-01)"
  artifacts:
    - path: "lovable-handoff/openapi.json"
      provides: "up-to-date API contract with follow_up_* fields + no_reply status"
      contains: "follow_up_interval_hours"
    - path: "/root/apps/aimly/aimly-tg-outreach"
      provides: "Follow Up settings block in the campaign form"
      contains: "Follow Up"
  key_links:
    - from: "lovable-handoff/openapi.json"
      to: "app/schemas CampaignCreate/Update/Response"
      via: "offline app.openapi() regeneration in the test container"
      pattern: "follow_up"
---

<objective>
Surface Phase 19 in the product: regenerate the API contract (openapi.json) and add the Follow Up settings block to the campaign create/edit form in the sibling frontend repo. The block follows the existing recontact / max_new_dialogs_per_day settings pattern — a toggle (default OFF) plus three numeric inputs with the D-12 bounds.

Purpose: The backend state machine is invisible to users without the form controls. This is a cross-repo task: openapi.json lives in the BACKEND repo (tg-outreach), the form lives in the SEPARATE frontend repo (/root/apps/aimly/aimly-tg-outreach → origin AGS-Venture-Lab/aimly-tg-outreach). Commits must NOT be mixed across repos.
Output: regenerated openapi.json (backend repo), Follow Up form block + regenerated TS types (frontend repo), human-verified UI.
</objective>

<execution_context>
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/workflows/execute-plan.md
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-no-reply-follow-up-and-auto-finish/19-CONTEXT.md
@.planning/phases/19-no-reply-follow-up-and-auto-finish/19-RESEARCH.md

<interfaces>
openapi regen precedent (Phase 16/18): regenerate OFFLINE via `app.openapi()` inside the test container
  (no un-gated prod deploy). Phase 18-05 SUMMARY: handoff regenerated offline in the test container +
  types regenerated via the export-handoff tooling.
Frontend settings pattern (sibling repo /root/apps/aimly/aimly-tg-outreach): the campaign create/edit form
  already renders recontact (allow_recontact toggle + recontact_min_age_days) and max_new_dialogs_per_day —
  the Follow Up block mirrors these controls (toggle + numeric inputs, bounds surfaced as min/max).
CROSS-REPO RULE (CLAUDE.md): tg-outreach commits go to Andrewbruce165/outreach-platform; sibling repo
  commits go to AGS-Venture-Lab/aimly-tg-outreach. Never `git add -A`; stage specific files only
  (memory: parallel-agent-careful-commits). The sibling may have concurrent Lovable commits — rebase onto
  origin/main, drop nothing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate lovable-handoff/openapi.json (backend repo)</name>
  <read_first>
    - .planning/phases/18-switchable-llm-provider/18-05-SUMMARY.md (the offline app.openapi() regen recipe / export-handoff tooling)
    - lovable-handoff/openapi.json (confirm current state, find the campaign schema section)
  </read_first>
  <action>
    Regenerate lovable-handoff/openapi.json OFFLINE (do NOT deploy prod to do it). Use the same mechanism Phase 18-05 used — run `app.openapi()` inside the test container (docker-compose test-overlay) and write the JSON to lovable-handoff/openapi.json, OR run the repo's export-handoff tooling if present. The regenerated spec must include follow_up_enabled / follow_up_interval_hours / follow_up_max_pings / auto_finish_hours on the campaign create/update/response schemas (added in Plan 19-02) and the no_reply status value (Plan 19-01).
  </action>
  <verify>
    <automated>grep -q "follow_up_interval_hours" lovable-handoff/openapi.json && grep -q "auto_finish_hours" lovable-handoff/openapi.json && grep -q "follow_up_max_pings" lovable-handoff/openapi.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - lovable-handoff/openapi.json contains all 4 follow_up_* field names in the campaign schemas
    - The regeneration was offline (no prod api rebuild required to produce it)
    - JSON is valid (`python -m json.tool lovable-handoff/openapi.json > /dev/null` succeeds)
  </acceptance_criteria>
  <done>openapi.json regenerated offline with the 4 follow-up fields; valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Follow Up block to the campaign form (sibling frontend repo)</name>
  <read_first>
    - /root/apps/aimly/aimly-tg-outreach — locate the campaign create/edit form component(s) and the existing recontact / max_new_dialogs_per_day controls to mirror; locate the generated TS types file consuming openapi.json
  </read_first>
  <action>
    In the sibling repo /root/apps/aimly/aimly-tg-outreach ONLY (separate git repo — do NOT commit these into tg-outreach):
    1. Regenerate the TS API types from the updated openapi.json (via the repo's types-generation step / export-handoff), so the 4 follow-up fields + no_reply status are typed.
    2. Add a "Follow Up" settings block to BOTH the create and edit campaign forms, placed alongside the existing recontact / max_new_dialogs_per_day settings (Claude's discretion on exact placement — CONTEXT.md). Controls, wired to the campaign create/update payload:
       - Enable Follow Up toggle → follow_up_enabled (default OFF)
       - Ping interval (hours) numeric input → follow_up_interval_hours, min 4 max 168, default 24 (disabled/greyed when toggle OFF)
       - Max pings numeric input → follow_up_max_pings, min 1 max 5, default 2
       - Auto-finish after (hours) numeric input → auto_finish_hours, min 24 max 720, default 72 — place near where finish criteria are described (per CONTEXT.md: "where they describe finish criteria")
    3. Ensure the fields flow through the existing create/edit mutation to the API (mirror how max_new_dialogs_per_day is sent).
    Stage ONLY the specific changed files; rebase onto origin/main if concurrent Lovable commits exist. `tsc` must be clean.
  </action>
  <verify>
    <automated>cd /root/apps/aimly/aimly-tg-outreach && grep -rq "follow_up_enabled\|followUpEnabled" src 2>/dev/null && grep -rq "auto_finish_hours\|autoFinishHours" src 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The sibling repo references all 4 follow-up fields (snake_case in payload or camelCase in state) in the campaign form
    - The Enable Follow Up toggle defaults OFF; the three numeric inputs carry the D-12 min/max bounds
    - TS types regenerated (no `any` for the new fields); `tsc` reports 0 errors
    - Changes are in the sibling repo working tree, NOT staged in tg-outreach (`cd /root/apps/aimly/tg-outreach && git status --porcelain | grep -v aimly-tg-outreach` shows no frontend files)
  </acceptance_criteria>
  <done>Follow Up block added to create+edit campaign forms wired to the 4 fields with correct bounds; tsc clean; sibling-repo isolation preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the Follow Up form + live no_reply flow</name>
  <action>Present the built Follow Up controls and deploy note to the user; pause for verification (see how-to-verify). No code changes — this is the verification gate.</action>
  <what-built>
    Backend FollowUpWorker + no_reply status + the 4 campaign follow-up fields (Plans 19-01..04), regenerated openapi.json, and the Follow Up block in the campaign create/edit form (this plan). Deploy requires rebuilding BOTH api and listener: `cd /root/apps/aimly/tg-outreach && git pull && docker compose up -d --build api listener`; frontend deploys via Cloudflare.
  </what-built>
  <how-to-verify>
    1. Open the campaign create form. Confirm a "Follow Up" block with an Enable toggle (default OFF), Ping interval (hours), Max pings, and Auto-finish (hours) inputs.
    2. Toggle Enable ON, set interval=4, max pings=2, auto-finish=24, save. Reopen the campaign (edit) — confirm the values persisted.
    3. Try to set interval=1 (below 4) — confirm the UI blocks it or the API returns 422.
    4. (Optional live) On a running campaign with a silent dialog, enable Follow Up with a short interval and confirm after the interval the conversation shows status `no_reply` in inbox filters and a ping was sent from the SAME sender account; confirm a reply reverts it to active and no further ping sends.
    5. Confirm auto-finish closes a silent dialog to `finished` and (if a finish webhook is configured) the payload carries reason='no_reply'.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (which control/behavior is wrong).</resume-signal>
</task>

</tasks>

<verification>
- openapi.json contains the 4 follow-up fields (grep) and is valid JSON
- Sibling repo `tsc` clean; form renders the Follow Up block
- Human confirms create/edit persistence + bounds + (optionally) live no_reply/ping/auto-finish behavior
</verification>

<success_criteria>
The API contract and the campaign form expose the Follow Up settings with correct defaults and bounds; cross-repo commit isolation preserved; human verifies the flow.
</success_criteria>

<output>
After completion, create `.planning/phases/19-no-reply-follow-up-and-auto-finish/19-05-SUMMARY.md`.
</output>
