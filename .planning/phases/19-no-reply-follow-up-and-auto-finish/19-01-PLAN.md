---
phase: 19-no-reply-follow-up-and-auto-finish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_follow_up.py
  - tests/conftest.py
  - migrations/045_follow_up.sql
  - app/models/__init__.py
autonomous: true
requirements: [NORP-01, NORP-02, NORP-03]
must_haves:
  truths:
    - "D-01: conversations.status accepts the value 'no_reply' (CHECK does not reject it)"
    - "D-08: campaigns has follow_up_max_pings column defaulting to 2"
    - "D-12: campaigns has follow_up_enabled (default false), follow_up_interval_hours (default 24), auto_finish_hours (default 72)"
    - "D-03/D-04/D-08: conversations has pings_sent (default 0) counter column for follow-up state"
    - "tests/test_follow_up.py exists and its NORP tests are RED (import the not-yet-built follow_up module in-body)"
  artifacts:
    - path: "migrations/045_follow_up.sql"
      provides: "no_reply CHECK extension + 4 campaign columns + conversations.pings_sent, all idempotent"
      contains: "no_reply"
    - path: "app/models/__init__.py"
      provides: "ORM mirrors with server_default matching migration DEFAULTs"
      contains: "follow_up_enabled"
    - path: "tests/test_follow_up.py"
      provides: "RED scaffold for NORP-01/02/04/06/07/12"
      contains: "def test_no_reply_status_allowed"
  key_links:
    - from: "migrations/045_follow_up.sql"
      to: "conversations_status_check"
      via: "DROP CONSTRAINT IF EXISTS + ADD CONSTRAINT with no_reply"
      pattern: "conversations_status_check"
    - from: "tests/conftest.py"
      to: "migrations/045_follow_up.sql"
      via: "migration applied in ephemeral test DB setup"
      pattern: "045_follow_up"
---

<objective>
Lay the schema + test foundation for Phase 19. Add migration 045 (extend the conversations.status CHECK with `no_reply`, add the `pings_sent` counter column to conversations, add the 4 campaign follow-up columns), mirror all new columns in the ORM with correct `server_default`, wire the migration into the conftest test-DB bootstrap, and land a RED test scaffold that every later plan verifies against.

Purpose: Every other Phase 19 plan depends on these columns and on the `no_reply` status being a legal value. This plan creates the contracts (schema + tests) so downstream plans build against known column names and defaults instead of guessing.
Output: migrations/045_follow_up.sql, ORM column mirrors, conftest migration registration, tests/test_follow_up.py RED scaffold.
</objective>

<execution_context>
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/workflows/execute-plan.md
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-no-reply-follow-up-and-auto-finish/19-CONTEXT.md
@.planning/phases/19-no-reply-follow-up-and-auto-finish/19-RESEARCH.md

<interfaces>
Current conversations.status CHECK (latest set — migration 017 added 'bot_ignored'):
  CHECK (status IN ('active','manual','paused','lead','handoff','finished','bot_ignored'))
Phase 19 must produce:
  CHECK (status IN ('active','manual','paused','lead','handoff','finished','bot_ignored','no_reply'))

Conversation ORM (app/models/__init__.py:305) currently:
  status = Column(String(20), default="active", server_default="active")
  paused_at = Column(DateTime(timezone=True), nullable=True)   # precedent for a conversation column

Campaign ORM (app/models/__init__.py:586) precedent columns to copy shape from:
  allow_recontact = Column(Boolean, nullable=False, server_default="false")
  recontact_min_age_days = Column(Integer, nullable=False, server_default="30")
  max_new_dialogs_per_day = Column(Integer, nullable=False, server_default="50")  # ge=1/le=100 at API only

Migration idempotency pattern (raw SQL only, auto-applied by app/database.py::_apply_migrations):
  DO $$ BEGIN
    ALTER TABLE conversations DROP CONSTRAINT IF EXISTS conversations_status_check;
    ALTER TABLE conversations ADD CONSTRAINT conversations_status_check
      CHECK (status IN ('active','manual','paused','lead','handoff','finished','bot_ignored','no_reply'));
  EXCEPTION WHEN duplicate_object THEN NULL; END $$;
  ALTER TABLE <table> ADD COLUMN IF NOT EXISTS <col> <type> NOT NULL DEFAULT <val>;

conftest migration registration (tests/conftest.py ~line 219): later migrations are appended
individually as `_mig_044 = PROJECT_ROOT / "migrations" / "044_llm_settings.sql"` then applied.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 045 (status CHECK + ping columns + 4 campaign columns)</name>
  <read_first>
    - migrations/044_llm_settings.sql (latest migration — copy the file header/idempotency style)
    - migrations/017_phase5.sql (the migration that last set the conversations.status CHECK — confirm the FULL current legal value set so none are dropped)
    - migrations/033*.sql or migrations/026_campaign_allow_recontact.sql (ADD COLUMN IF NOT EXISTS pattern for campaign columns)
  </read_first>
  <action>
    Create migrations/045_follow_up.sql. It must be idempotent raw SQL (no Alembic, transaction-safe — NO `ALTER TYPE ADD VALUE`).

    1. Extend the conversations.status CHECK via a DROP CONSTRAINT IF EXISTS + ADD CONSTRAINT inside a `DO $$ ... EXCEPTION WHEN duplicate_object THEN NULL; END $$;` block. The new legal set is EXACTLY:
       ('active','manual','paused','lead','handoff','finished','bot_ignored','no_reply')
       (Do NOT drop 'bot_ignored' — it was added in migration 017.)

    2. Add the ping-state column to conversations:
       ALTER TABLE conversations ADD COLUMN IF NOT EXISTS pings_sent INTEGER NOT NULL DEFAULT 0;

    3. Add the 4 campaign columns (all NOT NULL with DEFAULT so existing rows incl. running campaigns get safe values — no backfill, mirrors max_new_dialogs_per_day D-11/D-15):
       ALTER TABLE campaigns ADD COLUMN IF NOT EXISTS follow_up_enabled BOOLEAN NOT NULL DEFAULT false;
       ALTER TABLE campaigns ADD COLUMN IF NOT EXISTS follow_up_interval_hours INTEGER NOT NULL DEFAULT 24;
       ALTER TABLE campaigns ADD COLUMN IF NOT EXISTS follow_up_max_pings INTEGER NOT NULL DEFAULT 2;
       ALTER TABLE campaigns ADD COLUMN IF NOT EXISTS auto_finish_hours INTEGER NOT NULL DEFAULT 72;

    Add a leading comment block citing D-01/D-08/D-12 and noting bounds are enforced at the API layer (Pydantic), NOT via DB CHECK (matches recontact_min_age_days precedent).
  </action>
  <verify>
    <automated>grep -q "no_reply" migrations/045_follow_up.sql && grep -q "follow_up_enabled BOOLEAN NOT NULL DEFAULT false" migrations/045_follow_up.sql && grep -q "pings_sent INTEGER NOT NULL DEFAULT 0" migrations/045_follow_up.sql && grep -q "bot_ignored" migrations/045_follow_up.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ADD COLUMN IF NOT EXISTS" migrations/045_follow_up.sql` returns 5 (pings_sent + 4 campaign columns)
    - `grep "CHECK (status IN" migrations/045_follow_up.sql` contains all 8 values including both 'bot_ignored' and 'no_reply'
    - File contains `DROP CONSTRAINT IF EXISTS conversations_status_check`
    - No occurrence of `ALTER TYPE` or `ADD VALUE` in the file
  </acceptance_criteria>
  <done>Migration 045 exists, idempotent, extends the CHECK with no_reply (preserving bot_ignored), adds pings_sent + 4 campaign columns.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror new columns in the ORM with matching server_default</name>
  <read_first>
    - app/models/__init__.py (Conversation class ~line 305 and Campaign class ~line 586 — see current status/allow_recontact/max_new_dialogs_per_day columns)
  </read_first>
  <action>
    Edit app/models/__init__.py. Mirror EVERY new NOT NULL column with a matching `server_default` (Pitfall 2 / mig 040/042 drift — create_all rebuilds tables from the ORM after a DROP, so missing server_default => NotNullViolation on raw INSERT).

    In the Conversation class (~line 316, next to `status`):
      pings_sent = Column(Integer, nullable=False, default=0, server_default="0")
    Update the inline comment on the `status` column to include no_reply, e.g.:
      # active|manual|paused|lead|handoff|finished|bot_ignored|no_reply

    In the Campaign class (next to max_new_dialogs_per_day):
      follow_up_enabled = Column(Boolean, nullable=False, server_default="false")
      follow_up_interval_hours = Column(Integer, nullable=False, server_default="24")
      follow_up_max_pings = Column(Integer, nullable=False, server_default="2")
      auto_finish_hours = Column(Integer, nullable=False, server_default="72")
    Add a comment citing D-08/D-12 and noting API-layer bounds (interval 4–168, max_pings 1–5, auto_finish 24–720).
  </action>
  <verify>
    <automated>grep -q 'pings_sent = Column(Integer, nullable=False, default=0, server_default="0")' app/models/__init__.py && grep -q 'follow_up_enabled = Column(Boolean, nullable=False, server_default="false")' app/models/__init__.py && grep -q 'auto_finish_hours = Column(Integer, nullable=False, server_default="72")' app/models/__init__.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'server_default' app/models/__init__.py` increased by 5 vs before (pings_sent + 4 campaign columns)
    - Every one of the 5 new columns has BOTH `nullable=False` and a `server_default=` matching the migration DEFAULT
    - The Conversation.status inline comment includes `no_reply`
  </acceptance_criteria>
  <done>ORM mirrors all 5 new columns with server_default identical to the migration DEFAULTs.</done>
</task>

<task type="auto">
  <name>Task 3: Register migration 045 in conftest + write RED test scaffold</name>
  <read_first>
    - tests/conftest.py (migration list ~lines 159-219 and the `_mig_044` append pattern; test_campaign_factory ~637, test_running_campaign_factory ~864, test_conversation_factory ~881, test_queue_item_factory ~761)
    - tests/test_queue_even_pacing.py (Phase 13 RED-scaffold precedent — deferred in-body imports keep --collect-only clean)
  </read_first>
  <action>
    1. In tests/conftest.py, register migration 045 in the same way 044 is registered (append `_mig_045 = PROJECT_ROOT / "migrations" / "045_follow_up.sql"` and apply it in the same block/order as the other numbered migrations after 044).

    2. Create tests/test_follow_up.py — a RED scaffold (Phase 13/17 precedent: defer the `from app.services.follow_up import ...` and other not-yet-built imports INSIDE each test body so `pytest --collect-only` stays clean). Write these tests with real assertions (not `pass`):
       - test_no_reply_status_allowed: insert a conversation with status='no_reply' via the DB session and assert it commits (CHECK accepts it). This can pass now (migration landed) — it is the NORP-01 GREEN contract.
       - test_campaign_follow_up_fields (NORP-02): POST /api/v1/campaigns with follow_up_interval_hours=3 (below bound 4) returns 422; with follow_up_max_pings=6 (above bound 5) returns 422; with auto_finish_hours=12 (below bound 24) returns 422; valid values (24/2/72) return 201 and the response echoes follow_up_enabled. (These are RED until Plan 19-02 adds the schema fields.)
       - test_ping_on_interval (NORP-04): placeholder that imports `app.services.follow_up.FollowUpWorker` in-body — RED until Plan 19-04.
       - test_auto_finish (NORP-04): imports FollowUpWorker in-body — RED until 19-04.
       - test_finish_reason_marker (NORP-06): imports the follow_up auto-finish path and asserts webhook reason='no_reply' — RED until 19-04.
       - test_reply_cancels_pings (NORP-07): asserts listener revert no_reply→active — RED until Plan 19-03.
       - test_paused_frozen (NORP-12): asserts a paused campaign's conversation gets no ping — RED until 19-04.
       Reuse the existing conftest factories (test_running_campaign_factory, test_conversation_factory, test_queue_item_factory) rather than hand-rolling inserts.
  </action>
  <verify>
    <automated>grep -q "045_follow_up" tests/conftest.py && grep -q "def test_no_reply_status_allowed" tests/test_follow_up.py && grep -q "def test_campaign_follow_up_fields" tests/test_follow_up.py && grep -q "def test_paused_frozen" tests/test_follow_up.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_follow_up.py` >= 7
    - tests/conftest.py contains `045_follow_up`
    - `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_follow_up.py --collect-only -q` collects with 0 collection errors (deferred in-body imports)
    - Running `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_follow_up.py::test_no_reply_status_allowed -q` PASSES (migration active); the FollowUpWorker/listener tests are RED (expected — not yet built)
  </acceptance_criteria>
  <done>Migration 045 applies in the test DB; test_follow_up.py collects cleanly, NORP-01 status test GREEN, downstream tests RED as expected.</done>
</task>

</tasks>

<verification>
- `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_follow_up.py --collect-only -q` — 0 collection errors
- `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_follow_up.py::test_no_reply_status_allowed -q` — PASS
- Full suite still green except the intentionally-RED Phase 19 scaffold tests
</verification>

<success_criteria>
Migration 045 idempotent + registered in conftest; ORM mirrors 5 new columns with server_default; RED scaffold lands with the NORP-01 status test GREEN and the rest RED pending later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/19-no-reply-follow-up-and-auto-finish/19-01-SUMMARY.md`.
</output>
