---
phase: 19-no-reply-follow-up-and-auto-finish
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - app/schemas/__init__.py
  - app/routers/campaigns.py
  - app/services/ai_engine.py
autonomous: true
requirements: [NORP-02, NORP-05]
must_haves:
  truths:
    - "D-12: creating/editing a campaign accepts follow_up_enabled, follow_up_interval_hours (4–168), follow_up_max_pings (1–5), auto_finish_hours (24–720); out-of-range values return 422"
    - "D-12: CampaignResponse returns the 4 follow-up fields with defaults (enabled=false, interval=24, max_pings=2, auto_finish=72)"
    - "D-07: ai_engine exposes generate_followup_ping(session, conversation_id) that reuses the campaign+agent prompt assembly and Phase-18 provider, returns ping text only (no tools)"
  artifacts:
    - path: "app/schemas/__init__.py"
      provides: "4 follow-up fields on CampaignCreate/Update/Response with Field(ge/le) bounds"
      contains: "follow_up_interval_hours"
    - path: "app/routers/campaigns.py"
      provides: "create/update passthrough + duplicate carry-over of the 4 fields"
      contains: "follow_up_enabled"
    - path: "app/services/ai_engine.py"
      provides: "generate_followup_ping helper"
      contains: "def generate_followup_ping"
  key_links:
    - from: "app/routers/campaigns.py"
      to: "campaigns follow-up columns"
      via: "Campaign(...) create + PATCH update_data passthrough"
      pattern: "follow_up_enabled"
    - from: "app/services/ai_engine.py"
      to: "resolve_llm_config"
      via: "generate_followup_ping reuses the Phase 18 provider resolution"
      pattern: "resolve_llm_config"
---

<objective>
Expose the 4 follow-up campaign fields through the API (create/update/response/duplicate, with Pydantic bounds — D-12), and add the AI ping-text generation helper `generate_followup_ping` to ai_engine reusing the existing prompt-assembly + Phase-18 provider (D-07).

Purpose: The FollowUpWorker (Plan 19-04) needs (a) campaign settings readable via the model and (b) a callable that produces on-topic ping text through the campaign's configured LLM. This plan delivers both contracts. It does NOT touch listener/queue (Plan 19-03) or the worker (19-04) — clean file ownership for parallel execution with 19-03.
Output: schema fields + validation, router passthrough, ai_engine.generate_followup_ping.
</objective>

<execution_context>
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/workflows/execute-plan.md
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-no-reply-follow-up-and-auto-finish/19-CONTEXT.md
@.planning/phases/19-no-reply-follow-up-and-auto-finish/19-RESEARCH.md

<interfaces>
Existing campaign field precedent (app/schemas/__init__.py):
  CampaignCreate (658):   allow_recontact: bool = False
                          recontact_min_age_days: int = Field(default=30, ge=1, le=365)
                          max_new_dialogs_per_day: int = Field(default=50, ge=1, le=100)
  CampaignUpdate (730):   allow_recontact: Optional[bool] = None
                          recontact_min_age_days: Optional[int] = Field(default=None, ge=1, le=365)
                          max_new_dialogs_per_day: Optional[int] = Field(default=None, ge=1, le=100)
  CampaignResponse (785): allow_recontact: bool = False
                          recontact_min_age_days: int = 30
                          max_new_dialogs_per_day: int = 50

Router passthrough (app/routers/campaigns.py):
  create_campaign (399): Campaign(... allow_recontact=payload.allow_recontact,
                         max_new_dialogs_per_day=payload.max_new_dialogs_per_day, ...)  (~line 466)
  update_campaign PATCH (585): builds update_data from payload; applies fields onto the ORM row
  _campaign_to_response (~345): maps ORM → CampaignResponse
  duplicate_campaign (900): copies src.allow_recontact etc (~line 959)

ai_engine reuse points (app/services/ai_engine.py):
  get_context_for_conversation(conversation_id, session) -> dict (285)
  ai_engine.get_conversation_history(session, conversation_id, limit=...) (898, method on the service instance)
  ai_engine.build_system_prompt(context, contact_name) -> str (936)
  resolve_llm_config(session, workspace_id) -> llm_cfg  (Phase 18; used inside generate_response ~1356)
  generate_response is the full reference path (1318) — copy its context+provider resolution, DROP the tools.
  platform_fallback_config(settings) is the legacy-path config used when there is no campaign_context.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 follow-up fields to CampaignCreate/Update/Response with bounds</name>
  <read_first>
    - app/schemas/__init__.py (CampaignCreate ~658, CampaignUpdate ~730, CampaignResponse ~785 — the allow_recontact / max_new_dialogs_per_day blocks)
  </read_first>
  <action>
    Edit app/schemas/__init__.py. Add these fields next to max_new_dialogs_per_day in each class, with EXACT bounds (D-08/D-12):

    CampaignCreate:
      follow_up_enabled: bool = False
      follow_up_interval_hours: int = Field(default=24, ge=4, le=168)
      follow_up_max_pings: int = Field(default=2, ge=1, le=5)
      auto_finish_hours: int = Field(default=72, ge=24, le=720)

    CampaignUpdate (all Optional so PATCH can omit them):
      follow_up_enabled: Optional[bool] = None
      follow_up_interval_hours: Optional[int] = Field(default=None, ge=4, le=168)
      follow_up_max_pings: Optional[int] = Field(default=None, ge=1, le=5)
      auto_finish_hours: Optional[int] = Field(default=None, ge=24, le=720)

    CampaignResponse (echo the defaults so the frontend reads current state):
      follow_up_enabled: bool = False
      follow_up_interval_hours: int = 24
      follow_up_max_pings: int = 2
      auto_finish_hours: int = 72
  </action>
  <verify>
    <automated>grep -q "follow_up_interval_hours: int = Field(default=24, ge=4, le=168)" app/schemas/__init__.py && grep -q "follow_up_max_pings: int = Field(default=2, ge=1, le=5)" app/schemas/__init__.py && grep -q "auto_finish_hours: int = Field(default=72, ge=24, le=720)" app/schemas/__init__.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 4 field names appear in CampaignCreate, CampaignUpdate (as Optional), and CampaignResponse
    - Bounds are EXACTLY: interval ge=4 le=168, max_pings ge=1 le=5, auto_finish ge=24 le=720
    - follow_up_enabled defaults to False everywhere
  </acceptance_criteria>
  <done>Schema fields present with correct bounds and defaults in all three campaign schemas.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the 4 fields through create/update/response/duplicate</name>
  <read_first>
    - app/routers/campaigns.py (create_campaign ~399/466, update_campaign PATCH ~585, _campaign_to_response ~345, duplicate_campaign ~959)
  </read_first>
  <action>
    Edit app/routers/campaigns.py. Follow the max_new_dialogs_per_day precedent exactly (NO DB CHECK, no soft-cap warning — these are hard Pydantic bounds per D-12):

    1. create_campaign: in the Campaign(...) constructor call, add
         follow_up_enabled=payload.follow_up_enabled,
         follow_up_interval_hours=payload.follow_up_interval_hours,
         follow_up_max_pings=payload.follow_up_max_pings,
         auto_finish_hours=payload.auto_finish_hours,
    2. update_campaign (PATCH): the existing pattern applies non-None payload fields to the ORM row via update_data. Ensure the 4 fields are included in that passthrough (if PATCH uses `payload.model_dump(exclude_unset=True)` they are covered automatically; if it applies fields explicitly, add the 4 setattr/assignments).
    3. _campaign_to_response: map the 4 ORM columns onto the CampaignResponse (add
         follow_up_enabled=c.follow_up_enabled, follow_up_interval_hours=c.follow_up_interval_hours,
         follow_up_max_pings=c.follow_up_max_pings, auto_finish_hours=c.auto_finish_hours).
    4. duplicate_campaign: carry over src.follow_up_enabled / src.follow_up_interval_hours / src.follow_up_max_pings / src.auto_finish_hours into the new Campaign (~line 959, next to allow_recontact copy).
  </action>
  <verify>
    <automated>grep -q "follow_up_enabled=payload.follow_up_enabled" app/routers/campaigns.py && grep -q "follow_up_enabled=c.follow_up_enabled" app/routers/campaigns.py && grep -q "follow_up_enabled=src.follow_up_enabled" app/routers/campaigns.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - create_campaign Campaign(...) sets all 4 fields from payload
    - _campaign_to_response sets all 4 fields from the ORM row `c`
    - duplicate_campaign copies all 4 fields from `src`
    - PATCH passthrough covers all 4 (verify via a create→patch round-trip test)
    - `pytest tests/test_follow_up.py::test_campaign_follow_up_fields` PASSES (was RED after 19-01)
  </acceptance_criteria>
  <done>All 4 fields round-trip through create, patch, GET response, and duplicate; the NORP-02 test is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add generate_followup_ping to ai_engine (reuse prompt assembly + Phase 18 provider, no tools)</name>
  <read_first>
    - app/services/ai_engine.py (generate_response ~1318 for the reference context+provider resolution; get_context_for_conversation ~285; build_system_prompt ~936; get_conversation_history ~898; resolve_llm_config wiring ~1356; platform_fallback_config usage)
    - .planning/phases/18-switchable-llm-provider/18-CONTEXT.md (provider resolution semantics — D-02 default-off)
  </read_first>
  <behavior>
    - generate_followup_ping(session, conversation_id) returns a non-empty short text string for a conversation that has a campaign+agent (uses the same provider resolution as generate_response).
    - It resolves context via get_context_for_conversation and history via get_conversation_history; it passes NO tools to the provider (a ping never triggers lead/handoff/finish).
    - It appends a proactive follow-up directive to the prompt (e.g. "The contact has not replied. Send one short, natural, on-topic nudge; do NOT repeat the opener verbatim; reference the dialog so far if any.").
    - If context cannot be resolved (no campaign/agent), it returns None (caller skips the ping).
  </behavior>
  <action>
    Add an async helper `generate_followup_ping(session, conversation_id)` to app/services/ai_engine.py. Implement it by mirroring the front half of generate_response:
    1. `campaign_context = await get_context_for_conversation(conversation_id, session)`. If None → return None (no ping).
    2. Resolve the provider: `ws_id = campaign_context.get("workspace_id")`; `llm_cfg = await resolve_llm_config(session, ws_id)` (same as generate_response so pings honor the Phase-18 provider/model/key — D-07).
    3. Build history: `history = await ai_engine.get_conversation_history(session, conversation_id, limit=20)`.
    4. Build the base system prompt via `ai_engine.build_system_prompt(campaign_context, contact_name)` (derive contact_name from the context/conversation as generate_response does).
    5. Assemble the message list = system prompt + history + a final directive turn instructing a single short on-topic nudge (exact wording is Claude's discretion per CONTEXT.md, but it MUST say: not a reply to an inbound message, do not repeat the opener verbatim, one short message).
    6. Call the resolved provider WITHOUT tools (reuse the same completion path generate_response uses for the no-tool case; if generate_response uses a nested `_complete(msgs, tools=..., ...)` helper, call it with `tools=[]`/None). Return the text only.
    7. Never log the decrypted BYO key. Wrap provider errors so a failure returns None (caller retries next tick).
    Place the helper as a module-level `async def` near generate_response (not inside the class) OR as a method mirroring the existing style — match whichever the surrounding code uses for reuse.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_follow_up.py -k "ping" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def generate_followup_ping" app/services/ai_engine.py` succeeds
    - The helper calls `get_context_for_conversation` and `resolve_llm_config` (grep both within the function body)
    - The completion is invoked with no tools (grep the call site: `tools=[]` or `tools=None`)
    - A follow-up-ping test that stubs the provider asserts a non-empty string is returned; a conversation with no campaign context returns None
  </acceptance_criteria>
  <done>generate_followup_ping produces provider-routed, tool-free ping text; returns None when context is missing.</done>
</task>

</tasks>

<verification>
- `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_follow_up.py -k "field or ping" -q` — NORP-02 + ping tests GREEN
- Full suite green (no regression in existing campaign tests)
</verification>

<success_criteria>
4 follow-up fields validated + round-tripped through the campaign API; ai_engine.generate_followup_ping generates provider-routed tool-free ping text.
</success_criteria>

<output>
After completion, create `.planning/phases/19-no-reply-follow-up-and-auto-finish/19-02-SUMMARY.md`.
</output>
