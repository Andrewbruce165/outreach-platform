---
phase: 12-per-campaign-daily-new-dialog-limit-max-new-dialogs-per-day
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/033_campaign_max_new_dialogs.sql
  - app/models/__init__.py
autonomous: true
requirements: [NDLG-01]
must_haves:
  truths:
    - "Every campaign row (incl. existing running ones) has max_new_dialogs_per_day = 50 after the migration applies, with no backfill to a higher value (D-11)"
    - "A freshly inserted Campaign via the ORM without an explicit value gets 50 from the DB server_default (D-10)"
  artifacts:
    - path: "migrations/033_campaign_max_new_dialogs.sql"
      provides: "Idempotent ADD COLUMN max_new_dialogs_per_day INT NOT NULL DEFAULT 50 on campaigns"
      contains: "ADD COLUMN IF NOT EXISTS max_new_dialogs_per_day"
    - path: "app/models/__init__.py"
      provides: "ORM Campaign.max_new_dialogs_per_day column with server_default='50'"
      contains: "max_new_dialogs_per_day"
  key_links:
    - from: "app/models/__init__.py Campaign"
      to: "campaigns.max_new_dialogs_per_day"
      via: "ORM Column with server_default matching DB default"
      pattern: "max_new_dialogs_per_day.*server_default=\"50\""
---

<objective>
Add the `campaigns.max_new_dialogs_per_day` column (INT NOT NULL DEFAULT 50) via idempotent migration 033 and add the matching ORM column to `Campaign` with `server_default="50"`. This is the foundational schema change every other Phase-12 plan depends on (the queue filter reads it, the API schemas expose it).

Purpose: Establish the per-campaign daily new-dialog limit storage. DEFAULT 50 applies to ALL existing campaigns including `running` ones (D-11) — running campaigns may slow down under the new cap, which is the intended safety effect; no backfill to a higher value.
Output: migration 033 file + ORM column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-per-campaign-daily-new-dialog-limit-max-new-dialogs-per-day/12-CONTEXT.md

<interfaces>
<!-- Migration auto-applier: app/database.py::_apply_migrations runs every migrations/*.sql
     not yet in schema_migrations, in lexical order, behind pg_advisory_lock, fail-fast.
     File MUST be idempotent (ADD COLUMN IF NOT EXISTS). Next free slot = 033 (032 is last). -->

<!-- Pattern reference — migrations/032_phase11_field_split.sql (Phase 11), the most recent
     idempotent ALTER on `campaigns`:
       ALTER TABLE campaigns ADD COLUMN IF NOT EXISTS dialogue_flow JSONB NOT NULL DEFAULT '[]'::jsonb;
       (wrapped in BEGIN; ... COMMIT;) -->

<!-- ORM Campaign model lives at app/models/__init__.py:503-575. Existing NOT-NULL-with-default
     columns mirror the DB default via server_default, e.g.:
       work_hour_start = Column(Integer, nullable=False, server_default="9")
       recontact_min_age_days = Column(Integer, nullable=False, server_default="30")
     CLAUDE.md rule: ORM server_default MUST duplicate the DB default (create_all path after
     the 2026-05-26 DROP-SCHEMA incident rebuilds tables from the ORM, not from migrations). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create idempotent migration 033 adding max_new_dialogs_per_day</name>
  <read_first>
    - migrations/032_phase11_field_split.sql (idempotent ALTER-on-campaigns reference: BEGIN/COMMIT wrapper, ADD COLUMN IF NOT EXISTS … NOT NULL DEFAULT)
    - app/database.py (the _apply_migrations auto-applier contract — confirm lexical ordering and fail-fast so the file MUST be idempotent)
  </read_first>
  <action>
    Create `migrations/033_campaign_max_new_dialogs.sql`. Header comment block (mirror 032's style) stating: Phase 12 NDLG-01; adds campaigns.max_new_dialogs_per_day INT NOT NULL DEFAULT 50; idempotent (ADD COLUMN IF NOT EXISTS); auto-applied via _apply_migrations; fail-fast; DEFAULT 50 applies to ALL existing campaigns incl. running per D-11, NO backfill to a higher value.

    Body (exact DDL):
    ```
    BEGIN;
    ALTER TABLE campaigns ADD COLUMN IF NOT EXISTS max_new_dialogs_per_day INTEGER NOT NULL DEFAULT 50;
    COMMIT;
    ```
    Do NOT add any UPDATE/backfill statement — `ADD COLUMN ... DEFAULT 50` already sets every existing row (incl. running) to 50, which is exactly D-11. Do NOT add a CHECK constraint here — the ge=1/le=100 bounds are enforced at the API layer (D-12, plan 12-03), consistent with how senders rate caps are API-enforced (no DB CHECK).
  </action>
  <verify>
    <automated>grep -q "ADD COLUMN IF NOT EXISTS max_new_dialogs_per_day INTEGER NOT NULL DEFAULT 50" migrations/033_campaign_max_new_dialogs.sql && grep -q "^BEGIN;" migrations/033_campaign_max_new_dialogs.sql && grep -q "^COMMIT;" migrations/033_campaign_max_new_dialogs.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `migrations/033_campaign_max_new_dialogs.sql` exists
    - `grep -c "ADD COLUMN IF NOT EXISTS max_new_dialogs_per_day" migrations/033_campaign_max_new_dialogs.sql` returns 1
    - `grep -c "DEFAULT 50" migrations/033_campaign_max_new_dialogs.sql` returns 1
    - `grep -ci "UPDATE campaigns" migrations/033_campaign_max_new_dialogs.sql` returns 0 (no backfill — D-11)
    - `grep -ci "ALTER TYPE\|CHECK (" migrations/033_campaign_max_new_dialogs.sql` returns 0 (no DB-level bounds — API enforces D-12)
  </acceptance_criteria>
  <done>Migration 033 exists, idempotent, adds the column with DEFAULT 50, no backfill, no CHECK.</done>
</task>

<task type="auto">
  <name>Task 2: Add max_new_dialogs_per_day ORM column to Campaign</name>
  <read_first>
    - app/models/__init__.py (Campaign class at :503-575 — see existing server_default columns work_hour_start/recontact_min_age_days for the exact pattern; CLAUDE.md rule that ORM server_default must duplicate the DB default)
  </read_first>
  <action>
    In `app/models/__init__.py`, inside the `Campaign` class, add a new column. Place it near the other per-campaign policy fields (right after the `recontact_min_age_days` column at line ~561 is a sensible home). Exact column definition:
    ```
    # Phase 12 (D-10/D-11, NDLG-01): per-sender-per-campaign daily new-dialog cap.
    # DEFAULT 50 applies to ALL existing campaigns incl. running (D-11, no backfill).
    # server_default="50" duplicates the migration-033 DB default for the create_all
    # path (post-DROP-incident rebuild reconstructs tables from the ORM). API enforces
    # the ge=1/le=100 bounds (D-12); no DB CHECK.
    max_new_dialogs_per_day = Column(Integer, nullable=False, server_default="50")
    ```
    `Column` and `Integer` are already imported in this module (used by existing columns like `work_hour_start`). Do not change any other column.
  </action>
  <verify>
    <automated>grep -q 'max_new_dialogs_per_day = Column(Integer, nullable=False, server_default="50")' app/models/__init__.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'max_new_dialogs_per_day = Column(Integer, nullable=False, server_default="50")' app/models/__init__.py` returns 1
    - The line appears inside the `Campaign` class block (between `class Campaign(Base):` and the next `class ` definition) — verify with `grep -n "class Campaign(Base)\|class CampaignSender\|max_new_dialogs_per_day = Column" app/models/__init__.py` showing the column line number is between the two class line numbers
    - `python -c "import ast; ast.parse(open('app/models/__init__.py').read())"` exits 0 (file still parses)
  </acceptance_criteria>
  <done>Campaign ORM has max_new_dialogs_per_day Integer NOT NULL server_default="50", file parses.</done>
</task>

</tasks>

<verification>
- Migration 033 is idempotent and adds the column with DEFAULT 50, no backfill.
- ORM Campaign column matches the DB default (server_default="50").
- Both files for downstream plans (queue filter reads the column; API schemas expose it) are now backed by a real column.
- Optional smoke (executor discretion, not required): rebuild api (`docker compose up -d --build api`) and confirm api starts (fail-fast applier would block startup if 033 were broken).
</verification>

<success_criteria>
- `campaigns.max_new_dialogs_per_day` exists as INT NOT NULL DEFAULT 50 after the migration.
- A new ORM-created Campaign with no explicit value resolves to 50.
- No existing running campaign was backfilled to a value other than 50.
</success_criteria>

<output>
Create `.planning/phases/12-per-campaign-daily-new-dialog-limit-max-new-dialogs-per-day/12-01-SUMMARY.md` when done
</output>
