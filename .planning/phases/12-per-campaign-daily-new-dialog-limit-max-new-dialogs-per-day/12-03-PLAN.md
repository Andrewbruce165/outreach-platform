---
phase: 12-per-campaign-daily-new-dialog-limit-max-new-dialogs-per-day
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - app/schemas/__init__.py
  - app/routers/campaigns.py
  - tests/test_campaign_new_dialog_limit_api.py
autonomous: true
requirements: [NDLG-03, NDLG-04]
must_haves:
  truths:
    - "Creating a campaign with max_new_dialogs_per_day=70 returns 200/201 + a warnings[] entry; with 120 returns 422; with no value defaults to 50 (D-12/D-13/acceptance)"
    - "PATCHing max_new_dialogs_per_day to a value >50 re-validates and returns warnings; >100 returns 422 (D-14)"
    - "GET /campaigns and GET /campaigns/{id} carry NO warnings (D-14)"
    - "max_new_dialogs_per_day round-trips on the campaign create/patch/get response"
  artifacts:
    - path: "app/schemas/__init__.py"
      provides: "max_new_dialogs_per_day on CampaignCreate/Update/Response + a campaign write-response wrapper with warnings[]"
      contains: "max_new_dialogs_per_day"
    - path: "app/routers/campaigns.py"
      provides: "soft/hard-cap validation on create + patch, warnings[] on write responses, default 50 persisted"
      contains: "DIALOG_LIMIT_HARD_CAP"
    - path: "tests/test_campaign_new_dialog_limit_api.py"
      provides: "API tests for default/soft-cap-warning/hard-cap-422 on create and patch"
      contains: "max_new_dialogs_per_day"
  key_links:
    - from: "app/routers/campaigns.py create_campaign / patch_campaign"
      to: "_validate_max_new_dialogs (soft→warnings[], hard→422)"
      via: "validation helper mirroring senders _validate_rate_limits"
      pattern: "_validate_max_new_dialogs"
---

<objective>
Expose `max_new_dialogs_per_day` on the campaign API (CampaignCreate / CampaignUpdate / CampaignResponse, `Field(ge=1, le=100)`, default 50) and apply the D-14 soft/hard-cap machinery on the write path: value >50 and ≤100 → 200 + `warnings[]`; value >100 → 422; GET carries no warnings; PATCH re-validates when the field changes.

Purpose: Mirror the EXACT senders D-14 pattern (RATE_SOFT_CAP/RATE_HARD_CAP dicts, _validate_rate_limits, WarningItem, write-response-with-warnings) for the campaign new-dialog limit, so the green corridor (≤50) is advisory and the hard cap (100) is enforced.
Output: schema fields + a campaign write-response wrapper + create/patch validation + API tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-per-campaign-daily-new-dialog-limit-max-new-dialogs-per-day/12-CONTEXT.md
@.planning/phases/12-per-campaign-daily-new-dialog-limit-max-new-dialogs-per-day/12-01-SUMMARY.md

<interfaces>
<!-- SENDER D-14 PATTERN TO MIRROR (the source of truth this plan replicates):

app/routers/senders.py:61-63 — module-level cap dicts:
    RATE_HARD_CAP = {"rate_per_min": 10, "rate_per_hour": 50, "rate_per_day": 300}
    RATE_SOFT_CAP = {"rate_per_min": 4, "rate_per_hour": 20, "rate_per_day": 150}

app/routers/senders.py:135-176 — _validate_rate_limits(...) -> List[WarningItem]:
    for each field: if val > HARD_CAP[field]: raise HTTPException(422, detail={
        "code": "RATE_LIMIT_EXCEEDS_HARD_CAP", "field", "value", "hard_cap", "message"})
    elif val > SOFT_CAP[field]: warnings.append(WarningItem(field, value, recommended_max=SOFT_CAP[field]))

app/schemas/__init__.py:82-87 — class WarningItem(field:str, value:int, recommended_max:int, severity="warning")
app/schemas/__init__.py:158-161 — class SenderCreateResponse(sender: SenderResponse, warnings: List[WarningItem]=[]) -->

<!-- CAMPAIGN SCHEMAS TO EXTEND (app/schemas/__init__.py):
  CampaignCreate   :630-677  (default-bearing fields; e.g. work_hour_start: int = Field(default=9, ge=0, le=23))
  CampaignUpdate   :680-717  (all Optional; e.g. recontact_min_age_days: Optional[int] = Field(default=None, ge=1, le=365))
  CampaignResponse :720-772  (e.g. recontact_min_age_days: int = 30)
  CampaignListResponse :794  (GET list — must NOT carry warnings) -->

<!-- CAMPAIGN ROUTER (app/routers/campaigns.py):
  create_campaign  :349  @router.post("", response_model=CampaignResponse, status_code=201)
                         builds Campaign(...) kwargs, then `return await _campaign_to_response(db, ctx, camp)`
  patch_campaign   :526  @router.patch("/{campaign_id}", response_model=CampaignResponse)
                         applies update_data via setattr loop, then `return await _campaign_to_response(...)`
  get_campaign     :516  / list_campaigns :498 — GET paths, leave warnings off.
  WarningItem is imported from app.schemas in this router's import block (it imports CampaignResponse etc.). -->

<!-- IMPORTANT response-model nuance: create/patch currently declare response_model=CampaignResponse.
     D-14 says write paths must return warnings[]. Mirror senders: introduce a wrapper
     CampaignWriteResponse(campaign: CampaignResponse, warnings: List[WarningItem]=[]) and change
     ONLY the create + patch response_model + return shape. GET endpoints keep response_model=CampaignResponse. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add schema fields + CampaignWriteResponse wrapper</name>
  <read_first>
    - app/schemas/__init__.py (CampaignCreate :630, CampaignUpdate :680, CampaignResponse :720; WarningItem :82; SenderCreateResponse :158 as the wrapper template)
    - .planning/phases/12-per-campaign-daily-new-dialog-limit-max-new-dialogs-per-day/12-CONTEXT.md (D-12/D-13/D-14)
  </read_first>
  <behavior>
    - CampaignCreate without the field validates and carries default 50.
    - CampaignCreate/Update with max_new_dialogs_per_day=120 fails Pydantic validation (le=100 → 422).
    - CampaignResponse serializes max_new_dialogs_per_day.
    - CampaignWriteResponse carries {campaign, warnings[]}.
  </behavior>
  <action>
    In `app/schemas/__init__.py`:

    1. `CampaignCreate` (near the other default-bearing fields, e.g. after `recontact_min_age_days`): add
       `max_new_dialogs_per_day: int = Field(default=50, ge=1, le=100, description="Daily new-dialog cap per sender within this campaign (D-12). Green corridor <=50; soft-warn >50; hard cap 100.")`

    2. `CampaignUpdate` (all-Optional section): add
       `max_new_dialogs_per_day: Optional[int] = Field(default=None, ge=1, le=100)`

    3. `CampaignResponse` (near `recontact_min_age_days: int = 30`): add
       `max_new_dialogs_per_day: int = 50`

    4. Add a write-response wrapper modeled on `SenderCreateResponse` (schemas:158-161). Place it right after `CampaignResponse` (before `CampaignListResponse`):
       ```
       class CampaignWriteResponse(BaseModel):
           """Phase 12 D-14: campaign create/update response carrying soft-cap warnings[].
           GET paths keep returning CampaignResponse directly (no warnings)."""
           campaign: CampaignResponse
           warnings: List[WarningItem] = []
       ```
       `WarningItem`, `List`, `BaseModel`, `Field` are already imported/defined in this module. Do not modify `CampaignListResponse`.
  </action>
  <verify>
    <automated>grep -c "max_new_dialogs_per_day" app/schemas/__init__.py | grep -qv '^0$' && grep -q "class CampaignWriteResponse" app/schemas/__init__.py && python -c "import ast; ast.parse(open('app/schemas/__init__.py').read())" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "max_new_dialogs_per_day" app/schemas/__init__.py` returns >= 3 (Create, Update, Response)
    - `grep -c "ge=1, le=100" app/schemas/__init__.py` returns >= 2 (Create + Update)
    - `grep -c "class CampaignWriteResponse" app/schemas/__init__.py` returns 1
    - `grep -A4 "class CampaignWriteResponse" app/schemas/__init__.py` shows `campaign: CampaignResponse` and `warnings: List[WarningItem] = []`
    - `python -c "import ast; ast.parse(open('app/schemas/__init__.py').read())"` exits 0
  </acceptance_criteria>
  <done>Three campaign schemas carry the field with ge=1/le=100; CampaignWriteResponse wrapper exists; file parses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cap validation + warnings on create/patch, persist the value</name>
  <read_first>
    - app/routers/campaigns.py (create_campaign :349-449, patch_campaign :526-606; the import block for schema names; _campaign_to_response usage)
    - app/routers/senders.py (RATE_SOFT_CAP/RATE_HARD_CAP :61-63 and _validate_rate_limits :135-176 — the exact pattern to mirror, including the 422 detail shape)
  </read_first>
  <behavior>
    - create with value 70 → 201 + warnings[] with one entry (field="max_new_dialogs_per_day", recommended_max=50).
    - create with value 120 → 422 (Pydantic le=100 OR the explicit hard-cap check; either path is acceptable, but the explicit check gives the senders-style detail).
    - create with no value → 201, persisted value 50, warnings empty.
    - patch changing value to 80 → 200 + warnings[]; patch to 50 or below → 200, warnings empty.
    - GET endpoints unchanged (no warnings).
  </behavior>
  <action>
    In `app/routers/campaigns.py`:

    1. Add module-level cap constants near the top of the router (mirroring senders.py:61-63):
       ```
       # Phase 12 D-13/D-14: green corridor <=50; hard cap 100 (top of the "warmed" range).
       DIALOG_LIMIT_SOFT_CAP = 50
       DIALOG_LIMIT_HARD_CAP = 100
       ```

    2. Add a validation helper mirroring senders `_validate_rate_limits`:
       ```
       def _validate_max_new_dialogs(value: Optional[int]) -> List[WarningItem]:
           """Phase 12 D-13/D-14: hard cap 100 → 422; soft cap 50 → warnings[]."""
           warnings: List[WarningItem] = []
           if value is None:
               return warnings
           if value > DIALOG_LIMIT_HARD_CAP:
               raise HTTPException(
                   status_code=422,
                   detail={
                       "code": "NEW_DIALOG_LIMIT_EXCEEDS_HARD_CAP",
                       "field": "max_new_dialogs_per_day",
                       "value": value,
                       "hard_cap": DIALOG_LIMIT_HARD_CAP,
                       "message": "превышает максимально безопасный лимит новых диалогов на аккаунт; обратитесь в поддержку, если нужно выше",
                   },
               )
           if value > DIALOG_LIMIT_SOFT_CAP:
               warnings.append(
                   WarningItem(
                       field="max_new_dialogs_per_day",
                       value=value,
                       recommended_max=DIALOG_LIMIT_SOFT_CAP,
                   )
               )
           return warnings
       ```
       Import `WarningItem` and `CampaignWriteResponse` from `app.schemas` (extend the existing schema import line). `HTTPException`, `Optional`, `List` are already imported.

    3. `create_campaign`: change `@router.post("", response_model=CampaignResponse, status_code=201)` to `response_model=CampaignWriteResponse`. Before building the Campaign, call `warnings = _validate_max_new_dialogs(payload.max_new_dialogs_per_day)`. Add `max_new_dialogs_per_day=payload.max_new_dialogs_per_day` to the `Campaign(...)` kwargs. At the end, replace `return await _campaign_to_response(db, ctx, camp)` with:
       ```
       resp = await _campaign_to_response(db, ctx, camp)
       return CampaignWriteResponse(campaign=resp, warnings=warnings)
       ```

    4. `patch_campaign`: change `@router.patch("/{campaign_id}", response_model=CampaignResponse)` to `response_model=CampaignWriteResponse`. After `update_data = payload.model_dump(exclude_unset=True)`, add:
       ```
       warnings: List[WarningItem] = []
       if "max_new_dialogs_per_day" in update_data and update_data["max_new_dialogs_per_day"] is not None:
           warnings = _validate_max_new_dialogs(update_data["max_new_dialogs_per_day"])
       ```
       The existing `for k, v in update_data.items(): setattr(c, k, v)` loop already persists the field (it is a plain column). At the end replace `return await _campaign_to_response(db, ctx, c)` with:
       ```
       resp = await _campaign_to_response(db, ctx, c)
       return CampaignWriteResponse(campaign=resp, warnings=warnings)
       ```

    5. Do NOT change `get_campaign`, `list_campaigns`, or `_campaign_to_response` — GET paths keep `response_model=CampaignResponse` and carry no warnings (D-14). Also add `max_new_dialogs_per_day` to whatever explicit field mapping `_campaign_to_response` uses IF it constructs CampaignResponse field-by-field; if it uses `CampaignResponse.model_validate(camp)` / from_attributes, the new ORM column is picked up automatically — read the helper first and only add the field if the mapping is explicit.
  </action>
  <verify>
    <automated>grep -q "DIALOG_LIMIT_HARD_CAP = 100" app/routers/campaigns.py && grep -q "def _validate_max_new_dialogs" app/routers/campaigns.py && grep -c "response_model=CampaignWriteResponse" app/routers/campaigns.py | grep -q '^2$' && python -c "import ast; ast.parse(open('app/routers/campaigns.py').read())" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DIALOG_LIMIT_SOFT_CAP = 50" app/routers/campaigns.py` returns 1 and `grep -c "DIALOG_LIMIT_HARD_CAP = 100" app/routers/campaigns.py` returns 1
    - `grep -c "def _validate_max_new_dialogs" app/routers/campaigns.py` returns 1
    - `grep -c "response_model=CampaignWriteResponse" app/routers/campaigns.py` returns exactly 2 (create + patch only)
    - `grep -c "response_model=CampaignResponse" app/routers/campaigns.py` for GET endpoints still returns >= 2 (get_campaign + any other GET unchanged)
    - `grep -c "max_new_dialogs_per_day=payload.max_new_dialogs_per_day" app/routers/campaigns.py` returns 1 (persisted on create)
    - `grep -c "NEW_DIALOG_LIMIT_EXCEEDS_HARD_CAP" app/routers/campaigns.py` returns 1
    - `python -c "import ast; ast.parse(open('app/routers/campaigns.py').read())"` exits 0
  </acceptance_criteria>
  <done>Create/patch validate the cap, return CampaignWriteResponse with warnings[], persist default 50; GET paths unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: API tests — default / soft-cap warning / hard-cap 422 on create + patch</name>
  <read_first>
    - tests/test_campaign_router.py (existing campaign router test harness: auth fixture, async test client, how it POSTs/PATCHes /api/v1/campaigns and asserts response bodies)
    - tests/test_phase5_1_campaign_v2_router.py (newer field-on-campaign router test patterns for create/patch assertions)
    - tests/conftest.py (auth / client / workspace fixtures)
  </read_first>
  <action>
    Create `tests/test_campaign_new_dialog_limit_api.py` mirroring the existing campaign router test harness (same client + auth fixtures). Cases, each fully asserting status code + body shape:

    1. `test_create_default_is_50`: POST a minimal valid campaign WITHOUT max_new_dialogs_per_day. Assert 201, and `body["campaign"]["max_new_dialogs_per_day"] == 50`, and `body["warnings"] == []`. (Note: response is now the CampaignWriteResponse wrapper — assert against `body["campaign"]`.)

    2. `test_create_soft_cap_warns`: POST with `max_new_dialogs_per_day=70`. Assert 201, `body["campaign"]["max_new_dialogs_per_day"] == 70`, `len(body["warnings"]) == 1`, and the warning has `field == "max_new_dialogs_per_day"` and `recommended_max == 50`.

    3. `test_create_hard_cap_422`: POST with `max_new_dialogs_per_day=120`. Assert 422 (Pydantic le=100 or the explicit hard-cap check — both produce 422).

    4. `test_patch_soft_cap_warns`: create a default campaign, then PATCH `max_new_dialogs_per_day=80`. Assert 200, `body["campaign"]["max_new_dialogs_per_day"] == 80`, `len(body["warnings"]) == 1`.

    5. `test_patch_hard_cap_422`: PATCH an existing campaign with `max_new_dialogs_per_day=150`. Assert 422.

    6. `test_get_carries_no_warnings`: GET /campaigns/{id} after creating with a soft-cap value. Assert 200 and the body is the plain CampaignResponse (has `max_new_dialogs_per_day` directly, NOT nested under `campaign`, and no `warnings` key).

    If the existing campaign router tests assert against the old flat CampaignResponse shape for create/patch, note in the SUMMARY that those tests must read `body["campaign"]` now (the wrapper change is part of NDLG-04) — update any that break, or confirm the full suite still passes.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_campaign_new_dialog_limit_api.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_campaign_new_dialog_limit_api.py` exists with the 6 named test functions (`grep -c "def test_" tests/test_campaign_new_dialog_limit_api.py` returns 6)
    - `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_campaign_new_dialog_limit_api.py -q` exits 0
    - Run the broader campaign router suite to confirm the wrapper change did not silently break callers: `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_campaign_router.py tests/test_phase5_1_campaign_v2_router.py -q` exits 0 (fix any test that assumed the flat create/patch response shape)
    - Tests run ONLY via the test-overlay (NEVER bare pytest)
  </acceptance_criteria>
  <done>6 new API tests pass; existing campaign router suites still green after the write-response wrapper change.</done>
</task>

</tasks>

<verification>
- Default 50 persists; >50 → warnings[]; >100 → 422 on both create and patch (D-12/D-13/D-14).
- GET paths carry no warnings (D-14).
- Senders D-14 machinery faithfully mirrored (soft/hard dicts, validation helper, WarningItem, write-response wrapper).
- All campaign API tests green via test-overlay.
</verification>

<success_criteria>
- POST/PATCH /campaigns enforce the cap and surface warnings; default is 50; hard cap 100.
- GET responses unchanged (no warnings).
</success_criteria>

<output>
Create `.planning/phases/12-per-campaign-daily-new-dialog-limit-max-new-dialogs-per-day/12-03-SUMMARY.md` when done
</output>
