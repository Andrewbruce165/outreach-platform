---
phase: 12-per-campaign-daily-new-dialog-limit-max-new-dialogs-per-day
plan: 04
type: execute
wave: 3
depends_on: ["12-03"]
files_modified:
  - lovable-handoff/openapi.json
  - lovable-handoff/types/api.ts
autonomous: false
requirements: [NDLG-05, NDLG-06]
user_setup: []
must_haves:
  truths:
    - "lovable-handoff/openapi.json describes max_new_dialogs_per_day on CampaignCreate/Update/Response and the CampaignWriteResponse wrapper with warnings[] (D-15)"
    - "The regenerated types/api.ts contains max_new_dialogs_per_day so the frontend can type the field (D-15)"
    - "The campaign settings form in the sibling repo has a numeric max_new_dialogs_per_day field (default 50) with an inline warning when >50 whose copy says «на аккаунт» (D-16)"
  artifacts:
    - path: "lovable-handoff/openapi.json"
      provides: "Regenerated OpenAPI spec with the new campaign field + write-response wrapper"
      contains: "max_new_dialogs_per_day"
    - path: "lovable-handoff/types/api.ts"
      provides: "Regenerated TS types including the new field"
      contains: "max_new_dialogs_per_day"
  key_links:
    - from: "lovable-handoff/openapi.json"
      to: "lovable-handoff/types/api.ts"
      via: "export-handoff.sh openapi-typescript codegen"
      pattern: "max_new_dialogs_per_day"
---

<objective>
Regenerate `lovable-handoff/openapi.json` + generated TS types via the export-handoff flow (NDLG-05, D-15) so the new `max_new_dialogs_per_day` field and the `CampaignWriteResponse` wrapper are in the spec, then add the frontend campaign-settings form field with the >50 inline warning in the sibling repo (NDLG-06, D-16). The frontend change is cross-repo (separate git repo `AGS-Venture-Lab/aimly-tg-outreach`) and human-UAT, handled exactly like Phase 10 POOLV-03/04 and Phase 11 P04.

Purpose: Close the last two requirements — make the new limit visible/typed to the frontend (no manual spec editing) and give the user a single numeric control with the green-corridor warning whose copy uses per-sender semantics («на аккаунт», D-01/D-16).
Output: regenerated spec + types (this repo) + a frontend form field (sibling repo) pending human UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-per-campaign-daily-new-dialog-limit-max-new-dialogs-per-day/12-CONTEXT.md
@.planning/phases/12-per-campaign-daily-new-dialog-limit-max-new-dialogs-per-day/12-03-SUMMARY.md

<interfaces>
<!-- export-handoff flow (scripts/export-handoff.sh) — the ONLY supported way to regen the spec (D-15):
     1. `docker compose up -d --build api`  (MUST rebuild first so the running container has the
        Phase-12 schema changes — stale containers produced wrong specs in Phase 11 P04)
     2. run `bash scripts/export-handoff.sh` — it boots api, dumps /openapi.json → lovable-handoff/openapi.json
        (jq-formatted, with a project-title sanity check so it can't grab a neighbouring FastAPI),
        and runs `npx -y openapi-typescript@7` → lovable-handoff/types/api.ts.
     NEVER hand-edit openapi.json or api.ts. -->

<!-- Cross-repo frontend: sibling repo at /root/apps/aimly/aimly-tg-outreach
     (origin https://github.com/AGS-Venture-Lab/aimly-tg-outreach.git) — INDEPENDENT git repo.
     Phase 10 (08-04 / 10-04) and Phase 11 P04 are the precedent: copy the regenerated
     openapi.json + types into the sibling, edit the campaign form, commit in the sibling repo,
     leave human-UAT pending. The campaign settings/wizard form is where work_hour_start /
     recontact fields live — add the new field beside them. -->

<!-- D-16 copy (Russian, per-sender semantics — MUST say «на аккаунт», not «на кампанию»):
     inline warning when value > 50:
     «рекомендуем не больше 50 новых диалогов в сутки на аккаунт — выше растёт риск спам-бана» -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate openapi.json + types via export-handoff</name>
  <read_first>
    - scripts/export-handoff.sh (the regen flow + the rebuild-first requirement + the project-title sanity check)
    - .planning/phases/12-per-campaign-daily-new-dialog-limit-max-new-dialogs-per-day/12-03-SUMMARY.md (confirm the API schema changes — CampaignWriteResponse, max_new_dialogs_per_day — are merged before regenerating)
  </read_first>
  <action>
    1. Rebuild the api container FIRST so the running spec reflects Phase-12 changes: `docker compose up -d --build api` (CLAUDE.md: restart does not pick up code; export-handoff dumps the live container's /openapi.json). Wait for the container to be healthy.
    2. Run the export-handoff flow: `bash scripts/export-handoff.sh`. This overwrites `lovable-handoff/openapi.json` and `lovable-handoff/types/api.ts`. Do NOT hand-edit either file.
    3. Confirm the new field and wrapper landed in the regenerated spec (the grep checks below). If `max_new_dialogs_per_day` is absent, the container was stale — rebuild and re-run; do not patch the spec by hand.
  </action>
  <verify>
    <automated>grep -q "max_new_dialogs_per_day" lovable-handoff/openapi.json && grep -q "max_new_dialogs_per_day" lovable-handoff/types/api.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "max_new_dialogs_per_day" lovable-handoff/openapi.json` returns >= 3 (Create, Update, Response schemas)
    - `grep -c "max_new_dialogs_per_day" lovable-handoff/types/api.ts` returns >= 3
    - `grep -ci "CampaignWriteResponse" lovable-handoff/openapi.json` returns >= 1 (the write-response wrapper with warnings[] is in the spec)
    - `jq -r '.info.title' lovable-handoff/openapi.json` matches the Outreach Platform title (sanity check the spec is from THIS project, not a neighbour) — confirm it is not empty and not a different service name
    - The files were produced by the script, not hand-edited (no partial/manual diffs) — confirm `git diff --stat lovable-handoff/` shows only openapi.json + types/api.ts changed
  </acceptance_criteria>
  <done>openapi.json + types/api.ts regenerated via export-handoff, contain the new field + wrapper, from the correct project.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend campaign-settings field + inline >50 warning (sibling repo)</name>
  <read_first>
    - /root/apps/aimly/aimly-tg-outreach (sibling repo — locate the campaign create/edit form/wizard component where work_hour_start / recontact_min_age_days / allow_recontact fields are rendered; that is where the new numeric field belongs)
    - the sibling repo's existing numeric-with-inline-warning pattern if any (e.g. how senders rate-limit warnings or recontact_min_age_days are rendered) — mirror it
    - lovable-handoff/types/api.ts (the regenerated types — copy/sync into the sibling per the Phase 10/11 cross-repo convention before wiring the field)
  </read_first>
  <action>
    In the sibling repo `/root/apps/aimly/aimly-tg-outreach` (separate git repo — commits here go to AGS-Venture-Lab/aimly-tg-outreach, NOT the backend repo):

    1. Sync the regenerated `openapi.json` / `types` into the sibling using the same mechanism Phase 10/11 used (copy the relevant generated artifacts; do not hand-edit generated files).
    2. Add a numeric input field `max_new_dialogs_per_day` to the campaign settings form / wizard, defaulting to 50, with min=1 / max=100 matching the API bounds (ge=1/le=100).
    3. Render an inline warning, shown only when the value > 50, with EXACTLY this Russian copy (per-sender semantics — must say «на аккаунт»):
       `рекомендуем не больше 50 новых диалогов в сутки на аккаунт — выше растёт риск спам-бана`
       Label/help text should make the per-sender meaning clear (this is the cap PER attached account, not the whole campaign — D-01/D-16).
    4. Wire the field into the create/update payloads so it is sent to POST/PATCH /api/v1/campaigns; if the create/update response is now the CampaignWriteResponse wrapper, surface its `warnings[]` consistently with how senders warnings are shown (optional toast/banner — match existing pattern; the inline >50 warning is the required deliverable).
    5. Ensure the project type-checks: run the sibling's `tsc`/build (e.g. `bun run build` or `tsc --noEmit` per the sibling's scripts) and confirm clean.
    6. Commit in the sibling repo with a message referencing Phase 12 / NDLG-06. Do NOT push unless asked (per CLAUDE.md commit policy).

    This is a cross-repo, human-verified deliverable (consistent with Phase 10 POOLV-03/04 and Phase 11 P04): code complete in the sibling, UAT pending the frontend deploy.
  </action>
  <verify>
    <human-check>Open the campaign settings form on the deployed frontend, set max_new_dialogs_per_day to 70, confirm the inline warning «…на аккаунт…» appears; set to 50 or below, confirm it disappears; save and confirm the value persists (GET shows 70) and the API returned a warnings[] entry.</human-check>
  </verify>
  <acceptance_criteria>
    - In the sibling repo, the campaign form source contains an input bound to `max_new_dialogs_per_day` (grep the sibling source returns >= 1)
    - The inline warning copy contains the exact substring `новых диалогов в сутки на аккаунт` (grep the sibling source returns >= 1) — confirms per-sender semantics wording (D-16)
    - The field default is 50 and bounds are min 1 / max 100 in the component (grep/read confirms `50` default and `100` max)
    - The sibling project type-checks / builds clean (the sibling's `tsc --noEmit` or build script exits 0)
    - A commit exists in the sibling repo referencing Phase 12 / NDLG-06
  </acceptance_criteria>
  <done>Sibling campaign form has the numeric field (default 50, max 100) with the «на аккаунт» >50 inline warning, types synced, build clean, committed; human-UAT pending deploy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The regenerated openapi.json/types (backend repo) and the campaign-settings numeric field with the >50 «на аккаунт» inline warning (sibling frontend repo).</what-built>
  <how-to-verify>
    1. On the deployed/preview frontend, open a campaign's settings (create or edit).
    2. Locate the "новых диалогов в сутки на аккаунт" numeric field — confirm it defaults to 50.
    3. Set it to 70 → confirm the inline warning «рекомендуем не больше 50 новых диалогов в сутки на аккаунт — выше растёт риск спам-бана» appears.
    4. Set it to 50 → confirm the warning disappears.
    5. Set it to 120 → confirm the save is rejected (422 / validation error).
    6. Save at 70 → reload → confirm the value persisted as 70.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what's off (wrong copy, default, validation, persistence).</resume-signal>
</task>

</tasks>

<verification>
- openapi.json + types regenerated via export-handoff (no hand-edit), contain the field + CampaignWriteResponse wrapper (D-15).
- Sibling campaign form has the numeric field (default 50, max 100) with the per-sender «на аккаунт» >50 warning (D-16).
- Field persists through create/patch; >100 rejected; human-UAT confirms the flow.
</verification>

<success_criteria>
- Frontend exposes a single numeric max_new_dialogs_per_day control (default 50) with the green-corridor warning above 50.
- The handoff spec/types reflect the new field and write-response wrapper, regenerated from the live API.
</success_criteria>

<output>
Create `.planning/phases/12-per-campaign-daily-new-dialog-limit-max-new-dialogs-per-day/12-04-SUMMARY.md` when done
</output>
