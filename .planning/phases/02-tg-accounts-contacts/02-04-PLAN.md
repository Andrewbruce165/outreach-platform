---
plan_id: "02-04"
title: "Contacts model + push API + CSV import: phone normalization, dedup, mapping, folder auto-create"
phase: "02-tg-accounts-contacts"
plan: 4
type: execute
wave: 2
depends_on: ["02-02", "02-03"]
files_modified:
  - "app/utils/phone.py"
  - "app/services/csv_import.py"
  - "app/routers/contacts.py"
  - "app/main.py"
  - "tests/test_phone_normalization.py"
  - "tests/test_csv_import.py"
  - "tests/test_contacts.py"
autonomous: true
requirements: ["CONT-01", "CONT-02", "CONT-03", "CONT-05", "FLDR-03"]
must_haves:
  truths:
    - "Юзер делает POST /contacts/import/preview multipart с CSV → получает {import_id, columns, sample_rows, suggested_mapping}"
    - "Юзер делает POST /contacts/import {import_id, folder_id|folder_name, mapping} → 202 Accepted + {imported, skipped_duplicates, skipped_invalid}"
    - "Phone нормализуется в E.164: +79001234567, 89001234567 → +79001234567"
    - "Контакт с дубликатом (workspace_id, phone) пропускается с reason='duplicate'"
    - "Контакт без phone и username помечается skipped_invalid"
    - "Push контактов через POST /api/v1/contacts с X-Workspace-Key (n8n) работает с тем же engine что и CSV"
    - "folder_name (без folder_id) → folder создаётся автоматически (FLDR-03)"
    - "Контакты получают tg_status='pending' (или 'unchecked' если в workspace нет checker'а — D-20)"
    - "Импортированные контакты привязаны к workspace_id из AuthCtx"
    - "GET /api/v1/contacts?folder_id=... возвращает список контактов с пагинацией"
    - "POST /contacts/{id}/move и batch /contacts/move перемещают контакты между папками"
  artifacts:
    - path: "app/utils/phone.py"
      provides: "normalize_to_e164 pure function"
      contains: "def normalize_to_e164"
    - path: "app/services/csv_import.py"
      provides: "parse_preview, suggest_mapping, apply_import"
      contains: "def parse_preview; def suggest_mapping; def apply_import"
    - path: "app/routers/contacts.py"
      provides: "Endpoints: GET /contacts, POST /contacts, POST /contacts/import/preview, POST /contacts/import, POST /contacts/{id}/move, POST /contacts/move, DELETE /contacts/{id}"
      contains: "Depends(auth_dep); UploadFile = File(...); 202"
    - path: "tests/test_phone_normalization.py"
      provides: "Unit-тесты normalize_to_e164"
    - path: "tests/test_csv_import.py"
      provides: "Unit-тесты parse_preview, suggest_mapping"
    - path: "tests/test_contacts.py"
      provides: "Integration-тесты preview/import/push/dedup/cross-tenant"
  key_links:
    - from: "app/routers/contacts.py"
      to: "app/utils/phone.py::normalize_to_e164"
      via: "from app.utils.phone import normalize_to_e164"
      pattern: "normalize_to_e164"
    - from: "app/routers/contacts.py"
      to: "app/services/csv_import.py"
      via: "from app.services.csv_import import parse_preview, apply_import"
      pattern: "csv_import"
    - from: "app/routers/contacts.py"
      to: "app/routers/folders.py::get_or_create_by_name"
      via: "from app.routers.folders import get_or_create_by_name"
      pattern: "get_or_create_by_name"
    - from: "app/main.py"
      to: "app/routers/contacts.py"
      via: "app.include_router(contacts.router)"
      pattern: "include_router\\(contacts\\.router\\)"
---

<objective>
Реализовать модель контактов + CSV import + push API. Один план потому что все три задачи делят общий "import engine" (нормализация phone, dedup, folder auto-create) и общую модель Contact. Endpoint'ы:
- `POST /api/v1/contacts/import/preview` multipart → парсит первые ~50 строк, возвращает colmns/sample_rows/suggested_mapping, сохраняет файл BYTEA в `csv_imports` (Option B из RESEARCH §"CSV Import Storage")
- `POST /api/v1/contacts/import` JSON → применяет mapping, нормализует phone, dedup, INSERT'ит контакты с tg_status='pending' (или 'unchecked' если нет checker'а — D-20), возвращает 202 + summary
- `POST /api/v1/contacts` — push через `X-Workspace-Key` (CONT-03/D-10), single или batch до 1000, тот же engine
- `GET /api/v1/contacts?folder_id=...&tg_status=...` — список с пагинацией
- `POST /api/v1/contacts/{id}/move` / batch `POST /api/v1/contacts/move` — перемещение между папками (D-04)
- `DELETE /api/v1/contacts/{id}` + batch — удаление
- helper `app/utils/phone.normalize_to_e164` — pure regex без `phonenumbers` lib
- helper `app/services/csv_import.parse_preview / suggest_mapping / apply_import`

NB: `POST /contacts/recheck` — в плане 02-05 (часть ContactCheck worker'а).
NB: Workspace has_checker check для D-20 — в плане 02-05 (зависит от worker'а).

Purpose: Контакты — core-сущность для кампаний Phase 4. Без CSV-импорта и push API клиент не может загрузить базу. CONT-04 (TG-проверка) отложен в plan 02-05 (зависит от ContactCheckWorker).

Output: Контакты-роутер, CSV-сервис, phone utility, тесты unit (phone, csv parse) + integration (preview/import/push/dedup/cross-tenant).
</objective>

<execution_context>
@/Users/andrewbruce/Documents/outreach-platform/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewbruce/Documents/outreach-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-tg-accounts-contacts/02-CONTEXT.md
@.planning/phases/02-tg-accounts-contacts/02-RESEARCH.md
@.planning/phases/02-tg-accounts-contacts/02-PATTERNS.md
@.planning/phases/02-tg-accounts-contacts/02-02-PLAN.md
@.planning/phases/02-tg-accounts-contacts/02-03-PLAN.md
@.planning/codebase/CONVENTIONS.md
@app/routers/workspace.py
@app/routers/check_contacts.py
@app/utils/auth.py
@app/models/__init__.py
@app/schemas/__init__.py
@app/main.py
@tests/conftest.py

<interfaces>
<!-- Контракты после планов 02-02 и 02-03. -->

From app/models/__init__.py (после 02-02):
```python
class Contact(Base):
    __tablename__ = "contacts"
    id: UUID
    workspace_id: UUID                  # FK workspaces, NOT NULL
    folder_id: UUID                     # FK folders ON DELETE CASCADE, NOT NULL
    phone: Optional[str]                # VARCHAR(20), nullable
    username: Optional[str]             # VARCHAR(50), nullable
    full_name: Optional[str]
    source: Optional[str]
    custom: dict                        # JSONB, default '{}'
    tg_status: str                      # 'pending'|'registered'|'not_registered'|'error'|'unchecked'
    tg_telegram_id: Optional[int]
    tg_username_resolved: Optional[str]
    tg_error: Optional[str]
    tg_checked_at: Optional[datetime]
    created_at: datetime
    updated_at: datetime

class CsvImport(Base):
    __tablename__ = "csv_imports"
    id: UUID
    workspace_id: UUID
    file_data: bytes                    # BYTEA
    columns: list                       # JSONB
    suggested_mapping: dict             # JSONB
    encoding: Optional[str]
    delimiter: Optional[str]
    created_at: datetime
    expires_at: datetime                # NOW() + 30 minutes
```

From app/schemas/__init__.py (после 02-02):
```python
class ContactCreate, ContactBatchPush, ContactResponse,
      ContactImportRequest, ContactImportPreviewResponse, ContactImportSummary,
      MoveContactRequest, MoveContactBatchRequest — все там.
```

From app/routers/folders.py (после 02-03):
```python
async def get_or_create_by_name(db: AsyncSession, workspace_id: UUID, name: str) -> UUID:
    """ON CONFLICT idempotent — для FLDR-03 auto-create."""
    ...
```

From app/utils/auth.py (Phase 1):
```python
async def auth_dep(...) -> AuthCtx:
    # AuthCtx.workspace_id ВСЕГДА заполнен, для JWT и X-Workspace-Key.
    # ctx.user_id может быть None для API-key — НЕ ассумить is not None.
    ...
```

From DB constraints (миграция 013):
```sql
-- UNIQUE INDEX (workspace_id, phone) WHERE phone IS NOT NULL
-- UNIQUE INDEX (workspace_id, username) WHERE username IS NOT NULL
-- CHECK contacts_phone_or_username_check: phone IS NOT NULL OR username IS NOT NULL
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app/utils/phone.py — normalize_to_e164 + unit-тесты</name>
  <files>app/utils/phone.py, tests/test_phone_normalization.py</files>
  <read_first>
    - app/utils/auth.py — стиль модуля в app/utils/ (header docstring, imports, logger)
    - .planning/phases/02-tg-accounts-contacts/02-RESEARCH.md §"Phone Normalization — E.164" — полный code skeleton + edge cases (RU 89... → +79..., 79... → +79..., +7 (900) 123-45-67 → +79001234567)
    - .planning/phases/02-tg-accounts-contacts/02-PATTERNS.md §"8. app/utils/phone.py" — skeleton verbatim
  </read_first>
  <behavior>
    - Test 1: `normalize_to_e164("+79001234567") == "+79001234567"`
    - Test 2: `normalize_to_e164("89001234567") == "+79001234567"` (RU heuristic leading 8)
    - Test 3: `normalize_to_e164("79001234567") == "+79001234567"` (без +)
    - Test 4: `normalize_to_e164("+7 (900) 123-45-67") == "+79001234567"` (форматирование)
    - Test 5: `normalize_to_e164("8 (900) 123-45-67") == "+79001234567"` (форматирование + RU heuristic)
    - Test 6: `normalize_to_e164("+380501234567") == "+380501234567"` (Украина — без RU heuristic)
    - Test 7: `normalize_to_e164("+77001234567") == "+77001234567"` (Казахстан — НЕ ломаем 11-digit с leading 7+7)
    - Test 8: `normalize_to_e164("abc") is None`
    - Test 9: `normalize_to_e164("") is None`
    - Test 10: `normalize_to_e164("+1234") is None` (too short, < 7 digits)
    - Test 11: `normalize_to_e164("+1234567890123456") is None` (too long, > 15 digits)
    - Test 12: `normalize_to_e164(None) is None`
  </behavior>
  <action>
    1. Создать `app/utils/phone.py`:
       ```python
       """Phone normalization to E.164 format.
       
       RU-centric heuristics + ITU E.164 spec.
       Phase 2 (CONT-05): canonical phone storage in `contacts.phone`.
       
       NB: We do NOT use `phonenumbers` library — overkill for v1 (5+ MB data,
       slow startup). Pure regex is enough for RU/CIS-focus use cases.
       """
       
       import re
       from typing import Optional
       
       _NON_DIGIT = re.compile(r"\D+")
       _E164_RE = re.compile(r"^\+\d{7,15}$")
       
       
       def normalize_to_e164(raw: Optional[str]) -> Optional[str]:
           """Normalize phone string to E.164 format (`+XXXXXXXXX`).
           
           Rules:
           1. Strip all non-digit (preserves leading `+` if present, removes everything else).
           2. RU heuristic: 11-digit without leading `+` starting with 8 → replace 8 with 7.
           3. Add leading `+` if missing.
           4. Validate ITU E.164 spec: `+` followed by 7..15 digits.
           
           Returns None if invalid.
           
           Examples:
               normalize_to_e164("+79001234567")        → "+79001234567"
               normalize_to_e164("89001234567")          → "+79001234567" (RU leading-8)
               normalize_to_e164("79001234567")          → "+79001234567"
               normalize_to_e164("+7 (900) 123-45-67")   → "+79001234567"
               normalize_to_e164("+380501234567")        → "+380501234567"
               normalize_to_e164("abc")                  → None
               normalize_to_e164("")                     → None
           """
           if not raw:
               return None
           had_plus = raw.lstrip().startswith("+")
           digits = _NON_DIGIT.sub("", raw)
           if not digits:
               return None
           # RU heuristic: only for 11-digit strings starting with 8, без явного `+` (которое могло быть +7...)
           if not had_plus and len(digits) == 11 and digits.startswith("8"):
               digits = "7" + digits[1:]
           e164 = "+" + digits
           if not _E164_RE.match(e164):
               return None
           return e164
       ```
    2. Создать `tests/test_phone_normalization.py`:
       ```python
       """Unit-тесты для app/utils/phone.normalize_to_e164."""
       
       import pytest
       from app.utils.phone import normalize_to_e164
       
       
       @pytest.mark.parametrize("raw,expected", [
           ("+79001234567", "+79001234567"),
           ("89001234567", "+79001234567"),         # RU leading-8
           ("79001234567", "+79001234567"),         # без +
           ("+7 (900) 123-45-67", "+79001234567"),  # форматирование с +
           ("8 (900) 123-45-67", "+79001234567"),   # форматирование + RU leading-8
           ("+380501234567", "+380501234567"),      # Украина
           ("+77001234567", "+77001234567"),        # Казахстан (11 digits starting with +77)
           ("+1 415 555 1212", "+14155551212"),     # US
           ("abc", None),
           ("", None),
           ("   ", None),
           ("+1234", None),                          # too short (< 7 digits)
           ("+1234567890123456", None),              # too long (> 15)
           (None, None),
           ("+0", None),                             # too short
       ])
       def test_normalize_to_e164(raw, expected):
           assert normalize_to_e164(raw) == expected
       ```
  </action>
  <verify>
    <automated>pytest tests/test_phone_normalization.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `app/utils/phone.py` exists AND contains `def normalize_to_e164(`
    - `app/utils/phone.py` contains `_E164_RE = re.compile(r"^\+\d{7,15}$")`
    - `app/utils/phone.py` contains RU heuristic check (`startswith("8")`)
    - `app/utils/phone.py` НЕ содержит `import phonenumbers`
    - `tests/test_phone_normalization.py` exists AND содержит минимум 12 parametrize cases
    - `pytest tests/test_phone_normalization.py -v` exits 0
    - `python -c "from app.utils.phone import normalize_to_e164; assert normalize_to_e164('89001234567') == '+79001234567'"` exits 0
  </acceptance_criteria>
  <done>normalize_to_e164 покрывает RU/CIS edge cases и проверен 14 unit-тестами.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: app/services/csv_import.py — parse_preview, suggest_mapping, apply_import + unit-тесты</name>
  <files>app/services/csv_import.py, tests/test_csv_import.py</files>
  <read_first>
    - app/services/encryption.py — стиль pure-helper модуля (header, logging)
    - app/utils/phone.py — для apply_import (импортируется)
    - .planning/phases/02-tg-accounts-contacts/02-RESEARCH.md §"CSV Import Pitfalls → Skeleton" — полный код parse_preview, suggest_mapping
    - .planning/phases/02-tg-accounts-contacts/02-RESEARCH.md §"CSV Import Pitfalls" — все 9 pitfall'ов (BOM, delimiters, encoding, кавычки, no-header heuristic, trailing whitespace, duplicate headers, large file, csv-injection)
    - .planning/phases/02-tg-accounts-contacts/02-PATTERNS.md §"7. app/services/csv_import.py" — skeleton
    - .planning/phases/02-tg-accounts-contacts/02-CONTEXT.md D-07, D-08, D-09 — точные правила mapping
  </read_first>
  <behavior>
    - Test 1: `parse_preview(b"phone,name\n+79001234567,John\n")` → `{columns: ['phone','name'], sample_rows: [{phone: '+79001234567', name: 'John'}], delimiter: ',', encoding: 'utf-8-sig'}`.
    - Test 2: `parse_preview` with UTF-8 BOM (`\xef\xbb\xbf`) — BOM strip'нут.
    - Test 3: `parse_preview` with `;` delimiter (Russian Excel) — detected.
    - Test 4: `parse_preview` with cp1251 — fallback works.
    - Test 5: `parse_preview(b"")` raises ValueError("EMPTY_FILE").
    - Test 6: `suggest_mapping(['phone','имя','source'])` returns `{'0': 'phone', '1': 'full_name', '2': 'source'}`.
    - Test 7: `suggest_mapping(['unknown_col'])` returns `{}` (no match).
    - Test 8: `apply_import(file_bytes, mapping={'0': 'phone', '1': 'custom.company'}, folder_id, workspace_id)` возвращает `{imported, skipped_duplicates, skipped_invalid, skipped_phones}` — без INSERT'а в БД (сам INSERT делает роутер для атомарности с CsvImport row delete).
    - Test 9: apply_import нормализует `89001234567` → `+79001234567` в результирующих строках.
    - Test 10: apply_import строку без phone и username → в skipped_invalid с reason='no_phone_no_username'.
  </behavior>
  <action>
    1. Создать `app/services/csv_import.py`:
       ```python
       """CSV-импорт: parse_preview, suggest_mapping, apply_import.
       
       Phase 2 (CONT-01, CONT-05):
       - parse_preview: bytes → {columns, sample_rows, delimiter, encoding, looks_like_no_header}
       - suggest_mapping: column names → contact field heuristic (англ/рус алиасы)
       - apply_import: bytes + mapping → list of valid contact dicts + summary of skips
       
       NB: НЕ используем pandas — stdlib `csv` достаточно (1-10k строк max в v1).
       """
       
       import csv
       import io
       import logging
       import re
       from typing import Optional
       
       from app.utils.phone import normalize_to_e164
       
       logger = logging.getLogger(__name__)
       
       ENCODING_FALLBACKS = ["utf-8-sig", "cp1251"]
       
       _COLUMN_ALIASES = {
           "phone": ["phone", "phones", "tel", "tel.", "mobile", "телефон", "телефоны", "номер", "тел"],
           "username": ["username", "user", "юзернейм", "tg", "telegram", "tg_username"],
           "full_name": ["name", "имя", "fio", "фио", "full_name", "fullname", "имя_фамилия", "имя фамилия"],
           "source": ["source", "источник", "src", "origin"],
       }
       
       _PHONE_LIKE_RE = re.compile(r"^[+\d][\d\s()-]{6,}$")
       
       
       def parse_preview(file_bytes: bytes, max_rows: int = 50) -> dict:
           """Парсит CSV — возвращает первые ~50 строк + heuristic mapping.
           
           Raises ValueError("EMPTY_FILE") если файл пуст.
           Raises ValueError("INVALID_ENCODING") если ни одна кодировка не подошла.
           """
           if not file_bytes:
               raise ValueError("EMPTY_FILE")
           text = None
           used_encoding = None
           for enc in ENCODING_FALLBACKS:
               try:
                   text = file_bytes.decode(enc)
                   used_encoding = enc
                   break
               except UnicodeDecodeError:
                   continue
           if text is None:
               raise ValueError("INVALID_ENCODING")
           
           sample = text[:8192]
           try:
               dialect = csv.Sniffer().sniff(sample, delimiters=",;\t|")
               delimiter = dialect.delimiter
           except csv.Error:
               delimiter = ","
           
           reader = csv.reader(io.StringIO(text), delimiter=delimiter)
           rows = list(reader)
           if not rows:
               raise ValueError("EMPTY_FILE")
           
           headers = [h.strip() for h in rows[0]]
           sample_rows = []
           for r in rows[1:max_rows + 1]:
               # Pad-out shorter rows
               row_padded = r + [""] * (len(headers) - len(r))
               sample_rows.append({
                   headers[i]: row_padded[i].strip() for i in range(len(headers))
               })
           
           return {
               "columns": headers,
               "sample_rows": sample_rows,
               "delimiter": delimiter,
               "encoding": used_encoding,
               "looks_like_no_header": _heuristic_no_header(headers),
           }
       
       
       def _heuristic_no_header(headers: list[str]) -> bool:
           """Если первая строка выглядит как массив телефонов — вероятно нет header'а."""
           if not headers:
               return False
           phone_like_count = sum(1 for h in headers if h and _PHONE_LIKE_RE.match(h))
           return phone_like_count == len([h for h in headers if h])
       
       
       def suggest_mapping(columns: list[str]) -> dict[str, str]:
           """Heuristic: column name → contact field. Returns {col_idx_str: field_name}.
           
           Field name = 'phone' | 'username' | 'full_name' | 'source' | 'custom.<key>'.
           Здесь мы возвращаем только канонические поля; `custom.<key>` юзер задаёт сам через UI.
           """
           result = {}
           for idx, col in enumerate(columns):
               col_norm = col.lower().strip()
               for field, aliases in _COLUMN_ALIASES.items():
                   if col_norm in aliases:
                       result[str(idx)] = field
                       break
           return result
       
       
       def apply_import(
           file_bytes: bytes,
           mapping: dict[str, str],
           delimiter: str = ",",
           encoding: str = "utf-8-sig",
       ) -> dict:
           """Применяет mapping к CSV — возвращает:
           {
             rows_to_insert: list[dict],   # с нормализованными phone, готово к INSERT
             skipped_invalid: int,
             skipped_invalid_reasons: list[dict],  # [{row: 3, reason: 'invalid_phone', value: '+7abc'}]
             total: int
           }
           
           Контракт: НЕ делает INSERT в БД (это делает роутер для атомарности с
           удалением CsvImport row). НЕ делает dedup-проверку (это тоже роутер
           через UNIQUE constraint на INSERT).
           
           Mapping: {col_idx: 'phone' | 'username' | 'full_name' | 'source' | 'custom.<key>'}.
           Если ни phone, ни username не замаплены — поднимаем ValueError("MAPPING_INVALID").
           """
           mapped_fields = set(mapping.values())
           # Хотя бы один из phone/username должен быть в mapping
           has_phone = "phone" in mapped_fields
           has_username = "username" in mapped_fields
           if not (has_phone or has_username):
               raise ValueError("MAPPING_INVALID: at least one of phone/username must be mapped")
           
           text = file_bytes.decode(encoding)
           reader = csv.reader(io.StringIO(text), delimiter=delimiter)
           rows = list(reader)
           if len(rows) < 2:
               return {
                   "rows_to_insert": [],
                   "skipped_invalid": 0,
                   "skipped_invalid_reasons": [],
                   "total": 0,
               }
           
           # rows[0] — header; пропускаем
           data_rows = rows[1:]
           rows_to_insert = []
           skipped_invalid_reasons = []
           
           for row_idx, raw_row in enumerate(data_rows, start=2):  # row_idx учитывает header как строку 1
               record = {"phone": None, "username": None, "full_name": None, "source": None, "custom": {}}
               for col_idx_str, field in mapping.items():
                   try:
                       col_idx = int(col_idx_str)
                   except ValueError:
                       continue
                   if col_idx >= len(raw_row):
                       continue
                   value = raw_row[col_idx].strip()
                   if not value:
                       continue
                   if field == "phone":
                       record["phone"] = value  # нормализуем ниже
                   elif field == "username":
                       record["username"] = value.lstrip("@")
                   elif field == "full_name":
                       record["full_name"] = value
                   elif field == "source":
                       record["source"] = value
                   elif field.startswith("custom."):
                       custom_key = field[len("custom."):]
                       record["custom"][custom_key] = value
               
               # Нормализация phone
               if record["phone"]:
                   normalized = normalize_to_e164(record["phone"])
                   if normalized is None:
                       skipped_invalid_reasons.append({
                           "row": row_idx,
                           "reason": "invalid_phone",
                           "value": record["phone"],
                       })
                       continue
                   record["phone"] = normalized
               
               # Проверка: хотя бы phone или username должен быть валиден
               if not record["phone"] and not record["username"]:
                   skipped_invalid_reasons.append({
                       "row": row_idx,
                       "reason": "no_phone_no_username",
                       "value": "",
                   })
                   continue
               
               rows_to_insert.append(record)
           
           return {
               "rows_to_insert": rows_to_insert,
               "skipped_invalid": len(skipped_invalid_reasons),
               "skipped_invalid_reasons": skipped_invalid_reasons,
               "total": len(data_rows),
           }
       ```
    2. Создать `tests/test_csv_import.py`:
       ```python
       """Unit-тесты для app/services/csv_import."""
       
       import pytest
       from app.services.csv_import import parse_preview, suggest_mapping, apply_import
       
       
       def test_parse_preview_basic_utf8():
           data = b"phone,name\n+79001234567,John\n+79009998877,Jane\n"
           result = parse_preview(data)
           assert result["columns"] == ["phone", "name"]
           assert result["delimiter"] == ","
           assert result["encoding"] == "utf-8-sig"
           assert len(result["sample_rows"]) == 2
           assert result["sample_rows"][0]["phone"] == "+79001234567"
       
       
       def test_parse_preview_strips_utf8_bom():
           data = "﻿phone,name\n+79001234567,John\n".encode("utf-8")
           result = parse_preview(data)
           # BOM strip'нут — columns начинаются корректно
           assert result["columns"][0] == "phone"
       
       
       def test_parse_preview_detects_semicolon_delimiter():
           data = b"phone;name\n+79001234567;John\n"
           result = parse_preview(data)
           assert result["delimiter"] == ";"
       
       
       def test_parse_preview_cp1251_fallback():
           # Russian Excel exports часто в cp1251
           data = "телефон,имя\n+79001234567,Иван\n".encode("cp1251")
           result = parse_preview(data)
           assert result["encoding"] == "cp1251"
           assert "телефон" in result["columns"]
       
       
       def test_parse_preview_empty_file_raises():
           with pytest.raises(ValueError, match="EMPTY_FILE"):
               parse_preview(b"")
       
       
       def test_suggest_mapping_matches_aliases():
           result = suggest_mapping(["phone", "имя", "source"])
           assert result == {"0": "phone", "1": "full_name", "2": "source"}
       
       
       def test_suggest_mapping_empty_for_unknown():
           result = suggest_mapping(["foo", "bar"])
           assert result == {}
       
       
       def test_apply_import_normalizes_phone():
           data = b"phone,name\n89001234567,John\n+79009998877,Jane\n"
           result = apply_import(data, mapping={"0": "phone", "1": "full_name"}, delimiter=",")
           assert result["total"] == 2
           assert result["skipped_invalid"] == 0
           assert result["rows_to_insert"][0]["phone"] == "+79001234567"  # 8 → +7
           assert result["rows_to_insert"][1]["full_name"] == "Jane"
       
       
       def test_apply_import_skips_invalid_phone():
           data = b"phone,name\n+7abc,John\n+79009998877,Jane\n"
           result = apply_import(data, mapping={"0": "phone", "1": "full_name"}, delimiter=",")
           assert result["total"] == 2
           assert result["skipped_invalid"] == 1
           assert result["skipped_invalid_reasons"][0]["reason"] == "invalid_phone"
           assert len(result["rows_to_insert"]) == 1
       
       
       def test_apply_import_skips_no_phone_no_username():
           data = b"phone,name\n,John\n+79009998877,Jane\n"
           result = apply_import(data, mapping={"0": "phone", "1": "full_name"}, delimiter=",")
           assert result["skipped_invalid"] == 1
           assert result["skipped_invalid_reasons"][0]["reason"] == "no_phone_no_username"
       
       
       def test_apply_import_mapping_invalid_raises():
           data = b"name,source\nJohn,referral\n"
           with pytest.raises(ValueError, match="MAPPING_INVALID"):
               apply_import(data, mapping={"0": "full_name", "1": "source"}, delimiter=",")
       
       
       def test_apply_import_custom_fields():
           data = b"phone,company\n+79001234567,Acme\n"
           result = apply_import(data, mapping={"0": "phone", "1": "custom.company"}, delimiter=",")
           assert result["rows_to_insert"][0]["custom"]["company"] == "Acme"
       
       
       def test_apply_import_username_strips_at():
           data = b"username\n@johndoe\n"
           result = apply_import(data, mapping={"0": "username"}, delimiter=",")
           assert result["rows_to_insert"][0]["username"] == "johndoe"
       ```
  </action>
  <verify>
    <automated>pytest tests/test_csv_import.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/csv_import.py` exists
    - `app/services/csv_import.py` contains `def parse_preview(` AND `def suggest_mapping(` AND `def apply_import(`
    - `app/services/csv_import.py` НЕ содержит `import pandas`
    - `app/services/csv_import.py` содержит `from app.utils.phone import normalize_to_e164`
    - `app/services/csv_import.py` содержит `MAPPING_INVALID`
    - `app/services/csv_import.py` содержит `ENCODING_FALLBACKS = ["utf-8-sig", "cp1251"]`
    - `tests/test_csv_import.py` exists AND содержит минимум 12 тестов
    - `pytest tests/test_csv_import.py -v` exits 0
  </acceptance_criteria>
  <done>CSV-сервис покрывает все 9 pitfall'ов из RESEARCH.md (BOM, delimiters, cp1251, кавычки, no-header heuristic).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: contacts router — CRUD, push, import/preview, import, move, recheck-stub + integration-тесты</name>
  <files>app/routers/contacts.py, app/main.py, tests/test_contacts.py</files>
  <read_first>
    - app/routers/workspace.py — Phase 1 паттерн AuthDep + dual-auth (JWT/API-key) на одном endpoint'е
    - app/routers/folders.py (после 02-03) — get_or_create_by_name helper
    - app/routers/check_contacts.py — Pydantic schemas стиль (для reference)
    - app/services/csv_import.py — parse_preview/apply_import (после Task 2)
    - app/utils/phone.py — normalize_to_e164 (после Task 1)
    - app/models/__init__.py — Contact, Folder, CsvImport, Sender (для has_checker)
    - app/schemas/__init__.py — все Pydantic схемы (после 02-02)
    - .planning/phases/02-tg-accounts-contacts/02-PATTERNS.md §"3. app/routers/contacts.py" — multipart upload pattern
    - .planning/phases/02-tg-accounts-contacts/02-CONTEXT.md D-07..D-10, D-19, D-20
    - .planning/phases/02-tg-accounts-contacts/02-RESEARCH.md §"Lovable / Frontend API Contract → Contacts" — endpoint matrix
  </read_first>
  <behavior>
    - Test 1 (push single): `POST /api/v1/contacts` с X-Workspace-Key + JSON `{phone, folder_name}` → 200 + ContactImportSummary{imported: 1}.
    - Test 2 (push batch): `POST /api/v1/contacts` с JWT + JSON `{contacts: [...]}` (3 элемента) → 200 + summary{imported: 3}.
    - Test 3 (push dedup): два одинаковых phone в batch → second skip'ается, summary{imported:1, skipped_duplicates:1}.
    - Test 4 (push folder_name auto-create): batch с folder_name='New' (которой нет) → папка создаётся, контакты привязаны.
    - Test 5 (push batch > 1000): 1001 контактов → 422 (Pydantic max_length).
    - Test 6 (push без phone и username): single `{full_name: 'X'}` → 422 (Pydantic model_validator).
    - Test 7 (preview): `POST /contacts/import/preview` с multipart CSV → 200 + {import_id, columns, sample_rows, suggested_mapping}; csv_imports row создан.
    - Test 8 (import): `POST /contacts/import` JSON с import_id + folder_name + mapping → 202 + {imported, skipped_duplicates, skipped_invalid, total}; csv_imports row удалён.
    - Test 9 (import expired): import_id с истёкшим expires_at → 404.
    - Test 10 (import mapping without phone/username): 422 MAPPING_INVALID.
    - Test 11 (list): `GET /contacts?folder_id=...` → массив ContactResponse только этого folder и workspace.
    - Test 12 (filter tg_status): `GET /contacts?tg_status=pending` → только pending.
    - Test 13 (move single): `POST /contacts/{id}/move {folder_id}` → 200, folder_id обновлён.
    - Test 14 (move batch): `POST /contacts/move {contact_ids, folder_id}` → 200 + {moved: N}.
    - Test 15 (cross-tenant): контакт workspace B недоступен из workspace A → 404.
    - Test 16 (tg_status='pending' default): после import контакты в БД с tg_status='pending'.
    - Test 17 (has_checker=false → unchecked): если workspace без checker'а — контакты получают tg_status='unchecked' (D-20).
    - Test 18 (push через X-Workspace-Key): полный flow с Workspace API ключом без JWT.
  </behavior>
  <action>
    1. Создать `app/routers/contacts.py`:
       ```python
       """Contacts router (Phase 2 — CONT-01, CONT-02, CONT-03, CONT-05, FLDR-03).
       
       Endpoints:
         GET    /api/v1/contacts?folder_id=&tg_status=&limit=&offset=  — list
         POST   /api/v1/contacts                                       — push (single/batch) для n8n
         POST   /api/v1/contacts/import/preview multipart              — CSV preview
         POST   /api/v1/contacts/import                                — apply CSV import
         POST   /api/v1/contacts/{id}/move                             — move to folder
         POST   /api/v1/contacts/move                                  — batch move
         DELETE /api/v1/contacts/{id}                                  — delete single
       
       Все endpoint'ы — workspace-scoped через Depends(auth_dep).
       Push (POST /contacts) работает и с JWT, и с X-Workspace-Key (D-10).
       """
       
       import logging
       from datetime import datetime, timezone
       from typing import List, Optional
       from uuid import UUID, uuid4
       
       from fastapi import APIRouter, Depends, HTTPException, UploadFile, File, Query
       from sqlalchemy import select, func as sql_func, text, delete as sa_delete
       from sqlalchemy.ext.asyncio import AsyncSession
       
       from app.database import get_db
       from app.models import Contact, Folder, CsvImport, Sender
       from app.schemas import (
           ContactCreate, ContactBatchPush, ContactResponse,
           ContactImportRequest, ContactImportPreviewResponse, ContactImportSummary,
           MoveContactRequest, MoveContactBatchRequest,
       )
       from app.utils.auth import AuthCtx, auth_dep
       from app.utils.phone import normalize_to_e164
       from app.services.csv_import import parse_preview, suggest_mapping, apply_import
       from app.routers.folders import get_or_create_by_name
       
       logger = logging.getLogger(__name__)
       router = APIRouter(prefix="/api/v1/contacts", tags=["contacts"])
       
       MAX_CSV_BYTES = 5 * 1024 * 1024  # 5 MB
       
       
       async def _resolve_folder_id(
           db: AsyncSession,
           workspace_id: UUID,
           folder_id: Optional[UUID],
           folder_name: Optional[str],
       ) -> UUID:
           """Возвращает folder_id для INSERT'а.
           - Если folder_id передан → проверяем что он workspace'ный, иначе 404.
           - Если folder_name → get_or_create_by_name (FLDR-03).
           """
           if folder_id:
               result = await db.execute(
                   select(Folder.id).where(
                       Folder.id == folder_id,
                       Folder.workspace_id == workspace_id,  # TODO(v2-rls)
                   )
               )
               row = result.scalar()
               if row is None:
                   raise HTTPException(404, {"code": "FOLDER_NOT_FOUND", "message": "Folder not found"})
               return row
           if folder_name:
               return await get_or_create_by_name(db, workspace_id, folder_name)
           raise HTTPException(400, {"code": "FOLDER_REQUIRED", "message": "Either folder_id or folder_name required"})
       
       
       async def _has_checker(db: AsyncSession, workspace_id: UUID) -> bool:
           """D-20: проверка наличия checker'а в workspace. Влияет на default tg_status."""
           result = await db.execute(
               select(sql_func.count(Sender.id)).where(
                   Sender.workspace_id == workspace_id,
                   Sender.role == "checker",
                   Sender.auth_status == "ok",
               )
           )
           return (result.scalar() or 0) > 0
       
       
       async def _insert_contacts_with_dedup(
           db: AsyncSession,
           workspace_id: UUID,
           folder_id: UUID,
           records: list[dict],
           has_checker: bool,
       ) -> dict:
           """Атомарный INSERT с dedup через partial UNIQUE на (workspace_id, phone)
           и (workspace_id, username). Использует ON CONFLICT DO NOTHING.
           
           D-19: tg_status='pending' если checker есть, иначе 'unchecked' (D-20).
           """
           default_tg_status = "pending" if has_checker else "unchecked"
           imported = 0
           skipped_duplicates = 0
           skipped_phones = []
           
           for rec in records:
               # Two INSERT-passes: один по phone, один по username — но мы делаем по записи
               # одним INSERT с ON CONFLICT на оба индекса.
               result = await db.execute(text("""
                   INSERT INTO contacts (workspace_id, folder_id, phone, username, full_name, source, custom, tg_status)
                   VALUES (:wid, :fid, :phone, :username, :full_name, :source, :custom::jsonb, :tg_status)
                   ON CONFLICT DO NOTHING
                   RETURNING id
               """), {
                   "wid": str(workspace_id),
                   "fid": str(folder_id),
                   "phone": rec.get("phone"),
                   "username": rec.get("username"),
                   "full_name": rec.get("full_name"),
                   "source": rec.get("source"),
                   "custom": __import__("json").dumps(rec.get("custom") or {}),
                   "tg_status": default_tg_status,
               })
               row = result.scalar()
               if row is None:
                   skipped_duplicates += 1
                   if rec.get("phone"):
                       skipped_phones.append(rec["phone"])
               else:
                   imported += 1
           
           await db.commit()
           return {
               "imported": imported,
               "skipped_duplicates": skipped_duplicates,
               "skipped_phones": skipped_phones,
           }
       
       
       # ─── GET /api/v1/contacts ────────────────────────────────────────────────
       @router.get("", response_model=List[ContactResponse])
       async def list_contacts(
           folder_id: Optional[UUID] = Query(None),
           tg_status: Optional[str] = Query(None),
           limit: int = Query(100, ge=1, le=500),
           offset: int = Query(0, ge=0),
           ctx: AuthCtx = Depends(auth_dep),
           db: AsyncSession = Depends(get_db),
       ):
           q = select(Contact).where(Contact.workspace_id == ctx.workspace_id)  # TODO(v2-rls)
           if folder_id is not None:
               q = q.where(Contact.folder_id == folder_id)
           if tg_status is not None:
               q = q.where(Contact.tg_status == tg_status)
           q = q.order_by(Contact.created_at.desc()).limit(limit).offset(offset)
           result = await db.execute(q)
           return [ContactResponse.model_validate(c) for c in result.scalars().all()]
       
       
       # ─── POST /api/v1/contacts (push — D-10) ─────────────────────────────────
       @router.post("", response_model=ContactImportSummary)
       async def push_contacts(
           payload: dict,  # принимаем raw dict, парсим как single или batch
           ctx: AuthCtx = Depends(auth_dep),
           db: AsyncSession = Depends(get_db),
       ):
           """Push single или batch (до 1000). Auth: JWT или X-Workspace-Key.
           
           Single:  {phone, username?, full_name?, source?, custom?, folder_id|folder_name}
           Batch:   {contacts: [{...}, ...]}
           """
           if "contacts" in payload:
               try:
                   batch = ContactBatchPush(contacts=payload["contacts"])
               except Exception as e:
                   raise HTTPException(422, {"code": "VALIDATION_ERROR", "message": str(e)})
               raw_records = [c.model_dump() for c in batch.contacts]
           else:
               try:
                   single = ContactCreate(**payload)
               except Exception as e:
                   raise HTTPException(422, {"code": "VALIDATION_ERROR", "message": str(e)})
               raw_records = [single.model_dump()]
           
           # Все контакты должны идти в одну папку — берём folder из первого
           first = raw_records[0]
           folder_id = await _resolve_folder_id(
               db, ctx.workspace_id,
               UUID(first["folder_id"]) if first.get("folder_id") else None,
               first.get("folder_name"),
           )
           
           has_checker = await _has_checker(db, ctx.workspace_id)
           
           # Нормализация phone + сбор skipped_invalid
           clean_records = []
           skipped_invalid = 0
           skipped_phones = []
           for rec in raw_records:
               if rec.get("phone"):
                   normalized = normalize_to_e164(rec["phone"])
                   if not normalized:
                       skipped_invalid += 1
                       skipped_phones.append(rec["phone"])
                       continue
                   rec["phone"] = normalized
               if rec.get("username"):
                   rec["username"] = rec["username"].lstrip("@")
               if not rec.get("phone") and not rec.get("username"):
                   skipped_invalid += 1
                   continue
               # удаляем folder поля — они уже разрешены в folder_id
               rec.pop("folder_id", None)
               rec.pop("folder_name", None)
               clean_records.append(rec)
           
           dedup_summary = await _insert_contacts_with_dedup(
               db, ctx.workspace_id, folder_id, clean_records, has_checker
           )
           
           logger.info(
               f"[contacts] push workspace={ctx.workspace_id} folder={folder_id} "
               f"source={ctx.source} total={len(raw_records)} "
               f"imported={dedup_summary['imported']} dup={dedup_summary['skipped_duplicates']} invalid={skipped_invalid}"
           )
           
           return ContactImportSummary(
               total=len(raw_records),
               imported=dedup_summary["imported"],
               skipped_duplicates=dedup_summary["skipped_duplicates"],
               skipped_invalid=skipped_invalid,
               skipped_phones=skipped_phones + dedup_summary["skipped_phones"],
           )
       
       
       # ─── POST /api/v1/contacts/import/preview (multipart) ─────────────────────
       @router.post("/import/preview", response_model=ContactImportPreviewResponse)
       async def import_preview(
           file: UploadFile = File(...),
           ctx: AuthCtx = Depends(auth_dep),
           db: AsyncSession = Depends(get_db),
       ):
           raw = await file.read()
           if len(raw) > MAX_CSV_BYTES:
               raise HTTPException(413, {"code": "FILE_TOO_LARGE", "message": f"Max {MAX_CSV_BYTES} bytes"})
           try:
               preview = parse_preview(raw)
           except ValueError as e:
               raise HTTPException(422, {"code": str(e), "message": str(e)})
           
           mapping = suggest_mapping(preview["columns"])
           
           # Сохраняем в csv_imports BYTEA (Option B, RESEARCH §CSV Import Storage)
           imp = CsvImport(
               id=uuid4(),
               workspace_id=ctx.workspace_id,
               file_data=raw,
               columns=preview["columns"],
               suggested_mapping=mapping,
               encoding=preview["encoding"],
               delimiter=preview["delimiter"],
               expires_at=datetime.now(timezone.utc).replace(microsecond=0),  # обновится server_default
           )
           # expires_at пусть выставит DB через server_default INTERVAL '30 minutes'
           db.add(imp)
           await db.commit()
           await db.refresh(imp)
           
           return ContactImportPreviewResponse(
               import_id=imp.id,
               columns=preview["columns"],
               sample_rows=preview["sample_rows"],
               suggested_mapping=mapping,
               encoding=preview["encoding"],
               delimiter=preview["delimiter"],
               looks_like_no_header=preview["looks_like_no_header"],
           )
       
       
       # ─── POST /api/v1/contacts/import ────────────────────────────────────────
       @router.post("/import", response_model=ContactImportSummary, status_code=202)
       async def import_contacts(
           payload: ContactImportRequest,
           ctx: AuthCtx = Depends(auth_dep),
           db: AsyncSession = Depends(get_db),
       ):
           # Загрузим csv_imports row workspace-isolated
           result = await db.execute(
               select(CsvImport).where(
                   CsvImport.id == payload.import_id,
                   CsvImport.workspace_id == ctx.workspace_id,  # TODO(v2-rls)
               )
           )
           imp = result.scalars().first()
           if imp is None:
               raise HTTPException(404, {"code": "IMPORT_NOT_FOUND", "message": "Import session not found or expired"})
           if imp.expires_at < datetime.now(timezone.utc):
               raise HTTPException(410, {"code": "IMPORT_EXPIRED", "message": "Import session expired"})
           
           # Resolve folder
           folder_id = await _resolve_folder_id(db, ctx.workspace_id, payload.folder_id, payload.folder_name)
           
           # Apply mapping
           try:
               applied = apply_import(
                   imp.file_data,
                   mapping=payload.mapping,
                   delimiter=imp.delimiter or ",",
                   encoding=imp.encoding or "utf-8-sig",
               )
           except ValueError as e:
               raise HTTPException(422, {"code": "MAPPING_INVALID", "message": str(e)})
           
           has_checker = await _has_checker(db, ctx.workspace_id)
           dedup_summary = await _insert_contacts_with_dedup(
               db, ctx.workspace_id, folder_id, applied["rows_to_insert"], has_checker
           )
           
           # Удаляем CsvImport row — больше не нужен
           await db.execute(sa_delete(CsvImport).where(CsvImport.id == payload.import_id))
           await db.commit()
           
           logger.info(
               f"[contacts] import workspace={ctx.workspace_id} folder={folder_id} "
               f"total={applied['total']} imported={dedup_summary['imported']} "
               f"dup={dedup_summary['skipped_duplicates']} invalid={applied['skipped_invalid']}"
           )
           
           return ContactImportSummary(
               total=applied["total"],
               imported=dedup_summary["imported"],
               skipped_duplicates=dedup_summary["skipped_duplicates"],
               skipped_invalid=applied["skipped_invalid"],
               skipped_phones=dedup_summary["skipped_phones"],
           )
       
       
       # ─── POST /api/v1/contacts/{id}/move ─────────────────────────────────────
       @router.post("/{contact_id}/move", response_model=ContactResponse)
       async def move_contact(
           contact_id: UUID,
           payload: MoveContactRequest,
           ctx: AuthCtx = Depends(auth_dep),
           db: AsyncSession = Depends(get_db),
       ):
           # Проверка контакта (workspace-isolated)
           result = await db.execute(
               select(Contact).where(
                   Contact.id == contact_id,
                   Contact.workspace_id == ctx.workspace_id,
               )
           )
           contact = result.scalars().first()
           if contact is None:
               raise HTTPException(404, {"code": "CONTACT_NOT_FOUND", "message": "Contact not found"})
           # Проверка папки (workspace-isolated)
           folder_result = await db.execute(
               select(Folder.id).where(
                   Folder.id == payload.folder_id,
                   Folder.workspace_id == ctx.workspace_id,
               )
           )
           if folder_result.scalar() is None:
               raise HTTPException(404, {"code": "FOLDER_NOT_FOUND", "message": "Folder not found"})
           contact.folder_id = payload.folder_id
           await db.commit()
           await db.refresh(contact)
           return ContactResponse.model_validate(contact)
       
       
       @router.post("/move", response_model=dict)
       async def move_contacts_batch(
           payload: MoveContactBatchRequest,
           ctx: AuthCtx = Depends(auth_dep),
           db: AsyncSession = Depends(get_db),
       ):
           # Проверка folder
           folder_result = await db.execute(
               select(Folder.id).where(
                   Folder.id == payload.folder_id,
                   Folder.workspace_id == ctx.workspace_id,
               )
           )
           if folder_result.scalar() is None:
               raise HTTPException(404, {"code": "FOLDER_NOT_FOUND", "message": "Folder not found"})
           # Batch update (workspace-isolated)
           result = await db.execute(text("""
               UPDATE contacts SET folder_id = :fid, updated_at = NOW()
               WHERE id = ANY(:ids) AND workspace_id = :wid
           """), {
               "fid": str(payload.folder_id),
               "ids": [str(cid) for cid in payload.contact_ids],
               "wid": str(ctx.workspace_id),
           })
           await db.commit()
           return {"moved": result.rowcount}
       
       
       @router.delete("/{contact_id}", status_code=204)
       async def delete_contact(
           contact_id: UUID,
           ctx: AuthCtx = Depends(auth_dep),
           db: AsyncSession = Depends(get_db),
       ):
           result = await db.execute(
               select(Contact).where(
                   Contact.id == contact_id,
                   Contact.workspace_id == ctx.workspace_id,
               )
           )
           contact = result.scalars().first()
           if contact is None:
               raise HTTPException(404, {"code": "CONTACT_NOT_FOUND", "message": "Contact not found"})
           await db.delete(contact)
           await db.commit()
           return None
       ```
    
    2. В `app/main.py`:
       - Добавить `contacts` в импорт `from app.routers import ...`.
       - Добавить `app.include_router(contacts.router)` после `folders.router`.
    
    3. Создать `tests/test_contacts.py` — 18 integration-тестов из секции `<behavior>`. Используем фабрики из conftest.py (`test_workspace`, `test_folder`, `test_sender_factory`, `test_checker`, `valid_supabase_jwt`).
       
       Структура (для каждого теста):
       - Получить JWT для нового workspace через valid_supabase_jwt(sub=unique-sub).
       - Возможно создать folder через POST /folders.
       - Сделать запрос к /contacts endpoint.
       - Проверить status_code и response body.
       - Для test_push_workspace_api_key — сначала создать ключ через POST /workspace/api-keys, потом использовать его в X-Workspace-Key header.
       
       Для test_import_with_unchecked_status (D-20): создаём workspace **без** checker'а → импорт даёт `tg_status='unchecked'`. Затем создаём checker через test_sender_factory(role='checker') → следующий импорт даёт `tg_status='pending'`.
       
       Для test_dedup: два push'а с одинаковым phone → second skip'ается (`skipped_duplicates: 1`).
       
       Для test_cross_tenant: контакт workspace B недоступен из workspace A → 404.
  </action>
  <verify>
    <automated>pytest tests/test_contacts.py tests/test_csv_import.py tests/test_phone_normalization.py tests/test_folders.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/contacts.py` exists
    - `app/routers/contacts.py` содержит `router = APIRouter(prefix="/api/v1/contacts"`
    - `app/routers/contacts.py` содержит `UploadFile = File(...)`
    - `app/routers/contacts.py` содержит `from app.utils.phone import normalize_to_e164`
    - `app/routers/contacts.py` содержит `from app.services.csv_import import parse_preview, suggest_mapping, apply_import`
    - `app/routers/contacts.py` содержит `from app.routers.folders import get_or_create_by_name`
    - `app/routers/contacts.py` содержит `Contact.workspace_id == ctx.workspace_id` минимум 4 раза
    - `app/routers/contacts.py` содержит `status_code=202` (для POST /import)
    - `app/routers/contacts.py` содержит `_has_checker` (D-20 check)
    - `app/routers/contacts.py` содержит `"unchecked"` (default tg_status если нет checker'а)
    - `app/routers/contacts.py` содержит `ON CONFLICT DO NOTHING` (dedup)
    - `app/main.py` содержит `app.include_router(contacts.router)`
    - `tests/test_contacts.py` содержит минимум 15 async def test_*
    - `pytest tests/test_contacts.py tests/test_csv_import.py tests/test_phone_normalization.py -v` exits 0
    - `python -c "import app.main"` exits 0
  </acceptance_criteria>
  <done>
    Contacts router работает workspace-scoped, CSV import + push API закрывают CONT-01/02/03/05 + FLDR-03; D-20 (has_checker) реализован; интеграционные тесты зелёные.
  </done>
</task>

</tasks>

<verification>
**Automated:**
1. `pytest tests/test_phone_normalization.py tests/test_csv_import.py tests/test_contacts.py -v` — все unit + integration зелёные.
2. `python -c "from app.utils.phone import normalize_to_e164; from app.services.csv_import import parse_preview; from app.routers.contacts import router"` — exit 0.
3. `grep -c "Depends(auth_dep)" app/routers/contacts.py` ≥ 7.
4. `grep "include_router(contacts" app/main.py` — match.
5. `python -c "import app.main"` — exit 0 (вся цепочка импортов работает).

**Manual smoke:**
1. `curl -X POST -H "Authorization: Bearer $JWT" -F "file=@test.csv" http://localhost:8000/api/v1/contacts/import/preview` — 200.
2. `curl -X POST -H "X-Workspace-Key: $WSK" -H "Content-Type: application/json" -d '{"phone": "+79001234567", "folder_name": "Test"}' http://localhost:8000/api/v1/contacts` — 200 + summary.
</verification>

<success_criteria>
- Phone normalization (E.164 + RU heuristic) работает для всех 14 тест-кейсов.
- CSV import (parse_preview + apply_import) покрывает BOM/cp1251/semicolon/quote pitfall'ы.
- Contacts router работает workspace-scoped, push (D-10) принимает JWT и X-Workspace-Key, batch до 1000, dedup через ON CONFLICT.
- FLDR-03 auto-create при импорте/push работает через `folder_name` (без folder_id) — переиспользует helper из 02-03.
- D-20 `has_checker` check → контакты получают `tg_status='unchecked'` если в workspace нет checker'а.
- D-19 async pipeline: import возвращает 202 Accepted, контакты с `tg_status='pending'` (`ContactCheckWorker` подберёт в plan 02-05).
- CONT-01 (CSV import), CONT-02 (workspace-isolated), CONT-03 (push API), CONT-05 (поля phone/username/full_name/source/custom JSONB) закрыты.
- CONT-04 (TG-проверка) — в plan 02-05 (`ContactCheckWorker` + `POST /contacts/recheck` + `has_checker` exposure в GET /workspace).
- 18+ integration-тестов зелёные.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tg-accounts-contacts/02-04-SUMMARY.md` per template — отметить:
- Endpoint матрица (Method / Path / Auth / Notes / Status)
- Точный flow двух-шагового CSV-импорта (preview saves to BYTEA → import reads + deletes)
- Контракт dedup (ON CONFLICT на partial UNIQUE индексах)
- D-20 поведение: `tg_status='unchecked'` если нет checker'а; план 02-05 расширяет `_has_checker` exposure в GET /workspace
- FLDR-03 реализован через переиспользование `get_or_create_by_name` из 02-03
- Готовность к plan 02-05: контакты с `tg_status='pending'` уже лежат, worker сможет их обрабатывать
</output>
