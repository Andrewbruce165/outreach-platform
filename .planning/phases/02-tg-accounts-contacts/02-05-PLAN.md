---
plan_id: "02-05"
title: "ContactCheckWorker + recheck endpoint + has_checker exposure: async pipeline TG presence check"
phase: "02-tg-accounts-contacts"
plan: 5
type: execute
wave: 3
depends_on: ["02-04", "02-01"]
files_modified:
  - "app/services/contact_check_worker.py"
  - "app/routers/check_contacts.py"
  - "app/routers/workspace.py"
  - "app/main.py"
  - "tests/test_contact_check_worker.py"
  - "tests/test_check_contacts.py"
autonomous: true
requirements: ["CONT-04"]
must_haves:
  truths:
    - "ContactCheckWorker как singleton-task в lifespan API-контейнера; cycle: SELECT pending → batch ResolvePhone через checker → UPDATE tg_status"
    - "Worker берёт контактов с tg_status='pending' AND phone IS NOT NULL и резолвит через workspace checker (role='checker', auth_status='ok')"
    - "Worker уважает FloodWait checker'а через переиспользование CheckerService"
    - "POST /api/v1/contacts/recheck {contact_ids? | folder_id?} → batch UPDATE tg_status='pending' → worker подберёт"
    - "POST /api/v1/contacts/recheck возвращает 202 + {marked_pending: N}"
    - "GET /api/v1/workspace возвращает поле has_checker: bool"
    - "Контакты в workspace без checker'а получают tg_status='unchecked' (план 02-04 D-20); worker пропускает (нет checker → нечем резолвить)"
    - "После успешного resolve контакт имеет tg_telegram_id заполненный и tg_status='registered' либо 'not_registered'"
  artifacts:
    - path: "app/services/contact_check_worker.py"
      provides: "Background worker: poll DB pending contacts → batch resolve → update tg_status"
      contains: "class ContactCheckWorker; contact_check_worker = ContactCheckWorker()"
    - path: "app/routers/check_contacts.py"
      provides: "POST /api/v1/contacts/recheck + (legacy) ad-hoc check endpoint workspace-scoped"
      contains: "Depends(auth_dep); RecheckRequest; tg_status = 'pending'"
    - path: "tests/test_contact_check_worker.py"
      provides: "Unit-тесты worker._tick: pending → registered/not_registered, нет checker → skip"
    - path: "tests/test_check_contacts.py"
      provides: "Integration-тесты recheck endpoint + has_checker в GET /workspace"
  key_links:
    - from: "app/main.py"
      to: "app/services/contact_check_worker.py::contact_check_worker"
      via: "lifespan start/stop"
      pattern: "contact_check_worker"
    - from: "app/services/contact_check_worker.py"
      to: "app/services/checker.py::checker_service.check_phones"
      via: "переиспользование (lock per checker_slug + FloodWait handling)"
      pattern: "checker_service\\.check_phones"
    - from: "app/routers/check_contacts.py::recheck"
      to: "contacts table"
      via: "UPDATE tg_status='pending'"
      pattern: "tg_status = 'pending'"
    - from: "app/routers/workspace.py::get_workspace"
      to: "senders.role='checker'"
      via: "EXISTS query для has_checker"
      pattern: "has_checker"
---

<objective>
Реализовать async pipeline TG-проверки контактов через checker-аккаунт (D-19, D-20). Это последняя задача Phase 2 — закрывает CONT-04.

Компоненты:
1. **`app/services/contact_check_worker.py`** — singleton background worker по паттерну `QueueWorker`/`WarmupWorker`. Каждые 5 сек: `SELECT pending contacts JOIN LATERAL checker FROM senders WHERE role='checker'` → батч phones → `checker_service.check_phones(...)` (переиспользует existing CheckerService с lock + FloodWait + polite delay 2-3.5s) → UPDATE `tg_status='registered'|'not_registered'|'error'` + `tg_telegram_id` + `tg_checked_at`.

2. **`app/routers/check_contacts.py`** — переписан полностью workspace-scoped (текущий имеет broken import `from app.routers.auth import verify_api_key`). Endpoint `POST /api/v1/contacts/recheck` принимает `{contact_ids?, folder_id?}` → batch UPDATE `tg_status='pending'` (worker подберёт).

3. **`app/routers/workspace.py`** — расширение `GET /api/v1/workspace` добавлением поля `has_checker: bool` (D-20). UI рендерит баннер "Add a dedicated checker account" если false.

4. **Lifespan** в `app/main.py` запускает `contact_check_worker.start()` / `await contact_check_worker.stop()`.

Purpose: CONT-04 — последний открытый requirement Phase 2. После этого Phase 2 закрыт полностью.

Output: Worker, recheck endpoint, has_checker exposure, unit + integration тесты.
</objective>

<execution_context>
@/Users/andrewbruce/Documents/outreach-platform/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewbruce/Documents/outreach-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-tg-accounts-contacts/02-CONTEXT.md
@.planning/phases/02-tg-accounts-contacts/02-RESEARCH.md
@.planning/phases/02-tg-accounts-contacts/02-PATTERNS.md
@.planning/phases/02-tg-accounts-contacts/02-02-PLAN.md
@.planning/phases/02-tg-accounts-contacts/02-04-PLAN.md
@.planning/phases/02-tg-accounts-contacts/02-01-PLAN.md
@.planning/codebase/CONVENTIONS.md
@app/routers/workspace.py
@app/routers/check_contacts.py
@app/services/queue.py
@app/services/warmup.py
@app/services/checker.py
@app/utils/auth.py
@app/models/__init__.py
@app/schemas/__init__.py
@app/main.py
@tests/conftest.py

<interfaces>
<!-- Contract'ы после планов 02-02, 02-04, 02-01. -->

From app/services/checker.py (существующий, переиспользуем):
```python
class CheckerService:
    async def check_phones(
        self,
        checker_id: str,
        checker_slug: str,
        encrypted_session: str,
        phones: list[str],
        proxy: dict | None,
    ) -> dict:
        """Возвращает {
          checked: int, registered: int, not_registered: int,
          flood_wait_hit: bool,
          results: [{phone, is_registered, telegram_id?, error?}]
        }
        Lock per checker_slug, FloodWait handling, polite delay 2-3.5s между phones."""
        ...

checker_service = CheckerService()
```

From app/models/__init__.py (после 02-02):
```python
class Contact(Base):
    tg_status: str          # 'pending' | 'registered' | 'not_registered' | 'error' | 'unchecked'
    tg_telegram_id: Optional[int]
    tg_username_resolved: Optional[str]
    tg_error: Optional[str]
    tg_checked_at: Optional[datetime]

class Sender(Base):
    role: str               # 'sender' | 'checker'
    auth_status: str        # 'ok' | 'session_expired' | ...
    session_string: str     # encrypted
    proxy: Optional[dict]
    rate_per_min/hour/day: int
```

From app/schemas/__init__.py (после 02-02):
```python
class RecheckRequest(BaseModel):
    contact_ids: Optional[list[UUID]] = None
    folder_id: Optional[UUID] = None
    @model_validator(mode="after")
    def one_required(self): ...
```

From app/main.py (после 02-04, 02-01):
```python
# lifespan уже запускает:
queue_worker.start()
warmup_worker.start()
onboarding_cleanup_worker.start()
# нужно добавить:
contact_check_worker.start()
```

From app/routers/workspace.py (Phase 1, расширяем):
```python
class WorkspaceResponse(BaseModel):
    id: UUID
    name: str
    # ... existing fields
    # NEW: has_checker: bool
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ContactCheckWorker service + lifespan registration</name>
  <files>app/services/contact_check_worker.py, app/main.py, tests/test_contact_check_worker.py</files>
  <read_first>
    - app/services/queue.py — QueueWorker class (start/stop, _run, asyncio.create_task) — копируем структуру
    - app/services/warmup.py — WarmupWorker._run с CancelledError handling (lines 120-129)
    - app/services/checker.py — CheckerService.check_phones signature + behaviour (lock per slug, FloodWait, polite delay)
    - app/models/__init__.py — Contact, Sender (после 02-02)
    - app/database.py — AsyncSessionLocal
    - .planning/phases/02-tg-accounts-contacts/02-PATTERNS.md §"6. app/services/contact_check_worker.py" — полный skeleton
    - .planning/phases/02-tg-accounts-contacts/02-RESEARCH.md §"ContactCheckWorker — стратегия rate-limit и параллельность" — batch_size=5, poll_interval=5, reuse CheckerService
    - .planning/phases/02-tg-accounts-contacts/02-CONTEXT.md D-19, D-20, C-06 (env var)
  </read_first>
  <behavior>
    - Test 1: `contact_check_worker.start()` создаёт asyncio.Task; `stop()` отменяет gracefully (CancelledError).
    - Test 2 (no pending): пустая БД pending → tick'нем → нет вызовов checker_service.check_phones; не падает.
    - Test 3 (pending без checker): контакт с tg_status='pending', но в workspace нет sender role='checker' → tick → контакт остаётся 'pending' (worker SKIP, нет JOIN match).
    - Test 4 (pending с checker): создаём checker через test_sender_factory(role='checker'). monkeypatch'аем `checker_service.check_phones` чтобы возвращал `{checked: 1, registered: 1, not_registered: 0, flood_wait_hit: False, results: [{phone: '+79001234567', is_registered: True, telegram_id: 123}]}`. После tick → контакт имеет tg_status='registered', tg_telegram_id=123, tg_checked_at заполнен.
    - Test 5 (not registered): mock возвращает `is_registered=False` → tg_status='not_registered'.
    - Test 6 (FloodWait): mock возвращает `flood_wait_hit=True` + partial results → обработанные обновляются, остальные остаются 'pending'.
    - Test 7 (worker batch): 3 pending контакта одного workspace + один checker → один батч-вызов check_phones (не 3 отдельных).
    - Test 8 (workspace isolation): pending контакт в workspace A; checker в workspace B → НЕ обрабатывается (JOIN LATERAL по `s.workspace_id = c.workspace_id`).
  </behavior>
  <action>
    1. Создать `app/services/contact_check_worker.py`:
       ```python
       """ContactCheckWorker (Phase 2 — CONT-04, D-19).
       
       Background asyncio task в lifespan API-контейнера:
       - SELECT pending contacts с workspace's active checker
       - Батч → checker_service.check_phones (lock per checker_slug, FloodWait, polite delay)
       - UPDATE tg_status='registered'|'not_registered'|'error' + tg_telegram_id + tg_checked_at
       
       D-20: контакты в workspace без checker'а имеют tg_status='unchecked' — JOIN LATERAL
       по role='checker' их пропускает (нечем резолвить).
       """
       
       import asyncio
       import json
       import logging
       import os
       from datetime import datetime, timezone
       from itertools import groupby
       from typing import Optional
       
       from sqlalchemy import text
       
       from app.database import AsyncSessionLocal
       from app.services.checker import checker_service
       
       logger = logging.getLogger(__name__)
       
       CONTACT_CHECK_BATCH_SIZE = int(os.environ.get("CONTACT_CHECK_BATCH_SIZE", "5"))
       CONTACT_CHECK_POLL_INTERVAL = int(os.environ.get("CONTACT_CHECK_POLL_INTERVAL", "5"))
       
       
       class ContactCheckWorker:
           """Background worker: poll pending contacts → batch resolve via checker."""
           
           def __init__(self):
               self._task: Optional[asyncio.Task] = None
               self._running = False
               self.batch_size = CONTACT_CHECK_BATCH_SIZE
               self.poll_interval = CONTACT_CHECK_POLL_INTERVAL
           
           def start(self):
               if self._task is None or self._task.done():
                   self._running = True
                   self._task = asyncio.create_task(self._run(), name="contact-check-worker")
                   logger.info(
                       f"📋 ContactCheckWorker started (batch={self.batch_size}, poll={self.poll_interval}s)"
                   )
           
           async def stop(self):
               self._running = False
               if self._task and not self._task.done():
                   self._task.cancel()
                   try:
                       await self._task
                   except asyncio.CancelledError:
                       pass
               logger.info("📋 ContactCheckWorker stopped")
           
           async def _run(self):
               while self._running:
                   try:
                       await self._tick()
                   except asyncio.CancelledError:
                       break
                   except Exception as e:
                       logger.error(f"❌ ContactCheckWorker error: {e}", exc_info=True)
                   await asyncio.sleep(self.poll_interval)
           
           async def _tick(self):
               """Один tick: подобрать pending → resolve → update."""
               async with AsyncSessionLocal() as db:
                   result = await db.execute(text("""
                       SELECT c.id AS contact_id, c.workspace_id, c.phone, c.username,
                              s.id AS checker_id, s.slug AS checker_slug,
                              s.session_string, s.proxy
                       FROM contacts c
                       JOIN LATERAL (
                           SELECT id, slug, session_string, proxy
                           FROM senders
                           WHERE workspace_id = c.workspace_id
                             AND role = 'checker'
                             AND auth_status = 'ok'
                           LIMIT 1
                       ) s ON TRUE
                       WHERE c.tg_status = 'pending'
                         AND c.phone IS NOT NULL
                       ORDER BY c.created_at ASC
                       LIMIT :n
                   """), {"n": self.batch_size})
                   rows = result.fetchall()
               
               if not rows:
                   return
               
               # Группируем по checker_id (обычно один checker per workspace в v1)
               rows_sorted = sorted(rows, key=lambda r: str(r.checker_id))
               for checker_id, items_iter in groupby(rows_sorted, key=lambda r: r.checker_id):
                   items = list(items_iter)
                   phones = [r.phone for r in items if r.phone]
                   if not phones:
                       continue
                   first = items[0]
                   
                   try:
                       summary = await checker_service.check_phones(
                           checker_id=str(checker_id),
                           checker_slug=first.checker_slug,
                           encrypted_session=first.session_string,
                           phones=phones,
                           proxy=first.proxy,
                       )
                   except Exception as exc:
                       logger.error(
                           f"❌ ContactCheckWorker: checker {first.checker_slug} failed: {exc}",
                           exc_info=True,
                       )
                       continue
                   
                   # Применяем результаты
                   await self._apply_results(items, summary)
                   
                   logger.info(
                       f"📋 ContactCheckWorker: checker={first.checker_slug} "
                       f"checked={summary.get('checked', 0)} "
                       f"reg={summary.get('registered', 0)} "
                       f"not_reg={summary.get('not_registered', 0)} "
                       f"flood={summary.get('flood_wait_hit', False)}"
                   )
           
           async def _apply_results(self, items: list, summary: dict):
               """UPDATE contacts по результатам checker'а.
               
               summary['results']: list of {phone, is_registered, telegram_id?, error?}.
               Сматчиваем по phone (E.164 normalized — уже в БД).
               Если phone отсутствует в results — оставляем 'pending' (FloodWait partial).
               """
               results_by_phone = {r["phone"]: r for r in summary.get("results", [])}
               async with AsyncSessionLocal() as db:
                   for item in items:
                       res = results_by_phone.get(item.phone)
                       if res is None:
                           # Не обработан (partial из-за FloodWait) — оставляем pending
                           continue
                       if res.get("error"):
                           await db.execute(text("""
                               UPDATE contacts
                               SET tg_status = 'error',
                                   tg_error = :err,
                                   tg_checked_at = NOW(),
                                   updated_at = NOW()
                               WHERE id = :cid
                           """), {"err": str(res["error"])[:500], "cid": str(item.contact_id)})
                       elif res.get("is_registered"):
                           await db.execute(text("""
                               UPDATE contacts
                               SET tg_status = 'registered',
                                   tg_telegram_id = :tid,
                                   tg_username_resolved = :uname,
                                   tg_checked_at = NOW(),
                                   updated_at = NOW()
                               WHERE id = :cid
                           """), {
                               "tid": res.get("telegram_id"),
                               "uname": res.get("username"),
                               "cid": str(item.contact_id),
                           })
                       else:
                           await db.execute(text("""
                               UPDATE contacts
                               SET tg_status = 'not_registered',
                                   tg_checked_at = NOW(),
                                   updated_at = NOW()
                               WHERE id = :cid
                           """), {"cid": str(item.contact_id)})
                   await db.commit()
       
       
       # Module-level singleton
       contact_check_worker = ContactCheckWorker()
       ```
    
    2. В `app/main.py`:
       - Импорт: `from app.services.contact_check_worker import contact_check_worker`
       - В lifespan startup (после `onboarding_cleanup_worker.start()`): `contact_check_worker.start()`
       - В shutdown (до `await onboarding_cleanup_worker.stop()`): `await contact_check_worker.stop()`
    
    3. Создать `tests/test_contact_check_worker.py`:
       ```python
       """Unit-тесты для ContactCheckWorker._tick."""
       
       import pytest
       from unittest.mock import AsyncMock, patch
       from sqlalchemy import text
       
       from app.services.contact_check_worker import ContactCheckWorker
       
       pytestmark = pytest.mark.asyncio
       
       
       async def test_tick_no_pending_returns_early(async_db_session, test_workspace, test_checker):
           """Пустая БД pending → tick без вызовов check_phones."""
           worker = ContactCheckWorker()
           with patch("app.services.contact_check_worker.checker_service.check_phones",
                      new=AsyncMock()) as mock:
               await worker._tick()
               mock.assert_not_awaited()
       
       
       async def test_tick_skips_when_no_checker(async_db_session, test_workspace, test_contacts_factory):
           """Контакт с tg_status='pending', но в workspace нет checker'а → skip."""
           await test_contacts_factory(count=2, tg_status="pending")
           worker = ContactCheckWorker()
           with patch("app.services.contact_check_worker.checker_service.check_phones",
                      new=AsyncMock()) as mock:
               await worker._tick()
               mock.assert_not_awaited()
       
       
       async def test_tick_resolves_pending_to_registered(async_db_session, test_workspace, test_checker, test_contacts_factory):
           contacts = await test_contacts_factory(count=2, tg_status="pending")
           worker = ContactCheckWorker()
           
           fake_summary = {
               "checked": 2,
               "registered": 2,
               "not_registered": 0,
               "flood_wait_hit": False,
               "results": [
                   {"phone": contacts[0].phone, "is_registered": True, "telegram_id": 111},
                   {"phone": contacts[1].phone, "is_registered": True, "telegram_id": 222},
               ],
           }
           with patch("app.services.contact_check_worker.checker_service.check_phones",
                      new=AsyncMock(return_value=fake_summary)):
               await worker._tick()
           
           # Проверяем UPDATE через свежий SELECT
           result = await async_db_session.execute(text(
               "SELECT id, tg_status, tg_telegram_id FROM contacts WHERE id = ANY(:ids) ORDER BY phone"
           ), {"ids": [c.id for c in contacts]})
           rows = result.fetchall()
           assert all(r.tg_status == "registered" for r in rows)
           tg_ids = {r.tg_telegram_id for r in rows}
           assert tg_ids == {111, 222}
       
       
       async def test_tick_marks_not_registered(async_db_session, test_workspace, test_checker, test_contacts_factory):
           contact = await test_contacts_factory(count=1, tg_status="pending")
           worker = ContactCheckWorker()
           fake_summary = {
               "checked": 1,
               "registered": 0,
               "not_registered": 1,
               "flood_wait_hit": False,
               "results": [{"phone": contact.phone, "is_registered": False}],
           }
           with patch("app.services.contact_check_worker.checker_service.check_phones",
                      new=AsyncMock(return_value=fake_summary)):
               await worker._tick()
           
           result = await async_db_session.execute(text(
               "SELECT tg_status FROM contacts WHERE id = :cid"
           ), {"cid": contact.id})
           assert result.scalar() == "not_registered"
       
       
       async def test_tick_floodwait_partial(async_db_session, test_workspace, test_checker, test_contacts_factory):
           """FloodWait: только часть phones обработана, остальные → pending."""
           contacts = await test_contacts_factory(count=3, tg_status="pending")
           worker = ContactCheckWorker()
           # Только первый обработан, остальные пропущены
           fake_summary = {
               "checked": 1,
               "registered": 1,
               "not_registered": 0,
               "flood_wait_hit": True,
               "results": [{"phone": contacts[0].phone, "is_registered": True, "telegram_id": 555}],
           }
           with patch("app.services.contact_check_worker.checker_service.check_phones",
                      new=AsyncMock(return_value=fake_summary)):
               await worker._tick()
           
           result = await async_db_session.execute(text(
               "SELECT id, tg_status FROM contacts WHERE id = ANY(:ids)"
           ), {"ids": [c.id for c in contacts]})
           statuses = {r.id: r.tg_status for r in result.fetchall()}
           assert statuses[contacts[0].id] == "registered"
           assert statuses[contacts[1].id] == "pending"
           assert statuses[contacts[2].id] == "pending"
       
       
       async def test_tick_marks_error(async_db_session, test_workspace, test_checker, test_contacts_factory):
           contact = await test_contacts_factory(count=1, tg_status="pending")
           worker = ContactCheckWorker()
           fake_summary = {
               "checked": 1,
               "registered": 0,
               "not_registered": 0,
               "flood_wait_hit": False,
               "results": [{"phone": contact.phone, "is_registered": False, "error": "PHONE_NOT_OCCUPIED"}],
           }
           with patch("app.services.contact_check_worker.checker_service.check_phones",
                      new=AsyncMock(return_value=fake_summary)):
               await worker._tick()
           result = await async_db_session.execute(text(
               "SELECT tg_status, tg_error FROM contacts WHERE id = :cid"
           ), {"cid": contact.id})
           row = result.fetchone()
           assert row.tg_status == "error"
           assert "PHONE_NOT_OCCUPIED" in (row.tg_error or "")
       
       
       async def test_start_stop_idempotent():
           worker = ContactCheckWorker()
           worker.start()
           assert worker._task is not None
           await worker.stop()
           # stop повторно безопасно
           await worker.stop()
       ```
  </action>
  <verify>
    <automated>pytest tests/test_contact_check_worker.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/contact_check_worker.py` exists
    - `app/services/contact_check_worker.py` содержит `class ContactCheckWorker:`
    - `app/services/contact_check_worker.py` содержит `contact_check_worker = ContactCheckWorker()`
    - `app/services/contact_check_worker.py` содержит `from app.services.checker import checker_service`
    - `app/services/contact_check_worker.py` содержит `JOIN LATERAL` (SQL для workspace-isolated checker lookup)
    - `app/services/contact_check_worker.py` содержит `WHERE c.tg_status = 'pending'`
    - `app/services/contact_check_worker.py` содержит `role = 'checker'` AND `auth_status = 'ok'`
    - `app/services/contact_check_worker.py` содержит `CONTACT_CHECK_BATCH_SIZE` (env var)
    - `app/main.py` содержит `contact_check_worker.start()` AND `await contact_check_worker.stop()`
    - `tests/test_contact_check_worker.py` exists AND содержит минимум 7 тестов
    - `pytest tests/test_contact_check_worker.py -v` exits 0
    - `python -c "from app.services.contact_check_worker import contact_check_worker; assert contact_check_worker is not None"` exits 0
  </acceptance_criteria>
  <done>
    Worker подбирает pending контакты, резолвит через CheckerService (переиспользование), workspace-isolated через JOIN LATERAL, обновляет tg_status; FloodWait partial обрабатывается корректно (необработанные остаются pending — worker подберёт на следующий tick); 7 unit-тестов зелёные.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: check_contacts router rewrite (recheck) + has_checker в GET /workspace + integration-тесты</name>
  <files>app/routers/check_contacts.py, app/routers/workspace.py, app/main.py, tests/test_check_contacts.py</files>
  <read_first>
    - app/routers/check_contacts.py — текущий код (broken импорт verify_api_key — рерайтим целиком)
    - app/routers/workspace.py — Phase 1 структура GET /workspace + WorkspaceResponse Pydantic-схема
    - app/routers/contacts.py (после 02-04) — `_has_checker` helper (если экспортирован) или повторяем
    - app/utils/auth.py — AuthCtx
    - app/models/__init__.py — Contact, Sender
    - app/schemas/__init__.py — RecheckRequest (после 02-02)
    - .planning/phases/02-tg-accounts-contacts/02-CONTEXT.md D-19, D-20
    - .planning/phases/02-tg-accounts-contacts/02-RESEARCH.md §"Lovable / Frontend API Contract → Workspace" — has_checker
  </read_first>
  <behavior>
    - Test 1: `POST /api/v1/contacts/recheck {"contact_ids": [id1, id2]}` → 202 + `{marked_pending: 2}`; контакты переходят в tg_status='pending'.
    - Test 2: `POST /api/v1/contacts/recheck {"folder_id": fid}` → 202 + marked_pending; все контакты folder → pending.
    - Test 3: `POST /api/v1/contacts/recheck {}` без контентов → 422 (Pydantic model_validator).
    - Test 4: recheck с contact_ids из другого workspace → marked_pending=0 (cross-tenant фильтр).
    - Test 5: `GET /api/v1/workspace` без checker'а → response содержит `"has_checker": false`.
    - Test 6: `GET /api/v1/workspace` с checker (через test_checker фикстуру) → `"has_checker": true`.
    - Test 7: recheck workspace-isolated — workspace A не может recheck контакты workspace B.
    - Test 8: после recheck worker подберёт контакты и обновит (полный e2e — но в этом тесте только проверяем что recheck перевёл в pending, worker tick'аем явно).
  </behavior>
  <action>
    1. **Полный rewrite `app/routers/check_contacts.py`**:
       ```python
       """Check contacts router (Phase 2 — CONT-04 recheck endpoint).
       
       Endpoint:
         POST /api/v1/contacts/recheck {contact_ids? | folder_id?} → 202 {marked_pending: N}
       
       Logic: UPDATE contacts SET tg_status='pending' WHERE workspace_id matches AND
       (id IN ids OR folder_id = fid). ContactCheckWorker подберёт на следующий tick.
       """
       
       import logging
       
       from fastapi import APIRouter, Depends, HTTPException
       from sqlalchemy import text
       from sqlalchemy.ext.asyncio import AsyncSession
       
       from app.database import get_db
       from app.schemas import RecheckRequest
       from app.utils.auth import AuthCtx, auth_dep
       
       logger = logging.getLogger(__name__)
       router = APIRouter(prefix="/api/v1/contacts", tags=["contact-check"])
       
       
       @router.post("/recheck", status_code=202)
       async def recheck_contacts(
           payload: RecheckRequest,
           ctx: AuthCtx = Depends(auth_dep),
           db: AsyncSession = Depends(get_db),
       ):
           """Помечает контакты как pending → ContactCheckWorker переоценит."""
           if payload.contact_ids:
               # NB: ANY(:ids::uuid[]) для array param
               result = await db.execute(text("""
                   UPDATE contacts
                   SET tg_status = 'pending',
                       tg_error = NULL,
                       updated_at = NOW()
                   WHERE id = ANY(:ids)
                     AND workspace_id = :wid
                     AND phone IS NOT NULL
               """), {
                   "ids": [str(cid) for cid in payload.contact_ids],
                   "wid": str(ctx.workspace_id),
               })
           elif payload.folder_id:
               # Сначала убедимся что folder_id принадлежит workspace
               folder_check = await db.execute(text("""
                   SELECT 1 FROM folders WHERE id = :fid AND workspace_id = :wid
               """), {"fid": str(payload.folder_id), "wid": str(ctx.workspace_id)})
               if folder_check.scalar() is None:
                   raise HTTPException(404, {"code": "FOLDER_NOT_FOUND", "message": "Folder not found"})
               result = await db.execute(text("""
                   UPDATE contacts
                   SET tg_status = 'pending',
                       tg_error = NULL,
                       updated_at = NOW()
                   WHERE folder_id = :fid
                     AND workspace_id = :wid
                     AND phone IS NOT NULL
               """), {"fid": str(payload.folder_id), "wid": str(ctx.workspace_id)})
           else:
               # Этот случай не должен случаться — Pydantic model_validator отклонит
               raise HTTPException(422, {"code": "MISSING_TARGET", "message": "Either contact_ids or folder_id required"})
           
           marked = result.rowcount
           await db.commit()
           logger.info(f"[recheck] workspace={ctx.workspace_id} marked_pending={marked}")
           return {"marked_pending": marked}
       ```
    
    2. **Расширение `app/routers/workspace.py`**:
       - Найти существующую `WorkspaceResponse` Pydantic-схему (inline или импортируется). Добавить поле:
         ```python
         has_checker: bool = False
         ```
       - Найти endpoint `GET /api/v1/workspace`. Перед формированием response добавить query:
         ```python
         from sqlalchemy import select, func as sql_func
         from app.models import Sender
         
         checker_count_result = await db.execute(
             select(sql_func.count(Sender.id)).where(
                 Sender.workspace_id == ctx.workspace_id,
                 Sender.role == "checker",
                 Sender.auth_status == "ok",
             )
         )
         has_checker = (checker_count_result.scalar() or 0) > 0
         ```
       - При создании WorkspaceResponse — передать `has_checker=has_checker`.
    
    3. В `app/main.py`:
       - Добавить `check_contacts` в импорт `from app.routers import ...`.
       - Добавить `app.include_router(check_contacts.router)` после `contacts.router`.
    
    4. Создать `tests/test_check_contacts.py`:
       ```python
       """Integration-тесты POST /contacts/recheck + has_checker в GET /workspace."""
       
       import pytest
       from uuid import uuid4
       from sqlalchemy import text
       
       pytestmark = pytest.mark.asyncio
       
       
       async def test_recheck_by_contact_ids(async_client, valid_supabase_jwt, async_db_session):
           token = valid_supabase_jwt(sub=f"recheck-{uuid4()}", email=f"x-{uuid4()}@x.com")
           headers = {"Authorization": f"Bearer {token}"}
           # Создадим folder + контакты
           folder = await async_client.post("/api/v1/folders", headers=headers, json={"name": "RecheckFolder"})
           fid = folder.json()["id"]
           ws = await async_client.get("/api/v1/workspace", headers=headers)
           wid = ws.json()["id"]
           # Insert 2 контакта с tg_status='registered' (уже проверены)
           await async_db_session.execute(text("""
               INSERT INTO contacts (workspace_id, folder_id, phone, tg_status, tg_telegram_id)
               VALUES (:wid, :fid, :p1, 'registered', 111), (:wid, :fid, :p2, 'registered', 222)
               RETURNING id
           """), {"wid": wid, "fid": fid, "p1": "+79001110001", "p2": "+79001110002"})
           # Получим IDs
           ids_result = await async_db_session.execute(text(
               "SELECT id FROM contacts WHERE folder_id = :fid ORDER BY phone"
           ), {"fid": fid})
           contact_ids = [str(r.id) for r in ids_result.fetchall()]
           await async_db_session.commit()
           
           # Recheck
           response = await async_client.post(
               "/api/v1/contacts/recheck",
               headers=headers,
               json={"contact_ids": contact_ids},
           )
           assert response.status_code == 202
           assert response.json()["marked_pending"] == 2
           
           # Проверка БД
           statuses = await async_db_session.execute(text(
               "SELECT tg_status FROM contacts WHERE id = ANY(:ids)"
           ), {"ids": contact_ids})
           assert all(r.tg_status == "pending" for r in statuses.fetchall())
       
       
       async def test_recheck_by_folder_id(async_client, valid_supabase_jwt, async_db_session):
           token = valid_supabase_jwt(sub=f"recheck-fld-{uuid4()}", email=f"y-{uuid4()}@x.com")
           headers = {"Authorization": f"Bearer {token}"}
           folder = await async_client.post("/api/v1/folders", headers=headers, json={"name": "FldRe"})
           fid = folder.json()["id"]
           ws = await async_client.get("/api/v1/workspace", headers=headers)
           wid = ws.json()["id"]
           await async_db_session.execute(text("""
               INSERT INTO contacts (workspace_id, folder_id, phone, tg_status)
               VALUES (:wid, :fid, :p1, 'not_registered'), (:wid, :fid, :p2, 'error')
           """), {"wid": wid, "fid": fid, "p1": "+79002220001", "p2": "+79002220002"})
           await async_db_session.commit()
           
           response = await async_client.post(
               "/api/v1/contacts/recheck",
               headers=headers,
               json={"folder_id": fid},
           )
           assert response.status_code == 202
           assert response.json()["marked_pending"] == 2
       
       
       async def test_recheck_missing_target_returns_422(async_client, valid_supabase_jwt):
           token = valid_supabase_jwt(sub=f"recheck-miss-{uuid4()}", email=f"z-{uuid4()}@x.com")
           response = await async_client.post(
               "/api/v1/contacts/recheck",
               headers={"Authorization": f"Bearer {token}"},
               json={},
           )
           assert response.status_code == 422
       
       
       async def test_recheck_cross_tenant_marks_zero(async_client, valid_supabase_jwt, async_db_session):
           # Workspace A создаёт folder + контакт
           token_a = valid_supabase_jwt(sub=f"recheck-a-{uuid4()}", email=f"a-{uuid4()}@x.com")
           ha = {"Authorization": f"Bearer {token_a}"}
           folder = await async_client.post("/api/v1/folders", headers=ha, json={"name": "TenantA"})
           fid_a = folder.json()["id"]
           ws_a = await async_client.get("/api/v1/workspace", headers=ha)
           wid_a = ws_a.json()["id"]
           ins = await async_db_session.execute(text("""
               INSERT INTO contacts (workspace_id, folder_id, phone, tg_status)
               VALUES (:wid, :fid, :p, 'registered') RETURNING id
           """), {"wid": wid_a, "fid": fid_a, "p": "+79009990001"})
           contact_id = str(ins.scalar())
           await async_db_session.commit()
           
           # Workspace B пытается recheck контакт workspace A
           token_b = valid_supabase_jwt(sub=f"recheck-b-{uuid4()}", email=f"b-{uuid4()}@x.com")
           hb = {"Authorization": f"Bearer {token_b}"}
           response = await async_client.post(
               "/api/v1/contacts/recheck",
               headers=hb,
               json={"contact_ids": [contact_id]},
           )
           assert response.status_code == 202
           assert response.json()["marked_pending"] == 0  # ничего не задето
           # Проверка что контакт в workspace A остался 'registered'
           status = await async_db_session.execute(text(
               "SELECT tg_status FROM contacts WHERE id = :cid"
           ), {"cid": contact_id})
           assert status.scalar() == "registered"
       
       
       async def test_workspace_has_checker_false_when_none(async_client, valid_supabase_jwt):
           token = valid_supabase_jwt(sub=f"hc-false-{uuid4()}", email=f"hc-{uuid4()}@x.com")
           response = await async_client.get(
               "/api/v1/workspace",
               headers={"Authorization": f"Bearer {token}"},
           )
           assert response.status_code == 200
           assert response.json()["has_checker"] is False
       
       
       async def test_workspace_has_checker_true_when_present(async_client, valid_supabase_jwt, async_db_session):
           token = valid_supabase_jwt(sub=f"hc-true-{uuid4()}", email=f"hct-{uuid4()}@x.com")
           headers = {"Authorization": f"Bearer {token}"}
           ws = await async_client.get("/api/v1/workspace", headers=headers)
           wid = ws.json()["id"]
           # Добавим checker напрямую в БД
           await async_db_session.execute(text("""
               INSERT INTO senders (workspace_id, slug, name, phone, session_string, role,
                                    auth_status, lifecycle_status, rate_per_min, rate_per_hour, rate_per_day)
               VALUES (:wid, 'ch-1', 'Checker 1', '+79001234567', 'enc', 'checker',
                       'ok', 'active', 4, 20, 150)
           """), {"wid": wid})
           await async_db_session.commit()
           
           response = await async_client.get("/api/v1/workspace", headers=headers)
           assert response.status_code == 200
           assert response.json()["has_checker"] is True
       
       
       async def test_recheck_invalid_folder_returns_404(async_client, valid_supabase_jwt):
           token = valid_supabase_jwt(sub=f"recheck-404-{uuid4()}", email=f"n-{uuid4()}@x.com")
           response = await async_client.post(
               "/api/v1/contacts/recheck",
               headers={"Authorization": f"Bearer {token}"},
               json={"folder_id": str(uuid4())},
           )
           assert response.status_code == 404
           assert response.json()["detail"]["code"] == "FOLDER_NOT_FOUND"
       ```
  </action>
  <verify>
    <automated>pytest tests/test_check_contacts.py tests/test_contact_check_worker.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/check_contacts.py` exists
    - `app/routers/check_contacts.py` содержит `router = APIRouter(prefix="/api/v1/contacts"`
    - `app/routers/check_contacts.py` содержит `Depends(auth_dep)`
    - `app/routers/check_contacts.py` содержит `tg_status = 'pending'`
    - `app/routers/check_contacts.py` НЕ содержит `verify_api_key`
    - `app/routers/check_contacts.py` содержит `workspace_id = :wid` (cross-tenant guard в UPDATE)
    - `app/routers/workspace.py` содержит `has_checker` (поле в response + query)
    - `app/routers/workspace.py` содержит `Sender.role == "checker"`
    - `app/main.py` содержит `app.include_router(check_contacts.router)`
    - `tests/test_check_contacts.py` exists AND содержит минимум 7 тестов
    - `pytest tests/test_check_contacts.py -v` exits 0
    - `python -c "import app.main"` exits 0
  </acceptance_criteria>
  <done>
    Recheck endpoint работает workspace-scoped, support contact_ids И folder_id; has_checker exposed в GET /workspace; integration-тесты зелёные. Phase 2 closed: CONT-04 закрыт.
  </done>
</task>

</tasks>

<verification>
**Automated:**
1. `pytest tests/test_contact_check_worker.py tests/test_check_contacts.py -v` — все unit + integration зелёные.
2. `pytest tests/ -v` — все Phase 1 + Phase 2 тесты (миграция 012, 013, auth, workspace, senders, folders, contacts, csv, phone, onboarding, listener, worker, recheck) зелёные.
3. `grep "verify_api_key" app/routers/check_contacts.py` — 0 matches.
4. `grep "has_checker" app/routers/workspace.py` — match.
5. `python -c "import app.main; from app.services.contact_check_worker import contact_check_worker; assert contact_check_worker._task is None or contact_check_worker._task.done()"` — exit 0.

**Manual smoke:**
1. `docker compose up -d --build api listener` — оба стартуют; в логах api: `📋 ContactCheckWorker started (batch=5, poll=5s)`.
2. Полный e2e: онбординг checker'а через UI → импорт CSV с 5 контактами → в течение 30 сек логи: `📋 ContactCheckWorker: checker=test-checker checked=5 reg=3 not_reg=2`.
3. `GET /api/v1/workspace` без checker'а → `has_checker: false`; после онбординга checker'а → `has_checker: true`.
4. `POST /api/v1/contacts/recheck {"folder_id": fid}` → 202 + `{marked_pending: N}`; через 30 сек worker переоценит.
</verification>

<success_criteria>
- `ContactCheckWorker` запущен в lifespan, polls каждые 5 сек, batch=5, переиспользует `CheckerService.check_phones` (lock + FloodWait + polite delay).
- Workspace-isolation через `JOIN LATERAL ... WHERE s.workspace_id = c.workspace_id AND s.role='checker'`.
- D-20 fallback: контакты в workspace без checker'а остаются `tg_status='unchecked'` (план 02-04 ставит этот статус, worker SQL не находит JOIN match → пропускает).
- `POST /api/v1/contacts/recheck` workspace-scoped, поддерживает contact_ids И folder_id, 202 Accepted + `marked_pending`.
- `GET /api/v1/workspace` возвращает `has_checker: bool` для баннера UI.
- CONT-04 закрыт (последний requirement Phase 2).
- Все Phase 2 тесты зелёные: миграция 013, senders, folders, contacts, csv, phone, onboarding, listener reconcile, contact_check_worker, check_contacts.
- Phase 2 полностью закрыта: ONBD-01..05, SNDR-01..03, CONT-01..05, FLDR-01..03 (16 requirements).
</success_criteria>

<output>
After completion, create `.planning/phases/02-tg-accounts-contacts/02-05-SUMMARY.md` per template — отметить:
- Worker config: env vars CONTACT_CHECK_BATCH_SIZE (5), CONTACT_CHECK_POLL_INTERVAL (5 sec)
- SQL для подбора pending контактов (JOIN LATERAL — для аудита workspace-isolation)
- D-20 fallback flow: unchecked → user добавляет checker → новый импорт даёт pending → worker подберёт; existing unchecked требует ручного `POST /recheck` (auto-recheck deferred в v2)
- Endpoint матрица recheck + workspace расширение has_checker
- Phase 2 closure: все 16 requirements (ONBD/SNDR/CONT/FLDR) закрыты, готовность к Phase 3 (Agents)
</output>
