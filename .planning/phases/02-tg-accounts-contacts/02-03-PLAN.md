---
plan_id: "02-03"
title: "Folders CRUD: модель папок, workspace-scoped роутер, запрет удаления непустой папки"
phase: "02-tg-accounts-contacts"
plan: 3
type: execute
wave: 1
depends_on: ["02-02"]
files_modified:
  - "app/routers/folders.py"
  - "app/main.py"
  - "tests/test_folders.py"
autonomous: true
requirements: ["FLDR-01", "FLDR-02"]
must_haves:
  truths:
    - "Юзер создаёт папку через POST /api/v1/folders {name}"
    - "Юзер переименовывает папку через PATCH /api/v1/folders/{id} {name}"
    - "Юзер удаляет пустую папку через DELETE /api/v1/folders/{id}"
    - "Удаление папки с контактами возвращает 409 FOLDER_NOT_EMPTY с {contact_count, active_campaigns: []}"
    - "DELETE с ?force=true каскадно удаляет контакты папки"
    - "GET /api/v1/folders возвращает массив папок workspace с contact_count"
    - "Папки изолированы по workspace — папки workspace B недоступны из workspace A (404)"
  artifacts:
    - path: "app/routers/folders.py"
      provides: "Workspace-scoped CRUD папок"
      contains: "Depends(auth_dep); FOLDER_NOT_EMPTY; force=true; APIRouter(prefix='/api/v1/folders')"
    - path: "tests/test_folders.py"
      provides: "Integration-тесты CRUD + 409 + force + cross-tenant"
      contains: "test_create_folder; test_delete_folder_with_contacts_409; test_delete_folder_force_cascades; test_cross_tenant"
  key_links:
    - from: "app/main.py"
      to: "app/routers/folders.py"
      via: "app.include_router(folders.router)"
      pattern: "include_router\\(folders\\.router\\)"
    - from: "app/routers/folders.py"
      to: "app/utils/auth.py::auth_dep"
      via: "Depends(auth_dep)"
      pattern: "Depends\\(auth_dep\\)"
    - from: "app/routers/folders.py"
      to: "app.models.Folder + Contact"
      via: "SELECT Folder, COUNT Contact для contact_count"
      pattern: "Folder\\.workspace_id == ctx\\.workspace_id"
---

<objective>
Реализовать workspace-scoped CRUD папок: модель `Folder` (Phase 2 миграция 013 уже создала таблицу `folders` и ORM-класс в плане 02-02), роутер `app/routers/folders.py` с endpoint'ами `GET/POST/PATCH/DELETE`. Удаление непустой папки — 409 `FOLDER_NOT_EMPTY` с количеством контактов, опциональный `?force=true` для каскадного удаления. Все запросы — через `Depends(auth_dep)` с фильтром `workspace_id == ctx.workspace_id`.

Purpose: Папки — таргет кампании (Phase 4) и контейнер контактов (CONT-* план 02-04 + 02-05). Без модели Folder + CRUD ни импорт CSV, ни push API не работают.

Output: Workspace-scoped роутер папок, тесты CRUD + 409 conflict + force cascade + cross-tenant isolation.
</objective>

<execution_context>
@/Users/andrewbruce/Documents/outreach-platform/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewbruce/Documents/outreach-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-tg-accounts-contacts/02-CONTEXT.md
@.planning/phases/02-tg-accounts-contacts/02-RESEARCH.md
@.planning/phases/02-tg-accounts-contacts/02-PATTERNS.md
@.planning/phases/01-workspace-foundation/01-03-api-skeleton-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@app/routers/workspace.py
@app/utils/auth.py
@app/models/__init__.py
@app/schemas/__init__.py
@app/main.py
@tests/conftest.py
@tests/test_workspace_router.py
@.planning/phases/02-tg-accounts-contacts/02-02-PLAN.md

<interfaces>
<!-- ORM-модель Folder и Pydantic-схемы создаются в плане 02-02. Здесь только используем. -->

From app/models/__init__.py (после 02-02):
```python
class Folder(Base):
    __tablename__ = "folders"
    id: UUID
    workspace_id: UUID                 # FK workspaces, NOT NULL
    name: str                          # VARCHAR(100), UNIQUE (workspace_id, name)
    created_at: datetime
    updated_at: datetime

class Contact(Base):
    __tablename__ = "contacts"
    folder_id: UUID                    # FK folders ON DELETE CASCADE
    workspace_id: UUID
    # ... (см. 02-02)
```

From app/schemas/__init__.py (после 02-02):
```python
class FolderCreate(BaseModel):
    name: str  # min_length=1, max_length=100

class FolderUpdate(BaseModel):
    name: str

class FolderResponse(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: UUID
    name: str
    contact_count: int = 0
    created_at: datetime
    updated_at: datetime
```

From app/utils/auth.py (Phase 1):
```python
async def auth_dep(...) -> AuthCtx: ...  # JWT или X-Workspace-Key

@dataclass
class AuthCtx:
    workspace_id: UUID
    user_id: Optional[str]
    source: Literal["jwt", "api_key"]
    role: Optional[str]
```

From app/routers/workspace.py (Phase 1, паттерн копировать):
```python
router = APIRouter(prefix="/api/v1", tags=["workspace"])

# Cross-tenant guard pattern (см. workspace.py:292-306):
result = await db.execute(
    select(Model).where(
        Model.id == resource_id,
        Model.workspace_id == ctx.workspace_id,
        # TODO(v2-rls): replaced by RLS policy
    )
)
row = result.scalars().first()
if row is None:
    raise HTTPException(404, {"code": "*_NOT_FOUND", "message": "..."})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Folder router (CRUD + 409 + force + helper get_or_create_by_name)</name>
  <files>app/routers/folders.py, app/main.py</files>
  <read_first>
    - app/routers/workspace.py — Phase 1 паттерн workspace-scoped CRUD (импорты, APIRouter, error-dict, TODO(v2-rls), JWT-only enforcement через `_require_jwt`)
    - app/models/__init__.py — классы Folder и Contact (после 02-02)
    - app/schemas/__init__.py — FolderCreate/Update/Response (после 02-02)
    - app/utils/auth.py — auth_dep / AuthCtx
    - app/main.py — где добавить include_router (после workspace.router и senders.router)
    - .planning/phases/02-tg-accounts-contacts/02-CONTEXT.md D-04, D-05, D-06, D-09 — точные правила
    - .planning/phases/02-tg-accounts-contacts/02-PATTERNS.md §"2. app/routers/folders.py" — полный skeleton + 409 формат
    - .planning/phases/02-tg-accounts-contacts/02-RESEARCH.md §"Lovable / Frontend API Contract → Folders" — endpoint shapes
  </read_first>
  <behavior>
    - Test 1: `GET /api/v1/folders` без auth → 401 (`AUTH_REQUIRED`).
    - Test 2: `POST /api/v1/folders {"name": "Leads"}` с JWT → 201 + FolderResponse{contact_count: 0}.
    - Test 3: `GET /api/v1/folders` → список папок workspace с contact_count (для каждой папки = `SELECT COUNT(*) FROM contacts WHERE folder_id = ?`).
    - Test 4: `PATCH /api/v1/folders/{id} {"name": "Hot Leads"}` → 200 + обновлённое имя.
    - Test 5: `POST /api/v1/folders {"name": "Leads"}` дважды → 409 (duplicate name в workspace).
    - Test 6: `DELETE /api/v1/folders/{id}` пустой папки → 204.
    - Test 7: `DELETE /api/v1/folders/{id}` с 3 контактами → 409 `{code: 'FOLDER_NOT_EMPTY', contact_count: 3, active_campaigns: []}`.
    - Test 8: `DELETE /api/v1/folders/{id}?force=true` с контактами → 204; контакты тоже удалены (cascade через FK).
    - Test 9: cross-tenant — папка workspace B недоступна из workspace A → 404.
    - Test 10: `GET /api/v1/folders/{id_workspace_B}` из workspace A → 404 (не 403, security).
  </behavior>
  <action>
    1. Создать `app/routers/folders.py`:
       ```python
       """Folders router (Phase 2 — FLDR-01, FLDR-02).
       
       Endpoints:
         GET    /api/v1/folders                          — list workspace folders
         POST   /api/v1/folders                          — create folder
         GET    /api/v1/folders/{id}                     — single folder
         PATCH  /api/v1/folders/{id}                     — rename
         DELETE /api/v1/folders/{id}[?force=true]        — delete; 409 if not empty, force cascades
       
       FLDR-03 (auto-create by folder_name) реализуется в plan 02-04 (contacts router/CSV service)
       через переиспользование helper'а get_or_create_by_name(...) из этого файла.
       """
       
       import logging
       from typing import List, Optional
       from uuid import UUID
       
       from fastapi import APIRouter, Depends, HTTPException, Query
       from sqlalchemy import select, func as sql_func
       from sqlalchemy.ext.asyncio import AsyncSession
       
       from app.database import get_db
       from app.models import Folder, Contact
       from app.schemas import FolderCreate, FolderUpdate, FolderResponse
       from app.utils.auth import AuthCtx, auth_dep
       
       logger = logging.getLogger(__name__)
       router = APIRouter(prefix="/api/v1/folders", tags=["folders"])
       
       
       async def get_or_create_by_name(db: AsyncSession, workspace_id: UUID, name: str) -> UUID:
           """D-09: Helper для auto-create папки по имени (используется в contacts router).
           
           Использует ON CONFLICT для race-condition safety (RESEARCH Pitfall 4).
           """
           from sqlalchemy import text
           row = await db.execute(
               text("""
                   INSERT INTO folders (workspace_id, name)
                   VALUES (:wid, :name)
                   ON CONFLICT (workspace_id, name) DO UPDATE SET updated_at = NOW()
                   RETURNING id
               """),
               {"wid": str(workspace_id), "name": name.strip()},
           )
           return row.scalar()
       
       
       async def _folder_to_response(db: AsyncSession, folder: Folder) -> FolderResponse:
           count_result = await db.execute(
               select(sql_func.count(Contact.id)).where(Contact.folder_id == folder.id)
           )
           contact_count = count_result.scalar() or 0
           return FolderResponse(
               id=folder.id,
               name=folder.name,
               contact_count=contact_count,
               created_at=folder.created_at,
               updated_at=folder.updated_at,
           )
       
       
       @router.get("", response_model=List[FolderResponse])
       async def list_folders(
           ctx: AuthCtx = Depends(auth_dep),
           db: AsyncSession = Depends(get_db),
       ):
           result = await db.execute(
               select(Folder)
               .where(Folder.workspace_id == ctx.workspace_id)  # TODO(v2-rls)
               .order_by(Folder.created_at.desc())
           )
           folders = result.scalars().all()
           return [await _folder_to_response(db, f) for f in folders]
       
       
       @router.post("", response_model=FolderResponse, status_code=201)
       async def create_folder(
           payload: FolderCreate,
           ctx: AuthCtx = Depends(auth_dep),
           db: AsyncSession = Depends(get_db),
       ):
           name = payload.name.strip()
           if not name:
               raise HTTPException(400, {"code": "INVALID_NAME", "message": "Folder name cannot be empty"})
           # Проверка дубликата (более информативный ответ чем IntegrityError)
           existing = await db.execute(
               select(Folder).where(
                   Folder.workspace_id == ctx.workspace_id,
                   Folder.name == name,
               )
           )
           if existing.scalars().first():
               raise HTTPException(409, {"code": "FOLDER_NAME_DUPLICATE", "message": f"Folder '{name}' already exists"})
           folder = Folder(workspace_id=ctx.workspace_id, name=name)
           db.add(folder)
           await db.commit()
           await db.refresh(folder)
           logger.info(f"[folders] created workspace={ctx.workspace_id} name='{name}' id={folder.id}")
           return await _folder_to_response(db, folder)
       
       
       @router.get("/{folder_id}", response_model=FolderResponse)
       async def get_folder(
           folder_id: UUID,
           ctx: AuthCtx = Depends(auth_dep),
           db: AsyncSession = Depends(get_db),
       ):
           result = await db.execute(
               select(Folder).where(
                   Folder.id == folder_id,
                   Folder.workspace_id == ctx.workspace_id,  # TODO(v2-rls)
               )
           )
           folder = result.scalars().first()
           if folder is None:
               raise HTTPException(404, {"code": "FOLDER_NOT_FOUND", "message": "Folder not found"})
           return await _folder_to_response(db, folder)
       
       
       @router.patch("/{folder_id}", response_model=FolderResponse)
       async def rename_folder(
           folder_id: UUID,
           payload: FolderUpdate,
           ctx: AuthCtx = Depends(auth_dep),
           db: AsyncSession = Depends(get_db),
       ):
           result = await db.execute(
               select(Folder).where(
                   Folder.id == folder_id,
                   Folder.workspace_id == ctx.workspace_id,
               )
           )
           folder = result.scalars().first()
           if folder is None:
               raise HTTPException(404, {"code": "FOLDER_NOT_FOUND", "message": "Folder not found"})
           new_name = payload.name.strip()
           if not new_name:
               raise HTTPException(400, {"code": "INVALID_NAME", "message": "Folder name cannot be empty"})
           # Проверка дубликата (если переименовываем в существующее)
           if new_name != folder.name:
               dup = await db.execute(
                   select(Folder).where(
                       Folder.workspace_id == ctx.workspace_id,
                       Folder.name == new_name,
                   )
               )
               if dup.scalars().first():
                   raise HTTPException(409, {"code": "FOLDER_NAME_DUPLICATE", "message": f"Folder '{new_name}' already exists"})
           folder.name = new_name
           await db.commit()
           await db.refresh(folder)
           logger.info(f"[folders] renamed id={folder.id} to '{new_name}'")
           return await _folder_to_response(db, folder)
       
       
       @router.delete("/{folder_id}", status_code=204)
       async def delete_folder(
           folder_id: UUID,
           force: bool = Query(False, description="Force cascade delete contacts"),
           ctx: AuthCtx = Depends(auth_dep),
           db: AsyncSession = Depends(get_db),
       ):
           result = await db.execute(
               select(Folder).where(
                   Folder.id == folder_id,
                   Folder.workspace_id == ctx.workspace_id,
               )
           )
           folder = result.scalars().first()
           if folder is None:
               raise HTTPException(404, {"code": "FOLDER_NOT_FOUND", "message": "Folder not found"})
           
           # D-06: запрет удаления непустой папки (если force=false)
           count_result = await db.execute(
               select(sql_func.count(Contact.id)).where(Contact.folder_id == folder.id)
           )
           contact_count = count_result.scalar() or 0
           
           if contact_count > 0 and not force:
               raise HTTPException(409, {
                   "code": "FOLDER_NOT_EMPTY",
                   "message": f"Folder contains {contact_count} contact(s). Move them, delete them, or pass ?force=true.",
                   "contact_count": contact_count,
                   "active_campaigns": [],  # TODO(phase-4): also block on active campaign attachment
               })
           
           # force=true ИЛИ пустая → удаление (контакты каскадом через FK ondelete=CASCADE)
           await db.delete(folder)
           await db.commit()
           logger.info(f"[folders] deleted id={folder_id} force={force} cascade_contacts={contact_count}")
           return None
       ```
    2. Подключить в `app/main.py`:
       - Добавить `folders` в импорт `from app.routers import ...`.
       - Добавить `app.include_router(folders.router)` после `app.include_router(senders.router)`.
  </action>
  <verify>
    <automated>pytest tests/test_folders.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/folders.py` exists
    - `app/routers/folders.py` содержит `router = APIRouter(prefix="/api/v1/folders"`
    - `app/routers/folders.py` содержит `Depends(auth_dep)` минимум 5 раз (по разу на каждый endpoint)
    - `app/routers/folders.py` содержит `Folder.workspace_id == ctx.workspace_id` минимум 4 раза
    - `app/routers/folders.py` содержит `"FOLDER_NOT_EMPTY"`
    - `app/routers/folders.py` содержит `"active_campaigns": []`
    - `app/routers/folders.py` содержит `async def get_or_create_by_name(`
    - `app/routers/folders.py` содержит `TODO(v2-rls)` минимум 4 раза
    - `app/routers/folders.py` содержит `TODO(phase-4)` (active_campaigns)
    - `app/main.py` содержит `app.include_router(folders.router)`
    - `python -c "from app.routers.folders import router, get_or_create_by_name"` exits 0
  </acceptance_criteria>
  <done>Folders router зарегистрирован в main.py, get_or_create_by_name helper экспортируется для повторного использования в contacts (план 02-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration-тесты folders CRUD + 409 + force + cross-tenant</name>
  <files>tests/test_folders.py</files>
  <read_first>
    - tests/test_workspace_router.py — Phase 1 паттерн async_client + valid_supabase_jwt + assert на response.json()["detail"]["code"]
    - tests/conftest.py — фабрики test_workspace, test_folder, test_contacts_factory, valid_supabase_jwt (после 02-02)
    - app/routers/folders.py — реализация
    - .planning/phases/02-tg-accounts-contacts/02-PATTERNS.md §"14. Тесты" — async-test pattern
  </read_first>
  <behavior>
    - 10 тестов покрывают CRUD + 409 + force + cross-tenant + duplicate name.
  </behavior>
  <action>
    Создать `tests/test_folders.py`:
    ```python
    """Integration-тесты Folders router (FLDR-01, FLDR-02)."""
    
    import pytest
    from uuid import uuid4
    
    pytestmark = pytest.mark.asyncio
    
    
    async def test_list_folders_no_auth_returns_401(async_client):
        response = await async_client.get("/api/v1/folders")
        assert response.status_code == 401
        assert response.json()["detail"]["code"] == "AUTH_REQUIRED"
    
    
    async def test_create_folder_returns_201_with_contact_count_0(async_client, valid_supabase_jwt):
        token = valid_supabase_jwt(sub=f"folders-test-1-{uuid4()}", email=f"a-{uuid4()}@x.com")
        response = await async_client.post(
            "/api/v1/folders",
            headers={"Authorization": f"Bearer {token}"},
            json={"name": "Leads"},
        )
        assert response.status_code == 201
        body = response.json()
        assert body["name"] == "Leads"
        assert body["contact_count"] == 0
        assert "id" in body
    
    
    async def test_list_folders_returns_workspace_folders(async_client, valid_supabase_jwt):
        token = valid_supabase_jwt(sub=f"folders-test-2-{uuid4()}", email=f"b-{uuid4()}@x.com")
        headers = {"Authorization": f"Bearer {token}"}
        await async_client.post("/api/v1/folders", headers=headers, json={"name": "F1"})
        await async_client.post("/api/v1/folders", headers=headers, json={"name": "F2"})
        response = await async_client.get("/api/v1/folders", headers=headers)
        assert response.status_code == 200
        names = [f["name"] for f in response.json()]
        assert "F1" in names and "F2" in names
    
    
    async def test_create_duplicate_name_returns_409(async_client, valid_supabase_jwt):
        token = valid_supabase_jwt(sub=f"folders-test-3-{uuid4()}", email=f"c-{uuid4()}@x.com")
        headers = {"Authorization": f"Bearer {token}"}
        await async_client.post("/api/v1/folders", headers=headers, json={"name": "Dup"})
        response = await async_client.post("/api/v1/folders", headers=headers, json={"name": "Dup"})
        assert response.status_code == 409
        assert response.json()["detail"]["code"] == "FOLDER_NAME_DUPLICATE"
    
    
    async def test_patch_folder_renames(async_client, valid_supabase_jwt):
        token = valid_supabase_jwt(sub=f"folders-test-4-{uuid4()}", email=f"d-{uuid4()}@x.com")
        headers = {"Authorization": f"Bearer {token}"}
        create = await async_client.post("/api/v1/folders", headers=headers, json={"name": "Old"})
        fid = create.json()["id"]
        response = await async_client.patch(f"/api/v1/folders/{fid}", headers=headers, json={"name": "New"})
        assert response.status_code == 200
        assert response.json()["name"] == "New"
    
    
    async def test_delete_empty_folder_returns_204(async_client, valid_supabase_jwt):
        token = valid_supabase_jwt(sub=f"folders-test-5-{uuid4()}", email=f"e-{uuid4()}@x.com")
        headers = {"Authorization": f"Bearer {token}"}
        create = await async_client.post("/api/v1/folders", headers=headers, json={"name": "ToDel"})
        fid = create.json()["id"]
        response = await async_client.delete(f"/api/v1/folders/{fid}", headers=headers)
        assert response.status_code == 204
        get_response = await async_client.get(f"/api/v1/folders/{fid}", headers=headers)
        assert get_response.status_code == 404
    
    
    async def test_delete_folder_with_contacts_returns_409(async_client, valid_supabase_jwt, async_db_session):
        """Используем raw SQL для добавления контактов в обход API (план 02-04 ещё не закончен)."""
        from sqlalchemy import text
        token = valid_supabase_jwt(sub=f"folders-test-6-{uuid4()}", email=f"f-{uuid4()}@x.com")
        headers = {"Authorization": f"Bearer {token}"}
        create = await async_client.post("/api/v1/folders", headers=headers, json={"name": "Full"})
        fid = create.json()["id"]
        # Получим workspace_id через GET workspace
        ws = await async_client.get("/api/v1/workspace", headers=headers)
        wid = ws.json()["id"]
        # Вставим 2 контакта напрямую в БД
        await async_db_session.execute(text("""
            INSERT INTO contacts (workspace_id, folder_id, phone, tg_status)
            VALUES (:wid, :fid, :p1, 'pending'), (:wid, :fid, :p2, 'pending')
        """), {"wid": wid, "fid": fid, "p1": "+79001110001", "p2": "+79001110002"})
        await async_db_session.commit()
        # Delete без force → 409
        response = await async_client.delete(f"/api/v1/folders/{fid}", headers=headers)
        assert response.status_code == 409
        detail = response.json()["detail"]
        assert detail["code"] == "FOLDER_NOT_EMPTY"
        assert detail["contact_count"] == 2
        assert detail["active_campaigns"] == []
    
    
    async def test_delete_folder_force_cascades_contacts(async_client, valid_supabase_jwt, async_db_session):
        from sqlalchemy import text
        token = valid_supabase_jwt(sub=f"folders-test-7-{uuid4()}", email=f"g-{uuid4()}@x.com")
        headers = {"Authorization": f"Bearer {token}"}
        create = await async_client.post("/api/v1/folders", headers=headers, json={"name": "ForceDel"})
        fid = create.json()["id"]
        ws = await async_client.get("/api/v1/workspace", headers=headers)
        wid = ws.json()["id"]
        await async_db_session.execute(text("""
            INSERT INTO contacts (workspace_id, folder_id, phone, tg_status)
            VALUES (:wid, :fid, :p, 'pending')
        """), {"wid": wid, "fid": fid, "p": "+79002220001"})
        await async_db_session.commit()
        response = await async_client.delete(f"/api/v1/folders/{fid}?force=true", headers=headers)
        assert response.status_code == 204
        # Контакты тоже удалены каскадом
        rows = await async_db_session.execute(text(
            "SELECT COUNT(*) FROM contacts WHERE folder_id = :fid"
        ), {"fid": fid})
        assert rows.scalar() == 0
    
    
    async def test_cross_tenant_folder_returns_404(async_client, valid_supabase_jwt):
        # Workspace A создаёт папку
        token_a = valid_supabase_jwt(sub=f"folders-tenant-a-{uuid4()}", email=f"a-{uuid4()}@x.com")
        ha = {"Authorization": f"Bearer {token_a}"}
        create = await async_client.post("/api/v1/folders", headers=ha, json={"name": "PrivateA"})
        fid = create.json()["id"]
        # Workspace B пытается достать
        token_b = valid_supabase_jwt(sub=f"folders-tenant-b-{uuid4()}", email=f"b-{uuid4()}@x.com")
        hb = {"Authorization": f"Bearer {token_b}"}
        response = await async_client.get(f"/api/v1/folders/{fid}", headers=hb)
        assert response.status_code == 404  # не 403 — hide existence
        assert response.json()["detail"]["code"] == "FOLDER_NOT_FOUND"
    
    
    async def test_get_or_create_by_name_helper(async_db_session, test_workspace):
        """Helper для FLDR-03 (auto-create) — будет использован в plan 02-04."""
        from app.routers.folders import get_or_create_by_name
        # Первый вызов — создаёт
        fid1 = await get_or_create_by_name(async_db_session, test_workspace.id, "AutoFolder")
        await async_db_session.commit()
        # Второй вызов — возвращает тот же ID (idempotent через ON CONFLICT)
        fid2 = await get_or_create_by_name(async_db_session, test_workspace.id, "AutoFolder")
        await async_db_session.commit()
        assert fid1 == fid2
    ```
  </action>
  <verify>
    <automated>pytest tests/test_folders.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_folders.py` exists
    - `tests/test_folders.py` содержит минимум 10 async def test_*
    - `tests/test_folders.py` содержит `test_create_folder` AND `test_delete_folder_with_contacts_returns_409` AND `test_delete_folder_force_cascades_contacts` AND `test_cross_tenant_folder_returns_404` AND `test_get_or_create_by_name_helper`
    - `pytest tests/test_folders.py -v` exits 0
  </acceptance_criteria>
  <done>Все 10 integration-тестов зелёные; helper get_or_create_by_name готов для FLDR-03 в plan 02-04.</done>
</task>

</tasks>

<verification>
**Automated:**
1. `pytest tests/test_folders.py -v` — 10 тестов зелёных.
2. `grep -c "Depends(auth_dep)" app/routers/folders.py` ≥ 5.
3. `grep -c "Folder.workspace_id == ctx.workspace_id" app/routers/folders.py` ≥ 4.
4. `grep "include_router(folders" app/main.py` — match.
5. `python -c "import app.main; from app.routers.folders import get_or_create_by_name"` — exit 0.

**Manual smoke:**
1. `curl -X POST -H "Authorization: Bearer $JWT" -H "Content-Type: application/json" -d '{"name":"Test"}' http://localhost:8000/api/v1/folders` — 201.
2. `curl -X DELETE -H "Authorization: Bearer $JWT" http://localhost:8000/api/v1/folders/$ID` пустой папки — 204.
</verification>

<success_criteria>
- `app/routers/folders.py` существует и подключён в main.py.
- 6 endpoint'ов работают workspace-scoped через AuthCtx.
- DELETE возвращает 409 FOLDER_NOT_EMPTY если есть контакты (без force=true); 204 если пусто или force.
- `get_or_create_by_name` helper экспортируется для использования в plan 02-04 (FLDR-03 auto-create при импорте/push).
- `tests/test_folders.py` — 10 тестов зелёные.
- FLDR-01 закрыт (контакты группируются по папкам — через FK contacts.folder_id NOT NULL из 02-02). FLDR-02 закрыт (CRUD папок + 409 запрет удаления непустой + force cascade).
- FLDR-03 (auto-create при импорте) — НЕ закрывается здесь, helper готов; реализация будет в plan 02-04 в contacts/CSV-import logic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tg-accounts-contacts/02-03-SUMMARY.md` per template — отметить:
- Endpoint матрица (Method / Path / Auth / Notes)
- Точная сигнатура helper'а `get_or_create_by_name(db, workspace_id, name)` — для использования в plan 02-04
- TODO(phase-4) маркеры в коде (active_campaigns block) — для аудита
- Готовность к plan 02-04 (CSV import будет использовать get_or_create_by_name)
</output>
