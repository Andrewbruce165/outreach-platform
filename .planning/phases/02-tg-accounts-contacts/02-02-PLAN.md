---
plan_id: "02-02"
title: "Sender settings: миграция 013, lifecycle/rate-limits, queue.py constants выпил, senders router"
phase: "02-tg-accounts-contacts"
plan: 2
type: execute
wave: 0
depends_on: []
files_modified:
  - "migrations/013_phase2.sql"
  - "app/models/__init__.py"
  - "app/schemas/__init__.py"
  - "app/routers/senders.py"
  - "app/routers/health.py"
  - "app/services/queue.py"
  - "app/services/warmup.py"
  - "app/services/rotation.py"
  - "app/main.py"
  - "tests/conftest.py"
  - "tests/test_migration_013.py"
  - "tests/test_senders.py"
autonomous: true
requirements: ["SNDR-01", "SNDR-02", "SNDR-03"]
must_haves:
  truths:
    - "Миграция 013_phase2.sql создаёт таблицы folders/contacts/onboarding_sessions/csv_imports + расширяет senders + дропает is_active"
    - "Юзер задаёт rate limits per-sender через PATCH /senders/{id}; значения 4/20/150 — defaults, до 10/50/300 — warning, >10/50/300 — 422"
    - "API GET /senders/{id} возвращает derived status: error если auth_status!='ok', иначе lifecycle_status"
    - "Юзер назначает sender'у прокси из workspace pool через POST /senders/{id}/assign-proxy"
    - "queue.py читает sender.rate_per_min/hour/day из БД вместо глобальных MAX_MSGS_PER_*"
    - "Listener и warmup-worker фильтруют sender'ов по lifecycle_status='active' AND auth_status='ok' (без is_active)"
    - "tests/conftest.py имеет фабрики test_workspace, test_sender_factory, test_checker, test_folder, test_contacts_factory"
  artifacts:
    - path: "migrations/013_phase2.sql"
      provides: "DDL для всех Phase 2 таблиц + расширение senders"
      contains: "CREATE TABLE IF NOT EXISTS folders, contacts, onboarding_sessions, csv_imports; ALTER TABLE senders DROP COLUMN IF EXISTS is_active"
    - path: "app/models/__init__.py"
      provides: "ORM-классы Folder, Contact, OnboardingSession, CsvImport + расширение Sender"
      contains: "class Folder(Base), class Contact(Base), class OnboardingSession(Base), class CsvImport(Base); Sender.lifecycle_status, Sender.rate_per_min/hour/day; NO is_active Column"
    - path: "app/routers/senders.py"
      provides: "Workspace-scoped CRUD sender'а + assign-proxy + warnings"
      contains: "Depends(auth_dep); _derive_status; RATE_HARD_CAP; warnings list"
    - path: "app/schemas/__init__.py"
      provides: "Pydantic-схемы SenderResponse (derived status), SenderUpdate (Literal lifecycle_status, rate_per_*), WarningItem, RateLimits, AssignProxyRequest, ProxyResponse"
    - path: "app/services/queue.py"
      provides: "Чтение rate_per_min/hour/day из sender"
      contains: "sender.rate_per_min"
    - path: "tests/conftest.py"
      provides: "Фабрики Workspace/Sender/Folder/Contact для Phase 2 тестов"
      contains: "test_sender_factory, test_workspace, test_checker, test_folder"
    - path: "tests/test_migration_013.py"
      provides: "Smoke на миграцию 013 (таблицы/индексы/CHECK)"
    - path: "tests/test_senders.py"
      provides: "Integration-тесты на rate_limit warnings/hard cap, lifecycle переходы, derived status, cross-tenant"
  key_links:
    - from: "app/main.py"
      to: "app/routers/senders.py"
      via: "app.include_router(senders.router)"
      pattern: "include_router\\(senders\\.router\\)"
    - from: "app/routers/senders.py"
      to: "app/utils/auth.py::auth_dep"
      via: "Depends(auth_dep)"
      pattern: "Depends\\(auth_dep\\)"
    - from: "app/services/queue.py"
      to: "senders.rate_per_min/hour/day"
      via: "SELECT из БД при rate-limit check"
      pattern: "rate_per_min"
    - from: "app/services/listener.py"
      to: "senders.lifecycle_status AND auth_status"
      via: "filter в get_active_senders"
      pattern: "lifecycle_status = 'active'"
---

<objective>
План закладывает фундамент Phase 2: миграция `013_phase2.sql` создаёт все новые таблицы (folders, contacts, onboarding_sessions, csv_imports) + расширяет `senders` (lifecycle_status, rate_per_min/hour/day, CHECK на role) + дропает `senders.is_active`. Расширяет ORM-модели + Pydantic-схемы под Phase 2. Переписывает `app/routers/senders.py` поверх `AuthCtx` с derived `status`, warnings[] для rate-limits и assign-proxy endpoint'ом. Выпиливает глобальные `MAX_MSGS_PER_*` константы в `app/services/queue.py` — теперь читаем из sender. Точечно правит `warmup.py`, `rotation.py`, `health.py` — все 14 мест использования `is_active` (см. PATTERNS.md Hidden Dependencies). Расширяет `tests/conftest.py` фабриками Phase 2.

Purpose: Wave 0 для всей Phase 2 — без миграции и расширенного Sender'а никакой другой план запуститься не может. Один план в W0 (а не три) — для атомарной транзакции в одной миграции (C-01 — recommend single file from RESEARCH §Wave Organization).

Output: SQL-миграция, расширенные ORM/Pydantic модели, переписанный senders router с rate-warnings, обновлённые services (queue/listener/warmup/rotation/health), pytest-фабрики Phase 2.
</objective>

<execution_context>
@/Users/andrewbruce/Documents/outreach-platform/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewbruce/Documents/outreach-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tg-accounts-contacts/02-CONTEXT.md
@.planning/phases/02-tg-accounts-contacts/02-RESEARCH.md
@.planning/phases/02-tg-accounts-contacts/02-PATTERNS.md
@.planning/phases/01-workspace-foundation/01-CONTEXT.md
@.planning/phases/01-workspace-foundation/01-03-api-skeleton-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@migrations/012_workspace.sql
@app/models/__init__.py
@app/schemas/__init__.py
@app/routers/senders.py
@app/routers/workspace.py
@app/routers/health.py
@app/services/queue.py
@app/services/listener.py
@app/services/warmup.py
@app/services/rotation.py
@app/utils/auth.py
@tests/conftest.py
@tests/test_migration_012.py
@tests/test_workspace_router.py

<interfaces>
<!-- Контракты Phase 1 для AuthDep/AuthCtx — executor использует напрямую, без exploration. -->

From app/utils/auth.py (Phase 1):
```python
@dataclass
class AuthCtx:
    workspace_id: UUID
    user_id: Optional[str]      # None для API-key, str для JWT
    source: Literal["jwt", "api_key"]
    role: Optional[str]         # 'owner' | 'admin' | 'member' (для JWT)

async def auth_dep(...) -> AuthCtx: ...
```

From app/services/queue.py (текущий, до правки):
```python
MAX_MSGS_PER_MINUTE = 4   # CONSTANTS TO REMOVE (D-13)
MAX_MSGS_PER_HOUR = 20    # CONSTANTS TO REMOVE
MAX_MSGS_PER_DAY = 150    # CONSTANTS TO REMOVE
# Прочие константы (MIN_SEND_INTERVAL, LONG_PAUSE_*, FLOOD_HARD_THRESHOLD,
# WORK_HOUR_*) — НЕ ТРОГАТЬ (CLAUDE.md явный запрет).
```

From app/models/__init__.py (текущий Sender):
```python
class Sender(Base):
    __tablename__ = "senders"
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    workspace_id = Column(UUID(as_uuid=True), ForeignKey("workspaces.id", ondelete="CASCADE"), nullable=False)  # Phase 1
    slug = Column(String(50), unique=True, nullable=False, index=True)
    name = Column(String(100), nullable=False)
    phone = Column(String(20), nullable=False)
    session_string = Column(Text, nullable=False)
    ai_context_id = Column(UUID(as_uuid=True), ForeignKey("ai_contexts.id"))
    role = Column(String(20), nullable=False, default="sender")   # CHECK добавим в миграции 013
    proxy = Column(JSONB, nullable=True)
    is_active = Column(Boolean, default=True, server_default='true')   # DROPPED in 013
    auth_status = Column(String(20), nullable=False, default="ok")
    last_used_at = Column(DateTime(timezone=True))
    created_at = Column(DateTime(timezone=True), server_default=func.now())
```

From app/utils/auth.py (Phase 1 D-12, для справки):
```python
# AuthCtx.workspace_id всегда заполнен — фильтр workspace_id == ctx.workspace_id обязателен в каждом SELECT/UPDATE/DELETE.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Миграция 013_phase2.sql — все Phase 2 таблицы + расширение senders</name>
  <files>migrations/013_phase2.sql, tests/test_migration_013.py</files>
  <read_first>
    - migrations/012_workspace.sql (Phase 1 паттерн: BEGIN/COMMIT, IF NOT EXISTS, CONSTRAINT *_check)
    - tests/test_migration_012.py (паттерн smoke-теста миграции через exec_driver_sql)
    - tests/conftest.py (как _setup_database применяет миграции через conn.exec_driver_sql)
    - .planning/phases/02-tg-accounts-contacts/02-RESEARCH.md §"Code Examples → Migration 013 (skeleton)" — full DDL skeleton, используй verbatim
    - .planning/phases/02-tg-accounts-contacts/02-PATTERNS.md §"1. migrations/013_phase2.sql" — стиль и анти-паттерны
    - .planning/phases/02-tg-accounts-contacts/02-CONTEXT.md D-01, D-02, D-05, D-11, D-13, D-16 — точные схемы таблиц
  </read_first>
  <behavior>
    - Test 1 (smoke): миграция выполняется на test-БД без ошибок (idempotent: повторное применение не падает).
    - Test 2: после миграции таблицы folders, contacts, onboarding_sessions, csv_imports существуют (`SELECT 1 FROM <table> LIMIT 0`).
    - Test 3: `INSERT INTO contacts (workspace_id, folder_id, phone, tg_status) VALUES (..., 'invalid_status')` → IntegrityError (CHECK constraint contacts_tg_status_check).
    - Test 4: `INSERT INTO senders (..., role) VALUES (..., 'random_role')` → IntegrityError (senders_role_check).
    - Test 5: `INSERT INTO senders (..., lifecycle_status) VALUES (..., 'broken')` → IntegrityError.
    - Test 6: партишн-индекс `(workspace_id, phone) WHERE phone IS NOT NULL` отклоняет дубликат при `INSERT (ws1, '+79001234567')` дважды.
    - Test 7: после миграции колонка `senders.is_active` не существует (`information_schema.columns WHERE table_name='senders' AND column_name='is_active'` → 0 строк).
    - Test 8: колонки `senders.lifecycle_status`, `senders.rate_per_min/hour/day` существуют с правильными defaults ('active', 4, 20, 150).
  </behavior>
  <action>
    1. Создать `migrations/013_phase2.sql` с заголовком:
       ```sql
       -- migrations/013_phase2.sql
       -- Phase 2: TG Accounts & Contacts foundation
       -- Adds: contacts, folders, onboarding_sessions, csv_imports tables
       -- Modifies: senders (+ rate_per_min/hour/day, + lifecycle_status, + role CHECK; - is_active)
       -- БД пустая (Phase 1 D-01). Все операторы идемпотентны (IF NOT EXISTS / IF EXISTS).
       BEGIN;
       ```
    2. **folders** (D-05): `CREATE TABLE IF NOT EXISTS folders (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), workspace_id UUID NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE, name VARCHAR(100) NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), CONSTRAINT folders_workspace_name_unique UNIQUE (workspace_id, name));` + `CREATE INDEX IF NOT EXISTS idx_folders_workspace ON folders(workspace_id);`
    3. **contacts** (D-01): полная DDL из RESEARCH.md §"Code Examples → Migration 013" — копировать verbatim (поля phone/username/full_name/source/custom JSONB/tg_status/tg_telegram_id/tg_username_resolved/tg_error/tg_checked_at/created_at/updated_at + CHECK contacts_tg_status_check IN ('pending','registered','not_registered','error','unchecked') + CHECK contacts_phone_or_username_check (phone IS NOT NULL OR username IS NOT NULL)). FK folder_id REFERENCES folders(id) ON DELETE CASCADE.
    4. Индексы contacts: `idx_contacts_workspace`, `idx_contacts_folder`, partial `idx_contacts_tg_status ... WHERE tg_status = 'pending'`, partial UNIQUE `idx_contacts_workspace_phone_unique ON (workspace_id, phone) WHERE phone IS NOT NULL`, partial UNIQUE `idx_contacts_workspace_username_unique ON (workspace_id, username) WHERE username IS NOT NULL`.
    5. **onboarding_sessions** (D-16): DDL из RESEARCH skeleton с CHECK onboarding_sessions_role_check IN ('sender','checker') и CHECK onboarding_sessions_status_check IN ('code_sent','awaiting_2fa','completed','failed'). Индексы: idx_onboarding_sessions_workspace, idx_onboarding_sessions_expires_at.
    6. **csv_imports** (C-02, RESEARCH рекомендация Option B): DDL с `file_data BYTEA NOT NULL`, `columns JSONB NOT NULL`, `suggested_mapping JSONB NOT NULL`, `encoding VARCHAR(20)`, `delimiter VARCHAR(5)`, `expires_at TIMESTAMPTZ NOT NULL DEFAULT (NOW() + INTERVAL '30 minutes')`. Индексы: idx_csv_imports_workspace, idx_csv_imports_expires_at.
    7. **senders** ALTER (D-11, D-13, CONTEXT `<specifics>` role CHECK):
       ```sql
       ALTER TABLE senders ADD COLUMN IF NOT EXISTS lifecycle_status VARCHAR(20) NOT NULL DEFAULT 'active';
       ALTER TABLE senders DROP CONSTRAINT IF EXISTS senders_lifecycle_status_check;
       ALTER TABLE senders ADD CONSTRAINT senders_lifecycle_status_check CHECK (lifecycle_status IN ('active','warmup','paused'));
       ALTER TABLE senders ADD COLUMN IF NOT EXISTS rate_per_min INT NOT NULL DEFAULT 4;
       ALTER TABLE senders ADD COLUMN IF NOT EXISTS rate_per_hour INT NOT NULL DEFAULT 20;
       ALTER TABLE senders ADD COLUMN IF NOT EXISTS rate_per_day INT NOT NULL DEFAULT 150;
       ALTER TABLE senders DROP CONSTRAINT IF EXISTS senders_role_check;
       ALTER TABLE senders ADD CONSTRAINT senders_role_check CHECK (role IN ('sender','checker'));
       ALTER TABLE senders DROP COLUMN IF EXISTS is_active;
       ```
    8. `COMMIT;` в конце.
    9. Создать `tests/test_migration_013.py` по паттерну `tests/test_migration_012.py`:
       - Тест применяет миграцию к test-engine.
       - Проверяет существование всех 4 таблиц через `inspect(engine).get_table_names()`.
       - Проверяет CHECK constraints через попытку `INSERT` с невалидным значением и ожидаемый `IntegrityError`.
       - Проверяет partial UNIQUE index по `(workspace_id, phone)`.
       - Проверяет `column 'is_active' NOT IN information_schema.columns WHERE table_name='senders'`.
       - Проверяет defaults rate_per_min=4, rate_per_hour=20, rate_per_day=150, lifecycle_status='active'.
    10. Обновить `tests/conftest.py::_setup_database` — добавить применение миграции 013 ПОСЛЕ 012:
        ```python
        sql_013 = (PROJECT_ROOT / "migrations" / "013_phase2.sql").read_text()
        await conn.exec_driver_sql(sql_013)
        ```
  </action>
  <verify>
    <automated>pytest tests/test_migration_013.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File `migrations/013_phase2.sql` exists AND contains `CREATE TABLE IF NOT EXISTS folders`
    - `migrations/013_phase2.sql` contains `CREATE TABLE IF NOT EXISTS contacts`
    - `migrations/013_phase2.sql` contains `CREATE TABLE IF NOT EXISTS onboarding_sessions`
    - `migrations/013_phase2.sql` contains `CREATE TABLE IF NOT EXISTS csv_imports`
    - `migrations/013_phase2.sql` contains `ALTER TABLE senders DROP COLUMN IF EXISTS is_active`
    - `migrations/013_phase2.sql` contains `ADD CONSTRAINT senders_role_check CHECK (role IN ('sender','checker'))`
    - `migrations/013_phase2.sql` contains `ADD COLUMN IF NOT EXISTS rate_per_min INT NOT NULL DEFAULT 4`
    - `migrations/013_phase2.sql` contains `ON (workspace_id, phone) WHERE phone IS NOT NULL`
    - `migrations/013_phase2.sql` starts with `BEGIN;` AND ends with `COMMIT;`
    - `tests/test_migration_013.py` exists AND `pytest tests/test_migration_013.py -v` exits 0
    - `tests/conftest.py` contains `013_phase2.sql` (миграция применяется в setup)
    - Повторное применение миграции не падает (idempotent — все ALTER через IF NOT EXISTS / DROP CONSTRAINT IF EXISTS)
  </acceptance_criteria>
  <done>Миграция 013 создаёт все Phase 2 таблицы и расширяет senders; smoke-тест зелёный; conftest.py применяет 013 после 012.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ORM-модели + Pydantic-схемы + pytest-фабрики Phase 2</name>
  <files>app/models/__init__.py, app/schemas/__init__.py, tests/conftest.py</files>
  <read_first>
    - app/models/__init__.py — текущий стиль ORM моделей; раздел `# ─── Multi-tenant foundation`, классы Workspace/Sender/AIContext
    - app/schemas/__init__.py — стиль Pydantic v2 (ConfigDict, Field, Literal), классы SenderCreate/SenderUpdate/SenderResponse/ProxyConfig
    - .planning/phases/02-tg-accounts-contacts/02-PATTERNS.md §"9. app/models/__init__.py" — точный skeleton классов Folder, Contact, OnboardingSession, CsvImport
    - .planning/phases/02-tg-accounts-contacts/02-PATTERNS.md §"10. app/schemas/__init__.py" — стиль Literal + Field hard cap
    - .planning/phases/02-tg-accounts-contacts/02-RESEARCH.md §"pytest Fixtures для Phase 2" — фабрики test_workspace, test_sender_factory, test_checker, test_folder, test_contacts_factory, auth_headers
    - tests/conftest.py — текущий стиль pytest_asyncio.fixture + AsyncSessionLocal
  </read_first>
  <behavior>
    - Test 1: `from app.models import Folder, Contact, OnboardingSession, CsvImport` импортируется без ошибок.
    - Test 2: `Sender.__table__.columns` НЕ содержит 'is_active'; содержит 'lifecycle_status', 'rate_per_min', 'rate_per_hour', 'rate_per_day'.
    - Test 3: `from app.schemas import FolderResponse, FolderCreate, ContactResponse, ContactCreate, SenderUpdate, WarningItem, RateLimits, AssignProxyRequest` — все импортируются.
    - Test 4: `SenderUpdate(rate_per_min=11)` → `ValidationError` (hard cap le=10).
    - Test 5: `SenderUpdate(lifecycle_status="invalid")` → `ValidationError` (Literal restriction).
    - Test 6: фабрика `test_sender_factory(role='checker')` создаёт sender'а с role='checker', auth_status='ok'.
    - Test 7: фабрика `test_folder(name="X")` создаёт folder с workspace_id=test_workspace.id.
    - Test 8: фабрика `test_contacts_factory(count=3)` создаёт 3 контакта в test_folder.
  </behavior>
  <action>
    1. В `app/models/__init__.py` добавить **в конец файла** (после существующих моделей):
       ```python
       # ─── Phase 2: Folders, Contacts, Onboarding sessions, CSV imports ────────────
       
       class Folder(Base):
           __tablename__ = "folders"
           id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
           workspace_id = Column(UUID(as_uuid=True),
                                 ForeignKey("workspaces.id", ondelete="CASCADE"), nullable=False)
           name = Column(String(100), nullable=False)
           created_at = Column(DateTime(timezone=True), server_default=func.now())
           updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
       
       class Contact(Base):
           __tablename__ = "contacts"
           # NB: НЕ ПУТАТЬ с ContactCache (per-sender Telegram-resolve cache из Phase 0)
           id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
           workspace_id = Column(UUID(as_uuid=True),
                                 ForeignKey("workspaces.id", ondelete="CASCADE"), nullable=False)
           folder_id = Column(UUID(as_uuid=True),
                              ForeignKey("folders.id", ondelete="CASCADE"), nullable=False)
           phone = Column(String(20), nullable=True)
           username = Column(String(50), nullable=True)
           full_name = Column(String(200), nullable=True)
           source = Column(String(100), nullable=True)
           custom = Column(JSONB, nullable=False, server_default='{}')
           tg_status = Column(String(20), nullable=False, server_default='pending')
           tg_telegram_id = Column(BigInteger, nullable=True)
           tg_username_resolved = Column(String(50), nullable=True)
           tg_error = Column(Text, nullable=True)
           tg_checked_at = Column(DateTime(timezone=True), nullable=True)
           created_at = Column(DateTime(timezone=True), server_default=func.now())
           updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
           folder = relationship("Folder")
       
       class OnboardingSession(Base):
           __tablename__ = "onboarding_sessions"
           id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
           workspace_id = Column(UUID(as_uuid=True),
                                 ForeignKey("workspaces.id", ondelete="CASCADE"), nullable=False)
           phone = Column(String(20), nullable=False)
           phone_code_hash = Column(Text, nullable=False)
           encrypted_session_string = Column(Text, nullable=False)
           role = Column(String(20), nullable=False, server_default='sender')
           proxy = Column(JSONB, nullable=True)
           status = Column(String(20), nullable=False)
           expires_at = Column(DateTime(timezone=True), nullable=False)
           created_at = Column(DateTime(timezone=True), server_default=func.now())
       
       class CsvImport(Base):
           __tablename__ = "csv_imports"
           id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
           workspace_id = Column(UUID(as_uuid=True),
                                 ForeignKey("workspaces.id", ondelete="CASCADE"), nullable=False)
           file_data = Column(LargeBinary, nullable=False)
           columns = Column(JSONB, nullable=False)
           suggested_mapping = Column(JSONB, nullable=False)
           encoding = Column(String(20), nullable=True)
           delimiter = Column(String(5), nullable=True)
           created_at = Column(DateTime(timezone=True), server_default=func.now())
           expires_at = Column(DateTime(timezone=True), nullable=False)
       ```
       Импорт `from sqlalchemy import LargeBinary, BigInteger, Text` — добавить если отсутствует.
    2. В `app/models/__init__.py` **расширить класс Sender**:
       - **Удалить полностью** строку `is_active = Column(Boolean, default=True, server_default='true')`.
       - **Добавить** после `auth_status`:
         ```python
         lifecycle_status = Column(String(20), nullable=False, server_default='active')
         rate_per_min = Column(Integer, nullable=False, server_default='4')
         rate_per_hour = Column(Integer, nullable=False, server_default='20')
         rate_per_day = Column(Integer, nullable=False, server_default='150')
         ```
    3. В `app/schemas/__init__.py` **добавить в конец файла**:
       ```python
       # ─── Phase 2: Folders / Contacts / Senders extension ─────────────────────────
       
       class FolderCreate(BaseModel):
           name: str = Field(..., min_length=1, max_length=100)
       
       class FolderUpdate(BaseModel):
           name: str = Field(..., min_length=1, max_length=100)
       
       class FolderResponse(BaseModel):
           model_config = ConfigDict(from_attributes=True)
           id: UUID
           name: str
           contact_count: int = 0
           created_at: datetime
           updated_at: datetime
       
       class WarningItem(BaseModel):
           field: str
           value: int
           recommended_max: int
           severity: Literal["warning"] = "warning"
       
       class RateLimits(BaseModel):
           per_minute: int = 4
           per_hour: int = 20
           per_day: int = 150
       
       class AssignProxyRequest(BaseModel):
           proxy_id: UUID
       
       class ProxyPoolItem(BaseModel):
           model_config = ConfigDict(from_attributes=True)
           id: UUID
           host: str
           port: int
           type: str
           username: Optional[str] = None
       
       class ProxyPoolCreate(BaseModel):
           host: str
           port: int = Field(..., ge=1, le=65535)
           type: Literal["socks5", "http"] = "socks5"
           username: Optional[str] = None
           password: Optional[str] = None
       
       class ContactBase(BaseModel):
           phone: Optional[str] = None
           username: Optional[str] = None
           full_name: Optional[str] = None
           source: Optional[str] = None
           custom: dict = {}
       
       class ContactCreate(ContactBase):
           folder_id: Optional[UUID] = None
           folder_name: Optional[str] = None
           
           @model_validator(mode="after")
           def folder_or_name(self):
               if not self.folder_id and not self.folder_name:
                   raise ValueError("Either folder_id or folder_name required")
               if not self.phone and not self.username:
                   raise ValueError("Either phone or username required")
               return self
       
       class ContactBatchPush(BaseModel):
           contacts: list[ContactCreate] = Field(..., min_length=1, max_length=1000)
       
       class ContactResponse(BaseModel):
           model_config = ConfigDict(from_attributes=True)
           id: UUID
           folder_id: UUID
           phone: Optional[str]
           username: Optional[str]
           full_name: Optional[str]
           source: Optional[str]
           custom: dict
           tg_status: str
           tg_telegram_id: Optional[int]
           tg_username_resolved: Optional[str]
           tg_checked_at: Optional[datetime]
           created_at: datetime
       
       class ContactImportRequest(BaseModel):
           import_id: UUID
           folder_id: Optional[UUID] = None
           folder_name: Optional[str] = None
           mapping: dict[str, str]   # {"0": "phone", "1": "full_name", "2": "custom.company"}
           on_duplicate: Literal["skip"] = "skip"
           
           @model_validator(mode="after")
           def has_folder_target(self):
               if not self.folder_id and not self.folder_name:
                   raise ValueError("Either folder_id or folder_name required")
               return self
       
       class ContactImportPreviewResponse(BaseModel):
           import_id: UUID
           columns: list[str]
           sample_rows: list[dict]
           suggested_mapping: dict[str, str]
           encoding: Optional[str]
           delimiter: Optional[str]
           looks_like_no_header: bool = False
       
       class ContactImportSummary(BaseModel):
           total: int
           imported: int
           skipped_duplicates: int
           skipped_invalid: int
           skipped_phones: list[str] = []
       
       class MoveContactRequest(BaseModel):
           folder_id: UUID
       
       class MoveContactBatchRequest(BaseModel):
           contact_ids: list[UUID] = Field(..., min_length=1)
           folder_id: UUID
       
       class RecheckRequest(BaseModel):
           contact_ids: Optional[list[UUID]] = None
           folder_id: Optional[UUID] = None
           
           @model_validator(mode="after")
           def one_required(self):
               if not self.contact_ids and not self.folder_id:
                   raise ValueError("Either contact_ids or folder_id required")
               return self
       ```
    4. **Переписать SenderResponse и SenderUpdate** в `app/schemas/__init__.py`:
       ```python
       class SenderResponse(BaseModel):
           model_config = ConfigDict(from_attributes=True)
           id: UUID
           slug: str
           name: str
           phone: str
           status: Literal["active", "warmup", "paused", "error"]      # derived (D-11)
           auth_status: str
           lifecycle_status: Literal["active", "warmup", "paused"]
           rate_limits: RateLimits
           role: str
           proxy: Optional[ProxyConfig] = None
           ai_context_id: Optional[UUID] = None
           ai_context_name: Optional[str] = None
           last_used_at: Optional[datetime] = None
           created_at: datetime
       
       class SenderUpdate(BaseModel):
           name: Optional[str] = None
           lifecycle_status: Optional[Literal["active", "warmup", "paused"]] = None
           rate_per_min: Optional[int] = Field(None, ge=1, le=10)
           rate_per_hour: Optional[int] = Field(None, ge=1, le=50)
           rate_per_day: Optional[int] = Field(None, ge=1, le=300)
           proxy: Optional[ProxyConfig] = None
           ai_context_id: Optional[UUID] = None
       
       class SenderCreateResponse(BaseModel):
           sender: SenderResponse
           warnings: list[WarningItem] = []
       ```
       Удалить старое поле `is_active: bool` из SenderResponse и SenderUpdate.
       Поверх существующей `SenderCreate` в схеме — оставить `is_active` дропнуть, добавить `rate_per_min/hour/day: Optional[int]` поля с теми же hard cap'ами.
    5. **Расширить `tests/conftest.py`** новыми фабриками — добавить в конец файла (после существующих фикстур Phase 1):
       ```python
       from app.models import Folder, Contact, Sender, Workspace, OnboardingSession
       
       @pytest_asyncio.fixture
       async def test_workspace(async_db_session):
           ws = Workspace(name="Test Workspace")
           async_db_session.add(ws)
           await async_db_session.commit()
           await async_db_session.refresh(ws)
           return ws
       
       @pytest_asyncio.fixture
       async def test_sender_factory(async_db_session, test_workspace):
           counter = {"n": 0}
           async def _make(**overrides):
               counter["n"] += 1
               n = counter["n"]
               defaults = dict(
                   workspace_id=test_workspace.id,
                   slug=f"test-sender-{n}",
                   name=f"Test Sender {n}",
                   phone=f"+7900000{n:04d}",
                   session_string="encrypted_stub",
                   role="sender",
                   auth_status="ok",
                   lifecycle_status="active",
                   rate_per_min=4,
                   rate_per_hour=20,
                   rate_per_day=150,
               )
               defaults.update(overrides)
               s = Sender(**defaults)
               async_db_session.add(s)
               await async_db_session.commit()
               await async_db_session.refresh(s)
               return s
           return _make
       
       @pytest_asyncio.fixture
       async def test_checker(test_sender_factory):
           return await test_sender_factory(role="checker", slug="test-checker")
       
       @pytest_asyncio.fixture
       async def test_folder(async_db_session, test_workspace):
           f = Folder(workspace_id=test_workspace.id, name="Test Folder")
           async_db_session.add(f)
           await async_db_session.commit()
           await async_db_session.refresh(f)
           return f
       
       @pytest_asyncio.fixture
       async def test_contacts_factory(async_db_session, test_workspace, test_folder):
           async def _make(count=1, tg_status="pending", **overrides):
               contacts = []
               for i in range(count):
                   c = Contact(
                       workspace_id=test_workspace.id,
                       folder_id=test_folder.id,
                       phone=f"+7901000{i:04d}",
                       full_name=f"Contact {i}",
                       source="test",
                       tg_status=tg_status,
                       **overrides,
                   )
                   async_db_session.add(c)
                   contacts.append(c)
               await async_db_session.commit()
               return contacts if count > 1 else contacts[0]
           return _make
       ```
  </action>
  <verify>
    <automated>python -c "from app.models import Folder, Contact, OnboardingSession, CsvImport, Sender; print('is_active' not in [c.name for c in Sender.__table__.columns]); print('rate_per_min' in [c.name for c in Sender.__table__.columns])"</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/__init__.py` содержит `class Folder(Base):` AND `class Contact(Base):` AND `class OnboardingSession(Base):` AND `class CsvImport(Base):`
    - `app/models/__init__.py` НЕ содержит строку `is_active = Column(Boolean` (grep отсутствует)
    - `app/models/__init__.py` содержит `lifecycle_status = Column(String(20)` AND `rate_per_min = Column(Integer`
    - `app/schemas/__init__.py` содержит `class FolderResponse` AND `class ContactCreate` AND `class WarningItem` AND `class AssignProxyRequest` AND `class ContactImportRequest` AND `class ContactImportPreviewResponse`
    - `app/schemas/__init__.py` содержит `Literal["active", "warmup", "paused", "error"]` (derived status в SenderResponse)
    - `tests/conftest.py` содержит `test_sender_factory` AND `test_checker` AND `test_folder` AND `test_contacts_factory` AND `test_workspace`
    - `python -c "from app.models import Folder, Contact, OnboardingSession, CsvImport"` exits 0
    - `python -c "from app.schemas import FolderResponse, ContactCreate, WarningItem"` exits 0
  </acceptance_criteria>
  <done>ORM-модели Phase 2 импортируются; SenderResponse возвращает derived status; pytest-фабрики готовы.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: senders router rewrite + queue.py constants removal + is_active grep-fix в services/health</name>
  <files>app/routers/senders.py, app/services/queue.py, app/services/listener.py, app/services/warmup.py, app/services/rotation.py, app/routers/health.py, app/main.py, tests/test_senders.py</files>
  <read_first>
    - app/routers/senders.py — текущий код (источник CRUD-структуры; subprocess.run выпиливаем целиком)
    - app/routers/workspace.py — паттерн AuthDep + workspace-isolation (копируем 1-в-1 структуру imports, error-dict, TODO(v2-rls))
    - app/services/queue.py — `_check_rate_limits` функция и `MAX_MSGS_PER_*` константы (lines 42-44)
    - app/services/listener.py — `get_active_senders` фильтр (line ~331) и `_set_auth_status` (line 148-152) — обе функции меняем
    - app/services/warmup.py — line 171 `s.is_active` filter
    - app/services/rotation.py — lines 48, 147 — text() запросы с `is_active`
    - app/routers/health.py — line 37 `s.is_active`
    - .planning/phases/02-tg-accounts-contacts/02-PATTERNS.md §"4. app/routers/senders.py" — derived_status helper, RATE_HARD_CAP, _validate_rate_limits skeleton
    - .planning/phases/02-tg-accounts-contacts/02-RESEARCH.md §"Hidden Dependencies → senders.is_active" — таблица 14 мест
    - .planning/phases/02-tg-accounts-contacts/02-CONTEXT.md D-11..D-15, D-22, D-14 warnings shape, C-03
  </read_first>
  <behavior>
    - Test 1: `GET /api/v1/senders` без auth → 401 (`AUTH_REQUIRED`).
    - Test 2: `GET /api/v1/senders` с JWT для workspace A → возвращает только sender'ов workspace A; sender'ы workspace B не видны.
    - Test 3: `GET /api/v1/senders/{id}` возвращает `status: 'active'` если `lifecycle_status='active' AND auth_status='ok'`.
    - Test 4: `GET /api/v1/senders/{id}` возвращает `status: 'error'` если `auth_status='session_expired'` (независимо от lifecycle_status).
    - Test 5: `PATCH /api/v1/senders/{id} {"rate_per_min": 7}` → 200 + `warnings: [{field: 'rate_per_min', value: 7, recommended_max: 4, severity: 'warning'}]`.
    - Test 6: `PATCH /api/v1/senders/{id} {"rate_per_min": 15}` → 422 (`RATE_LIMIT_EXCEEDS_HARD_CAP`).
    - Test 7: `PATCH /api/v1/senders/{id} {"lifecycle_status": "paused"}` → 200, статус persisted, derived status='paused'.
    - Test 8: `POST /api/v1/senders/{id}/assign-proxy {"proxy_id": ...}` → 200, sender.proxy обновлён JSON'ом из proxy_pool row.
    - Test 9: cross-tenant — sender в workspace B недоступен из workspace A → 404 (не 403).
    - Test 10: `GET /api/v1/workspace/proxies` возвращает только прокси workspace; `POST /api/v1/workspace/proxies` создаёт; `DELETE` удаляет.
    - Test 11: queue.py читает `sender.rate_per_min` per-tick (через инкремент кастомного значения и проверку поведения).
  </behavior>
  <action>
    1. **Полный rewrite `app/routers/senders.py`** — копировать стиль `app/routers/workspace.py`:
       ```python
       """Senders router (Phase 2 — SNDR-01, SNDR-02, SNDR-03).
       
       Endpoints:
         GET    /api/v1/senders               — list workspace senders с derived status
         GET    /api/v1/senders/{id}          — single
         POST   /api/v1/senders               — create (после онбординга обычно)
         PATCH  /api/v1/senders/{id}          — update + warnings[] для rate limits
         DELETE /api/v1/senders/{id}          — cascade delete
         POST   /api/v1/senders/{id}/assign-proxy
         GET    /api/v1/senders/{id}/spambot-check
         GET    /api/v1/workspace/proxies     — list workspace proxies (D-22)
         POST   /api/v1/workspace/proxies     — add proxy
         DELETE /api/v1/workspace/proxies/{id} — remove proxy
       """
       
       import logging
       from typing import Optional
       from uuid import UUID
       from fastapi import APIRouter, Depends, HTTPException, Query
       from sqlalchemy import select, func as sql_func
       from sqlalchemy.ext.asyncio import AsyncSession
       from sqlalchemy.orm import selectinload
       
       from app.database import get_db
       from app.models import Sender, ProxyPool, AIContext
       from app.schemas import (
           SenderResponse, SenderCreate, SenderUpdate, SenderCreateResponse,
           WarningItem, RateLimits, AssignProxyRequest, ProxyPoolItem,
           ProxyPoolCreate, ProxyConfig,
       )
       from app.utils.auth import AuthCtx, auth_dep
       
       logger = logging.getLogger(__name__)
       router = APIRouter(prefix="/api/v1", tags=["senders"])
       
       # D-14 caps:
       RATE_HARD_CAP = {"rate_per_min": 10, "rate_per_hour": 50, "rate_per_day": 300}
       RATE_SOFT_CAP = {"rate_per_min": 4, "rate_per_hour": 20, "rate_per_day": 150}
       
       def _derive_status(sender: Sender) -> str:
           if sender.auth_status != "ok":
               return "error"
           return sender.lifecycle_status
       
       def _sender_to_response(sender: Sender) -> SenderResponse:
           return SenderResponse(
               id=sender.id,
               slug=sender.slug,
               name=sender.name,
               phone=sender.phone,
               status=_derive_status(sender),
               auth_status=sender.auth_status,
               lifecycle_status=sender.lifecycle_status,
               rate_limits=RateLimits(
                   per_minute=sender.rate_per_min,
                   per_hour=sender.rate_per_hour,
                   per_day=sender.rate_per_day,
               ),
               role=sender.role,
               proxy=ProxyConfig(**sender.proxy) if sender.proxy else None,
               ai_context_id=sender.ai_context_id,
               ai_context_name=sender.ai_context.name if sender.ai_context else None,
               last_used_at=sender.last_used_at,
               created_at=sender.created_at,
           )
       
       def _validate_rate_limits(payload: SenderUpdate) -> list[WarningItem]:
           warnings = []
           for field, hard in RATE_HARD_CAP.items():
               val = getattr(payload, field, None)
               if val is None:
                   continue
               if val > hard:
                   raise HTTPException(422, {
                       "code": "RATE_LIMIT_EXCEEDS_HARD_CAP",
                       "field": field,
                       "value": val,
                       "hard_cap": hard,
                       "message": "exceeds maximum safe limit, contact support if you need higher",
                   })
               if val > RATE_SOFT_CAP[field]:
                   warnings.append(WarningItem(
                       field=field, value=val, recommended_max=RATE_SOFT_CAP[field],
                   ))
           return warnings
       ```
       Затем все endpoint'ы — list/get/create/patch/delete/spambot-check/assign-proxy/proxy_pool — ВСЕ через `ctx: AuthCtx = Depends(auth_dep)` + `where(Sender.workspace_id == ctx.workspace_id)` + комментарий `# TODO(v2-rls): replaced by RLS policy`. ВЕЗДЕ возвращать через `_sender_to_response`.
       
       **ВЫПИЛИТЬ** из текущего senders.py:
       - `_restart_listener()` функция (lines 36-50)
       - все 3 вызова `subprocess.run(["docker", "restart", "telegram-listener"], ...)` (lines 43, 148, 221)
       - `import subprocess` (line ~17)
       - `from app.routers.auth import verify_api_key` импорт
       - `is_active = False` в SpamBot-check (line 308) — заменить на простой `sender.auth_status = new_auth_status`
       - параметр `_: str = Depends(verify_api_key)` во всех endpoint'ах — заменить на `ctx: AuthCtx = Depends(auth_dep)`
       
       **PATCH endpoint должен:**
       - Загрузить sender (workspace-isolated)
       - Вызвать `_validate_rate_limits(payload)` → если >hard cap → 422
       - Применить изменения, await commit + refresh
       - Вернуть `SenderCreateResponse(sender=_sender_to_response(s), warnings=warnings)`
       
       **POST /senders/{id}/assign-proxy:**
       - Загрузить ProxyPool row workspace-isolated by `proxy_id`
       - Записать в sender.proxy JSON: `{type, host, port, username, password}` из proxy_pool row
       - Commit + refresh
       
       **GET /workspace/proxies, POST /workspace/proxies, DELETE /workspace/proxies/{id}** — стандартный CRUD на ProxyPool (workspace-scoped через Phase 1 миграцию).
       
       **GET /senders/{id}/spambot-check** — оставить логику spambot (через checker_service.check_spambot), но БЕЗ `sender.is_active = False` — только `sender.auth_status` update'ом.
    
    2. **`app/services/queue.py`** — точечная правка:
       - **Удалить** строки 42-44:
         ```python
         MAX_MSGS_PER_MINUTE = 4
         MAX_MSGS_PER_HOUR = 20
         MAX_MSGS_PER_DAY = 150
         ```
       - Добавить вместо них комментарий:
         ```python
         # Per-sender rate limits live on senders.rate_per_min/hour/day columns (Phase 2 D-13).
         # The same empirically-tuned 4/20/150 "green corridor" live as DB server_default.
         # NB: Other rate constants (MIN_SEND_INTERVAL, LONG_PAUSE_*, FLOOD_HARD_THRESHOLD,
         #     WORK_HOUR_*) — NOT TOUCHED per CLAUDE.md (deferred to Phase 4 per-campaign).
         ```
       - Найти функцию `_check_rate_limits` (примерно lines 225-325). В каждом сравнении со старыми константами вставить чтение из sender'а ОДИН раз в начале функции:
         ```python
         sender_row = await db.execute(
             text("SELECT rate_per_min, rate_per_hour, rate_per_day FROM senders WHERE id = :sid"),
             {"sid": str(sender_id)},
         )
         limits_row = sender_row.fetchone()
         if not limits_row:
             return  # sender deleted concurrently
         max_per_min = limits_row.rate_per_min
         max_per_hour = limits_row.rate_per_hour
         max_per_day = limits_row.rate_per_day
         ```
         Затем все `MAX_MSGS_PER_MINUTE` → `max_per_min`, `MAX_MSGS_PER_HOUR` → `max_per_hour`, `MAX_MSGS_PER_DAY` → `max_per_day`.
       - `grep -n "is_active" app/services/queue.py` — если найдено: каждое `is_active=true` (в text() SQL) → `lifecycle_status='active' AND auth_status='ok'`; каждое ORM-`Sender.is_active` → `Sender.lifecycle_status == 'active'` и parallel `Sender.auth_status == 'ok'`.
    
    3. **`app/services/listener.py`** — точечные правки (НЕ добавлять reconcile-loop, это задача плана 02-01):
       - В `_set_auth_status` (lines 148-152): убрать `is_active = false` из UPDATE.
         ```python
         await db.execute(
             text("UPDATE senders SET auth_status = :status WHERE id = :sid"),
             {"status": auth_status, "sid": sender_id},
         )
         ```
       - В `get_active_senders` (line ~320-345): заменить фильтр в text() запросе на `WHERE role = 'sender' AND lifecycle_status = 'active' AND auth_status = 'ok'`. Полностью удалить `AND is_active = true` если есть.
       - Все остальные строки `is_active` (lines 1003, 1061, 1066): UPDATE-stmts с `is_active = false` → удалить, оставить только UPDATE `auth_status` (derived 'error' computed на чтении).
    
    4. **`app/services/warmup.py`** — line 171:
       - Заменить фильтр SELECT-запроса: `WHERE s.is_active = true AND s.role = 'sender'` → `WHERE s.lifecycle_status = 'active' AND s.auth_status = 'ok' AND s.role = 'sender'`.
    
    5. **`app/services/rotation.py`** — lines 48, 147:
       - Заменить `s.is_active` в text() запросах на `s.lifecycle_status = 'active' AND s.auth_status = 'ok'`.
    
    6. **`app/routers/health.py`** — line 37:
       - `active = sum(1 for s in senders if s.is_active)` → `active = sum(1 for s in senders if s.lifecycle_status == 'active' and s.auth_status == 'ok')`.
    
    7. **`app/main.py`** — добавить `include_router`:
       ```python
       from app.routers import senders
       ...
       app.include_router(senders.router)
       ```
       (после `workspace.router`)
    
    8. Создать `tests/test_senders.py` (использует фабрики Phase 2 из conftest.py):
       - test_list_senders_workspace_isolated
       - test_get_sender_derived_status_active
       - test_get_sender_derived_status_error_when_auth_expired
       - test_patch_sender_rate_limit_soft_returns_warnings
       - test_patch_sender_rate_limit_hard_cap_422
       - test_patch_sender_lifecycle_status_pause_resume
       - test_assign_proxy_from_workspace_pool
       - test_assign_proxy_cross_tenant_404
       - test_workspace_proxies_crud
       - test_senders_create_returns_warnings_when_above_soft
  </action>
  <verify>
    <automated>pytest tests/test_senders.py tests/test_migration_013.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/senders.py` exists AND содержит `Depends(auth_dep)` AND `RATE_HARD_CAP = {"rate_per_min": 10, "rate_per_hour": 50, "rate_per_day": 300}` AND `def _derive_status(`
    - `app/routers/senders.py` НЕ содержит `subprocess` (grep `subprocess` → 0 matches)
    - `app/routers/senders.py` НЕ содержит `verify_api_key`
    - `app/routers/senders.py` НЕ содержит `sender.is_active`
    - `app/routers/senders.py` содержит `assign-proxy`
    - `app/services/queue.py` НЕ содержит `MAX_MSGS_PER_MINUTE` AND НЕ содержит `MAX_MSGS_PER_HOUR` AND НЕ содержит `MAX_MSGS_PER_DAY`
    - `app/services/queue.py` содержит `rate_per_min` (читает из sender)
    - `app/services/listener.py` НЕ содержит `is_active = false` (grep)
    - `app/services/listener.py` содержит `lifecycle_status = 'active'` (в SQL фильтре get_active_senders)
    - `app/services/warmup.py` НЕ содержит `s.is_active`
    - `app/services/rotation.py` НЕ содержит `s.is_active`
    - `app/routers/health.py` НЕ содержит `s.is_active` (заменено на derived check)
    - `app/main.py` содержит `app.include_router(senders.router)`
    - `pytest tests/test_senders.py -v` exits 0
    - `pytest tests/test_migration_013.py -v` exits 0
    - `python -c "import app.main"` exits 0 (импорт-цепочка не падает)
    - `grep -rn "is_active" app/services/ app/routers/health.py app/routers/senders.py | grep -v warmup_pool` returns nothing (warmup_pool.is_active — другая колонка, оставляем)
  </acceptance_criteria>
  <done>
    senders router workspace-scoped с warnings/hard-cap и derived status; queue.py читает rate-limits из sender; все 14 мест is_active исправлены (кроме warmup_pool.is_active — это другая колонка); тесты зелёные; импорт app.main работает.
  </done>
</task>

</tasks>

<verification>
**Automated:**
1. `pytest tests/test_migration_013.py -v` — миграция применяется, CHECK constraints работают.
2. `pytest tests/test_senders.py -v` — все 10 integration-тестов зелёные.
3. `python -c "from app.models import Folder, Contact, OnboardingSession, CsvImport, Sender; assert 'is_active' not in [c.name for c in Sender.__table__.columns]; assert 'lifecycle_status' in [c.name for c in Sender.__table__.columns]"` — exit 0.
4. `python -c "import app.main"` — импорт всей цепочки без ошибок.
5. `grep -rn "MAX_MSGS_PER" app/` — 0 строк.
6. `grep -rn "is_active" app/services/ app/routers/health.py app/routers/senders.py | grep -v warmup_pool` — 0 строк.
7. `grep -rn "subprocess" app/routers/senders.py` — 0 строк.

**Manual smoke:**
1. `docker compose up -d db && pytest tests/ -v` локально — все 4 Phase 1 + 3 Phase 2 тестовых файла зелёные.
2. `GET /api/v1/senders` через curl с JWT — возвращает массив SenderResponse с derived status.
</verification>

<success_criteria>
- Миграция 013_phase2.sql создаёт folders, contacts, onboarding_sessions, csv_imports + расширяет senders (lifecycle_status, rate_per_min/hour/day, CHECK на role) + дропает is_active.
- ORM-модели Folder, Contact, OnboardingSession, CsvImport импортируются; Sender больше не имеет is_active.
- Pydantic-схемы FolderCreate/Response, ContactCreate/Response, SenderResponse (derived status), SenderUpdate (Literal lifecycle, hard cap), WarningItem, AssignProxyRequest, ContactImportRequest/Preview/Summary существуют.
- `app/routers/senders.py` — workspace-scoped CRUD через `Depends(auth_dep)` + derived status + warnings[] + assign-proxy + workspace proxy CRUD. Без subprocess. Без is_active writes.
- `app/services/queue.py` — глобальные `MAX_MSGS_PER_*` выпилены; rate-limit check читает sender.rate_per_min/hour/day.
- `app/services/listener.py`, `warmup.py`, `rotation.py`, `routers/health.py` — все 14 мест `senders.is_active` заменены на `lifecycle_status='active' AND auth_status='ok'`.
- `tests/conftest.py` имеет фабрики Phase 2 (test_workspace, test_sender_factory, test_checker, test_folder, test_contacts_factory) + применяет миграцию 013.
- `tests/test_migration_013.py` + `tests/test_senders.py` зелёные.
- SNDR-01 закрыт (rate-limit warnings — D-13/D-14). SNDR-02 закрыт (assign-proxy + workspace proxy CRUD — D-22). SNDR-03 закрыт (derived status — D-11/D-12).
</success_criteria>

<output>
After completion, create `.planning/phases/02-tg-accounts-contacts/02-02-SUMMARY.md` per template — отметить:
- Какие 14 мест is_active были исправлены (file:line list)
- Точные DDL операторы миграции (для аудита)
- Структура warnings response (для Lovable team)
- Список фабрик в conftest.py (для последующих планов 02-01/02-03/02-04/02-05 — они их используют)
- Готовность к Wave 1: 02-03 и 02-04 могут стартовать параллельно
</output>
