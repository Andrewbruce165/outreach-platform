---
phase: quick-260702-kf2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/analytics.py
  - tests/test_phase5_analytics_since.py
autonomous: true
requirements: [KF2-SINCE-01]
must_haves:
  truths:
    - "Все 4 cards-endpoints (workspace/campaigns/agents/senders) принимают опциональный ?since=1d|7d|30d|90d"
    - "since=None (не передан) → all-time поведение байт-в-байт как сейчас (регрессия зелёная, response shape не меняется)"
    - "При since!=None sent+replied фильтруются по m.created_at >= окно"
    - "При since!=None leads+finishes фильтруются по c.updated_at >= окно"
    - "При since!=None llm_spend фильтруется по lc.created_at >= окно"
    - "contacts_messaged/registered_contacts НЕ фильтруются периодом при любом since (прогресс кампании)"
    - "Значение дней в SQL через bind-param (:days), не f-string — как в /analytics/llm"
  artifacts:
    - path: "app/routers/analytics.py"
      provides: "since-параметр в _compute_cards + 4 endpoints"
      contains: "since"
    - path: "tests/test_phase5_analytics_since.py"
      provides: "параметризованные since-тесты"
      contains: "since"
  key_links:
    - from: "app/routers/analytics.py::workspace_analytics/campaign_analytics/agent_analytics/sender_analytics"
      to: "_compute_cards(..., since=since)"
      via: "проброс query-параметра"
      pattern: "since=since"
    - from: "_compute_cards"
      to: "SQL WHERE"
      via: "параметризованный interval clause"
      pattern: ":days.*days.*INTERVAL"
---

<objective>
KPI-карточки дашборда должны реагировать на выбранный период. Добавить опциональный query-параметр `since` во все 4 analytics cards-endpoints и пробросить его в `_compute_cards`, где он временно-фильтрует метрики.

Purpose: фронт (отдельный репо, оркестратор перевяжет сам) сможет передавать период фильтра, и карточки перестанут быть только all-time.
Output: изменённый `app/routers/analytics.py` + новый параметризованный тест-файл `tests/test_phase5_analytics_since.py`.

Backend-only. Схема БД НЕ меняется (миграций нет). Response shape `AnalyticsCards` НЕ меняется — существующие консюмеры не ломаются.
</objective>

<execution_context>
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/workflows/execute-plan.md
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/routers/analytics.py
@app/schemas/__init__.py
@tests/test_phase5_analytics.py
@tests/test_phase5_1_llm_aggregates.py

<interfaces>
<!-- Ключевые контракты из кодовой базы — использовать напрямую, без доп. разведки. -->

Существующий since-паттерн уже в этом файле (`/analytics/llm`, analytics.py:486-541):
```python
since: Literal["1d", "7d", "30d", "90d"] = "7d"
...
days = _SINCE_DAYS[since]                         # analytics.py:120: {"1d":1,"7d":7,"30d":30,"90d":90}
params = {..., "days": str(days)}
# в SQL:  AND lc.created_at >= NOW() - (:days || ' days')::INTERVAL
```

Текущая сигнатура (analytics.py:144):
```python
async def _compute_cards(db, workspace_id, scope: Optional[tuple[str, UUID]] = None) -> AnalyticsCards
```

Timestamp-колонки (подтверждено):
- messages.created_at   — TIMESTAMPTZ (migrations/017_phase5.sql:27)  → sent + replied
- conversations.updated_at — TIMESTAMPTZ (models/__init__.py:327, onupdate=func.now()) → leads + finishes
- llm_calls.created_at  — TIMESTAMPTZ (migrations/017_phase5.sql:61)  → llm_spend

Тестовые фикстуры (conftest.py):
- test_conversation_factory(status=..., campaign_id=..., sender=...) → dict с ключом "id"
- test_message_factory(conv_id, count=, direction='outbound'|'inbound', sent_by='ai'|'contact', workspace_id=)
- НИ ОДНА фикстура не принимает custom created_at/updated_at → бэкдейтить строки надо raw UPDATE:
    UPDATE messages SET created_at = NOW() - INTERVAL '40 days' WHERE conversation_id = :cid
    UPDATE conversations SET updated_at = NOW() - INTERVAL '40 days' WHERE id = :cid
- llm_calls вставляются raw INSERT (см. test_phase5_1_llm_aggregates.py:90); бэкдейт: UPDATE llm_calls SET created_at = ...
- Хелперы в тестах: `_auth_headers(jwt, sub)`, `_bind(db, ws_id, uid)` — скопировать из test_phase5_analytics.py:22-30.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: since-тесты (RED)</name>
  <files>tests/test_phase5_analytics_since.py</files>
  <behavior>
    Параметризованные async-тесты через async_client + фикстуры conftest. Скопировать хелперы `_auth_headers`/`_bind` из tests/test_phase5_analytics.py.
    - Регрессия: без ?since (или since не передан) ответ идентичен текущему all-time — сидим 1 outbound + 1 inbound давностью 40 дней, БЕЗ since карточки их считают (sent=1, replied.conversation_count=1).
    - since-окно отсекает старое: тот же 40-дневный набор + ?since=7d → sent=0, replied.conversation_count=0 (строки старше окна). Свежий набор (created_at=now) при ?since=7d → считается.
    - leads/finishes по updated_at: conversation со status='lead' с updated_at 40 дней назад → ?since=7d даёт leads=0; свежая → leads=1. Аналогично finishes (status='finished').
    - llm_spend по окну: 2 llm_calls (модель с известной ценой, напр. 'gpt-test' или существующая из llm_pricing) backdate 40 дней → ?since=7d даёт llm_spend_usd_cents=0; свежие → >0. Скоуп campaign (llm_calls.campaign_id).
    - contacts_messaged НЕ фильтруется: campaign-scope, outbound-сообщения backdate 40 дней → ?since=7d, contacts_messaged остаётся = числу distinct контактов (период НЕ применяется к прогресс-полям).
    - Проверить все 4 endpoints принимают since (хотя бы smoke: 200 на ?since=30d для каждого).
    Бэкдейт строк — raw UPDATE created_at/updated_at через async_db_session (фикстуры custom timestamp не принимают).
  </behavior>
  <action>
    Создать tests/test_phase5_analytics_since.py по образцу tests/test_phase5_analytics.py и tests/test_phase5_1_llm_aggregates.py.
    Импорты/фикстуры: async_client, valid_supabase_jwt, async_db_session, test_workspace, test_conversation_factory, test_message_factory, test_campaign_factory (для campaign-scope и contacts_messaged/llm).
    Хелперы `_auth_headers`, `_bind` — скопировать verbatim из test_phase5_analytics.py:22-30.
    Бэкдейт: после сидинга — `await async_db_session.execute(text("UPDATE messages SET created_at = NOW() - INTERVAL '40 days' WHERE conversation_id = :cid"), {"cid": ...}); await async_db_session.commit()`. Аналогично conversations.updated_at и llm_calls.created_at.
    Окно теста: since=7d, старьё = 40 дней → надёжно вне окна.
    Каждый уникальный workspace-bind — свой sub (см. паттерн test_all_4_endpoints_same_schema) чтобы не биться в user_workspaces UNIQUE.
    Эти тесты изначально RED (since ещё не реализован — endpoints вернут all-time и since-окно НЕ отсечёт → assert упадёт).
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_phase5_analytics_since.py -x</automated>
  </verify>
  <done>Тест-файл существует, собирается без ошибок, since-фильтрующие ассерты FAIL (RED) до Task 2, регрессионный (без since) PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: since-параметр в _compute_cards + 4 endpoints (GREEN)</name>
  <files>app/routers/analytics.py</files>
  <behavior>
    Все since-тесты из Task 1 GREEN; полный suite tests/test_phase5_analytics*.py + test_phase5_1_llm_aggregates.py GREEN (регрессия).
  </behavior>
  <action>
    В app/routers/analytics.py:

    1. Расширить сигнатуру `_compute_cards`:
       `async def _compute_cards(db, workspace_id, scope=None, since: Optional[str] = None) -> AnalyticsCards`
       Обновить docstring: since=None → all-time (текущее); since!=None → окно `_SINCE_DAYS[since]` дней.
       ЗАДОКУМЕНТИРОВАТЬ допущения: (а) leads/finishes фильтруются по c.updated_at — момент последнего изменения ≈ смена статуса, точного status-change timestamp нет; (б) contacts_messaged/registered_contacts период НЕ применяется — это прогресс кампании (числитель/знаменатель), period-agnostic.

    2. Построить временные clause через bind-param (переиспользовать `_SINCE_DAYS`, значение дней ТОЛЬКО через :days, никогда f-string):
       ```python
       msg_time_clause = ""
       conv_time_clause = ""
       llm_time_clause = ""
       if since is not None:
           days = _SINCE_DAYS[since]
           params["days"] = str(days)
           llm_params["days"] = str(days)   # llm_params строится отдельно ниже — добавить туда же
           msg_time_clause = " AND m.created_at >= NOW() - (:days || ' days')::INTERVAL"
           conv_time_clause = " AND c.updated_at >= NOW() - (:days || ' days')::INTERVAL"
           llm_time_clause = " AND lc.created_at >= NOW() - (:days || ' days')::INTERVAL"
       ```
       (`days` в оба dict — `params` для sent/replied/leads/finishes, `llm_params` для spend. `:days` в SQL, где clause не подставлен, не появится → безопасно.)

    3. Вставить clause в соответствующие SQL:
       - sent (строка ~182): добавить `{msg_time_clause}` в WHERE.
       - replied (строка ~193): добавить `{msg_time_clause}`.
       - leads (строка ~208): добавить `{conv_time_clause}`.
       - finishes (строка ~217): добавить `{conv_time_clause}`.
       - llm spend (строка ~278): добавить `{llm_time_clause}` в WHERE перед GROUP BY.
       - contacts_messaged (строка ~236) и registered_contacts (строка ~246): НЕ трогать (period-agnostic, комментарий-задокументировать).

    4. Добавить `since: Optional[Literal["1d","7d","30d","90d"]] = None` во все 4 endpoint-сигнатуры (workspace_analytics, campaign_analytics, agent_analytics, sender_analytics) как query-параметр и пробросить `since=since` в вызов `_compute_cards`. `Literal` уже импортирован (analytics.py:21).
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_phase5_analytics_since.py tests/test_phase5_analytics.py tests/test_phase5_analytics_correctness.py tests/test_phase5_1_llm_aggregates.py -x</automated>
  </verify>
  <done>Все since-тесты GREEN; регрессионные phase5-тесты GREEN; since=None даёт байт-в-байт прежний результат; значение дней в SQL только через :days bind-param.</done>
</task>

</tasks>

<verification>
- `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_phase5_analytics_since.py tests/test_phase5_analytics.py tests/test_phase5_analytics_correctness.py tests/test_phase5_1_llm_aggregates.py` — всё GREEN.
- Ручная проверка: grep подтверждает `since=since` в 4 endpoint-вызовах и отсутствие f-string со значением дней (только `:days` bind).
- Деплой НЕ делать — оркестратор задеплоит api сам.
</verification>

<success_criteria>
- 4 cards-endpoints принимают опциональный `?since=1d|7d|30d|90d`; отсутствие → all-time (регрессия зелёная).
- sent/replied по m.created_at; leads/finishes по c.updated_at; llm_spend по lc.created_at; contacts_messaged/registered_contacts не фильтруются.
- Значение дней параметризовано (:days), не интерполируется в SQL.
- Response shape AnalyticsCards не изменён.
</success_criteria>

<output>
После завершения создать `.planning/quick/260702-kf2-kpi-since-analytics-cards-endpoints/260702-kf2-SUMMARY.md`
</output>
