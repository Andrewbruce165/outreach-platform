---
phase: quick-260709-f7l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/                                   # new subtree (whole frontend repo under prefix)
  - frontend/vite.config.ts
  - frontend/.env
  - deploy-frontend.sh
  - /etc/nginx/sites-available/aimly.agsventurelab.com
  - CLAUDE.md
# autonomous is false because Task 5 is a human-verify checkpoint (prod nginx cutover)
autonomous: false
requirements: [QT-260709-f7l]
must_haves:
  truths:
    - "Frontend source lives under frontend/ in the monorepo with git history preserved (git log -- frontend/ shows frontend commits)"
    - "vite build produces a static SPA bundle (client assets + an index/shell HTML) with no Cloudflare Workers server module and no required Node runtime"
    - "aimly.agsventurelab.com serves the SPA shell at / (HTTP 200, HTML) and hashed assets under /assets/"
    - "Deep-link client routes survive a hard refresh (nginx try_files falls back to the SPA shell, not 404)"
    - "Frontend API calls reach the backend same-origin at /api/v1/... and succeed (e.g. /api/v1/health returns 200 through the domain)"
    - "The SNI stream chain and TLS cert are untouched; the API backend on 127.0.0.1:8005 is still reachable"
    - "CLAUDE.md Стек / Git & Deploy / Сетевая топология describe the monorepo frontend and VPS deploy path"
  artifacts:
    - path: "frontend/vite.config.ts"
      provides: "SPA-mode build config (nitro:false + tanstackStart.spa.enabled)"
      contains: "spa"
    - path: "deploy-frontend.sh"
      provides: "Docker-bun build + rsync to /var/www/aimly"
      contains: "rsync"
    - path: "/var/www/aimly"
      provides: "Static SPA served by nginx"
      contains: "index.html (or the emitted shell)"
    - path: "/etc/nginx/sites-available/aimly.agsventurelab.com"
      provides: "location / static root + location /api/ proxy to :8005"
      contains: "/var/www/aimly"
    - path: "CLAUDE.md"
      provides: "Updated monorepo frontend + deploy docs"
      contains: "frontend/"
  key_links:
    - from: "nginx location /api/"
      to: "127.0.0.1:8005"
      via: "proxy_pass"
      pattern: "proxy_pass http://127.0.0.1:8005"
    - from: "nginx location /"
      to: "/var/www/aimly"
      via: "try_files SPA fallback"
      pattern: "try_files"
    - from: "frontend/vite.config.ts"
      to: "static SPA output"
      via: "nitro:false + spa enabled"
      pattern: "nitro"
---

<objective>
Fold the Lovable-generated frontend (`AGS-Venture-Lab/aimly-tg-outreach`, TanStack Start + Vite + bun + shadcn) into this backend monorepo (`Andrewbruce165/outreach-platform`) as a `frontend/` subtree with preserved git history, convert the build from a Cloudflare Workers SSR target to a static SPA (SSR is inert — zero server functions, all data fetching is client-side), and serve it from the existing VPS nginx + docker stack at `aimly.agsventurelab.com` — exactly like the `vitrina` service does. After this, one repo, one `git log`, one manual deploy path; Cloudflare is no longer the source of the frontend deploy.

Purpose: Single source of truth and single deploy for back+front. Drop the Lovable/Cloudflare handoff loop.
Output: `frontend/` subtree, SPA build config, `deploy-frontend.sh`, static site in `/var/www/aimly`, additive nginx vhost change, updated CLAUDE.md.

SAFETY (see constraints): This touches the LIVE nginx vhost for `aimly.agsventurelab.com` and its SNI/TLS chain. All nginx edits are additive/reversible with a backup taken first; the cutover is gated behind a human-verify checkpoint. The old frontend repo and its Cloudflare deploy are LEFT INTACT (out of scope to decommission). No `docker compose down -v`, no certbot `--nginx`, no touching the `:443` SNI stream block.
</objective>

<execution_context>
@/root/apps/aimly/tg-outreach/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260709-f7l-aimly-tg-outreach-outreach-platform-git-/260709-f7l-CONTEXT.md
@.planning/quick/260709-f7l-aimly-tg-outreach-outreach-platform-git-/260709-f7l-RESEARCH.md
@CLAUDE.md

<key_facts>
Verified on this box during planning:
- Repo `/root/apps/aimly/tg-outreach` on branch `main`, remote `origin` = git@github.com:Andrewbruce165/outreach-platform.git. git 2.51.0 (subtree available). Top-level dirs: app docs lovable-handoff migrations scripts tests → `frontend/` prefix is free.
- WORKING-TREE STATE (planning-time): the repo has PRE-EXISTING, UNRELATED tracked changes that predate this task — `M .planning/debug/telegram-profile-update-not-applying.md` and `D .planning/phases/22-warmup-.../22-CONTEXT.md` — plus several untracked `??` files. `git subtree add` runs `ensure_clean()` (`git diff-index HEAD --exit-code --quiet`), which fails on those tracked modifications/deletions but is INDIFFERENT to untracked files. Task 1 must clean the tracked state before `git subtree add` (see Task 1 step 1).
- Frontend clone `/root/apps/aimly/aimly-tg-outreach`: root commit 86becaa, remote AGS-Venture-Lab/aimly-tg-outreach, 342 commits. Unrelated history to backend → use `git subtree add` (not copy). node_modules/dist/.tanstack are gitignored (won't import). `.env` IS tracked but holds browser-public Supabase URL+anon key (no secret leak).
- Build wrapper: `@lovable.dev/vite-tanstack-config@2.7.1`. Current `vite.config.ts` sets `tanstackStart: { server: { entry: "server" } }`; wrapper auto-injects the Cloudflare plugin (preset cloudflare-module). Wrapper levers verified: `nitro: false` (drops Cloudflare deploy plugin), `nitro.preset` (override), `tanstackStart` passthrough (supports `spa: { enabled: true }`).
- Current build output `dist/client/assets` + `dist/server/server.js` with NO index.html → not statically servable as-is. Zero server functions (grep confirmed) → SSR is inert. `src/lib/api.ts`: `BACKEND_URL = VITE_BACKEND_URL ?? ""` → empty ⇒ same-origin relative `/api/...` requests.
- bun is NOT installed on host (node v22 + docker present). Repo uses bun.lock + bunfig.toml → build in `oven/bun:1` Docker stage.
- Backend serves under `/api/v1/...` (health at `/api/v1/health`, verified app/main.py) → NO path collision with SPA `/` and `/assets/`.
- Analog service `vitrina`: `/root/apps/vitrina/deploy.sh` = `cd frontend && npm run build` → `rsync -a --delete frontend/dist/ /var/www/vitrina/`; API `cd api && docker compose up -d --build`. Layout `api/` + `frontend/`. API on 127.0.0.1:8006.
- CURRENT aimly vhost `/etc/nginx/sites-available/aimly.agsventurelab.com`: single `location / { proxy_pass http://127.0.0.1:8005; ... }` (ALL → API), `listen 127.0.0.1:8444 ssl proxy_protocol` behind the SNI stream dispatcher. TLS via letsencrypt live/aimly.agsventurelab.com. THIS is the working prod route being changed.
</key_facts>

<target_vhost>
Target aimly vhost (Route A — from RESEARCH Finding 3). Only the `location` blocks and `root` change; `listen ... proxy_protocol`, ssl_* lines, and the :80 acme/redirect server block stay verbatim:

    server {
        server_name aimly.agsventurelab.com;
        include snippets/security-headers.conf;
        client_max_body_size 10m;
        root /var/www/aimly;

        location /api/ {
            proxy_pass http://127.0.0.1:8005;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $proxy_protocol_addr;
            proxy_set_header X-Forwarded-For $proxy_protocol_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 300s;
            proxy_connect_timeout 10s;
        }

        location / {
            try_files $uri $uri/ /index.html;   # replace /index.html with the actual shell name if build emits _shell.html
        }

        listen 127.0.0.1:8444 ssl proxy_protocol;
        ssl_certificate     /etc/letsencrypt/live/aimly.agsventurelab.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/aimly.agsventurelab.com/privkey.pem;
        include /etc/letsencrypt/options-ssl-nginx.conf;
        ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
    }
</target_vhost>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bring frontend into the monorepo as a history-preserving frontend/ subtree</name>
  <files>frontend/ (new)</files>
  <action>
Work in `/root/apps/aimly/tg-outreach`. Per CLAUDE.md/MEMORY: parallel-agent-safe — do NOT `git add -A`; the subtree merge stages its own commit. There are PRE-EXISTING, UNRELATED tracked changes in the tree (`M .planning/debug/telegram-profile-update-not-applying.md`, `D .planning/phases/22-warmup-.../22-CONTEXT.md`) plus untracked `??` files that predate this task.

1. CLEAN THE TREE FIRST (mandatory — `git subtree add` runs `ensure_clean()` and will abort on any tracked modification). Do this on `main`, BEFORE creating the migration branch:
   - Inspect what is dirty: `git status --porcelain`.
   - Stash ONLY the tracked changes (untracked `??` files do NOT block subtree, so leave them in place — no `-u`): `git stash push -m "pre-subtree-wip-260709-f7l"`.
   - `git stash` handles the modified file and the deletion cleanly and is branch-independent, so it survives the `git checkout -b` below. Do NOT `git add -A`. Do NOT commit the unrelated files.
   - Confirm the tracked tree is now clean: `git diff-index HEAD --exit-code --quiet && echo CLEAN` (this is exactly the check `ensure_clean()` performs; it must print CLEAN before proceeding).
2. Create and switch to a dedicated branch: `git checkout -b frontend-monorepo-migration`.
3. Add the frontend remote and fetch: `git remote add aimly-frontend https://github.com/AGS-Venture-Lab/aimly-tg-outreach.git` then `git fetch aimly-frontend`.
4. Import with history under the `frontend/` prefix: `git subtree add --prefix=frontend aimly-frontend main`.
   - This creates a merge commit; full history is browsable via `git log -- frontend/`. Only tracked files import (node_modules/dist/.tanstack gitignored upstream → excluded).
5. RESTORE the stashed unrelated changes so nothing is lost: `git stash pop`. They return as uncommitted working-tree changes on the migration branch (still not committed, still owned by whoever made them) — leave them alone. If `git stash pop` reports a conflict (it should not — the subtree only touched `frontend/`), resolve by keeping the stashed version of the unrelated files and stop to flag it.
6. Keep the `aimly-frontend` remote for now (future subtree pulls / rollback reference) — do NOT remove it. Do NOT delete or push to the upstream frontend repo (out of scope).
7. Review the imported `frontend/.env` — it holds browser-public Supabase URL + anon key (expected, not a secret). Do not scrub; just confirm no unexpected server-only secret was tracked.

Do NOT push to origin in this task — pushing happens only when the user asks.
  </action>
  <verify>
    <automated>test -f frontend/package.json && test -f frontend/vite.config.ts && test -d frontend/src && git log --oneline -- frontend/ | head -1 | grep -q . && echo OK</automated>
  </verify>
  <done>Tracked tree was cleaned (stashed) before the import so `git subtree add` succeeded; `frontend/` exists with package.json, vite.config.ts, src/; `git log -- frontend/` shows imported frontend commits; no node_modules/dist imported; the pre-existing unrelated changes were restored via `git stash pop` (not lost, not committed).</done>
</task>

<task type="auto">
  <name>Task 2: Convert the build to static SPA mode and confirm output shape</name>
  <files>frontend/vite.config.ts, frontend/.env</files>
  <action>
Convert from the Cloudflare Workers SSR target to a static SPA (Route A, RESEARCH Finding 2). SSR is inert here (zero server functions) so nothing functional is lost.

1. Edit `frontend/vite.config.ts` to drop the Cloudflare deploy plugin and enable SPA mode:

    import { defineConfig } from "@lovable.dev/vite-tanstack-config";
    export default defineConfig({
      nitro: false,                       // drop the Cloudflare Workers deploy plugin
      tanstackStart: {
        spa: { enabled: true },           // static shell + client-only routing
      },
    });

   (The old `tanstackStart: { server: { entry: "server" } }` line is removed — `src/server.ts` is the SSR error wrapper and becomes dead under SPA mode. Leave the file on disk for now; removing dead Cloudflare artifacts is optional cleanup, not required for this task.)
2. Ensure same-origin API calls: in `frontend/.env` set `VITE_BACKEND_URL=""` (empty ⇒ relative `/api/...`, no CORS). Leave `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` as-is (public by design, baked at build time).
3. Run one build inside a Docker bun stage (bun is NOT on the host — do not install it):

    docker run --rm -v "$PWD/frontend":/app -w /app oven/bun:1 sh -c "bun install --frozen-lockfile && bun run build"

4. Inspect the emitted static output and RECORD two facts for Tasks 3 & 4:
   - the output directory (expect `frontend/dist/` root, possibly `.output/public`), and
   - the shell/index filename (`index.html` vs `_shell.html`).
   Confirm hashed `assets/` are present and there is NO Cloudflare Workers `server.js` module required to serve the site.

MEDIUM-confidence item (RESEARCH): exact output dir + shell filename are only knowable after this build. Whatever this build emits is the source of truth for the rsync path (Task 3) and nginx `try_files` fallback (Task 4).
  </action>
  <verify>
    <automated>grep -q "nitro" frontend/vite.config.ts && grep -q "spa" frontend/vite.config.ts && (find frontend/dist frontend/.output -maxdepth 2 -name '*.html' 2>/dev/null | grep -q . ) && (find frontend/dist frontend/.output -maxdepth 3 -type d -name assets 2>/dev/null | grep -q .) && echo OK</automated>
  </verify>
  <done>vite.config.ts has `nitro:false` + `spa` enabled; `VITE_BACKEND_URL=""` set; the Docker bun build succeeds and emits a static HTML shell + hashed `assets/`; the output dir and shell filename are recorded for downstream tasks; no Node/Workers server bundle is required to serve.</done>
</task>

<task type="auto">
  <name>Task 3: Add deploy-frontend.sh and stage the SPA into /var/www/aimly</name>
  <files>deploy-frontend.sh, /var/www/aimly (new, filesystem)</files>
  <action>
Create a manual deploy script mirroring `/root/apps/vitrina/deploy.sh`, but building in a Docker bun stage (host has no bun) and rsyncing to `/var/www/aimly`. Match the existing manual `git pull && docker compose up -d --build` deploy ethos — no CI/CD workflow, no always-on container (Route A needs none).

1. Write `/root/apps/aimly/tg-outreach/deploy-frontend.sh` (chmod +x), roughly:

    #!/usr/bin/env bash
    set -euo pipefail
    cd "$(dirname "$0")/frontend"
    echo "==> Build SPA (docker bun)"
    docker run --rm -v "$PWD":/app -w /app oven/bun:1 sh -c "bun install --frozen-lockfile && bun run build"
    echo "==> Publish to /var/www/aimly"
    mkdir -p /var/www/aimly
    rsync -a --delete <BUILD_OUTPUT_DIR>/ /var/www/aimly/      # <BUILD_OUTPUT_DIR> = the dir recorded in Task 2 (e.g. dist)
    echo "==> Done"

   Substitute `<BUILD_OUTPUT_DIR>` with the actual directory from Task 2. Keep it simple and reversible (rsync into a plain webroot; no volume juggling).
2. Run `./deploy-frontend.sh` once to populate `/var/www/aimly`.
3. Confirm the shell HTML and `assets/` landed in `/var/www/aimly`.

This task does NOT touch nginx or the API container — it only writes files to a webroot nginx does not yet serve, so it is non-disruptive to prod.
  </action>
  <verify>
    <automated>test -x deploy-frontend.sh && (test -f /var/www/aimly/index.html || test -f /var/www/aimly/_shell.html) && (find /var/www/aimly -maxdepth 2 -type d -name assets | grep -q .) && echo OK</automated>
  </verify>
  <done>`deploy-frontend.sh` is executable, builds via Docker bun, and rsyncs the recorded build output into `/var/www/aimly`; the webroot contains the SPA shell HTML + `assets/`. Prod nginx/API untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Additively rewire the aimly nginx vhost (backup first, validate, reload)</name>
  <files>/etc/nginx/sites-available/aimly.agsventurelab.com</files>
  <action>
SAFETY-CRITICAL, prod-touching, reversible. This changes the LIVE vhost from "all → API" to "SPA at /, /api/ → API". The SNI stream chain and TLS are NOT touched.

1. BACKUP the current vhost first (mandatory rollback point):
   `cp /etc/nginx/sites-available/aimly.agsventurelab.com /etc/nginx/sites-available/aimly.agsventurelab.com.bak.$(date +%Y%m%d-%H%M%S)`
2. Edit ONLY the `server { ... listen 127.0.0.1:8444 ssl proxy_protocol; ... }` block to match `<target_vhost>` in context:
   - add `root /var/www/aimly;`
   - add `location /api/ { proxy_pass http://127.0.0.1:8005; ... }` (keep the existing proxy_set_header / Upgrade / timeout lines).
   - change `location /` to `try_files $uri $uri/ /index.html;` — replace `/index.html` with the actual shell filename recorded in Task 2 if it is `_shell.html`.
   - Leave `listen 127.0.0.1:8444 ssl proxy_protocol`, all `ssl_*` lines, `include snippets/security-headers.conf`, `client_max_body_size 10m`, and the entire `:80` acme/redirect server block UNCHANGED.
   - Do NOT touch the `:443` SNI stream dispatcher config elsewhere. Do NOT run certbot.
3. Validate syntax: `nginx -t`. If it fails, restore the backup and stop.
4. Apply: `systemctl reload nginx` (reload, not restart; reversible by restoring the .bak and reloading again).
5. Smoke-check locally through the chain that BOTH the SPA and the API still resolve:
   - `curl -sko /dev/null -w '%{http_code}' https://aimly.agsventurelab.com/` → 200 (HTML shell)
   - `curl -sk https://aimly.agsventurelab.com/api/v1/health` → backend health 200 (API still reachable = SNI/TLS/API path intact)

ROLLBACK (document in the task output for the user): `cp <the .bak file> /etc/nginx/sites-available/aimly.agsventurelab.com && nginx -t && systemctl reload nginx`.
  </action>
  <verify>
    <automated>ls /etc/nginx/sites-available/aimly.agsventurelab.com.bak.* >/dev/null 2>&1 && grep -q "/var/www/aimly" /etc/nginx/sites-available/aimly.agsventurelab.com && grep -q "location /api/" /etc/nginx/sites-available/aimly.agsventurelab.com && grep -q "listen 127.0.0.1:8444 ssl proxy_protocol" /etc/nginx/sites-available/aimly.agsventurelab.com && nginx -t && [ "$(curl -sko /dev/null -w '%{http_code}' https://aimly.agsventurelab.com/)" = "200" ] && [ "$(curl -sk https://aimly.agsventurelab.com/api/v1/health -o /dev/null -w '%{http_code}')" = "200" ] && echo OK</automated>
  </verify>
  <done>A timestamped `.bak` of the vhost exists; the vhost serves `/var/www/aimly` at `/` with SPA fallback and proxies `/api/` to `127.0.0.1:8005`; the `listen ... proxy_protocol` + ssl lines are unchanged; `nginx -t` passes and nginx is reloaded; the automated `curl` checks confirm both `/` (200 HTML) and `/api/v1/health` (200) resolve through the domain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human-verify SPA cutover (login, deep-link refresh, live API)</name>
  <what-built>
The aimly frontend now serves as a static SPA from `/var/www/aimly` via the existing nginx chain at `https://aimly.agsventurelab.com`, with `/api/` reverse-proxied to the backend on `127.0.0.1:8005`. Backend, SNI stream, and TLS were not modified; a `.bak` of the vhost exists for instant rollback. Cloudflare deploy and the upstream frontend repo are untouched.
  </what-built>
  <how-to-verify>
1. Open `https://aimly.agsventurelab.com` in a browser — the app shell loads (not the raw API JSON that the domain used to return).
2. Log in (Supabase magic link) — auth flow completes and the authenticated area loads.
3. Navigate to a nested route (e.g. inbox / a campaign), then HARD REFRESH (Ctrl/Cmd-R) on that deep URL — the app reloads instead of a 404 (confirms SPA try_files fallback).
4. Confirm data loads (senders/campaigns/inbox lists populate) and an action that hits the backend succeeds — confirms same-origin `/api/v1/...` requests reach `:8005`. Check the browser Network tab: API calls go to `aimly.agsventurelab.com/api/v1/...` and return 200, not Cloudflare.
5. If anything is broken, roll back immediately: restore the `.bak` vhost, `nginx -t && systemctl reload nginx` — the domain returns to the previous all-to-API behavior (frontend still available on Cloudflare, which was never touched).
  </how-to-verify>
  <action>Human-in-the-loop UAT: this task is a blocking manual checkpoint. The executor must PAUSE after Task 4's automated cutover and ask the user to perform the browser verification steps above against the live domain, then wait for the resume signal. No code is written in this task; it confirms the SPA cutover behavior (visual load, Supabase login, deep-link hard-refresh survival, same-origin `/api/v1/...` calls) that cannot be fully asserted by an automated test. If the user reports breakage, roll back via the vhost `.bak` before proceeding to Task 6.</action>
  <resume-signal>Type "approved" if the SPA loads, deep-link refresh works, login works, and API calls succeed — or describe what broke (screenshots / failing route / Network errors).</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Update CLAUDE.md for the monorepo frontend + VPS deploy</name>
  <files>CLAUDE.md</files>
  <action>
Update `/root/apps/aimly/tg-outreach/CLAUDE.md` to reflect the new reality (only after the checkpoint is approved):

1. "Стек" section: change the frontend line — it now lives in this monorepo under `frontend/` (TanStack Start built in SPA mode via `@lovable.dev/vite-tanstack-config`), no longer a separate repo / Cloudflare-deployed. Note SSR is inert (client-only data fetching) and Lovable is no longer the source of truth (direct code edits).
2. "Git & Deploy" section: the frontend is now part of `Andrewbruce165/outreach-platform` under `frontend/`. Add the frontend deploy step: `./deploy-frontend.sh` (Docker bun build → rsync to `/var/www/aimly`). Note the upstream `AGS-Venture-Lab/aimly-tg-outreach` repo + its Cloudflare deploy are retained as an archive/rollback point but are no longer the edit/deploy source. Correct the earlier note that described the frontend as an independent sibling repo.
3. "Сетевая топология" section: document that `aimly.agsventurelab.com` now serves the static SPA at `/` from `/var/www/aimly` and reverse-proxies `/api/` to `127.0.0.1:8005`; the `:443 → SNI stream → nginx:8444 ssl proxy_protocol → api:8000` chain and certbot-webroot TLS are unchanged. Mention the vhost `.bak` rollback convention.

Keep edits surgical — do not rewrite unrelated sections. Prose in Russian to match the file.
  </action>
  <verify>
    <automated>grep -q "frontend/" CLAUDE.md && grep -q "deploy-frontend.sh" CLAUDE.md && grep -q "/var/www/aimly" CLAUDE.md && echo OK</automated>
  </verify>
  <done>CLAUDE.md Стек / Git & Deploy / Сетевая топология describe the monorepo `frontend/`, the `deploy-frontend.sh` VPS deploy, the retained-but-inactive Cloudflare/upstream-repo, and the unchanged SNI/TLS chain with static-SPA + /api proxy routing.</done>
</task>

</tasks>

<verification>
- `git log -- frontend/` shows preserved frontend history; `frontend/` contains the app source, no node_modules/dist.
- `frontend/vite.config.ts` builds a static SPA (nitro:false + spa) via Docker bun; output has a shell HTML + hashed assets, no Workers server module.
- `deploy-frontend.sh` builds and rsyncs into `/var/www/aimly`.
- aimly vhost: `.bak` exists; serves SPA at `/` + proxies `/api/` to :8005; `listen ... proxy_protocol` + ssl unchanged; `nginx -t` passes.
- Through the domain: `/` → 200 HTML, `/api/v1/health` → 200. Human checkpoint confirms login + deep-link refresh + live API calls.
- CLAUDE.md updated (Стек / Git & Deploy / Сетевая топология).
- NOT touched: SNI `:443` stream block, TLS certs (no certbot run), old frontend repo, Cloudflare deploy, API container (no down -v).
</verification>

<success_criteria>
- One repo, one `git log`: frontend lives at `frontend/` in `Andrewbruce165/outreach-platform` with history.
- `aimly.agsventurelab.com` serves the SPA from the VPS with working client-side routing and same-origin `/api/v1/...` calls; backend + SNI/TLS chain intact.
- Deploy is a single manual `./deploy-frontend.sh`, matching the backend's manual deploy ethos and the vitrina pattern.
- Every prod-touching step is reversible (vhost `.bak`; old repo + Cloudflare left as fallback).
- CLAUDE.md reflects the new structure.
</success_criteria>

<output>
After completion, create `.planning/quick/260709-f7l-aimly-tg-outreach-outreach-platform-git-/260709-f7l-SUMMARY.md`.
Do NOT push to origin unless the user asks. If pushing, respect CLAUDE.md parallel-agent rule: stage specific files only, never `git add -A`.
</output>
