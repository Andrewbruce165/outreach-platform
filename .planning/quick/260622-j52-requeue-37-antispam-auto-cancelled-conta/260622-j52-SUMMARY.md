---
quick_id: 260622-j52
description: Requeue 37 antispam auto-cancelled contacts in campaign b7cc7d06
date: 2026-06-22
status: complete
type: ops / data-mutation (production DB)
code_changes: none
---

# Summary — 260622-j52

Operational data-mutation on the production DB. **No code changed** — pure queue requeue.

## What was done

1. **Backup** — `./backup.sh` → `/root/backups/tg-outreach/outreach_20260622_134700.sql.gz` (112K).
2. **Requeue** — single transaction with a pre-mutation count guard (`target_rows=37`), then
   `UPDATE 37` flipping the 37 antispam-cancelled rows back to `pending` (attempts reset to 0,
   `error_message`/`started_at`/`finished_at` cleared, `scheduled_at=NOW()`).
3. **Verified** queue post-state: `pending=37 / sent=9 / failed=2`. The 2 remaining failures
   (1 PEER_FLOOD attempts=3, 1 PRIVACY_PREMIUM_REQUIRED attempts=3) were intentionally left.

## Pre-flight checks (why this was safe)

- Sender `sender-8218483045` healthy: `restriction_status=none`, `restricted_until=NULL`,
  `auth_status=ok`, `lifecycle_status=active`.
- Campaign `b7cc7d06` is `running`; clock 16:47 МСК — inside the 09–20 МСК send window.
- All 37 cancelled in one event (`2026-06-19 13:07:55 UTC`), `attempts=NULL` → never sent.
- Requeue cannot re-trigger auto-cancel by itself; the self-check guard (260622-gxt) covers
  solicited SpamBot replies.

## Observed result

Worker picked up the queue ~15–19s after commit:
- `Sent queued item 6c48457f to +79262203487 via sender-8218483045`
- `Queue item 0a72811e will retry (attempt 1/3)`
- Counts moved `pending 37→36 / sent 9→10` within 2 min, no new flood/antispam signals,
  rate within the 4/min limit.

## Not done (optional follow-up)

- The 1 **PEER_FLOOD** row is a legitimate retry candidate now that the account is healthy.
  Left out to keep the operation matching the stated `pending=37` expectation. To include it:
  `… AND status='failed' AND error_message LIKE '%Спам-ограничение%'` (or PEER_FLOOD).
- The 1 **PRIVACY_PREMIUM_REQUIRED** row is unreachable (recipient privacy setting) — do not retry.
