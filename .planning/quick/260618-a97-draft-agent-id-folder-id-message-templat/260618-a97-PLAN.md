---
phase: quick-260618-a97
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/024_campaign_draft_nullable.sql
  - app/models/__init__.py
  - app/schemas/__init__.py
  - app/routers/campaigns.py
  - tests/test_campaign_draft_optional.py
autonomous: true
requirements: [CAMP-DRAFT-OPT]
must_haves:
  truths:
    - "POST /api/v1/campaigns без agent_id / folder_id / message_template создаёт draft (201)"
    - "POST /api/v1/campaigns с переданными agent_id/folder_id всё ещё валидирует их принадлежность workspace (404 на чужой)"
    - "POST /{id}/start на неполном драфте возвращает 422 CAMPAIGN_INCOMPLETE со списком missing полей"
    - "POST /{id}/start на полном драфте (agent+folder+template+≥1 sender) переводит в running"
    - "GET/PATCH кампании с NULL agent_id/folder_id не падает (CampaignResponse сериализуется, is_exhausted=False)"
    - "Уже-полные кампании работают как раньше (контракт не сломан)"
  artifacts:
    - path: "migrations/024_campaign_draft_nullable.sql"
      provides: "DROP NOT NULL для campaigns.agent_id и campaigns.folder_id (идемпотентно)"
      contains: "ALTER TABLE campaigns ALTER COLUMN agent_id DROP NOT NULL"
    - path: "app/models/__init__.py"
      provides: "Campaign.agent_id / folder_id nullable=True"
      contains: "agent_id"
    - path: "app/schemas/__init__.py"
      provides: "CampaignCreate optional agent_id/folder_id/message_template + CampaignResponse Optional agent_id/folder_id"
    - path: "app/routers/campaigns.py"
      provides: "conditional validation в create_campaign + CAMPAIGN_INCOMPLETE guard в start_campaign + None-safe _compute_is_exhausted"
    - path: "tests/test_campaign_draft_optional.py"
      provides: "draft-optional + start-incomplete + start-complete тесты"
  key_links:
    - from: "app/routers/campaigns.py::create_campaign"
      to: "_validate_workspace_owns_agent / _validate_workspace_owns_folder"
      via: "conditional call only when payload value is not None"
      pattern: "if payload.agent_id"
    - from: "app/routers/campaigns.py::start_campaign"
      to: "CAMPAIGN_INCOMPLETE 422"
      via: "pre-running readiness check"
      pattern: "CAMPAIGN_INCOMPLETE"
    - from: "app/routers/campaigns.py::_campaign_to_response"
      to: "_compute_is_exhausted"
      via: "skip when folder_id is None"
      pattern: "folder_id is None"
---

<objective>
Разрешить сохранять неполный draft кампании при создании: сделать `agent_id` / `folder_id` / `message_template` опциональными для драфта, а проверку обязательных полей перенести на запуск (`POST /{id}/start`). Это enabler для фронтового автосохранения драфта после каждого шага 7-шагового визарда (UI-SPEC §5.5) — UI может вызвать POST на шаге 1 и докручивать кампанию через PATCH.

Purpose: убрать жёсткое требование заполнить всё разом; драфт = всегда сохраняемое незавершённое состояние, валидация полноты — только барьер на запуске.
Output: миграция 024 + ORM/schema опциональность + conditional-validation в create + readiness-guard в start + тесты.
</objective>

<execution_context>
@/root/apps/telegram-api/.claude/get-shit-done/workflows/execute-plan.md
@/root/apps/telegram-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/root/CLAUDE.md
@/root/apps/aimly/tg-outreach/CLAUDE.md
@.planning/STATE.md

@app/routers/campaigns.py
@app/schemas/__init__.py
@app/models/__init__.py

<interfaces>
<!-- Контракты, уже существующие в коде — использовать напрямую, без переисследования. -->

ORM Campaign (app/models/__init__.py:457):
  agent_id  = Column(UUID, ForeignKey("ai_contexts.id", ondelete="RESTRICT"), nullable=False)  # → nullable=True
  folder_id = Column(UUID, ForeignKey("folders.id",     ondelete="RESTRICT"), nullable=False)  # → nullable=True
  message_template = Column(Text, nullable=False, server_default="")  # НЕ ТРОГАТЬ (пустая строка = ок для драфта)

CampaignCreate (app/schemas/__init__.py:565):
  agent_id: UUID                       # → Optional[UUID] = None
  folder_id: UUID                      # → Optional[UUID] = None
  message_template: constr(min_length=1)  # → Optional[constr(min_length=1)] = None
  @model_validator _check_work_hours — касается только work_hour_start/end, поля agent/folder/template НЕ трогает (безопасно).

CampaignResponse (app/schemas/__init__.py:634):
  agent_id: UUID    # → Optional[UUID] = None
  folder_id: UUID   # → Optional[UUID] = None
  message_template: str  # OK — server_default="" гарантирует строку, оставить str

create_campaign (campaigns.py:274):
  ll.290-291: безусловно вызывает _validate_workspace_owns_agent(payload.agent_id) / _owns_folder(payload.folder_id)
  l.315: message_template=payload.message_template

start_campaign (campaigns.py:539):
  проверяет status ∈ (draft,paused), затем sender_count==0 → 422 NO_SENDERS_ATTACHED, затем _check_sender_lock → 409.
  НЕ проверяет наличие agent/folder/template.

_campaign_to_response (campaigns.py:206) → l.210: is_exhausted = await _compute_is_exhausted(db, campaign.id, campaign.folder_id)
_compute_is_exhausted (campaigns.py:146): SQL `WHERE c.folder_id = :fid` — при folder_id=None даст бессмысленный результат, нужно short-circuit return False.
</interfaces>

# Тестовые фикстуры (из tests/test_campaign_router.py — переиспользовать):
#   async_client, valid_supabase_jwt, async_db_session, test_workspace, test_agent_factory, test_folder
#   helper _bind(db, ws_id, uid) — INSERT user_workspaces ON CONFLICT DO NOTHING
</context>

<tasks>

<task type="auto">
  <name>Task 1: Миграция 024 + ORM/schema опциональность + conditional create + start readiness-guard</name>
  <files>migrations/024_campaign_draft_nullable.sql, app/models/__init__.py, app/schemas/__init__.py, app/routers/campaigns.py</files>
  <action>
1. **migrations/024_campaign_draft_nullable.sql** (новый файл, идемпотентный — ALTER ... DROP NOT NULL безопасно повторяется):
   ```sql
   -- 024: campaign draft may be incomplete — agent_id/folder_id become nullable.
   -- message_template уже nullable=False server_default='' (пустая строка = ок для драфта) — НЕ трогаем.
   -- FK RESTRICT остаётся (nullable FK допустим).
   ALTER TABLE campaigns ALTER COLUMN agent_id  DROP NOT NULL;
   ALTER TABLE campaigns ALTER COLUMN folder_id DROP NOT NULL;
   ```
   (DROP NOT NULL — no-op если уже nullable, так что повторный прогон applier'а безопасен.)

2. **app/models/__init__.py** Campaign (≈l.470-475): `agent_id` → `nullable=True`, `folder_id` → `nullable=True`. FK и ondelete="RESTRICT" оставить как есть. `message_template` НЕ трогать.

3. **app/schemas/__init__.py**:
   - CampaignCreate (l.571-574): `agent_id: Optional[UUID] = None`, `folder_id: Optional[UUID] = None`, `message_template: Optional[constr(min_length=1)] = None`. `_check_work_hours` validator не трогать.
   - CampaignResponse (l.642-643): `agent_id: Optional[UUID] = None`, `folder_id: Optional[UUID] = None`. `message_template: str` оставить (server_default="" гарантирует строку).

4. **app/routers/campaigns.py create_campaign** (l.289-315):
   - Заменить безусловные вызовы validators на conditional:
     ```python
     if payload.agent_id is not None:
         await _validate_workspace_owns_agent(db, ctx, payload.agent_id)
     if payload.folder_id is not None:
         await _validate_workspace_owns_folder(db, ctx, payload.folder_id)
     ```
   - В конструкторе Campaign(...): `message_template=payload.message_template or ""` (чтобы NULL не ушёл в nullable=False колонку). `agent_id`/`folder_id` передавать как есть (могут быть None).

5. **app/routers/campaigns.py _compute_is_exhausted call-site** (_campaign_to_response l.206-210): сделать None-safe:
   ```python
   if campaign.folder_id is None:
       is_exhausted = False
   else:
       is_exhausted = await _compute_is_exhausted(db, campaign.id, campaign.folder_id)
   ```
   (Драфт без папки не может быть «исчерпан» — рассылать некуда.)

6. **app/routers/campaigns.py start_campaign** (l.546, ПЕРЕД проверкой sender_count): добавить readiness-guard:
   ```python
   missing = []
   if c.agent_id is None:
       missing.append("agent_id")
   if c.folder_id is None:
       missing.append("folder_id")
   if not (c.message_template or "").strip():
       missing.append("message_template")
   if missing:
       raise HTTPException(
           status_code=422,
           detail={"code": "CAMPAIGN_INCOMPLETE",
                   "message": "Campaign is missing required fields before start",
                   "missing": missing},
       )
   ```
   Существующие проверки (status transition, sender_count==0 → NO_SENDERS_ATTACHED, sender-lock → 409) сохранить как есть, после readiness-guard.

НЕ менять: эмпирические rate-limit интервалы, очередь, FK RESTRICT, message_template NOT NULL, duplicate_campaign (копирует существующие значения — None-safe by passthrough).
  </action>
  <verify>
    <automated>cd /root/apps/aimly/tg-outreach && docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api python -c "import app.routers.campaigns; import app.schemas; import app.models; from app.schemas import CampaignCreate; c=CampaignCreate(name='x'); print('OK draft-only create accepted', c.agent_id, c.folder_id, c.message_template)"</automated>
  </verify>
  <done>
    - migrations/024_campaign_draft_nullable.sql существует, идемпотентен, DROP NOT NULL на agent_id+folder_id.
    - Campaign.agent_id / folder_id nullable=True; message_template без изменений.
    - CampaignCreate принимает только name (остальные Optional); CampaignResponse.agent_id/folder_id Optional.
    - create_campaign валидирует agent/folder только когда не None; message_template → "" при None.
    - _compute_is_exhausted не вызывается при folder_id is None.
    - start_campaign возвращает 422 CAMPAIGN_INCOMPLETE с missing[] когда agent/folder/template отсутствуют.
  </done>
</task>

<task type="auto">
  <name>Task 2: Тесты draft-optional create + start readiness</name>
  <files>tests/test_campaign_draft_optional.py</files>
  <action>
Создать tests/test_campaign_draft_optional.py. Использовать фикстуры из conftest (как в tests/test_campaign_router.py): `async_client`, `valid_supabase_jwt`, `async_db_session`, `test_workspace`, `test_agent_factory`, `test_folder` + локальный helper `_bind(db, ws_id, uid)` (скопировать из test_campaign_router.py: INSERT user_workspaces ON CONFLICT DO NOTHING + commit).

`pytestmark = pytest.mark.asyncio`.

Кейсы:

1. **test_create_draft_without_agent_folder_template_returns_201**:
   - `_bind(async_db_session, test_workspace.id, "u-draft")`.
   - POST /api/v1/campaigns с body `{"name": "DraftOnly"}` (без agent_id/folder_id/message_template), auth "u-draft".
   - assert 201; body status == "draft"; body agent_id is None; folder_id is None; message_template == "".

2. **test_start_incomplete_draft_returns_422_campaign_incomplete**:
   - `_bind(...)`. POST создать `{"name": "Incomplete"}` → 201, взять id.
   - POST /api/v1/campaigns/{id}/start.
   - assert 422; body detail["code"] == "CAMPAIGN_INCOMPLETE"; "agent_id" и "folder_id" и "message_template" присутствуют в detail["missing"].

3. **test_start_complete_draft_transitions_to_running**:
   - agent = await test_agent_factory(); folder = test_folder; нужен ≥1 sender в workspace.
     Создать sender напрямую через ORM (см. как делают другие campaign-тесты с senders — Sender(workspace_id=test_workspace.id, ...)); если фикстура sender отсутствует — вставить минимальный Sender через async_db_session и взять его id.
   - POST создать полный campaign `{"name":"Complete","agent_id":..,"folder_id":..,"sender_ids":[..],"message_template":"Hi {{name}}"}` → 201.
   - POST /{id}/start → assert 200; body status == "running".

4. **test_create_with_foreign_agent_still_validates_404** (контракт не сломан):
   - `_bind(...)`. POST `{"name":"X","agent_id":"<random uuid не из workspace>"}`.
   - assert 404; detail["code"] == "AGENT_NOT_FOUND".

Для sender'а в кейсе 3: посмотреть существующий паттерн создания Sender в tests/test_campaign_router.py или tests/conftest.py (грепни `Sender(` в tests/) и переиспользуй минимальный набор обязательных полей.
  </action>
  <verify>
    <automated>cd /root/apps/aimly/tg-outreach && docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_campaign_draft_optional.py -x -q</automated>
  </verify>
  <done>
    - 4 теста проходят через test-overlay.
    - Покрыты: draft-only 201, start→422 CAMPAIGN_INCOMPLETE, start→running на полном, foreign agent→404.
  </done>
</task>

</tasks>

<verification>
- Миграция 024 идемпотентна (ALTER DROP NOT NULL повторяется без ошибок) и автоприменяется applier'ом при старте api.
- Существующие campaign-тесты не сломаны: `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm api pytest tests/test_campaign_router.py tests/test_phase5_1_campaign_v2_router.py -q`.
- Деплой после мерджа: `cd /root/apps/aimly/tg-outreach && docker compose up -d --build api` (restart НЕ подхватывает код; applier подхватит 024 на старте).
</verification>

<success_criteria>
- POST /api/v1/campaigns с одним только name создаёт draft (201, agent_id/folder_id null, message_template "").
- POST /{id}/start на неполном драфте → 422 CAMPAIGN_INCOMPLETE с missing[].
- POST /{id}/start на полном драфте с ≥1 sender → running.
- Чужой agent_id/folder_id при создании по-прежнему → 404 (контракт сохранён).
- GET/PATCH/list кампаний с NULL agent_id/folder_id не падают (is_exhausted=False).
- Новые + существующие campaign-тесты зелёные через test-overlay.
</success_criteria>

<output>
After completion, create `.planning/quick/260618-a97-draft-agent-id-folder-id-message-templat/260618-a97-SUMMARY.md`
</output>
