# Quick Task 260702-c5k: посмотри warmup почему-то все стоит на месте

**Type:** diagnostic (read-only, без изменений кода/данных)
**Date:** 2026-07-02

## Task

Warmup-прогрев аккаунтов остановился — сессии не продвигаются. Найти причину.

## Approach

1. Состояние `warmup_sessions` в прод-БД: статусы, `messages_sent`, `next_message_at`, due-ность
2. Состояние сендеров-участников: `lifecycle_status`, `auth_status`, `restriction_status`
3. Логи api-контейнера (warmup_worker живёт в api, `app/main.py:58`)
4. Код `app/services/warmup.py::_process_due_sessions` / `_process_session` — механика выборки и скипа

## Done

Диагноз в 260702-c5k-SUMMARY.md. Фиксы не применялись (task = «посмотри»; код-фикс требует обсуждения по CLAUDE.md).
