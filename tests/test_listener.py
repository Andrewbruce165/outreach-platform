"""Phase 3 — listener.get_active_senders adapter (RESEARCH Pitfall 5).

Skeleton — filled by Task 4 of plan 03-01.
"""
import pytest

pytestmark = pytest.mark.asyncio


@pytest.mark.skip(reason="pending: filled by Task 4 of plan 03-01")
async def test_get_active_senders_no_ai_context_id():
    """TODO(03-01 Task 4): get_active_senders returns dicts without ai_context_id key."""
    pass
