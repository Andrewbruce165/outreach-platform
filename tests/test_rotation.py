"""Phase 3 — rotation._pick_best_sender adapter (Phase 3 D-04).

Skeleton — filled by Task 5 of plan 03-01.
"""
import pytest

pytestmark = pytest.mark.asyncio


@pytest.mark.skip(reason="pending: filled by Task 5 of plan 03-01")
async def test_pick_best_sender_workspace_only():
    """TODO(03-01 Task 5): _pick_best_sender filters by workspace, not by ai_context_id."""
    pass
