"""Phase 3 — ai_engine.get_context adapter (RESEARCH Pitfall 1).

Skeleton — filled by Task 3 of plan 03-01.
"""
import pytest

pytestmark = pytest.mark.asyncio


@pytest.mark.skip(reason="pending: filled by Task 3 of plan 03-01")
async def test_get_context_phase3_schema():
    """TODO(03-01 Task 3): ai_engine.get_context works without dropped columns."""
    pass
