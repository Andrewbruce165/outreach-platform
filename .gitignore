.env
.env.bak*
.env.*.bak
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
*.log
.DS_Store

# Local scratch / data imports (never commit — may contain contact PII)
scratchpad/
barter_roma_registered.csv

# Lovable handoff bundle artifacts (regenerated by scripts/export-handoff.sh).
# We commit them deliberately (RESEARCH §"Open Questions" #1 — committed > gitignored
# so Lovable has a stable URL/commit to ingest), but rebuild artifacts on every
# release. Keep these lines as documentation; do NOT add an ignore here.
# (intentionally no ignore patterns for lovable-handoff/* — committed by design)
