.env
.env.bak*
.env.*.bak
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
*.log
.DS_Store

# GSD agent worktree scratch (per-agent isolated checkouts — never commit)
.claude/worktrees/

# Local scratch / data imports (never commit — may contain contact PII)
scratchpad/
barter_roma_registered.csv

# Knowledge graph build artifacts (regenerated by post-commit hook — never commit)
graphify-out/
.planning/graphs/
